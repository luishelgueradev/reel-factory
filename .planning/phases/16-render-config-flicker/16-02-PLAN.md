---
phase: "16-render-config-flicker"
plan: 02
type: execute
wave: 1
depends_on:
  - "16-01"
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "A real /process render confirms pipeline_config.loaded=true in remotion-info.json"
    - "The render uses the bar layout and the studio config's fontFamily (not tiktok env fallback)"
    - "remotion-info.json reports subtitle_layout: bar (not tiktok)"
    - "Issue B flicker is observed/measured on the correct-layout render so Wave 2 fix is informed"
  artifacts:
    - path: "pipeline/{jobId}/remotion-renderer/remotion-info.json"
      provides: "Render diagnostics confirming config source and layout"
      contains: "pipeline_config"
  key_links:
    - from: "/data/pipeline/pipeline-config.json"
      to: "pipeline/{jobId}/remotion-renderer/pipeline-config.json"
      via: "process.ts seeding block (copyFileSync)"
      pattern: "ACTIVE_PIPELINE_CONFIG_PATH.*exists"
---

<objective>
Human checkpoint: run a real /process render with the studio config loaded, verify
pipeline_config.loaded=true in remotion-info.json, and observe whether Issue B flicker
is present or eliminated on the correct bar layout.

This checkpoint gate MUST complete before Issue B layout fixes are applied (Wave 2). The
CONTEXT.md mandate is explicit: fix Issue A → re-measure Issue B on correct layout → then
apply Issue B fix. Wave 2 plans depend on this checkpoint.

Purpose: Confirms that the server.ts fix from Plan 16-01 successfully threads the studio
config through process.ts seeding into the renderer — and gives the developer real data
about flicker severity on bar layout before touching layout files.

Output: Human confirmation that pipeline_config.loaded=true + layout=bar. Notes on
observed flicker behavior (present/absent/reduced) documented as the resume signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-render-config-flicker/16-CONTEXT.md
@.planning/phases/16-render-config-flicker/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild and start studio container with Issue A fix</name>
  <files></files>
  <read_first>
    .planning/phases/16-render-config-flicker/16-01-SUMMARY.md — confirm Plan 16-01 completed
    successfully before proceeding.
    docker-compose.yml lines 163–185 — remotion-studio service definition to confirm the
    ACTIVE_PIPELINE_CONFIG_PATH env var was added.
  </read_first>
  <action>
    1. Verify Plan 16-01 artifacts are in place:
       - grep -c "ACTIVE_PIPELINE_CONFIG_PATH" services/remotion-studio/src/server.ts (expect >= 1)
       - grep -c "ACTIVE_PIPELINE_CONFIG_PATH=/data/pipeline/pipeline-config.json" docker-compose.yml (expect 1)
       If either check fails, stop and report; do not proceed to render.

    2. Ensure the studio is running with the new server.ts. If running locally (not Docker):
       pkill -f "tsx src/server.ts" 2>/dev/null || true
       cd services/remotion-studio && setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor npx tsx src/server.ts > /tmp/remotion-server.log 2>&1 &
       Wait 3 seconds, then verify: curl -s http://localhost:3123/api/config | python3 -m json.tool | head -5

    3. Send the bar-layout studio config via PUT:
       curl -s -X PUT http://localhost:3123/api/config \
         -H "Content-Type: application/json" \
         -d '{"subtitle":{"layout":"bar","fontFamily":"Inter","fontSize":60,"activeColor":"#FFFFFF","inactiveColor":"#888888","letterSpacing":0,"lineHeight":1.4,"position":"bottom-center","bottomOffset":120,"pastWordOpacity":0.5,"backgroundHighlight":{"enabled":false,"color":"rgba(0,0,0,0.5)","padding":8,"borderRadius":4}},"titles":[]}' \
         | python3 -m json.tool | grep -E '"layout"|"fontFamily"'
       Expected: "layout": "bar", "fontFamily": "Inter"

    4. Confirm the active config file was created:
       cat pipeline/pipeline-config.json | python3 -m json.tool | grep -E '"layout"|"_meta"'
       Expected: "layout": "bar" present, "_meta" absent.
  </action>
  <verify>
    <automated>grep -c "ACTIVE_PIPELINE_CONFIG_PATH" /home/luis/proyectos/reel-factory/services/remotion-studio/src/server.ts && cat /home/luis/proyectos/reel-factory/pipeline/pipeline-config.json | python3 -m json.tool | grep '"layout"'</automated>
  </verify>
  <acceptance_criteria>
    - services/remotion-studio/src/server.ts contains ACTIVE_PIPELINE_CONFIG_PATH (from Plan 16-01)
    - pipeline/pipeline-config.json exists and contains "layout": "bar"
    - pipeline/pipeline-config.json does NOT contain "_meta"
    - Studio /api/config GET returns 200 (healthcheck passes)
  </acceptance_criteria>
  <done>Studio is running with bar-layout config saved to ACTIVE_PIPELINE_CONFIG_PATH</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 16-01 added the ACTIVE_PIPELINE_CONFIG_PATH write to PUT /api/config. Task 1 above
    saved a bar-layout config to pipeline/pipeline-config.json. Now run a real /process render
    and verify the config threads through end-to-end.
  </what-built>
  <how-to-verify>
    1. Find a short test MP4 clip (any clip in pipeline/ or your test-clips directory). 30s or
       shorter renders fastest at scale:1.

    2. Submit the render:
       curl -s -X POST http://localhost:3000/process \
         -F "video=@/path/to/test-clip.mp4" \
         | python3 -m json.tool | grep -E '"jobId"|"status"'
       Note the returned jobId.

    3. Wait for the render to complete (check /api/status/{jobId} or watch the container logs).
       Completion indicator: status "done" in the status response, or
       ls pipeline/{jobId}/remotion-renderer/output.mp4 exists.

    4. Inspect the render diagnostic:
       cat pipeline/{jobId}/remotion-renderer/remotion-info.json | python3 -m json.tool | grep -A6 '"pipeline_config"'

       PASS condition:
         "pipeline_config": { "loaded": true, "source": "...", "subtitle_layout": "bar", ... }

       FAIL condition (still broken):
         "pipeline_config": { "loaded": false, "source": "env_vars", "subtitle_layout": "tiktok", ... }

    5. If PASS: Play or scrub the rendered video at pipeline/{jobId}/remotion-renderer/output.mp4.
       Observe subtitle flicker. Note one of:
         (a) No visible flicker — Issue B may be resolved by correct layout alone
         (b) Flicker visible — blank gaps between subtitle pages persist on bar layout
         (c) Flicker reduced but present — gaps are smaller than with tiktok layout

    6. Report your observation in the resume signal below.
  </how-to-verify>
  <resume-signal>
    Type one of:
      "approved: loaded=true, flicker=none"       — Issue A fixed; Issue B resolved by layout fix
      "approved: loaded=true, flicker=present"    — Issue A fixed; Issue B still needs layout fix
      "approved: loaded=true, flicker=reduced"    — Issue A fixed; Issue B partially resolved
      "failed: loaded=false"                      — Issue A fix did not propagate; describe what you saw
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkpoint output → Wave 2 plan execution | Human verification result gates Issue B layout changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-CP | Tampering | Human checkpoint gate | accept | Checkpoint is blocking — Wave 2 cannot proceed until human confirms loaded=true. Prevents Issue B fix from running against uncorrected layout. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages. |
</threat_model>

<verification>
Checkpoint is the verification. The plan is complete when the human types an "approved: ..."
resume signal confirming pipeline_config.loaded=true in remotion-info.json.

If the resume signal is "failed: loaded=false", Wave 2 MUST NOT execute. Report back to
the orchestrator — Plan 16-01 needs a gap-closure fix before proceeding.
</verification>

<success_criteria>
- pipeline_config.loaded=true in remotion-info.json for the test render
- subtitle_layout field is "bar" (not "tiktok")
- Human has observed and documented flicker behavior so Wave 2 knows whether Issue B fix is still needed
</success_criteria>

<output>
Create .planning/phases/16-render-config-flicker/16-02-SUMMARY.md when done.
Include in the summary: the jobId used, the remotion-info.json pipeline_config section
verbatim, and the human's flicker observation (none/present/reduced).
</output>
