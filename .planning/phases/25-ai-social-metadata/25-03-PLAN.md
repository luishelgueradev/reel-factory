---
phase: 25-ai-social-metadata
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - services/remotion-studio/src/preview/PreviewApp.tsx
  - services/remotion-studio/src/preview/MetadataPanel.tsx
  - services/remotion-studio/src/preview/metadata-panel.test.tsx
autonomous: true
requirements: [META-01, META-02, META-03, META-04]
must_haves:
  truths:
    - "The Phase 22 col3 'Metadata de redes' placeholder is replaced by a live MetadataPanel (META-02)"
    - "Generar/Regenerar calls POST /api/metadata with { jobId, platform, tone } and fills title/description/hashtags (META-01, META-04)"
    - "Each field is inline-editable and independently copyable to the clipboard (META-03)"
    - "Tone + Platform selectors drive regeneration without re-running the pipeline (META-04)"
    - "Generate is gated on a completed render jobId (D-02); green discipline holds — panel uses --accent/--danger, never --action (D-10/UI-SPEC)"
  artifacts:
    - "services/remotion-studio/src/preview/MetadataPanel.tsx"
    - "services/remotion-studio/src/preview/metadata-panel.test.tsx"
  key_links:
    - "MetadataPanel calls POST /api/metadata and GET /api/metadata/:jobId (25-02)"
    - "PreviewApp passes the last successful render jobId (23-04 success state) to MetadataPanel"
---

<objective>
Replace the static col3 placeholder with the live AI metadata panel per 25-UI-SPEC.md:
platform+tone selectors, Generar/Regenerar against /api/metadata, three editable+copyable
fields, all states designed, gated on a completed render. Honors the UI quality mandate.
</objective>

<context>
@.planning/phases/25-ai-social-metadata/25-CONTEXT.md
@.planning/phases/25-ai-social-metadata/25-UI-SPEC.md
@.planning/phases/25-ai-social-metadata/25-AI-SPEC.md
- col3 placeholder is in PreviewApp.tsx ~L887-932 ("Metadata de redes — Próximamente").
- The last successful render jobId comes from the 23-04 render-success state in PreviewApp (the success overlay already has jobId/resultUrl). Thread it to the panel.
- On mount / when jobId changes, GET /api/metadata/:jobId to restore a persisted result (D-05).
- Reuse existing token system + the "✓ Guardado"-style transient chip + clipboard pattern.
</context>

<ui_quality_mandate>
NON-NEGOTIABLE (AGENTS.md): frontend work. At the START you MUST (1) invoke the `impeccable` skill, (2) use the `frontend-design` plugin for the panel, (3) read `sketch-findings-reel-factory` and follow the `AI metadata column` grammar. Match the col3 card language. Green discipline: panel uses `--accent`/`--danger`, NEVER `--action` (green stays Render/Guardar config).
</ui_quality_mandate>

<tasks>

<task type="auto">
  <name>Task 1: MetadataPanel component + tests</name>
  <files>services/remotion-studio/src/preview/MetadataPanel.tsx, services/remotion-studio/src/preview/metadata-panel.test.tsx</files>
  <action>
    MetadataPanel.tsx per 25-UI-SPEC.md:
    - Props: { jobId: string | null }.
    - Platform selector (tiktok/instagram/youtube_shorts) + Tone selector (cercano/profesional/llamativo), --accent active.
    - "✨ Generar metadata" → "↻ Regenerar" once a result exists; --accent (NOT green); disabled with muted hint when jobId is null.
    - On jobId set/change: GET /api/metadata/:jobId → restore persisted result if any.
    - Generate/Regenerate: POST /api/metadata { jobId, platform, tone }; generating state (spinner + shimmer, controls disabled); on success populate; on error inline --danger line + Reintentar.
    - Fields: Título (input), Descripción (textarea), Hashtags (chips, editable as text). Each with a copy button → clipboard + "✓ Copiado" chip. (META-03)
    - Edits update local state; tone/platform change does NOT auto-regenerate.
    - Backend chip from _meta.backend. Accessibility: tabbable controls/fields/copy; reduced-motion.
    Tests (metadata-panel.test.tsx, vitest + @testing-library/react, mock fetch):
    - jobId null → Generar disabled + hint.
    - With jobId, Generar POSTs {jobId,platform,tone} and renders title/description/hashtags. (META-01/02)
    - Changing platform/tone then Regenerar re-POSTs with new values; no auto-call on select change. (META-04)
    - Editing a field updates it; copy button writes that field to clipboard (mock navigator.clipboard). (META-03)
    - Error response → inline error + Reintentar.
    - Assert no --action (green) token used inside the panel (green discipline).
  </action>
  <verify><automated>cd services/remotion-studio && npx vitest run src/preview/metadata-panel.test.tsx 2>&1 | tail -8</automated></verify>
  <acceptance_criteria>
    - Panel implements generate/regenerate/edit/copy against the 25-02 API; states designed; green-discipline test passes.
  </acceptance_criteria>
  <done>Live metadata panel built + tested against the API contract.</done>
</task>

<task type="auto">
  <name>Task 2: Mount MetadataPanel in PreviewApp col3 + thread jobId</name>
  <files>services/remotion-studio/src/preview/PreviewApp.tsx</files>
  <action>
    - Replace the static col3 placeholder card (~L887-932) with <MetadataPanel jobId={lastRenderJobId} />.
    - lastRenderJobId = the jobId from the most recent successful render (from the 23-04 render-success state); null until a render completes. Thread it (lift/keep the jobId in PreviewApp state when a render succeeds).
    - Keep the col3 width/visibility behavior (320px, the col3-metadata class) intact.
    - Green discipline: no new --action element. Keep existing suite green; build:editor succeeds.
  </action>
  <verify><automated>cd services/remotion-studio && npx vitest run 2>&1 | tail -6 && npm run build:editor 2>&1 | tail -2</automated></verify>
  <acceptance_criteria>
    - col3 shows the live panel; after a successful render the panel can generate; full studio suite green + build OK.
  </acceptance_criteria>
  <done>The metadata column is live in the Studio, gated on a completed render.</done>
</task>

</tasks>

<verification>
- `npx vitest run` (studio) green; `npm run build:editor` succeeds.
- Visual: one green on screen; col3 panel with all states; fields editable + copyable.
</verification>

<success_criteria>
- After a render, the user generates title/description/hashtags in the metadata column, edits + copies each field, and regenerates by tone/platform — all in the Studio, no pipeline re-run.
</success_criteria>

<output>
Create `.planning/phases/25-ai-social-metadata/25-03-SUMMARY.md` when done.
</output>
