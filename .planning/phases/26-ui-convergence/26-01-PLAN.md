---
phase: 26-ui-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/editor/components/LayoutSelector.tsx
  - services/remotion-studio/src/preview/z-layers.ts
  - services/remotion-studio/src/preview/z-layers.test.ts
  - services/remotion-studio/src/editor/components/StyleControls.tsx
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
  - services/remotion-studio/src/preview/PreviewApp.tsx
autonomous: true
requirements: [UICONV-01, UICONV-02]
must_haves:
  truths:
    - "The color law holds everywhere: NO --action/green (#4CAF50 / rgba(76,175,80,*)) on any selection/active/focus state; --accent (blue) is used instead (D-04)"
    - "LayoutSelector active state uses --accent (blue) border + --accent-tint bg, not green (fixes the one documented divergence)"
    - "A shared z-index ladder module (z-layers.ts: toast 60, palette 40, takeover 30, sheet 20, base) exists and is applied to layered surfaces (ProfilesMenu popover, render overlays) for consistent stacking"
    - "Hardcoded type/spacing/color where a token exists are replaced with --t-*/--s-*/--r-*/--accent tokens across the swept components (D-05)"
  artifacts:
    - "services/remotion-studio/src/preview/z-layers.ts"
    - "services/remotion-studio/src/preview/z-layers.test.ts"
  key_links: []
---

<objective>
The core convergence pass: enforce the north-star color law (kill the one green-on-active
violation), sweep remaining hardcoded type/spacing/color to tokens, and introduce a shared
z-index ladder so layered surfaces stack consistently. No behavior change — visual/structural
convergence only. Anchors: 26-RESEARCH.md, 26-UI-SPEC.md "Non-negotiable laws".
</objective>

<context>
@.planning/phases/26-ui-convergence/26-CONTEXT.md
@.planning/phases/26-ui-convergence/26-UI-SPEC.md
@.planning/phases/26-ui-convergence/26-RESEARCH.md
- Tokens live in services/remotion-studio/src/editor/index.html :root (OKLCH). --accent (blue), --accent-tint, --accent-strong, --action (green, CTA-only), --danger.
- The violation: LayoutSelector.tsx:51-52 uses `#4CAF50` border + `rgba(76,175,80,0.12)` bg on the selected layout. Change to var(--accent)/var(--accent-tint) (mirror the segmented-button active style already used elsewhere).
- Grep the swept files for hardcoded hex/px that duplicate a token; replace with var(--token, fallback). Do NOT change the --action green CTAs (Render, Guardar, cold-start "Elegir archivo").
</context>

<ui_quality_mandate>
Invoke `impeccable` at the start; use `frontend-design`; read `sketch-findings-reel-factory`. Match the existing token language exactly. This is convergence — do not introduce new colors or restyle beyond the laws.
</ui_quality_mandate>

<tasks>

<task type="auto">
  <name>Task 1: z-index ladder module + tests</name>
  <files>services/remotion-studio/src/preview/z-layers.ts, services/remotion-studio/src/preview/z-layers.test.ts</files>
  <action>
    Create z-layers.ts exporting `Z = { base: 0, sheet: 20, takeover: 30, palette: 40, toast: 60 } as const` (the documented modal-stack ladder) + a short doc comment citing sketch 041 modal-stack-choreography. Add z-layers.test.ts asserting the ordering invariants (toast > palette > takeover > sheet > base) so the ladder can't be silently reordered.
  </action>
  <verify><automated>cd services/remotion-studio && npx vitest run src/preview/z-layers.test.ts 2>&1 | tail -5</automated></verify>
  <acceptance_criteria>
    - z-layers.ts exports the ordered ladder; test asserts the ordering.
  </acceptance_criteria>
  <done>Shared z-ladder exists + guarded.</done>
</task>

<task type="auto">
  <name>Task 2: color-law fix + apply z-ladder + token sweep</name>
  <files>services/remotion-studio/src/editor/components/LayoutSelector.tsx, services/remotion-studio/src/editor/components/StyleControls.tsx, services/remotion-studio/src/editor/components/TitleEditor.tsx, services/remotion-studio/src/preview/PreviewApp.tsx</files>
  <action>
    - LayoutSelector.tsx: replace `#4CAF50` border + `rgba(76,175,80,0.12)` bg on the selected option with `var(--accent, #90caf9)` border + `var(--accent-tint, rgba(144,202,249,0.12))` bg (blue active). No other behavior change.
    - Apply the z-ladder (import Z from ../preview/z-layers.js where appropriate): the ProfilesMenu popover and the render overlays in PreviewApp should use Z.sheet/Z.takeover/Z.toast instead of ad-hoc z-index numbers, so layering is consistent. (If ProfilesMenu is in preview/, import locally.)
    - Sweep the four files for hardcoded hex colors / px font-sizes / px gaps that duplicate an existing token and replace with `var(--token, fallback)`. Keep --action green ONLY on the canonical CTAs. Do NOT touch functional logic.
  </action>
  <verify>
    <automated>cd services/remotion-studio && ! grep -nE "#4CAF50|rgba\(76, ?175, ?80" src/editor/components/LayoutSelector.tsx && echo "no green in LayoutSelector"; npx vitest run 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - No green/#4CAF50 on any active/selected state in the swept files (grep clean).
    - z-ladder applied to layered surfaces; full studio suite green.
  </acceptance_criteria>
  <done>Color law holds; layering consistent; tokens standardized — no behavior change.</done>
</task>

<task type="auto">
  <name>Task 3: color-law regression test</name>
  <files>services/remotion-studio/src/editor/components/LayoutSelector.tsx</files>
  <action>
    Add (or extend) a test that renders LayoutSelector with a selected option and asserts the active style uses the accent token / NOT the green `#4CAF50` (guard against regression of the documented violation). Place it next to LayoutSelector or in an existing editor test file.
  </action>
  <verify><automated>cd services/remotion-studio && npx vitest run 2>&1 | tail -4</automated></verify>
  <acceptance_criteria>
    - A test fails if LayoutSelector's active state reverts to green.
  </acceptance_criteria>
  <done>Color-law fix is regression-guarded.</done>
</task>

</tasks>

<verification>
- `npx vitest run` (studio) green; grep shows no green-on-active in swept files; z-ladder test green.
</verification>

<success_criteria>
- North-star color law holds across the Studio (no green except canonical CTAs), layered surfaces share one z-ladder, tokens standardized — zero behavior change.
</success_criteria>

<output>
Create `.planning/phases/26-ui-convergence/26-01-SUMMARY.md` when done.
</output>
