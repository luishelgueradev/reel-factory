---
phase: 06-animated-intros-outros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-renderer/src/pipeline-config.ts
  - services/remotion-renderer/src/Root.tsx
  - services/remotion-renderer/src/render.ts
autonomous: true
requirements: [VISU-01, VISU-02]

must_haves:
  truths:
    - "Pipeline config schema defines all subtitle layout modes, styling, title overlays, and position presets"
    - "Root.tsx accepts PipelineConfig as inputProps and passes config to composition"
    - "render.ts reads pipeline-config.json from input directory and passes it as inputProps"
    - "When pipeline-config.json is missing, render.ts falls back to environment variable defaults"
    - "Existing TikTok subtitle behavior is preserved as the default layout mode"
  artifacts:
    - path: "services/remotion-renderer/src/pipeline-config.ts"
      provides: "PipelineConfig TypeScript interfaces and validation function"
      exports: ["PipelineConfig", "SubtitleConfig", "TitleConfig", "TitleStyleProps", "SubtileLayoutMode", "SubtitlePosition", "BackgroundHighlight", "validatePipelineConfig"]
    - path: "services/remotion-renderer/src/Root.tsx"
      provides: "Config-driven composition accepting full PipelineConfig"
      exports: ["RemotionRoot"]
      contains: "SubtitledVideo"
    - path: "services/remotion-renderer/src/render.ts"
      provides: "Pipeline-config.json loading with env var fallback"
      contains: "pipeline-config.json"
  key_links:
    - from: "services/remotion-renderer/src/pipeline-config.ts"
      to: "services/remotion-renderer/src/Root.tsx"
      via: "import PipelineConfig and related types"
      pattern: "import.*PipelineConfig.*from.*pipeline-config"
    - from: "services/remotion-renderer/src/render.ts"
      to: "services/remotion-renderer/src/pipeline-config.ts"
      via: "import PipelineConfig, validatePipelineConfig, and read pipeline-config.json"
      pattern: "import.*pipeline-config"
    - from: "services/remotion-renderer/src/render.ts"
      to: "pipeline-config.json"
      via: "fs.readFileSync for JSON config file"
      pattern: "pipeline-config"
---

<objective>
Create the PipelineConfig schema and refactor the Remotion composition to be config-driven. This is the foundation layer — all subsequent plans (layout modes, title overlays, studio) depend on this config architecture being in place.

Purpose: Establish the single-config architecture (D-01) where one PipelineConfig JSON drives all rendering decisions — subtitle layout mode, style parameters, title overlays. This replaces the env-var-only approach with a config-first design that the web UI will later write to.

Output: PipelineConfig TypeScript interfaces, config-driven Root.tsx composition, and render.ts that reads pipeline-config.json with env var fallbacks (D-02, D-03).
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-animated-intros-outros/06-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md

@services/remotion-renderer/src/Root.tsx
@services/remotion-renderer/src/render.ts
@services/remotion-renderer/src/captions.ts
@services/remotion-renderer/src/compositions/Subtitles.tsx
@services/remotion-renderer/package.json
@services/remotion-renderer/Dockerfile
@docker-compose.yml
@services/ffmpeg-finalizer/src/schema.py
</context>

<interfaces>
<!-- Existing interfaces the executor will need -->

From services/remotion-renderer/src/Root.tsx:
```typescript
export interface RemotionProps {
  videoSrc: string;
  captionPages: TikTokPage[];
  videoWidth?: number;
  videoHeight?: number;
  totalDurationMs?: number;
  lineHeight?: number;
  fontSize?: number;
  activeColor?: string;
  inactiveColor?: string;
  outlineColor?: string;
  outlineWidth?: number;
  bottomOffset?: number;
}
```

From services/remotion-renderer/src/captions.ts:
```typescript
export function transcriptToCaptionPages(
  transcript: WhisperTranscript,
  options: {
    combineTokensWithinMilliseconds?: number;
    silenceCuts?: SilenceCutList | null;
  } = {}
): TikTokPage[];
export function remapTimestamps(originalTimeMs: number, silenceCuts: SilenceCutList | null): number;
export function remapWordTimestamps(words: WhisperWord[], silenceCuts: SilenceCutList | null): WhisperWord[];
export type { SilenceCutList, SafeZone, FinalizerInfo, WhisperWord, WhisperTranscript };
```

From services/remotion-renderer/src/render.ts:
```typescript
// Current env var parsing: INPUT_PATH, OUTPUT_PATH, TRANSCRIPT_PATH, SILENCE_CUTS_PATH, 
// FINALIZER_INFO_PATH, ACTIVE_COLOR, INACTIVE_COLOR, FONT_SIZE
// Output: inputProps passed to renderMedia with RemotionProps
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PipelineConfig TypeScript interfaces and validation</name>
  <files>services/remotion-renderer/src/pipeline-config.ts, services/remotion-renderer/src/pipeline-config.test.ts</files>
  <behavior>
    - Test: validatePipelineConfig returns empty errors array for valid minimal config (only subtitleLayout field)
    - Test: validatePipelineConfig returns empty errors for full config with all fields
    - Test: validatePipelineConfig returns errors for invalid subtitleLayout value
    - Test: validatePipelineConfig returns errors for negative startTimeMs in titles
    - Test: validatePipelineConfig accepts all 4 layout modes: tiktok, sentence, bar, karaoke
    - Test: validatePipelineConfig accepts all 3 position presets: bottom-center, top-center, center-screen
    - Test: SubtitleConfig defaults merge correctly (fontSize=58, activeColor="#FFFF00", etc.)
    - Test: TitleConfig with entranceAnimation values slide-up, fade-in, none all validate
  </behavior>
  <action>
    Per D-02 and D-17: Create `services/remotion-renderer/src/pipeline-config.ts` with full TypeScript interfaces for PipelineConfig. This is the contract between the web UI and Remotion composition, shared by both remotion-studio and remotion-renderer.

    Define these types:

    1. `SubtitleLayoutMode` — union type: `'tiktok' | 'sentence' | 'bar' | 'karaoke'` (per D-04, D-05)
    2. `SubtitlePosition` — union type: `'bottom-center' | 'top-center' | 'center-screen'` (per D-09)
    3. `BackgroundHighlight` — interface: `{ enabled: boolean; color: string; padding: number; borderRadius: number }` (per D-08)
    4. `SubtitleConfig` — interface with all style fields from D-06: `layout: SubtitleLayoutMode`, `fontFamily`, `fontSize`, `activeColor`, `inactiveColor`, `outlineColor`, `outlineWidth`, `backgroundHighlight: BackgroundHighlight`, `textShadow` (on/off + params), `letterSpacing`, `position: SubtitlePosition`. All optional with defaults matching current behavior (fontSize=58, activeColor="#FFFF00", inactiveColor="#FFFFFF", outlineColor="#000000", outlineWidth=3, position="bottom-center")
    5. `TitleStyleProps` — interface per D-11: `entranceAnimation: 'slide-up' | 'fade-in' | 'none'`, `backgroundColor`, `textColor`
    6. `TitleConfig` — interface per D-12: `{ text: string; subtitle?: string; startTimeMs: number; durationMs: number; style?: TitleStyleProps }[]`
    7. `PipelineConfig` — top-level interface: `{ subtitle: SubtitleConfig; titles?: TitleConfig[] }`

    Implement `validatePipelineConfig(config: unknown): { valid: boolean; errors: string[] }` — validates the schema, checks subtitleLayout is one of the 4 values, checks title startTimeMs >= 0 and durationMs > 0, checks position values, returns specific error messages with field names.

    Per D-03: All fields in SubtitleConfig are optional — the config overrides defaults. When fields are missing, fall back to existing env var values or hardcoded defaults.

    Write tests first (TDD), then implement.
  </action>
  <verify>
    <automated>cd services/remotion-renderer && npx vitest run src/pipeline-config.test.ts</automated>
  </verify>
  <done>PipelineConfig interfaces defined with all 20 decisions covered. validatePipelineConfig with comprehensive tests passing. Export path from render.ts importable.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Root.tsx for config-driven props and render.ts for pipeline-config.json</name>
  <files>services/remotion-renderer/src/Root.tsx, services/remotion-renderer/src/render.ts</files>
  <read_first>
    services/remotion-renderer/src/Root.tsx
    services/remotion-renderer/src/render.ts
    services/remotion-renderer/src/pipeline-config.ts
  </read_first>
  <acceptance_criteria>
    - "Root.tsx imports PipelineConfig from pipeline-config.ts"
    - "RemotionProps extends with subtitle config fields: subtitleLayout, subtitleConfig, titles"
    - "RemotionRoot defaultProps include subtitle layout mode 'tiktok' and empty titles array"
    - "render.ts reads pipeline-config.json using try/catch with fallback to env vars"
    - "render.ts passes PipelineConfig fields as inputProps to renderMedia"
    - "When pipeline-config.json is missing, render.ts logs warning and uses env var defaults (D-03)"
    - "Existing Subtitles composition still renders correctly with TikTok layout when no config provided"
  </acceptance_criteria>
  <action>
    Per D-01, D-02, D-03: Refactor Root.tsx and render.ts to accept PipelineConfig.

    **Root.tsx changes:**
    1. Import `PipelineConfig`, `SubtitleConfig`, `TitleConfig` from `./pipeline-config.ts`
    2. Extend `RemotionProps` interface to add:
       - `subtitleLayout?: SubtitleLayoutMode` (default: 'tiktok')
       - `subtitleConfig?: SubtitleConfig` (default: {})
       - `titles?: TitleConfig[]` (default: [])
    3. Update `defaultProps` in the Composition to include the new fields with sensible defaults matching current behavior
    4. The `SubtitledVideo` composition component temporarily receives these new props but continues rendering the existing TikTok layout unchanged — layout dispatching comes in Plan 02
    5. Keep all existing props (videoSrc, captionPages, etc.) unchanged for backward compatibility

    **render.ts changes:**
    1. Add `PIPELINE_CONFIG_PATH` environment variable pointing to pipeline-config.json path
    2. At startup, try to read pipeline-config.json using try/catch (following the established pattern for silence-cuts.json and finalizer-info.json)
    3. If pipeline-config.json exists and is valid JSON, call validatePipelineConfig and use its values
    4. If missing or invalid, log a warning and fall back to environment variable defaults (D-03)
    5. Merge pipeline config values with env var values: config takes precedence, env vars are fallback
    6. Build the complete inputProps object including the new PipelineConfig fields
    7. Write pipeline config info to remotion-info.json (for debugging)

    Per D-19: The renderer reads pipeline-config.json from `/data/pipeline/{job_id}/remotion-renderer/pipeline-config.json` — following the established step contract pattern of reading input artifacts from the shared volume.
  </action>
  <verify>
    <automated>cd services/remotion-renderer && npx vitest run src/pipeline-config.test.ts && grep -c "pipeline-config" src/render.ts && grep -c "subtitleLayout" src/Root.tsx</automated>
  </verify>
  <done>Root.tsx accepts PipelineConfig props. render.ts reads pipeline-config.json with env var fallback. Existing TikTok subtitle behavior preserved as default. All PipelineConfig fields passed through to composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → render.ts | Untrusted pipeline-config.json file input |
| render.ts → Remotion composition | Config values passed as inputProps |
| env vars → render.ts | System-level config (trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-01 | Tampering | pipeline-config.json | mitigate | ValidatePipelineConfig rejects malformed/invalid configs before passing to composition |
| T-06-02 | Denial of Service | render.ts | accept | Invalid config causes render failure with descriptive error message — expected behavior |
| T-06-03 | Tampering | env vars | accept | Env vars are infrastructure-level config, not user input |
</threat_model>

<verification>
1. All PipelineConfig tests pass: `cd services/remotion-renderer && npx vitest run src/pipeline-config.test.ts`
2. Root.tsx imports PipelineConfig: `grep "PipelineConfig" services/remotion-renderer/src/Root.tsx`
3. render.ts reads pipeline-config.json: `grep "pipeline-config" services/remotion-renderer/src/render.ts`
4. Existing render pipeline still works: run remotion-renderer with env vars only (no pipeline-config.json) and verify TikTok subtitles render
5. run remotion-renderer with a pipeline-config.json and verify config values appear in remotion-info.json
</verification>

<success_criteria>
- PipelineConfig schema covers all 20 decisions from CONTEXT.md (D-01 through D-20)
- validatePipelineConfig rejects invalid configs with specific error messages
- render.ts reads pipeline-config.json with try/catch and falls back to env vars when missing
- Root.tsx accepts new config props alongside existing subtitle props
- Existing TikTok subtitle rendering works unchanged when no pipeline-config.json is present
</success_criteria>

<output>
After completion, create `.planning/phases/06-animated-intros-outros/06-01-SUMMARY.md`
</output>