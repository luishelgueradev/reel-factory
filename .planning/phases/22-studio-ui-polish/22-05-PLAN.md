---
phase: 22-studio-ui-polish
plan: 05
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - services/remotion-studio/src/editor/components/StyleControls.tsx
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
  - services/remotion-studio/src/editor/components/OverlayEditor.tsx
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-11, D-03]
must_haves:
  truths:
    - "All three editors (Subtitles, Titles, Overlays) consume the shared PositionPresets 9-point grid"
    - "The old 3-button subtitle position presets are removed and replaced by the shared component without regressing the bottom-center/top-center/center-screen behavior"
    - "Titles and Overlays gain a working 9-point preset that writes x/y through their existing handleDraftChange path"
    - "OverlayEditor has a Capa (Detrás | Delante) segmented control writing the layer field, default Detrás"
    - "Each editor orders controls Posición → Estilo → Avanzado with the established dark-theme tokens; selection states are blue (green is Render-only)"
    - "Icon-only delete buttons carry Spanish aria-labels and 44px hit areas"
  artifacts:
    - path: "services/remotion-studio/src/editor/components/StyleControls.tsx"
      provides: "PositionPresets in Posición section; old 3-button presets removed"
      contains: "PositionPresets"
    - path: "services/remotion-studio/src/editor/components/TitleEditor.tsx"
      provides: "PositionPresets in Posición; Posición→Estilo→Avanzado ordering; aria-labeled delete"
      contains: "PositionPresets"
    - path: "services/remotion-studio/src/editor/components/OverlayEditor.tsx"
      provides: "PositionPresets + Capa layer control; aria-labeled delete"
      contains: "layer"
  key_links:
    - from: "StyleControls/TitleEditor/OverlayEditor"
      to: "PositionPresets.onApply"
      via: "onChange / handleDraftChange writing x/y (or position)"
      pattern: "PositionPresets[\\s\\S]*onApply"
    - from: "OverlayEditor Capa control"
      to: "PngOverlayConfig.layer"
      via: "handleDraftChange((prev) => ({...prev, layer}))"
      pattern: "layer: \"(back|front)\""
---

<objective>
Wire the shared `PositionPresets` (from 22-02) into all three editors, run the
full impeccable densification + Posición→Estilo→Avanzado reordering pass over
each editor (D-05/D-06/D-11), migrate the subtitle 3-button presets onto the
shared grid without regression (D-08), and add the OverlayEditor `Capa`
(Detrás | Delante) layer control that writes the `layer` field from 22-01
(D-03). Flip selection states from green to blue and add the required
accessible names (UI-SPEC).

Purpose: Deliver the dense, prioritized, deliberate control panel across the
whole right column — not one component — with the unified position affordance
and the overlay layer toggle.
Output: Three densified editors consuming PositionPresets; subtitle preset path
not regressed; overlay layer control live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-PATTERNS.md
@.planning/phases/22-studio-ui-polish/22-UI-SPEC.md
@.planning/phases/22-studio-ui-polish/22-02-SUMMARY.md
@.planning/phases/22-studio-ui-polish/22-03-SUMMARY.md

<interfaces>
<!-- PositionPresets (built in 22-02) — the LOCKED prop signature to consume -->
<PositionPresets
  elementWidth={number} elementHeight={number}
  frameWidth?={1080} frameHeight?={1920}
  onApply={(x, y) => void}
  activeAnchor?={string}
/>

<!-- StyleControls.tsx — remove these, replace with <PositionPresets> -->
POSITION_OPTIONS array (L17-21): bottom-center | top-center | center-screen
Position selector block (L185-213): the 3 green buttons writing onChange({ position })
<!-- Regression guard (D-08): subtitle position model is `config.position` (SubtitlePosition enum),
     NOT x/y. Map the 9-grid onto the existing position semantics OR keep the 3 working anchors
     wired through onChange({ position }); a nominal height estimate is acceptable for subtitles. -->

<!-- TitleEditor.tsx — Posición section is the X/Y inputs (L295-341); handleDraftChange L106-110 -->
onApply: (x,y) => handleDraftChange((prev) => ({ ...prev, style: { ...prev.style!, x, y } }))
title size input for elementWidth/Height estimate: style.titleFontSize (L426 default 72) + padding (L495 default 40)
existing x/y writers: L307-311 (x), L329-333 (y)
delete button needing aria-label: L228-233 (✕ → aria-label="Eliminar título")

<!-- OverlayEditor.tsx — X/Y inputs L300-338; handleDraftChange L72-76; DEFAULT_OVERLAY L27-33 -->
onApply: (x,y) => handleDraftChange((prev) => ({ ...prev, x, y }))
elementWidth = draft.displayWidth (L31 default 200); height auto → nominal estimate
DEFAULT_OVERLAY (L27-33): add `layer: "back"`
Capa control writes: handleDraftChange((prev) => ({ ...prev, layer: "back" | "front" }))
delete button needing aria-label: L220-225 (Delete → aria-label="Eliminar overlay")

<!-- Color law (UI-SPEC): selection/active = var(--accent) BLUE. Green (#4CAF50/var(--action)) is
     Render-CTA ONLY. Existing editors use green for selected segmented buttons — flip to blue. -->
<!-- Section ordering (D-11): Posición → Estilo → Avanzado, consistent across all three. -->
<!-- Overlays tab direction: 019-C list-forward inline cards (UI-SPEC § Overlays). Fallback 019-A
     (list + single-column form) IS permitted without further discussion if inline cards read
     off-pattern at build review (UI-SPEC Implementation Note 7). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke impeccable + frontend-design; migrate StyleControls subtitle presets to PositionPresets and reorder Posición→Estilo→Avanzado</name>
  <read_first>
    - AGENTS.md § "UI/frontend work — REQUIRED tooling" (impeccable + frontend-design — invoke FIRST)
    - services/remotion-studio/src/editor/components/StyleControls.tsx (full file — POSITION_OPTIONS L17-21, position block L185-213, all control rows)
    - services/remotion-studio/src/editor/components/PositionPresets.tsx (the component built in 22-02 + its prop signature)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Subtítulos tab" + § "Position Presets" + § "Color"
    - .planning/phases/22-studio-ui-polish/22-02-SUMMARY.md (PositionPresets contract as actually built)
  </read_first>
  <action>
    FIRST invoke `impeccable` + `frontend-design`. In StyleControls.tsx: remove the 3-button Position selector block (L185-213) and the now-unused `POSITION_OPTIONS` array (L17-21), and mount `<PositionPresets>` in a `Posición` section instead. REGRESSION GUARD (D-08): the subtitle position model is the `config.position` enum (bottom-center/top-center/center-screen), not free x/y — so wire `onApply` to map the chosen 9-grid anchor onto the nearest existing `position` value (keep `onChange({ position })` semantics) OR, if the grid cannot map cleanly, retain the three working anchors as the canonical writes through the same shared component; the working subtitle preview MUST NOT regress. Pass a nominal subtitle height estimate for `elementHeight` (subtitles are bottom-anchored). Reorder the StyleControls fields into Posición → Estilo → Avanzado titled sections per UI-SPEC (Posición: position presets + bottomOffset; Estilo: font family, size, weight, color matrix, glow; Avanzado: letterSpacing, lineHeight, pastWordOpacity, highlightDuration/transition, subtitleWidth, outlineWidth). Flip ALL selected/active segmented-button states from green (`#4CAF50` / `rgba(76,175,80,...)` / `#a5d6a7`) to the blue tokens (`var(--accent-tint)` bg, `var(--accent-strong)` border, `var(--accent)` text); slider `accentColor` may stay neutral/accent but NOT action-green. Use default.css tokens for surfaces/text/spacing. Keep all existing `onChange` field writes intact.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'PositionPresets' services/remotion-studio/src/editor/components/StyleControls.tsx` ≥ 1; `grep -c 'POSITION_OPTIONS' StyleControls.tsx` returns 0 (old array removed).
    - Regression guard: StyleControls still writes the subtitle position via `onChange({ position` (the working `config.position` path is preserved) — grep confirms a `position:` write remains reachable from the preset.
    - Color law: `grep -c '#4CAF50\|rgba(76, 175, 80' StyleControls.tsx` for SELECTED-state styling is 0 (selection flipped to blue); any remaining green is not a selection state.
    - Section order: the file contains `Posición`/`Estilo`/`Avanzado` section markers in that source order.
    - Build: `npm run build:editor` exits 0.
  </acceptance_criteria>
  <done>StyleControls uses the shared PositionPresets, old 3-button block + array gone, subtitle position path preserved, sections reordered, selection blue; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add PositionPresets + Posición→Estilo→Avanzado + aria-labels to TitleEditor</name>
  <read_first>
    - services/remotion-studio/src/editor/components/TitleEditor.tsx (full file — X/Y inputs L295-341, handleDraftChange L106-110, delete button L228-233, entrance/size/glow blocks)
    - services/remotion-studio/src/editor/components/PositionPresets.tsx (prop signature)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Títulos tab" + § "Accessible Name Contract" + § "Color"
  </read_first>
  <action>
    Mount `<PositionPresets>` adjacent to the X/Y inputs (L295-341) inside a `Posición` section, wired `onApply={(x, y) => handleDraftChange((prev) => ({ ...prev, style: { ...prev.style!, x, y } }))}`. Estimate `elementWidth`/`elementHeight` from `newTitle.style?.titleFontSize` (default 72) + `padding` (default 40) — a rendered-box estimate is acceptable. Reorder fields into Posición → Estilo → Avanzado: Posición = X/Y inputs + PositionPresets; Estilo = title text, font, size, weight/style, color pair, entrance animation, glow; Avanzado = borderRadius, lineHeight, padding, timing (start/duration). Add `aria-label="Eliminar título"` to the ✕/Delete button (L228-233) and ensure a 44×44px hit area; if a drag handle is introduced add `aria-label="Reordenar título"` + `role="button"` (drag reorder is optional this phase — only add the handle if you implement reordering, otherwise skip it). Flip selected segmented states (entrance animation buttons L349-372, weight/style toggles) from green to blue tokens. Keep all existing `handleDraftChange` field writes intact.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'PositionPresets' TitleEditor.tsx` ≥ 1 and it is wired with `onApply` writing `style: { ...prev.style!, x, y }`.
    - Source assertion: `grep -c 'aria-label="Eliminar título"' TitleEditor.tsx` returns 1.
    - Color law: SELECTED segmented states no longer use `#4CAF50`/`rgba(76, 175, 80`/`#a5d6a7` (flipped to blue var(--accent) tokens).
    - Section order: `Posición`/`Estilo`/`Avanzado` markers present in source order.
    - Build: `npm run build:editor` exits 0.
  </acceptance_criteria>
  <done>TitleEditor has a working 9-point preset writing style.x/y, reordered sections, aria-labeled delete, blue selection; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Add PositionPresets + Capa layer control + aria-labels to OverlayEditor</name>
  <read_first>
    - services/remotion-studio/src/editor/components/OverlayEditor.tsx (full file — DEFAULT_OVERLAY L27-33, handleDraftChange L72-76, X/Y inputs L300-338, delete button L220-225, MAX_OVERLAYS cap L22/L167/L416-439)
    - services/remotion-studio/src/editor/components/PositionPresets.tsx (prop signature)
    - .planning/phases/22-studio-ui-polish/22-01-SUMMARY.md (layer field shape) and 22-04-SUMMARY.md if present (how layer drives paint order — for the badge semantics)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Overlays tab" + § "Accessible Name Contract" + § "Color"
  </read_first>
  <action>
    Add `layer: "back"` to `DEFAULT_OVERLAY` (L27-33). Mount `<PositionPresets>` in the overlay form's position area, `elementWidth={draft.displayWidth}` (height auto → nominal estimate), `onApply={(x, y) => handleDraftChange((prev) => ({ ...prev, x, y }))}`. Add a `Capa` segmented control (2-way `Detrás | Delante`) mirroring the existing segmented-button pattern, writing `handleDraftChange((prev) => ({ ...prev, layer: "back" | "front" }))`; default selection reflects `draft.layer ?? "back"`. On each overlay list card, show a read-only layer badge: `Detrás` muted (`var(--text-muted)` / `var(--surface-2)`), `Delante` blue (`var(--accent)` / `var(--accent-tint)`). Add `aria-label="Eliminar overlay"` to the Delete button (L220-225) with a 44px hit area; if a drag handle is added for reorder, `aria-label="Reordenar overlay"` + `role="button"` (reorder optional this phase). Apply the impeccable density pass; prefer the UI-SPEC 019-C list-forward inline-card direction, but the 019-A fallback (list + single-column form) is explicitly permitted if inline cards read off-pattern (UI-SPEC Note 7) — pick one and keep it coherent with Titles/Subtitles. Flip selected segmented states green→blue. Preserve the 3-overlay cap, 5MB/PNG file gate, and all existing field writes.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'layer: "back"' OverlayEditor.tsx` ≥ 1 (DEFAULT_OVERLAY) and a Capa control writes both `layer: "back"` and `layer: "front"`.
    - Source assertion: `grep -c 'PositionPresets' OverlayEditor.tsx` ≥ 1 wired with `onApply` writing `{ ...prev, x, y }`.
    - Source assertion: `grep -c 'aria-label="Eliminar overlay"' OverlayEditor.tsx` returns 1.
    - Source assertion: a `Capa` label and `Detrás`/`Delante` strings are present.
    - Color law: SELECTED segmented states (incl. Capa) use blue tokens, not `#4CAF50`/green selection.
    - Cap preserved: `MAX_OVERLAYS = 3` and the `atCap` disabled add path are intact.
    - Build: `npm run build:editor` exits 0.
  </acceptance_criteria>
  <done>OverlayEditor has a 9-point preset, a Capa Detrás|Delante control writing layer (default Detrás), layer badges, aria-labeled delete, cap+file-gate preserved, blue selection; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → editor draft → PUT /api/config | Title/overlay/subtitle config edits (incl. new layer + preset-driven x/y) persist via the existing config save path. No new network surface; same boundary as Phase 20/21. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Tampering | preset-written x/y + layer | mitigate | x/y are bounded integers (existing number inputs clamp 0–1080/0–1920); `layer` is enum-validated at 22-01's validator before persist. |
| T-22-10 | Repudiation/Accessibility gap | icon-only delete/drag controls | mitigate | Required aria-labels added (Eliminar/Reordenar) per UI-SPEC Accessible Name Contract — keyboard/SR users get named controls. |
| T-22-11 | Information disclosure | editor UI | accept | Internal Studio screen behind existing basic auth; edits are local config only, no PII. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (consumes existing PositionPresets + React deps). |
</threat_model>

<verification>
- All three editors consume PositionPresets; subtitle path not regressed.
- OverlayEditor writes layer (default back) via Capa control; badges correct.
- Selection states blue; Render remains the only green.
- aria-labels present; build green.
</verification>

<success_criteria>
- The whole right-panel control surface is densified, reordered (Posición→Estilo→Avanzado), uses the shared 9-point presets, and the overlay layer toggle is wired — matching the UI-SPEC per-tab contracts.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-05-SUMMARY.md` when done.
</output>
