---
phase: "14-remotion-supersampling-quality-finalizer"
plan: 03
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
files_modified:
  - services/api-server/src/orchestrator.ts
  - docker-compose.yml
autonomous: false
requirements:
  - RENDER-03
  - RENDER-04

must_haves:
  truths:
    - "orchestrator.ts STEPS array contains a quality-finalizer entry after remotion-renderer and before srt-exporter"
    - "remotion-renderer step in STEPS has REMOTION_SCALE='2' and REMOTION_IMAGE_FORMAT='png' env vars added"
    - "quality-finalizer step in STEPS has INPUT_PATH pointing to remotion-renderer output and OUTPUT_PATH to quality-finalizer/output.mp4"
    - "videoUrl return value points to quality-finalizer/output.mp4 (not remotion-renderer/output.mp4)"
    - "docker-compose.yml has a quality-finalizer service after remotion-renderer with correct image, build context, and healthcheck"
    - "Step order comment in orchestrator.ts includes quality-finalizer"
    - "scale:2 render-time benchmark measured on the Phase 14 baseline.mp4 or phase-13.mp4 clip and recorded"
    - "A/V parity verified: ffprobe duration delta between remotion-renderer output and quality-finalizer output is within ±33ms"
  artifacts:
    - path: "services/api-server/src/orchestrator.ts"
      provides: "Pipeline STEPS with quality-finalizer wired and REMOTION_SCALE=2 active"
      contains: "quality-finalizer"
    - path: "docker-compose.yml"
      provides: "quality-finalizer service definition"
      contains: "reel-factory-quality-finalizer"
  key_links:
    - from: "orchestrator.ts STEPS remotion-renderer.envVars"
      to: "REMOTION_SCALE: '2'"
      via: "envVars object"
      pattern: "REMOTION_SCALE.*2"
    - from: "orchestrator.ts"
      to: "videoUrl: /artifacts/${jobId}/quality-finalizer/output.mp4"
      via: "return statement"
      pattern: "quality-finalizer/output.mp4"
    - from: "docker-compose.yml quality-finalizer service"
      to: "reel-factory-quality-finalizer image"
      via: "build: context"
      pattern: "quality-finalizer"
---

<objective>
Wire the quality-finalizer step into the pipeline: add it to orchestrator.ts STEPS after remotion-renderer, enable REMOTION_SCALE=2 + PNG on the remotion-renderer step, repoint videoUrl, and register the quality-finalizer service in docker-compose.yml. Then run a benchmark checkpoint to measure scale:2 render time and verify A/V parity on the output.

Purpose: RENDER-03 requires quality-finalizer to be an active pipeline step. RENDER-04 requires the render-time impact to be measured and documented. The co-dependency constraint (D-01 / key_design_constraints) requires scale:2 and quality-finalizer to activate together — this plan is waved after both Plan 01 (render.ts) and Plan 02 (container) so the orchestrator changes that "turn on" scale:2 are not active before the downscale step exists.

Output: A fully wired pipeline where POST /process runs remotion-renderer at scale:2 with PNG frames, passes the 2160x3840 output through quality-finalizer, and returns a 1080x1920 MP4 to the caller. Benchmark artifact recorded under .planning/phases/14-remotion-supersampling-quality-finalizer/uat/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-remotion-supersampling-quality-finalizer/14-CONTEXT.md
@.planning/phases/14-remotion-supersampling-quality-finalizer/14-PATTERNS.md
@.planning/phases/14-remotion-supersampling-quality-finalizer/14-01-SUMMARY.md
@.planning/phases/14-remotion-supersampling-quality-finalizer/14-02-SUMMARY.md
</context>

<interfaces>
<!-- orchestrator.ts STEPS array current shape — confirmed from file read at lines 56-117 -->
<!--
export const STEPS: PipelineStepConfig[] = [
  { name: "whisper", ... },
  { name: "silence-cutter", ... },
  { name: "ffmpeg-finalizer", ... },
  {
    name: "remotion-renderer",
    image: "reel-factory-remotion-renderer",
    envVars: {
      INPUT_PATH: "/data/pipeline/{jobId}/ffmpeg-finalizer/output.mp4",
      OUTPUT_PATH: "/data/pipeline/{jobId}/remotion-renderer/output.mp4",
      PIPELINE_JOB_ID: "{jobId}",
      TRANSCRIPT_PATH: "/data/pipeline/{jobId}/whisper/transcript.json",
      SILENCE_CUTS_PATH: "/data/pipeline/{jobId}/silence-cutter/silence-cuts.json",
      FINALIZER_INFO_PATH: "/data/pipeline/{jobId}/ffmpeg-finalizer/finalizer-info.json",
      ACTIVE_COLOR: "#FFFF00",
      INACTIVE_COLOR: "#FFFFFF",
      FONT_SIZE: "58",
      // ADD: REMOTION_SCALE: "2", REMOTION_IMAGE_FORMAT: "png"
    },
  },
  // INSERT quality-finalizer entry HERE, before srt-exporter
  { name: "srt-exporter", ... },
];
-->

<!-- videoUrl current value — orchestrator.ts line 331 -->
<!--
videoUrl: `/artifacts/${jobId}/remotion-renderer/output.mp4`,
// CHANGE TO:
videoUrl: `/artifacts/${jobId}/quality-finalizer/output.mp4`,
-->

<!-- Step order comment — orchestrator.ts line 53 -->
<!--
// Step order: whisper → silence-cutter → ffmpeg-finalizer → remotion-renderer → srt-exporter
// CHANGE TO:
// Step order: whisper → silence-cutter → ffmpeg-finalizer → remotion-renderer → quality-finalizer → srt-exporter
-->

<!-- docker-compose.yml service pattern (confirmed from ffmpeg-finalizer service at line 241): -->
<!--
quality-finalizer:
  profiles: ["pipeline"]
  image: reel-factory-quality-finalizer:latest
  build:
    context: ./services/quality-finalizer
  volumes:
    - pipeline-data:/data/pipeline
  environment:
    - INPUT_PATH=/data/pipeline/${PIPELINE_JOB_ID}/remotion-renderer/output.mp4
    - OUTPUT_PATH=/data/pipeline/${PIPELINE_JOB_ID}/quality-finalizer/output.mp4
    - PIPELINE_JOB_ID=${PIPELINE_JOB_ID}
  depends_on:
    remotion-renderer:
      condition: service_completed_successfully
  healthcheck:
    test: ["CMD", "test", "-f", "/data/pipeline/${PIPELINE_JOB_ID}/quality-finalizer/manifest.json"]
    interval: 10s
    timeout: 5s
    retries: 30
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire quality-finalizer into orchestrator.ts and docker-compose.yml</name>
  <files>
    services/api-server/src/orchestrator.ts,
    docker-compose.yml
  </files>

  <read_first>
    - services/api-server/src/orchestrator.ts — read lines 49-120 (STEPS array) and lines 320-335 (return statement with videoUrl); confirm exact current structure before editing
    - docker-compose.yml — read lines 98-125 (remotion-renderer service environment block) and the surrounding service definitions to locate the insertion point for quality-finalizer; the remotion-renderer service uses a static environment list with ${VAR:-default} patterns (e.g. ACTIVE_COLOR=${ACTIVE_COLOR:-#FFFF00}, FONT_SIZE=${FONT_SIZE:-58})
    - .planning/phases/14-remotion-supersampling-quality-finalizer/14-PATTERNS.md — §orchestrator.ts for the exact STEPS entry to insert and the videoUrl repoint; confirm env var names and path templates
    - .planning/phases/14-remotion-supersampling-quality-finalizer/14-CONTEXT.md — D-06 (REMOTION_SCALE and REMOTION_IMAGE_FORMAT must be set explicitly by orchestrator), D-07 (defaults in render.ts are scale=1+JPEG so the orchestrator MUST set these to get scale:2 behavior), D-08 (quality-finalizer INPUT_PATH = remotion-renderer output)
    - .planning/phases/14-remotion-supersampling-quality-finalizer/14-02-SUMMARY.md — confirm the quality-finalizer image name and service structure produced by Plan 02
  </read_first>

  <action>
    In orchestrator.ts:
    1. Update the step-order comment at ~line 53 to read: "Step order: whisper → silence-cutter → ffmpeg-finalizer → remotion-renderer → quality-finalizer → srt-exporter"
    2. In the remotion-renderer STEPS entry, add two env vars after FONT_SIZE: REMOTION_SCALE: "2" and REMOTION_IMAGE_FORMAT: "png". Add a comment above these two vars: "// Phase 14 (D-06, D-07): Enable scale:2 supersampling + lossless PNG frame capture in the pipeline."
    3. Insert a new STEPS entry for quality-finalizer immediately after the remotion-renderer entry and before srt-exporter. Entry fields: name: "quality-finalizer", image: "reel-factory-quality-finalizer", envVars: { INPUT_PATH: "/data/pipeline/{jobId}/remotion-renderer/output.mp4", OUTPUT_PATH: "/data/pipeline/{jobId}/quality-finalizer/output.mp4", PIPELINE_JOB_ID: "{jobId}" }. Add a comment above the entry: "// Phase 14 (RENDER-03): Lanczos-downscale 2160x3840 supersampled output to deliverable 1080x1920."
    4. Update the videoUrl in the return statement from "/artifacts/${jobId}/remotion-renderer/output.mp4" to "/artifacts/${jobId}/quality-finalizer/output.mp4".

    In docker-compose.yml:
    5. In the remotion-renderer service's existing static environment block (lines ~98-125), add two lines using the same ${VAR:-default} style as the existing entries: "- REMOTION_SCALE=${REMOTION_SCALE:-2}" and "- REMOTION_IMAGE_FORMAT=${REMOTION_IMAGE_FORMAT:-png}". Place them after the FONT_SIZE line to mirror the orchestrator.ts ordering. This keeps the docker-compose direct-run path consistent with the orchestrator path.
    6. Add a new quality-finalizer service entry after remotion-renderer. Use the service pattern in the <interfaces> block above: profiles ["pipeline"], image reel-factory-quality-finalizer:latest, build.context ./services/quality-finalizer, volumes pipeline-data:/data/pipeline, environment with INPUT_PATH/OUTPUT_PATH/PIPELINE_JOB_ID, depends_on remotion-renderer condition service_completed_successfully, and healthcheck testing for /data/pipeline/${PIPELINE_JOB_ID}/quality-finalizer/manifest.json (interval: 10s, timeout: 5s, retries: 30).

    Do NOT modify any other existing service entries in docker-compose.yml. Do NOT modify whisper, silence-cutter, ffmpeg-finalizer, or srt-exporter STEPS entries in orchestrator.ts.
  </action>

  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/api-server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>
    - orchestrator.ts TypeScript compiles with no errors
    - grep "REMOTION_SCALE" services/api-server/src/orchestrator.ts returns a line with value "2"
    - grep "quality-finalizer" services/api-server/src/orchestrator.ts returns at least 3 matches (step entry name, INPUT_PATH comment, videoUrl)
    - grep "quality-finalizer/output.mp4" services/api-server/src/orchestrator.ts returns a match containing videoUrl
    - grep "quality-finalizer" docker-compose.yml returns at least 3 matches (service name, image, build context)
    - grep "REMOTION_SCALE" docker-compose.yml returns a match under the remotion-renderer environment block (the line reads "- REMOTION_SCALE=${REMOTION_SCALE:-2}")
    - grep "REMOTION_IMAGE_FORMAT" docker-compose.yml returns a match under the remotion-renderer environment block (the line reads "- REMOTION_IMAGE_FORMAT=${REMOTION_IMAGE_FORMAT:-png}")
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 modified render.ts to read REMOTION_SCALE/REMOTION_IMAGE_FORMAT from env and raise the timeout to 3 hours.
    Plan 02 created the quality-finalizer Docker container (Dockerfile + main.py + src/).
    Plan 03 Task 1 wired quality-finalizer into orchestrator.ts STEPS and docker-compose.yml, and enabled REMOTION_SCALE=2 for the pipeline.

    Now: build both images and run a scale:2 render of the Phase 14 benchmark clip to measure render time and verify A/V parity.
  </what-built>

  <how-to-verify>
    Before you start: the benchmark clip is at .planning/phases/13-encode-quality/uat/phase-13.mp4 (the Phase 13 UAT 60s clip reused per D-02). The baseline.mp4 at .planning/phases/13-encode-quality/uat/baseline.mp4 is the pre-Phase-13 reference.

    Step 1 — Build both images:
      docker build -t reel-factory-remotion-renderer:latest services/remotion-renderer/
      docker build -t reel-factory-quality-finalizer:latest services/quality-finalizer/

    Step 2 — Set up a test job directory on the pipeline volume (adjust PIPELINE_DATA_DIR to your local volume path, typically /home/luis/proyectos/reel-factory/data/pipeline or the named volume mountpoint):
      JOBID=benchmark-phase14
      mkdir -p $PIPELINE_DATA_DIR/$JOBID/remotion-renderer
      mkdir -p $PIPELINE_DATA_DIR/$JOBID/quality-finalizer
      # Copy benchmark clip to simulate ffmpeg-finalizer output:
      cp .planning/phases/13-encode-quality/uat/phase-13.mp4 \
         $PIPELINE_DATA_DIR/$JOBID/ffmpeg-finalizer/output.mp4
      # Also need transcript.json — copy from any prior run or generate a minimal one

    Step 3 — Run remotion-renderer at scale:2 (time it):
      time docker run --rm \
        -v $PIPELINE_DATA_DIR:/data/pipeline \
        -e INPUT_PATH=/data/pipeline/$JOBID/ffmpeg-finalizer/output.mp4 \
        -e OUTPUT_PATH=/data/pipeline/$JOBID/remotion-renderer/output.mp4 \
        -e PIPELINE_JOB_ID=$JOBID \
        -e TRANSCRIPT_PATH=/data/pipeline/$JOBID/whisper/transcript.json \
        -e REMOTION_SCALE=2 \
        -e REMOTION_IMAGE_FORMAT=png \
        reel-factory-remotion-renderer:latest

    Step 4 — Verify remotion-renderer output is 2160x3840:
      ffprobe -v quiet -show_entries stream=width,height \
        $PIPELINE_DATA_DIR/$JOBID/remotion-renderer/output.mp4

      Expected: width=2160 height=3840

    Step 5 — Run quality-finalizer:
      docker run --rm \
        -v $PIPELINE_DATA_DIR:/data/pipeline \
        -e INPUT_PATH=/data/pipeline/$JOBID/remotion-renderer/output.mp4 \
        -e OUTPUT_PATH=/data/pipeline/$JOBID/quality-finalizer/output.mp4 \
        -e PIPELINE_JOB_ID=$JOBID \
        reel-factory-quality-finalizer:latest

    Step 6 — Verify quality-finalizer output is 1080x1920 with BT.709 tags:
      ffprobe -v quiet -show_entries stream=width,height,color_space,color_primaries,color_transfer \
        -of json $PIPELINE_DATA_DIR/$JOBID/quality-finalizer/output.mp4

      Expected: width=1080 height=1920 color_space=bt709 color_primaries=bt709 color_transfer=bt709

    Step 7 — Verify A/V parity (±33ms duration tolerance, D-10):
      # Get duration of remotion-renderer input and quality-finalizer output
      ffprobe -v quiet -show_entries format=duration -of csv=p=0 \
        $PIPELINE_DATA_DIR/$JOBID/remotion-renderer/output.mp4
      ffprobe -v quiet -show_entries format=duration -of csv=p=0 \
        $PIPELINE_DATA_DIR/$JOBID/quality-finalizer/output.mp4
      # Difference must be <= 0.033 seconds

    Step 8 — Subjective sharpness check (success criterion #1):
      Open $PIPELINE_DATA_DIR/$JOBID/quality-finalizer/output.mp4 in your video player.
      Compare subtitle text sharpness with .planning/phases/13-encode-quality/uat/baseline.mp4.
      The subtitle text edges should be visibly crisper/smoother.

    Record in .planning/phases/14-remotion-supersampling-quality-finalizer/uat/14-UAT.md:
      - scale:2 remotion-renderer wall-clock time (from Step 3 `time` output)
      - scale:1 baseline time (from Phase 13 UAT or prior measurement) for comparison
      - remotion-renderer output resolution (Step 4 ffprobe)
      - quality-finalizer output resolution + color tags (Step 6 ffprobe)
      - Duration delta ms (Step 7 calculation)
      - Subjective sharpness assessment (Step 8)
  </how-to-verify>

  <resume-signal>
    Type "approved" when:
    - quality-finalizer output is confirmed 1080x1920 with color_space=bt709
    - Duration delta is within ±33ms
    - Render time documented in 14-UAT.md
    - Subjective sharpness is acceptable (or describe what you see)

    If quality-finalizer output is NOT 1080x1920, describe the actual dimensions.
    If color tags are missing, describe what ffprobe shows.
    If render time is unacceptably long, note the measured value — per D-01, do NOT automatically downgrade to scale:1.5; instead report the measurement and we will decide together.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator.ts envVars → Docker container env | REMOTION_SCALE="2" injected into renderer container; no external input surface |
| videoUrl return value | Client-facing artifact URL points to quality-finalizer output; path constructed from jobId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | jobId used in videoUrl path construction | accept | jobId is generated internally (UUID) by the API server; not directly from user input to URL construction. Existing orchestrator already uses the same jobId pattern for all other artifact URLs. Risk level unchanged from prior phases. |
| T-14-03-02 | Denial of Service | REMOTION_SCALE=2 dramatically increases render time; concurrent jobs could exhaust disk/CPU | mitigate | BullMQ concurrency settings (Phase 10) limit concurrent pipeline runs. quality-finalizer downscale has subprocess timeout=600s. The 3h Remotion timeout is a ceiling, not an expectation. Existing resource controls apply. |
| T-14-03-03 | Information Disclosure | docker-compose.yml static env vars leak scale value to any user who reads compose file | accept | docker-compose.yml is a developer file in a private repo; not a user-facing credential. Scale value has no security sensitivity. |
| T-14-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan. orchestrator.ts and docker-compose.yml changes are configuration only. No package.json modifications. |
</threat_model>

<verification>
1. `cd services/api-server && npx tsc --noEmit` — must pass with zero errors
2. `grep -c "quality-finalizer" services/api-server/src/orchestrator.ts` — returns >= 3
3. `grep "videoUrl" services/api-server/src/orchestrator.ts` — line contains "quality-finalizer/output.mp4"
4. `grep "REMOTION_SCALE" services/api-server/src/orchestrator.ts` — line contains "2"
5. `grep -c "quality-finalizer" docker-compose.yml` — returns >= 3
6. `grep "reel-factory-quality-finalizer" docker-compose.yml` — match found
7. `grep "REMOTION_SCALE" docker-compose.yml` — match found under remotion-renderer environment block
8. `grep "REMOTION_IMAGE_FORMAT" docker-compose.yml` — match found under remotion-renderer environment block
9. Human checkpoint: quality-finalizer output confirmed 1080x1920 + color_space=bt709 + duration delta ≤ 33ms + benchmark time recorded in 14-UAT.md
</verification>

<success_criteria>
- orchestrator.ts compiles cleanly with TypeScript
- Pipeline order comment updated to include quality-finalizer
- STEPS array has quality-finalizer entry between remotion-renderer and srt-exporter
- remotion-renderer STEPS entry has REMOTION_SCALE: "2" and REMOTION_IMAGE_FORMAT: "png"
- videoUrl points to quality-finalizer/output.mp4
- docker-compose.yml remotion-renderer environment block has REMOTION_SCALE=${REMOTION_SCALE:-2} and REMOTION_IMAGE_FORMAT=${REMOTION_IMAGE_FORMAT:-png}
- docker-compose.yml has quality-finalizer service with build context, healthcheck, and depends_on remotion-renderer
- Benchmark measured: scale:2 render time documented in .planning/phases/14-remotion-supersampling-quality-finalizer/uat/14-UAT.md
- quality-finalizer output verified: 1080x1920, color_space=bt709, A/V duration delta ≤ 33ms
- Subjective sharpness assessment: subtitle text visibly crisper than baseline (success criterion #1)
</success_criteria>

<output>
Create `.planning/phases/14-remotion-supersampling-quality-finalizer/14-03-SUMMARY.md` when done.
</output>
