---
phase: 13-encode-quality
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - services/ffmpeg-finalizer/tests/test_finalizer.py
  - services/ffmpeg-finalizer/tests/test_encode_quality.py
  - services/silence-cutter/tests/test_silence_cutter.py
autonomous: true
requirements:
  - ENC-01
  - ENC-02
  - ENC-03
  - ENC-04
  - ENC-05

must_haves:
  truths:
    - "Existing test_finalizer.py fixtures use h264_crf=18 wherever the test exercises validation; tests pass under the new CRF 18 invariant"
    - "A new test_encode_quality.py drives a real silence-cutter→finalizer run on a synthetic fixture and asserts: stream-copy concat (no fresh encoder tag), bitrate 5000-8000 kbps, color_space/primaries/transfer all bt709, duration parity within ±33ms"
    - "test_silence_cutter.py optionally exercises validate_concat_mode against a real ffmpeg stream-copy output, returning [] (or a single explicit skip when ffmpeg is unavailable)"
    - "All pytest suites for ffmpeg-finalizer and silence-cutter exit 0 after this plan"
  artifacts:
    - path: "services/ffmpeg-finalizer/tests/test_finalizer.py"
      provides: "CRF-18-aware fixtures + ENC-02 assertion"
      contains: "h264_crf == 18"
    - path: "services/ffmpeg-finalizer/tests/test_encode_quality.py"
      provides: "End-to-end ffprobe-based encode-quality assertions"
      contains: "validate_color_tags"
    - path: "services/silence-cutter/tests/test_silence_cutter.py"
      provides: "validate_concat_mode exercised against a stream-copy fixture"
      contains: "validate_concat_mode"
  key_links:
    - from: "services/ffmpeg-finalizer/tests/test_encode_quality.py"
      to: "services/ffmpeg-finalizer/src/validate.py"
      via: "imports validate_color_tags, validate_bitrate_range, validate_duration_parity"
      pattern: "from src.validate import"
    - from: "services/ffmpeg-finalizer/tests/test_encode_quality.py"
      to: "ffmpeg binary"
      via: "subprocess.run(['ffmpeg', ...]) to synthesize a tiny fixture clip"
      pattern: "subprocess.run"
---

<objective>
Make the Phase 13 source-side changes test-enforceable. Sweep CRF fixture values in `test_finalizer.py` from 20 → 18 wherever validation is driven; assert config.H264_CRF == 18 (ENC-02). Add a new pytest module `test_encode_quality.py` that synthesizes a tiny real fixture clip, runs both the silence-cutter concat AND the ffmpeg-finalizer against it, and uses the three new ffprobe-based validators to assert color tags, bitrate range, and duration parity. Wire `validate_concat_mode` into `test_silence_cutter.py` with a parallel real-ffmpeg fixture, gracefully skipping when ffmpeg isn't on PATH.

Purpose: Closes the verification half of every ENC requirement. Without these tests the source-side changes from Plans 01 and 02 are unverified — a future regression could silently restore CRF 20, drop BT.709 tags, or revert stream-copy.

Output: Updated existing test module + one new test module + an extension to the silence-cutter test module. No source-code changes outside `tests/` directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-encode-quality/13-CONTEXT.md
@.planning/phases/13-encode-quality/13-PATTERNS.md
@services/ffmpeg-finalizer/tests/test_finalizer.py
@services/silence-cutter/tests/test_silence_cutter.py
@services/ffmpeg-finalizer/src/validate.py
@services/ffmpeg-finalizer/src/crop.py
@services/silence-cutter/src/validate.py
@services/silence-cutter/src/cut_video.py

<interfaces>
<!-- Key validator signatures Plan 03 consumes (introduced by Plan 01 + Plan 02) -->

From services/ffmpeg-finalizer/src/validate.py (post Plan 02):
- validate_finalizer_info(info_data: dict) -> List[str]  (existing, CRF check updated to 18 / ENC-02)
- validate_crop_logic(info_data: dict) -> List[str]      (existing, unchanged)
- validate_color_tags(output_mp4_path: str) -> List[str]                                   (NEW — ENC-03/D-10)
- validate_bitrate_range(output_mp4_path: str, min_kbps: int = 5000, max_kbps: int = 8000) -> List[str]  (NEW — ENC-02/D-11)
- validate_duration_parity(silence_cutter_output: str, finalizer_output: str, tolerance_ms: int = 33) -> List[str]  (NEW — ENC-05/D-11/D-14)

From services/silence-cutter/src/validate.py (post Plan 01):
- validate_silence_cuts(cut_list_data: dict) -> List[str]       (existing, unchanged)
- validate_cross_reference_logic(...) -> List[str]              (existing, unchanged)
- validate_concat_mode(concat_output_path: str) -> List[str]    (NEW — ENC-01/D-01)

From services/ffmpeg-finalizer/src/crop.py (post Plan 02):
- probe_video(input_path: str) -> dict                                          (existing, unchanged)
- compute_crop(input_width, input_height, target_width, target_height) -> Tuple (existing, unchanged)
- apply_finalizer(input_path, output_path, target_width, target_height, probe_info: dict) -> dict
  (existing; return dict now includes lanczos_scaling, unsharp_filter, color_space, color_primaries, color_transfer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep h264_crf=20 to 18 across test_finalizer.py fixtures and update TestConfig (ENC-02, D-03)</name>
  <files>services/ffmpeg-finalizer/tests/test_finalizer.py</files>
  <read_first>
    - services/ffmpeg-finalizer/tests/test_finalizer.py (lines 1-19 module docstring with current D-XX list; lines 38-91 valid_finalizer_info_dict + no_crop_info_dict fixtures with h264_crf at lines 54, 82; lines 98-121 TestConfig with the test_encoding_params docstring at lines 113-116; lines 196-272 TestSchema with h264_crf at lines 211, 234, 268; lines 279-376 TestValidation with h264_crf at lines 294, 308, 322)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"Fixture-update locations" — the exhaustive line-number sweep list)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-03 CRF 18 + ENC-02 traceability)
  </read_first>
  <behavior>
    - `pytest services/ffmpeg-finalizer/tests/test_finalizer.py -x -q` exits 0 after the sweep.
    - TestConfig.test_encoding_params asserts `config.H264_CRF == 18` with comment `(ENC-02 / D-03)`.
    - At least 5 h264_crf fixture literals were changed from 20 to 18 across the file (the validation-driving ones per PATTERNS.md).
    - The two test fixtures at lines 308 and 322 inside test_validate_crop_not_center and test_validate_missing_safe_zone — where CRF is incidental, not asserted on — are NOT swept (PATTERNS.md guidance preserves the existing convention that incidental fixture values can remain). They CAN be swept for uniformity, but the planner-locked criterion is on the validation-driving fixtures only.
    - One new TestConfig assertion confirms H264_PRESET still equals "medium" (D-04 invariant — already present at line 116; verify it's unchanged).
  </behavior>
  <action>
    In services/ffmpeg-finalizer/tests/test_finalizer.py:

    PART A — update module docstring (lines 1-19):
    1. Append `ENC-01`, `ENC-02`, `ENC-03`, `ENC-04`, `ENC-05` to the "Referenced requirements:" list.
    2. Update the existing `- D-08: H.264 CRF 20` line to `- D-08 (superseded): originally H.264 CRF 20 — see ENC-02 / D-03 for Phase 13 update to 18`.
    3. Add a `- D-03 (Phase 13): H.264 CRF 18` line (note: D-03 is reused between phases — this is the Phase 13 D-03, not the Phase 4 D-03; the Phase 4 D-03 entry at line 12 stays).

    PART B — update fixtures (mandatory sweep):
    1. Line 54 inside `valid_finalizer_info_dict`: change `"h264_crf": 20,` to `"h264_crf": 18,`.
    2. Line 82 inside `no_crop_info_dict`: change `"h264_crf": 20,` to `"h264_crf": 18,`.
    3. Line 211 inside `test_finalizer_info_with_crop`: change `h264_crf=20,` to `h264_crf=18,`.
    4. Line 234 inside `test_finalizer_info_without_crop`: change `h264_crf=20,` to `h264_crf=18,`.
    5. Line 268 inside `test_crop_applied_field_exists`: change `h264_crf=20,` to `h264_crf=18,`.
    6. Line 294 inside `test_validate_wrong_dimensions`: change `"h264_crf": 20,` to `"h264_crf": 18,` (this dict is fed to validate_finalizer_info, which now enforces 18 — leaving it at 20 would produce an ENC-02 error that masks the VERT-01 assertion the test is making).

    PART C — update TestConfig.test_encoding_params (lines 113-116):
    1. Change docstring from `"""H264_CRF=20 (D-08), H264_PRESET='medium' (D-09)."""` to `"""H264_CRF=18 (ENC-02 / D-03), H264_PRESET='medium' (D-04 / Phase 4 D-09 preserved)."""`.
    2. Change `assert config.H264_CRF == 20  # D-08` to `assert config.H264_CRF == 18  # ENC-02 / D-03`.
    3. Keep `assert config.H264_PRESET == "medium"  # D-09` (still valid — Phase 4 D-09 is preserved by Phase 13 D-04).

    PART D — optional sweep (planner discretion: PATTERNS.md flags these as incidental):
    1. Lines 308 and 322 inside `test_validate_crop_not_center` and `test_validate_missing_safe_zone` MAY be swept from 20 to 18 for uniformity. If swept, no test behavior changes (CRF is not asserted in these tests). If left at 20, the tests still pass because the assertions target VERT-02 and VERT-03, not CRF. PATTERNS.md line 354 explicitly notes "keep as-is (test asserts on crop_strategy, CRF is irrelevant)" for line 308. Default: leave 308 and 322 at 20.

    Do not modify TestComputeCrop class (D-01/D-03/VERT-02 — unrelated to Phase 13).
    Do not modify the imports at lines 21-31.
  </action>
  <verify>
    <automated>cd services/ffmpeg-finalizer &amp;&amp; grep -cE '"h264_crf":\s*18' tests/test_finalizer.py | grep -qE '^[3-9]$|^[1-9][0-9]+$' &amp;&amp; grep -cE 'assert\s+config\.H264_CRF\s*==\s*18' tests/test_finalizer.py | grep -q '^1$' &amp;&amp; grep -cE 'assert\s+config\.H264_CRF\s*==\s*20' tests/test_finalizer.py | grep -q '^0$' &amp;&amp; grep -cF 'ENC-02' tests/test_finalizer.py | grep -q '^[1-9]' &amp;&amp; python -m pytest tests/test_finalizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE '"h264_crf":\s*18' services/ffmpeg-finalizer/tests/test_finalizer.py` returns at least `3` (lines 54, 82, 294 at minimum).
    - Source assertion: `grep -cE 'h264_crf=18' services/ffmpeg-finalizer/tests/test_finalizer.py` returns at least `3` (lines 211, 234, 268).
    - Source assertion: `grep -cE 'assert\s+config\.H264_CRF\s*==\s*18' services/ffmpeg-finalizer/tests/test_finalizer.py` returns exactly `1` (the updated TestConfig assertion).
    - Source assertion: `grep -cE 'assert\s+config\.H264_CRF\s*==\s*20' services/ffmpeg-finalizer/tests/test_finalizer.py` returns `0` (old assertion removed).
    - Source assertion: `grep -F 'ENC-02' services/ffmpeg-finalizer/tests/test_finalizer.py` returns at least one match (traceability comment).
    - Test command: `pytest services/ffmpeg-finalizer/tests/test_finalizer.py -x -q` exits 0.
  </acceptance_criteria>
  <done>All validation-driving CRF fixtures use 18; TestConfig asserts H264_CRF == 18 with ENC-02/D-03 traceability; the existing test suite passes. Plan 02's validate_finalizer_info CRF check (enforcing 18) is now exercised by test_validate_valid_info via valid_finalizer_info_dict.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_encode_quality.py — end-to-end ffprobe assertions on a real fixture clip (ENC-02, ENC-03, ENC-05)</name>
  <files>services/ffmpeg-finalizer/tests/test_encode_quality.py</files>
  <read_first>
    - services/ffmpeg-finalizer/tests/test_finalizer.py (lines 1-31 — module docstring + sys.path insertion + imports — copy the pattern for the new module; also lines 38-91 for the conftest/fixture style)
    - services/ffmpeg-finalizer/src/validate.py (post Plan 02 — the three new validator signatures and their return contracts)
    - services/ffmpeg-finalizer/src/crop.py (lines 35-71 probe_video; lines 107-202 apply_finalizer — to drive a real finalizer run in-process)
    - services/silence-cutter/src/cut_video.py (lines 200-237 _concatenate_segments — to drive a real concat in-process for the duration-parity test)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-11 verification protocol; D-14 -c copy edge cases — variable keyframe spacing + audio-shorter-than-video)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"E2E ffprobe test pattern" — planner picks pytest module OR shell-script approach; this plan picks pytest module per CONTEXT.md D-11 Claude's Discretion)
    - scripts/test-ffmpeg-finalizer.sh (lines 36-67 — the pattern for synthesizing tiny ffmpeg test fixtures via `ffmpeg -f lavfi -i testsrc=...`)
  </read_first>
  <behavior>
    - A new pytest module `tests/test_encode_quality.py` exists in `services/ffmpeg-finalizer/`.
    - The module defines a session-scoped pytest fixture that uses `subprocess.run(["ffmpeg", "-f", "lavfi", ...])` to synthesize a 5-second 1920×1080 test source MP4 in a tempdir; if `ffmpeg` is not on PATH, the fixture calls `pytest.skip("ffmpeg not available on host")`.
    - Test 1 (`test_finalizer_color_tags_are_bt709`): runs `apply_finalizer` against the synthesized source, then calls `validate_color_tags(output_path)` and asserts `errors == []`. Maps to ENC-03 / D-10.
    - Test 2 (`test_finalizer_bitrate_in_band`): same finalizer run; calls `validate_bitrate_range(output_path, min_kbps=2000, max_kbps=15000)` and asserts `errors == []`. The band is wider than the production 5000-8000 (the production band targets a 60s talking-head fixture; the synthetic 5s lavfi testsrc has different content statistics, so the test uses a relaxed band that is still tight enough to catch a misconfigured encode that would produce e.g. <500 kbps or >50000 kbps). Add a docstring noting why the band is relaxed for the synthetic fixture and that the production 5000-8000 band is exercised in the visual A/B (Plan 04).
    - Test 3 (`test_finalizer_unsharp_and_lanczos_in_manifest`): inspects the dict returned by `apply_finalizer` directly and asserts `result["lanczos_scaling"] is True`, `result["unsharp_filter"] == "5:5:0.5:5:5:0.3"`, `result["color_space"] == "bt709"`, `result["color_primaries"] == "bt709"`, `result["color_transfer"] == "bt709"`. Maps to ENC-04 manifest fields.
    - Test 4 (`test_duration_parity_after_finalizer`): synthesizes a second tempfile by running the finalizer on the first; calls `validate_duration_parity(source_mp4, finalizer_output, tolerance_ms=100)` (relaxed tolerance for the synthetic fixture because lavfi testsrc duration rounding can differ by a few frames vs the production 33ms). Asserts `errors == []`. Maps to ENC-05 / D-11 / D-14.
    - All tests skip gracefully if `ffmpeg` or `ffprobe` are missing from PATH (use `shutil.which` checks).
    - `pytest tests/test_encode_quality.py -x -q` exits 0 (passing or all-skipped) on a host with ffmpeg available.
  </behavior>
  <action>
    Create services/ffmpeg-finalizer/tests/test_encode_quality.py with this structure:

    1. Module docstring matching test_finalizer.py style (lines 1-19): list referenced requirements ENC-02, ENC-03, ENC-04, ENC-05 and the D-XX decisions enforced (D-03, D-05, D-06, D-08, D-09, D-10, D-11, D-13, D-14). One paragraph noting this module exercises the post-Plan-02 source changes end-to-end with a synthetic lavfi fixture.

    2. Imports block matching test_finalizer.py lines 21-31:
       - `import pytest`
       - `import subprocess`
       - `import shutil`
       - `import sys, os` + `sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))`
       - `from src.crop import apply_finalizer, probe_video`
       - `from src.validate import validate_color_tags, validate_bitrate_range, validate_duration_parity, validate_finalizer_info`
       - `from src import config`

    3. Fixture `synth_source_mp4(tmp_path_factory)` — session-scoped:
       - If `shutil.which("ffmpeg") is None: pytest.skip("ffmpeg not on PATH")`.
       - Builds path `tmp_path_factory.mktemp("encode-quality") / "source.mp4"`.
       - subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i", "testsrc=duration=5:size=1920x1080:rate=30", "-f", "lavfi", "-i", "sine=frequency=440:duration=5", "-c:v", "libx264", "-pix_fmt", "yuv420p", "-c:a", "aac", str(path)], capture_output=True, text=True, timeout=60). Assert returncode 0.
       - Yields the path as a string.

    4. Fixture `finalizer_output(synth_source_mp4, tmp_path)` — function-scoped:
       - If `shutil.which("ffprobe") is None: pytest.skip("ffprobe not on PATH")`.
       - Computes `probe_info = probe_video(synth_source_mp4)`.
       - Builds `output_path = str(tmp_path / "finalized.mp4")`.
       - Calls `result = apply_finalizer(synth_source_mp4, output_path, config.VERTICAL_WIDTH, config.VERTICAL_HEIGHT, probe_info)`.
       - Yields `(output_path, result)`.

    5. Class TestEncodeQuality (matches the TestConfig/TestValidation class style):
       - `test_finalizer_color_tags_are_bt709(self, finalizer_output)`: `output_path, _ = finalizer_output; errors = validate_color_tags(output_path); assert errors == [], errors`. Docstring: "ENC-03 / D-10: ffprobe confirms color_space, color_primaries, color_transfer all equal bt709."
       - `test_finalizer_bitrate_in_band(self, finalizer_output)`: `output_path, _ = finalizer_output; errors = validate_bitrate_range(output_path, min_kbps=2000, max_kbps=15000); assert errors == [], errors`. Docstring as described above (note relaxed band for synthetic fixture; production band exercised in visual A/B).
       - `test_finalizer_unsharp_and_lanczos_in_manifest(self, finalizer_output)`: `_, result = finalizer_output; assert result["lanczos_scaling"] is True; assert result["unsharp_filter"] == "5:5:0.5:5:5:0.3"; assert result["color_space"] == "bt709"; assert result["color_primaries"] == "bt709"; assert result["color_transfer"] == "bt709"`. Docstring: "ENC-04 / D-05, D-06, D-08: manifest fields confirm Lanczos + unsharp were applied and BT.709 metadata was set."
       - `test_finalizer_crf_is_18(self, finalizer_output)`: `_, result = finalizer_output; assert result["h264_crf"] == 18`. Docstring: "ENC-02 / D-03: manifest reports CRF 18 (was 20 in Phase 4)."
       - `test_duration_parity_after_finalizer(self, synth_source_mp4, finalizer_output)`: `output_path, _ = finalizer_output; errors = validate_duration_parity(synth_source_mp4, output_path, tolerance_ms=100); assert errors == [], errors`. Docstring: "ENC-05 / D-11, D-14: source-to-finalizer duration delta within tolerance."

    Do NOT add any test that depends on the silence-cutter container being running. Do NOT shell out to docker compose. All tests run in-process against the synthesized fixture.
  </action>
  <verify>
    <automated>cd services/ffmpeg-finalizer &amp;&amp; test -f tests/test_encode_quality.py &amp;&amp; grep -cE 'def test_' tests/test_encode_quality.py | grep -qE '^[5-9]$|^[1-9][0-9]+$' &amp;&amp; grep -cF 'validate_color_tags' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'validate_bitrate_range' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'validate_duration_parity' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; python -m pytest tests/test_encode_quality.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f services/ffmpeg-finalizer/tests/test_encode_quality.py` exits 0.
    - Source assertion: `grep -cE 'def test_' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least `5` (one per test method).
    - Source assertion: `grep -F 'validate_color_tags' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'validate_bitrate_range' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'validate_duration_parity' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'shutil.which' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match (ffmpeg/ffprobe availability gate present).
    - Source assertion: `grep -F 'ENC-03' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'ENC-05' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Test command: `pytest services/ffmpeg-finalizer/tests/test_encode_quality.py -x -q` exits 0 (passing on hosts with ffmpeg, all-skipped otherwise).
    - Test command: `pytest services/ffmpeg-finalizer/tests/ -x -q` exits 0 (both test_finalizer.py and test_encode_quality.py pass together).
  </acceptance_criteria>
  <done>test_encode_quality.py exists with five focused tests exercising each ENC requirement against a real synthesized fixture; tests gracefully skip when ffmpeg/ffprobe are unavailable; the full ffmpeg-finalizer test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire validate_concat_mode into test_silence_cutter.py with real-ffmpeg fixture (ENC-01)</name>
  <files>services/silence-cutter/tests/test_silence_cutter.py</files>
  <read_first>
    - services/silence-cutter/tests/test_silence_cutter.py (lines 1-29 module docstring + imports; existing TestValidation class style for adding new tests)
    - services/silence-cutter/src/validate.py (post Plan 01 — validate_concat_mode signature and ENC-01 error format)
    - services/silence-cutter/src/cut_video.py (lines 200-237 — the post-Plan-01 stream-copy concat command; lines 240-273 get_video_duration ffprobe pattern)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"services/silence-cutter/src/validate.py" — the validator's behavior and error format)
  </read_first>
  <behavior>
    - Import for `validate_concat_mode` added to test_silence_cutter.py (extending the existing `from src.validate import ...` line).
    - A new test class `TestConcatMode` (matching the existing class-per-feature style) contains at minimum two tests:
      - `test_validate_concat_mode_stream_copy`: synthesizes two 2-second clips with ffmpeg lavfi, concats them via the SAME ffmpeg command shape Plan 01's `_concatenate_segments` uses (`-c copy` + `-reset_timestamps 1`), then calls `validate_concat_mode(output_path)` and asserts `errors == []`. Skips if ffmpeg/ffprobe missing.
      - `test_validate_concat_mode_detects_reencode`: same synthesis, but concats with `-c:v libx264 -c:a aac` (the pre-Phase-13 shape — to confirm the validator flags re-encode). Asserts `any("ENC-01" in e for e in errors)`. Skips if ffmpeg/ffprobe missing.
    - The existing tests in test_silence_cutter.py continue to pass.
    - `pytest services/silence-cutter/tests/test_silence_cutter.py -x -q` exits 0.
  </behavior>
  <action>
    In services/silence-cutter/tests/test_silence_cutter.py:

    PART A — extend imports (line 28):
    1. Change `from src.validate import validate_silence_cuts, validate_cross_reference_logic` to `from src.validate import validate_silence_cuts, validate_cross_reference_logic, validate_concat_mode`.
    2. Add `import subprocess` and `import shutil` to the top imports (after the existing `import json` at line 17, if not already present — check existing imports first).

    PART B — extend module docstring (lines around 1-15):
    1. Add `ENC-01` and `D-01` (Phase 13) to the referenced-requirements list. Keep the existing SILC-XX references.

    PART C — add new test class TestConcatMode at the bottom of the file:

    1. Class docstring: "Verify validate_concat_mode against real ffmpeg stream-copy and re-encode outputs (ENC-01 / D-01)."

    2. Helper method or module-level helper `_synth_clip(path, duration=2, seed=0)`:
       - Calls `subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i", f"testsrc=duration={duration}:size=320x240:rate=30:decimals=2", "-f", "lavfi", "-i", f"sine=frequency={440+seed*55}:duration={duration}", "-c:v", "libx264", "-pix_fmt", "yuv420p", "-c:a", "aac", str(path)], capture_output=True, text=True, timeout=30)`. Asserts returncode 0.
       - Small clips (320×240) keep the test fast.

    3. Test `test_validate_concat_mode_stream_copy(self, tmp_path)`:
       - Skip-guards: `if shutil.which("ffmpeg") is None or shutil.which("ffprobe") is None: pytest.skip("ffmpeg/ffprobe not on PATH")`.
       - Use the helper to synthesize two clips at `tmp_path / "a.mp4"` and `tmp_path / "b.mp4"`.
       - Write a concat list file at `tmp_path / "list.txt"` containing the two `file '...'` lines.
       - subprocess.run the EXACT ffmpeg shape from cut_video.py::_concatenate_segments (post Plan 01): `["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", list_path, "-map", "0:v:0", "-map", "0:a:0?", "-c", "copy", "-reset_timestamps", "1", str(out_path)]`. Assert returncode 0.
       - Call `errors = validate_concat_mode(str(out_path))`. Assert `errors == []`, attach the list as the assertion message for debuggability.
       - Docstring: "ENC-01 / D-01: stream-copied concat output has no fresh libx264/Lavc encoder tag, so the validator passes."

    4. Test `test_validate_concat_mode_detects_reencode(self, tmp_path)`:
       - Same skip-guards.
       - Same two synthesized clips + concat list.
       - subprocess.run a re-encode concat: `["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", list_path, "-map", "0:v:0", "-map", "0:a:0?", "-c:v", "libx264", "-c:a", "aac", "-reset_timestamps", "1", str(out_path)]`. Assert returncode 0.
       - Call `errors = validate_concat_mode(str(out_path))`. Assert `any("ENC-01" in e for e in errors)`.
       - Docstring: "ENC-01: a re-encoded concat output carries a fresh encoder tag, so the validator flags it."

    Do not touch any other test class. Do not modify imports beyond adding `validate_concat_mode`, `subprocess`, `shutil`.
  </action>
  <verify>
    <automated>cd services/silence-cutter &amp;&amp; grep -cF 'validate_concat_mode' tests/test_silence_cutter.py | grep -qE '^[3-9]$|^[1-9][0-9]+$' &amp;&amp; grep -cE 'class TestConcatMode' tests/test_silence_cutter.py | grep -q '^1$' &amp;&amp; grep -cF 'shutil.which' tests/test_silence_cutter.py | grep -q '^[1-9]' &amp;&amp; python -m pytest tests/test_silence_cutter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cF 'validate_concat_mode' services/silence-cutter/tests/test_silence_cutter.py` returns at least `3` (one import + at least two test usages).
    - Source assertion: `grep -cE 'class TestConcatMode' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -cE 'def test_validate_concat_mode_stream_copy' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -cE 'def test_validate_concat_mode_detects_reencode' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -F '"-c", "copy"' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (positive case argv mirrors Plan 01).
    - Source assertion: `grep -F '"-c:v", "libx264"' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (negative case argv).
    - Source assertion: `grep -F 'shutil.which' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (skip gate present).
    - Test command: `pytest services/silence-cutter/tests/test_silence_cutter.py -x -q` exits 0.
  </acceptance_criteria>
  <done>validate_concat_mode is exercised by two real-ffmpeg-driven tests (positive + negative) under TestConcatMode; tests skip gracefully when ffmpeg/ffprobe are unavailable; the full silence-cutter test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → ffmpeg subprocess | Tests synthesize fixtures via `subprocess.run(["ffmpeg", ...])` with hardcoded argv (no user-input interpolation). |
| test process → tempfile filesystem | All artifacts under `tmp_path_factory.mktemp(...)` or pytest's `tmp_path`; auto-cleaned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.03-01 | Tampering | test argv construction | mitigate | Tests use the same list-based argv pattern as production. No shell strings. Argv values are literals or pytest tmp_path Path objects. |
| T-13.03-02 | Denial of Service | tests synthesize 5-second clips that consume disk space | accept | Tests use 320×240 (concat tests) and 1920×1080×5s (encode-quality tests) — fixtures stay under 10 MB total per session and live in pytest tmpdir (auto-cleaned). No risk to long-lived storage. |
| T-13.03-03 | Tampering | future regression that removes the ENC checks | mitigate | The test_encode_quality.py module exercises every ENC requirement via real ffprobe output; the TestConfig assert on H264_CRF == 18 makes CRF drift fail CI; the TestConcatMode tests make stream-copy drift fail CI. Phase 13 invariants survive future refactors. |
| T-13.03-04 | Information Disclosure | ffmpeg stderr captured into assertion messages | accept | Same surface as Phase 4 / Phase 3 test patterns; local-only. |
| T-13.03-SC | Supply Chain | npm/pip/cargo installs | not_applicable | Phase 13 plan adds no new dependencies. `pytest`, `subprocess`, `shutil`, `json` are all pre-existing or stdlib. |

</threat_model>

<verification>
1. `pytest services/ffmpeg-finalizer/tests/ -x -q` exits 0.
2. `pytest services/silence-cutter/tests/ -x -q` exits 0.
3. All source greps in the three tasks' acceptance criteria pass.
4. Source code in services/ffmpeg-finalizer/src/ and services/silence-cutter/src/ is unchanged (this plan touches only `tests/`).
</verification>

<success_criteria>
- ENC-01 verified by test_validate_concat_mode_stream_copy / test_validate_concat_mode_detects_reencode against real ffmpeg argv shapes.
- ENC-02 verified by test_finalizer_crf_is_18 (manifest field), updated TestConfig.test_encoding_params (constant), test_finalizer_bitrate_in_band (ffprobe).
- ENC-03 verified by test_finalizer_color_tags_are_bt709 (ffprobe) + test_finalizer_unsharp_and_lanczos_in_manifest (manifest fields).
- ENC-04 verified by test_finalizer_unsharp_and_lanczos_in_manifest (manifest fields) — the perceptual halo-free claim is exercised in Plan 04's visual A/B.
- ENC-05 verified by test_duration_parity_after_finalizer.
- Tests degrade gracefully (skip, do not fail) when ffmpeg/ffprobe are absent — CI without those binaries doesn't false-fail.
</success_criteria>

<output>
Create `.planning/phases/13-encode-quality/13-03-SUMMARY.md` when done. Include:
- Diff hunks applied to test_finalizer.py (fixture sweep + TestConfig update).
- Full path of the new test_encode_quality.py with method list.
- Diff hunks applied to test_silence_cutter.py (imports + TestConcatMode class).
- Confirmation that both `pytest services/ffmpeg-finalizer/tests/ -x -q` and `pytest services/silence-cutter/tests/ -x -q` exit 0.
- Confirmation that all source-grep acceptance criteria pass.
- Note that the perceptual ENC-04 "sin halos" claim is NOT covered here by design — Plan 04's visual A/B is the gate.
</output>
