---
phase: 18-studio-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
autonomous: true
requirements:
  - STUDIO-02
  - STUDIO-03

must_haves:
  truths:
    - "TitleEditor accepts only titles + onChange — no onPreviewChange or onSave props exist"
    - "TitleEditor no longer auto-saves or auto-previews on add/edit/remove"
    - "build:editor exits 0 with no TypeScript errors after the change"
  artifacts:
    - path: "services/remotion-studio/src/editor/components/TitleEditor.tsx"
      provides: "Simplified TitleEditor component with 2-prop interface"
      contains: "interface TitleEditorProps"
  key_links:
    - from: "PreviewApp.tsx (Phase 18 Wave 2)"
      to: "TitleEditor.tsx"
      via: "onChange prop only"
      pattern: "onChange.*setTitles"
---

<objective>
Simplify TitleEditor by removing the onPreviewChange and onSave props (D-10). This makes title
edits behave like every other control: live preview via in-memory state (onChange → parent setState),
persistence on explicit "Save Config" click. Removes the special-case wiring that caused title
state to diverge from subtitle state.

Purpose: Gate for Wave 2 — PreviewApp's unified StudioApp must consume a 2-prop TitleEditor.
Output: Simplified TitleEditor.tsx with clean onChange-only interface.
</objective>

<execution_context>
MANDATORY FIRST STEP — invoke impeccable skill + frontend-design plugin before touching any file.
Per AGENTS.md §"UI/frontend work — REQUIRED tooling": every UI phase task must start with these
two tools. This is non-negotiable.

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-studio-ui-redesign/18-CONTEXT.md
@.planning/phases/18-studio-ui-redesign/18-RESEARCH.md
@.planning/phases/18-studio-ui-redesign/18-PATTERNS.md
@.planning/phases/18-studio-ui-redesign/18-UI-SPEC.md

<interfaces>
<!-- Current TitleEditor.tsx interface — executor reads the file first, uses these as orientation -->
<!-- Source: 18-PATTERNS.md §"src/editor/components/TitleEditor.tsx" -->

Current TitleEditorProps (lines ~9–14 of TitleEditor.tsx):
```typescript
interface TitleEditorProps {
  titles: TitleConfig[];
  onChange: (titles: TitleConfig[]) => void;
  onPreviewChange?: (titles: TitleConfig[]) => void;  // REMOVE
  onSave?: (titles: TitleConfig[]) => void;            // REMOVE
}
```

Target TitleEditorProps (after D-10):
```typescript
interface TitleEditorProps {
  titles: TitleConfig[];
  onChange: (titles: TitleConfig[]) => void;
}
```

useEffect to delete entirely (~lines 93–127):
```typescript
useEffect(() => {
  if (!onPreviewChange) return;
  // ...entire block...
}, [newTitle, editingIndex, addingNew, titles, onPreviewChange]);
```

Call sites to remove (handleAdd ~line 144, handleRemove ~line 156, handleSaveEdit ~line 188):
```typescript
// each has these two lines — remove both:
if (onPreviewChange) onPreviewChange(updated);
onSave?.(updated);
```

Function signature to update (~line 68):
```typescript
// Before:
export function TitleEditor({ titles, onChange, onPreviewChange, onSave }: TitleEditorProps) {
// After:
export function TitleEditor({ titles, onChange }: TitleEditorProps) {
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove onPreviewChange and onSave from TitleEditor (D-10)</name>
  <files>services/remotion-studio/src/editor/components/TitleEditor.tsx</files>
  <read_first>
    - services/remotion-studio/src/editor/components/TitleEditor.tsx — read the ENTIRE file before editing; verify exact line numbers for the useEffect block, the three call sites in handleAdd/handleRemove/handleSaveEdit, and the function signature destructure
  </read_first>
  <action>
    Apply all four D-10 removals to TitleEditor.tsx:

    1. Interface: Remove the two optional prop declarations — `onPreviewChange?: (titles: TitleConfig[]) => void` and `onSave?: (titles: TitleConfig[]) => void` — from TitleEditorProps. Interface body shrinks to exactly two members: `titles` and `onChange`.

    2. Function signature: Update the destructure from `{ titles, onChange, onPreviewChange, onSave }` to `{ titles, onChange }`. Do NOT add type annotation inline; the interface above provides it.

    3. useEffect block: Delete the ENTIRE useEffect that has `onPreviewChange` in its dependency array. This is the block that calls `onPreviewChange` with a preview title array derived from the current editing state. Delete from the `useEffect(` opening to the closing `}, [newTitle, editingIndex, addingNew, titles, onPreviewChange]);` inclusive.

    4. Call sites in CRUD handlers: In each of handleAdd, handleRemove, and handleSaveEdit, remove the two lines that call `onPreviewChange` and `onSave`. These lines appear as a pair after `onChange(updated)` is called:
       - `if (onPreviewChange) onPreviewChange(updated);`
       - `onSave?.(updated);`
       Remove both lines from all three handlers. The `onChange(updated)` call stays.

    After editing, the component still renders exactly the same form UI. The only behavioral change is that adding/removing/editing a title fires only `onChange` (live preview in parent state), not an extra `onPreviewChange`/`onSave` pair.

    Run the build to confirm no TypeScript errors: `cd services/remotion-studio && npm run build:editor`
    If build errors reference "onPreviewChange is not defined" or similar — the destructure was not fully cleaned. Fix until build is green.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-studio && npm run build:editor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TitleEditor.tsx: TitleEditorProps interface has exactly two members — `titles: TitleConfig[]` and `onChange: (titles: TitleConfig[]) => void`. No onPreviewChange or onSave lines present.
    - TitleEditor.tsx: function signature destructures only `{ titles, onChange }`.
    - TitleEditor.tsx: the useEffect block that references `onPreviewChange` is entirely gone. No `useEffect` in the file has `onPreviewChange` in its dependency list.
    - TitleEditor.tsx: the strings "onPreviewChange" and "onSave" do not appear anywhere in the file (`grep -c "onPreviewChange\|onSave" services/remotion-studio/src/editor/components/TitleEditor.tsx` returns 0).
    - `npm run build:editor` exits 0 with no TypeScript errors.
  </acceptance_criteria>
  <done>TitleEditor accepts only `{titles, onChange}` — no optional props. Build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component props | TitleEditor receives title data from PreviewApp — client-side only, no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Build must exit 0. The string "onPreviewChange" must not appear in TitleEditor.tsx.
Existing remotion-renderer tests must still pass (no composition changes):
`cd /home/luis/proyectos/reel-factory/services/remotion-renderer && npm test`
</verification>

<success_criteria>
TitleEditor.tsx is simplified to a 2-prop interface. Build is green. No onPreviewChange or onSave
references remain in TitleEditor.tsx. The CRUD handlers (handleAdd, handleRemove, handleSaveEdit)
each call only onChange(updated) — no extra callbacks.
</success_criteria>

<output>
Create `.planning/phases/18-studio-ui-redesign/18-01-SUMMARY.md` when done.
</output>
