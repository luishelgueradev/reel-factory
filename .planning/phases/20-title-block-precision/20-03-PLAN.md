---
phase: 20-title-block-precision
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
autonomous: true
requirements: [TITLE-01, TITLE-02, TITLE-03]

must_haves:
  truths:
    - "TitleEditor form shows X (px) and Y (px) number inputs side-by-side instead of topOffset slider (D-01, D-02, D-05)"
    - "TitleEditor form shows Border Radius slider (0–50, step 1, accentColor #4CAF50) (D-09)"
    - "No subtitle input, subtitle color, subtitle size, or subtitle font controls in the form (D-07, D-08)"
    - "No subtitle line displayed in the title list items (D-07)"
    - "DEFAULT_TITLE_STYLE in TitleEditor.tsx has x:200, y:960, borderRadius:12 and no topOffset/subtitle fields (D-06, D-09)"
    - "handleAdd and handleSaveEdit do not include subtitle in the created/updated TitleConfig (D-07)"
  artifacts:
    - path: "services/remotion-studio/src/editor/components/TitleEditor.tsx"
      provides: "Updated editor with x/y inputs and borderRadius slider; subtitle fields removed"
      contains: "X (px)"
  key_links:
    - from: "services/remotion-studio/src/editor/components/TitleEditor.tsx"
      to: "services/remotion-studio/src/pipeline-config.ts"
      via: "TitleConfig type used in state and handlers"
      pattern: "TitleConfig"
---

<objective>
Update TitleEditor.tsx to replace the topOffset slider with X/Y number inputs, add a border-radius slider, and remove all subtitle-related form fields and list display. This is the UI side of the three schema changes from Plan 01.

Purpose: Without this update, the editor form still shows deprecated controls (topOffset, subtitle inputs) that no longer map to any schema field, causing form submissions with dead data.
Output: TitleEditor.tsx with correct form layout per UI-SPEC: X/Y inputs at position 3, borderRadius slider at position 8, subtitle fields gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

IMPORTANT (AGENTS.md requirement): Invoke `impeccable` skill + `frontend-design` plugin at start of session if not already active from a prior Plan 01/02 execute session.
</execution_context>

<context>
@.planning/phases/20-title-block-precision/20-CONTEXT.md
@.planning/phases/20-title-block-precision/20-RESEARCH.md
@.planning/phases/20-title-block-precision/20-PATTERNS.md
@.planning/phases/20-title-block-precision/20-UI-SPEC.md
@.planning/phases/20-title-block-precision/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite TitleEditor.tsx — remove subtitle controls, add x/y inputs and borderRadius slider</name>
  <files>services/remotion-studio/src/editor/components/TitleEditor.tsx</files>
  <read_first>
    - services/remotion-studio/src/editor/components/TitleEditor.tsx (full file — locate DEFAULT_TITLE_STYLE at lines 25–38; state initializers at ~67–85; handleAdd/handleSaveEdit/handleStartEdit at ~88–145; title list item subtitle display at ~173–175; subtitle text input at ~229–241; subtitle color column at ~351–363; subtitle size slider at ~387–406; subtitle font select at ~483–500; topOffset slider at ~409–425)
    - .planning/phases/20-title-block-precision/20-PATTERNS.md §"TitleEditor.tsx" (all before/after patterns, new X/Y row, new borderRadius slider, DEFAULT_TITLE_STYLE target)
    - .planning/phases/20-title-block-precision/20-UI-SPEC.md §"Component Contracts §1" (X/Y layout spec) and §"Component Contracts §2" (border-radius slider spec) and §"Interaction Contract — Form Layout Order"
    - services/remotion-studio/src/pipeline-config.ts (confirm updated TitleStyleProps from Plan 01 — new x/y/borderRadius fields, no topOffset/subtitle fields)
  </read_first>
  <action>
    Apply all changes to `services/remotion-studio/src/editor/components/TitleEditor.tsx` in this order:

    1. `DEFAULT_TITLE_STYLE` (plain object, lines 25–38): remove `subtitleFontSize: 42`, `subtitleColor: "#FFFFFF"`, `subtitleFontFamily: "PlusJakartaSans"`, `topOffset: 50`. Add `x: 200`, `y: 960`, `borderRadius: 12`. Preserve all other fields.

    2. State initializers and `resetForm`: in every occurrence of the initial `newTitle` object (initial state, resetForm, handleStartEdit population), remove `subtitle: ""` or `subtitle: title.subtitle ?? ""`.

    3. `handleAdd`: remove `subtitle: newTitle.subtitle || undefined` from the TitleConfig object passed to onAdd.

    4. `handleSaveEdit`: remove `subtitle: newTitle.subtitle || undefined` from the TitleConfig object passed to onEdit.

    5. Title list item render (around lines 173–175): remove the `{title.subtitle && (<div ...>{title.subtitle}</div>)}` block entirely.

    6. Form: remove the "Subtitle (optional)" text input div block (the one with `<input type="text" ... value={newTitle.subtitle ?? ""}>`).

    7. Form: in the style colors row (Title Color + Subtitle Color columns), remove the entire Subtitle Color column div (`<div style={{ flex: 1 }}>` block containing the subtitle color label and color input). The Title Color column becomes full-width (or the flex row becomes single-item — remove the row wrapper and use a single full-width div if cleaner).

    8. Form: in the font sizes row, remove the Subtitle Size slider half (the `<div style={{ flex: 1 }}>` for subtitleFontSize). The Title Size slider becomes the full-width or single-item in the row. If the flex row only has one item, simplify to a single `<div style={{ marginBottom: 12 }}>`.

    9. Form: remove the topOffset slider div (the `<div>` block containing label "Top Offset: {value}%" and range input). Replace it with the X/Y coordinate row per the spec below. Position: same location as topOffset was (after Duration row, before Entrance Animation row — form layout order position 3 per UI-SPEC).

       X/Y row spec (from UI-SPEC §Component Contracts §1 and 20-PATTERNS.md):
       - Outer div: `display: "flex"`, `gap: 16`, `marginBottom: 10`
       - X child: `flex: 1`, label "X (px)" (fontSize 12, color "#bbb", display block, marginBottom 4), input type="number" min=0 max=1080 step=1, value `newTitle.style?.x ?? 200`, onChange spreads style and sets x to parseInt(e.target.value)
       - Y child: `flex: 1`, label "Y (px)", input type="number" min=0 max=1920 step=1, value `newTitle.style?.y ?? 960`, onChange spreads style and sets y to parseInt(e.target.value)
       - Input styling: `width: "100%"`, `padding: 8`, `background: "#2a2a3e"`, `border: "1px solid #444"`, `borderRadius: 4`, `color: "#e0e0e0"`, `fontSize: 14`

    10. Form: add the borderRadius slider at form layout position 8 (after Title Size slider, before Line Height/Padding row). Spec (from UI-SPEC §Component Contracts §2 and 20-PATTERNS.md):
        - Outer div: `marginBottom: 12`
        - Label: "Border Radius: {newTitle.style?.borderRadius ?? 12}px" (fontSize 12, color "#bbb", display block, marginBottom 4)
        - Input: type="range", min=0, max=50, step=1, value `newTitle.style?.borderRadius ?? 12`, onChange sets borderRadius to parseInt(e.target.value), style `width: "100%"`, `accentColor: "#4CAF50"`
        - Range bounds div: `display: "flex"`, `justifyContent: "space-between"`, `fontSize: 11`, `color: "#666"` with `<span>0px</span>` and `<span>50px</span>`

    11. Form: in the font families row, remove the Subtitle Font select column (the `<div style={{ flex: 1 }}>` for `subtitleFontFamily`). If the font families flex row is now single-item, simplify it to a `<div style={{ marginBottom: 12 }}>` with the Title Font select full-width.

    Do NOT change any other form fields. Do NOT change any styling of fields not listed above. The onChange pattern for all new inputs MUST follow the existing spread pattern: `setNewTitle((prev) => ({ ...prev, style: { ...prev.style!, fieldName: value } }))`.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-studio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compilation exits 0. File contains "X (px)" label text. File contains "Border Radius:" label text. File does NOT contain "Subtitle" string (case-sensitive) outside of comments. File does NOT contain "topOffset" identifier. DEFAULT_TITLE_STYLE in this file contains x: 200, y: 960, borderRadius: 12. handleAdd and handleSaveEdit do not contain `subtitle:` in their TitleConfig construction.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → TitleEditor onChange → in-memory state | All input flows through React controlled inputs; no direct DOM manipulation; persisted only on Save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | TitleEditor form inputs | accept | Values are local in-memory state; HTML min/max attributes enforce range on input; no server call until Save |
| T-20-SC | Tampering | npm installs | accept | No new packages in this task |
</threat_model>

<verification>
After task completes:
- `grep -n "topOffset\|subtitle\|subtitleFontSize\|subtitleColor\|subtitleFontFamily" services/remotion-studio/src/editor/components/TitleEditor.tsx | grep -v "^.*\/\/"` — should return zero matches outside comments
- `grep -n "X (px)" services/remotion-studio/src/editor/components/TitleEditor.tsx` — should return at least 1 match
- `grep -n "Border Radius" services/remotion-studio/src/editor/components/TitleEditor.tsx` — should return at least 1 match
- `grep -n "accentColor.*4CAF50" services/remotion-studio/src/editor/components/TitleEditor.tsx` — the borderRadius slider appears
- `cd services/remotion-studio && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Form layout matches UI-SPEC §"Form Layout Order" — topOffset slider replaced by X/Y row at position 3; borderRadius slider at position 8; no subtitle inputs anywhere
- X input: type=number, min=0, max=1080, step=1, default 200
- Y input: type=number, min=0, max=1920, step=1, default 960
- Border Radius: type=range, min=0, max=50, step=1, accentColor #4CAF50, with 0px/50px bounds labels
- All inputs use in-memory onChange pattern matching existing controls
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/20-title-block-precision/20-03-SUMMARY.md` when done
</output>
