---
phase: 22-studio-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/editor/components/PositionPresets.tsx
autonomous: true
requirements: [D-07, D-08, D-09]
must_haves:
  truths:
    - "A shared PositionPresets component renders a 3×3 grid of 9 directional arrow buttons"
    - "Clicking a preset computes a size-aware X/Y against the 1080×1920 frame and calls onApply(x, y)"
    - "Center/right presets subtract element width; center/bottom subtract element height"
    - "The component owns no config state — it only reports x/y via onApply"
  artifacts:
    - path: "services/remotion-studio/src/editor/components/PositionPresets.tsx"
      provides: "Shared 9-point preset grid consumed by StyleControls, TitleEditor, OverlayEditor"
      min_lines: 60
      exports: ["PositionPresets"]
  key_links:
    - from: "PositionPresets"
      to: "host handleDraftChange / onChange"
      via: "onApply(x, y) callback"
      pattern: "onApply\\("
---

<objective>
Build the new shared `PositionPresets.tsx` component: a 3×3 arrow-button grid
(`↖ ↑ ↗ / ← • → / ↙ ↓ ↘`) with size-aware X/Y math against the 1080×1920 frame
and a top-left anchor. It is a pure, stateless presentational component —
consumers (StyleControls, TitleEditor, OverlayEditor in 22-03) wire its
`onApply(x, y)` into their existing draft-update paths (D-07/D-08/D-09).

Purpose: One DRY affordance for all three tabs, defined as a contract BEFORE
its consumers so 22-03 builds against a known prop signature (interface-first).
Output: A self-contained component file, no consumer wiring yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-PATTERNS.md
@.planning/phases/22-studio-ui-polish/22-UI-SPEC.md

<interfaces>
<!-- Prop shape the PATTERNS map prescribes (PATTERNS.md L155-162). LOCK this signature. -->
interface PositionPresetsProps {
  elementWidth: number;
  elementHeight: number;
  frameWidth?: number;   // default 1080
  frameHeight?: number;  // default 1920
  onApply: (x: number, y: number) => void;
  activeAnchor?: string; // optional, drives blue highlight on the matching cell
}

<!-- Size-aware math — UI-SPEC §"Position Presets", 1080×1920 top-left anchor -->
left=0 ; centerX=round((1080-elementWidth)/2) ; right=round(1080-elementWidth)
top=0  ; centerY=round((1920-elementHeight)/2); bottom=round(1920-elementHeight)

<!-- Theme tokens (default.css). Active cell = BLUE per color law (NOT green). -->
active cell:  background var(--accent-tint); border-color var(--accent-strong); color var(--accent)
idle cell:    background var(--surface-2);   border 1px var(--border);          color var(--text-2)
grid:         display grid; gridTemplateColumns repeat(3, 30px); gap 3px
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invoke impeccable + frontend-design, then build PositionPresets.tsx</name>
  <read_first>
    - AGENTS.md § "UI/frontend work — REQUIRED tooling" (impeccable skill + frontend-design plugin are NON-NEGOTIABLE; invoke at the START of this task, not as an afterthought)
    - .claude/skills/sketch-findings-reel-factory/sources/themes/default.css (the OKLCH token set — use var(--…) names verbatim)
    - services/remotion-studio/src/editor/components/StyleControls.tsx (L17-21 POSITION_OPTIONS array + L185-213 segmented-button render — the affordance being generalized; note the active state is GREEN there and must be BLUE here per UI-SPEC color law)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Position Presets" and § "Color" (color law: green = Render CTA only; selection = blue)
  </read_first>
  <behavior>
    - Test (pure math helper `computePresetXY(anchorX, anchorY, elementWidth, elementHeight, frameWidth=1080, frameHeight=1920)`):
      - ("left","top", 200, 100) → { x: 0, y: 0 }
      - ("center","center", 200, 100) → { x: 440, y: 910 }   // round((1080-200)/2)=440, round((1920-100)/2)=910
      - ("right","bottom", 200, 100) → { x: 880, y: 1820 }   // 1080-200=880, 1920-100=1820
      - ("center","bottom", 0, 0) → { x: 540, y: 1920 }
    - Test: rendering the grid produces exactly 9 buttons, each with an accessible name (aria-label) describing its anchor (e.g. "Posición arriba-izquierda").
  </behavior>
  <action>
    FIRST invoke the `impeccable` skill and the `frontend-design` plugin (AGENTS.md non-negotiable). Create services/remotion-studio/src/editor/components/PositionPresets.tsx. Export a pure function `computePresetXY(anchorX: "left"|"center"|"right", anchorY: "top"|"center"|"bottom", elementWidth, elementHeight, frameWidth=1080, frameHeight=1920)` returning `{x, y}` using: left/top=0, center=round((frame-element)/2), right/bottom=round(frame-element). Export the `PositionPresets` React component with the LOCKED prop signature from <interfaces> (elementWidth, elementHeight, frameWidth?=1080, frameHeight?=1920, onApply, activeAnchor?). Render a 3×3 CSS grid (`gridTemplateColumns: repeat(3, 30px)`, `gap: 3px`) of 9 buttons with glyphs in row order `↖ ↑ ↗ / ← • → / ↙ ↓ ↘`, each cell mapped to an `{anchorX, anchorY}` pair. On click, call `onApply(computePresetXY(...))`. Active cell (matching `activeAnchor`) gets BLUE treatment via the default.css tokens (`var(--accent-tint)` bg, `var(--accent-strong)` border, `var(--accent)` color) — NEVER green (color law). Each button MUST carry an `aria-label` (Spanish, e.g. `Posición centro`, `Posición arriba-derecha`) and a 44×44px minimum hit area (use padding/wrapper even though the visible cell is 30px) per UI-SPEC Accessible Name Contract. Component owns NO config state. Do NOT wire it into any consumer in this plan (that is 22-03). Co-locate the test as PositionPresets.test.ts if a studio test runner exists; otherwise put the `computePresetXY` cases in an inline `if (import.meta.vitest)` block or a sibling test file the renderer vitest can pick up — prefer a `services/remotion-studio/src/editor/components/PositionPresets.test.ts` that imports only the pure helper.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "PositionPresets" || echo "no PositionPresets type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export function computePresetXY' services/remotion-studio/src/editor/components/PositionPresets.tsx` returns 1.
    - Source assertion: `grep -c 'export function PositionPresets\|export const PositionPresets' services/remotion-studio/src/editor/components/PositionPresets.tsx` returns 1.
    - Source assertion: file contains all 9 glyphs `↖ ↑ ↗ ← • → ↙ ↓ ↘` and references `var(--accent` (blue active) with ZERO `#4CAF50`/`rgba(76, 175, 80` green literals (color law).
    - Source assertion: `grep -c 'aria-label' services/remotion-studio/src/editor/components/PositionPresets.tsx` returns 9 (one per button).
    - Behavior: the 4 computePresetXY cases in <behavior> return the stated {x,y} exactly.
    - Type check: tsc reports no errors referencing PositionPresets.
    - Negative: no consumer (StyleControls/TitleEditor/OverlayEditor) is modified in this plan (`git diff --name-only` lists only PositionPresets.tsx[.test.ts]).
  </acceptance_criteria>
  <done>PositionPresets.tsx exists with the locked prop signature + exported pure math helper, 9 aria-labeled blue-active cells, math cases verified, no consumer wiring, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → onApply(x,y) → host draft state | Preset writes numeric x/y into the existing config draft path. Numbers only; no string interpolation into markup or paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | computePresetXY output | accept | Output is bounded integer px derived from frame/element constants; consumers already clamp x∈[0,1080]/y∈[0,1920] via existing number inputs. No injection surface. |
| T-22-04 | Denial of service | render math | accept | Pure synchronous arithmetic, O(1); no loops over user data. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (new React component using existing deps only). |
</threat_model>

<verification>
- PositionPresets.tsx compiles, exports the component + pure helper.
- 9 aria-labeled cells; blue (not green) active treatment.
- No consumers touched.
</verification>

<success_criteria>
- Shared, stateless 9-point grid component with size-aware math, ready for 22-03 to consume.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-02-SUMMARY.md` when done.
</output>
