---
phase: 12-subtitle-preview-lab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-renderer/src/pipeline-config.ts
  - services/remotion-renderer/src/compositions/TikTokLayout.tsx
  - services/remotion-renderer/src/compositions/SentenceLayout.tsx
  - services/remotion-renderer/src/compositions/BarLayout.tsx
  - services/remotion-renderer/src/compositions/KaraokeLayout.tsx
  - services/remotion-renderer/src/compositions/shared-styles.ts
  - services/remotion-renderer/src/Root.tsx
  - services/remotion-studio/src/editor/components/StyleControls.tsx
  - services/remotion-studio/src/editor/App.tsx
autonomous: true
requirements:
  - PREV-03

must_haves:
  truths:
    - "pastWordOpacity field exists in SubtitleConfig with default 0.4"
    - "All 4 layout components apply pastWordOpacity to was-active words"
    - "StyleControls exposes lineHeight, pastWordOpacity, and bottomOffset controls"
    - "Root.tsx SubtitledVideo merges pastWordOpacity from config into merged SubtitleConfig"
    - "SubtitledVideo exported from Root.tsx for @remotion/player import"
    - "Root.tsx RemotionProps includes rawVideoSrc field; OffthreadVideo uses rawVideoSrc ?? staticFile(videoSrc)"
  artifacts:
    - path: "services/remotion-renderer/src/pipeline-config.ts"
      provides: "SubtitleConfig.pastWordOpacity field, DEFAULT_SUBTITLE_CONFIG.pastWordOpacity, validation"
      contains: "pastWordOpacity"
    - path: "services/remotion-renderer/src/compositions/TikTokLayout.tsx"
      provides: "CaptionWord applies pastWordOpacity to wasActive words"
      contains: "pastWordOpacity"
    - path: "services/remotion-renderer/src/compositions/SentenceLayout.tsx"
      provides: "SentencePage uses config.pastWordOpacity instead of hardcoded PAST_OPACITY"
      contains: "pastWordOpacity"
    - path: "services/remotion-renderer/src/compositions/BarLayout.tsx"
      provides: "BarWord applies pastWordOpacity to wasActive words"
      contains: "pastWordOpacity"
    - path: "services/remotion-renderer/src/compositions/KaraokeLayout.tsx"
      provides: "KaraokeWord applies pastWordOpacity to wasActive baseline layer"
      contains: "pastWordOpacity"
    - path: "services/remotion-renderer/src/compositions/shared-styles.ts"
      provides: "Helper for pastWordOpacity styling"
    - path: "services/remotion-studio/src/editor/components/StyleControls.tsx"
      provides: "lineHeight slider + pastWordOpacity slider"
      contains: "pastWordOpacity"
  key_links:
    - from: "TikTokLayout.tsx"
      to: "pipeline-config.ts"
      via: "import DEFAULT_SUBTITLE_CONFIG, pastWordOpacity from config"
      pattern: "config\\.pastWordOpacity"
    - from: "StyleControls.tsx"
      to: "pipeline-config.ts"
      via: "SubtitleConfig type"
      pattern: "pastWordOpacity"
---

<objective>
Add pastWordOpacity to SubtitleConfig and apply it in all 4 layout components, extending StyleControls with lineHeight, pastWordOpacity, and bottomOffset sliders.

Purpose: Per D-07, pastWordOpacity (default 0.4) must be a SubtitleConfig field so preview can control it in real-time and production renders respect it. SentenceLayout currently hardcodes PAST_OPACITY = 0.5; all layouts need this configurable. Per D-09, StyleControls needs lineHeight and pastWordOpacity controls for the preview page. Per PREV-03, bottomOffset must also be adjustable in real-time — it already exists in SubtitleConfig (default 250) but has no UI control.

Output: Updated SubtitleConfig type, 4 layout components with pastWordOpacity support, extended StyleControls with 3 new sliders (lineHeight, pastWordOpacity, bottomOffset), updated Root.tsx default merge.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-subtitle-preview-lab/12-CONTEXT.md
@.planning/phases/12-subtitle-preview-lab/12-RESEARCH.md
@services/remotion-renderer/src/pipeline-config.ts
@services/remotion-renderer/src/compositions/TikTokLayout.tsx
@services/remotion-renderer/src/compositions/SentenceLayout.tsx
@services/remotion-renderer/src/compositions/BarLayout.tsx
@services/remotion-renderer/src/compositions/KaraokeLayout.tsx
@services/remotion-renderer/src/compositions/shared-styles.ts
@services/remotion-renderer/src/Root.tsx
@services/remotion-studio/src/editor/components/StyleControls.tsx
@services/remotion-studio/src/editor/App.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From services/remotion-renderer/src/pipeline-config.ts:
```typescript
export interface SubtitleConfig {
  layout: SubtitleLayoutMode;
  fontFamily?: string;
  fontSize?: number;
  activeColor?: string;
  inactiveColor?: string;
  outlineColor?: string;
  outlineWidth?: number;
  backgroundHighlight?: BackgroundHighlight;
  textShadow?: TextShadow;
  letterSpacing?: number;
  position?: SubtitlePosition;
  lineHeight?: number;
  bottomOffset?: number;
  // NEW: pastWordOpacity?: number; // opacity for was-active words (0-1, default 0.4)
}

export const DEFAULT_SUBTITLE_CONFIG: Required<Pick<SubtitleConfig, "fontSize" | "activeColor" | "inactiveColor" | "outlineColor" | "outlineWidth" | "position" | "lineHeight" | "bottomOffset">> = {
  fontSize: 58,
  activeColor: "#FFFF00",
  inactiveColor: "#FFFFFF",
  outlineColor: "#000000",
  outlineWidth: 3,
  position: "bottom-center" as SubtitlePosition,
  lineHeight: 1.3,
  bottomOffset: 250,
  // NEW: pastWordOpacity: 0.4,
};
```

From services/remotion-renderer/src/compositions/LayoutDispatcher.tsx:
```typescript
export interface SubtitleLayoutProps {
  captionPages: TikTokPage[];
  config: SubtitleConfig;
  totalDurationMs?: number;
}
```

From services/remotion-renderer/src/compositions/SentenceLayout.tsx:
```typescript
// Currently hardcoded:
const PAST_OPACITY = 0.5; // Must change to: config.pastWordOpacity ?? DEFAULT_SUBTITLE_CONFIG.pastWordOpacity
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pastWordOpacity to SubtitleConfig, export SubtitledVideo, add rawVideoSrc prop, and apply pastWordOpacity in all 4 layout components</name>
  <files>services/remotion-renderer/src/pipeline-config.ts, services/remotion-renderer/src/compositions/TikTokLayout.tsx, services/remotion-renderer/src/compositions/SentenceLayout.tsx, services/remotion-renderer/src/compositions/BarLayout.tsx, services/remotion-renderer/src/compositions/KaraokeLayout.tsx, services/remotion-renderer/src/compositions/shared-styles.ts, services/remotion-renderer/src/Root.tsx</files>
  <read_first>services/remotion-renderer/src/pipeline-config.ts, services/remotion-renderer/src/compositions/TikTokLayout.tsx, services/remotion-renderer/src/compositions/SentenceLayout.tsx, services/remotion-renderer/src/compositions/BarLayout.tsx, services/remotion-renderer/src/compositions/KaraokeLayout.tsx, services/remotion-renderer/src/compositions/shared-styles.ts, services/remotion-renderer/src/Root.tsx</read_first>
  <action>
Per D-07: Add `pastWordOpacity` field to SubtitleConfig, default 0.4, and apply it in all 4 layout components. Additionally, export `SubtitledVideo` and add a `rawVideoSrc` prop to enable @remotion/player usage in the preview (Blocker fix from plan verification).

1. **pipeline-config.ts**: Add `pastWordOpacity?: number` to SubtitleConfig interface (after bottomOffset). Add `pastWordOpacity: 0.4` to DEFAULT_SUBTITLE_CONFIG. Add validation in `validatePipelineConfig`: if `sub.pastWordOpacity !== undefined`, it must be a number between 0 and 1 (inclusive).

2. **shared-styles.ts**: Add a helper function:
```typescript
export function getPastWordOpacity(config: SubtitleConfig): number {
  return config.pastWordOpacity ?? DEFAULT_SUBTITLE_CONFIG.pastWordOpacity;
}
```

3. **TikTokLayout.tsx**: In CaptionWord component, add a `pastWordOpacity` prop (number). In the style of CaptionWord, when `wasActive === true`, apply `opacity: pastWordOpacity`. In CaptionPage, read `const pastWordOpacity = getPastWordOpacity(config)` and pass it down to each CaptionWord. Remove the current color-only differentiation for wasActive — keep `color: isActive ? activeColor : inactiveColor` but now also add `opacity: wasActive ? pastWordOpacity : 1` for words where `wasActive && !isActive`. Words that are neither active nor wasActive keep opacity 1 with inactiveColor.

4. **SentenceLayout.tsx**: Replace `const PAST_OPACITY = 0.5` with `const pastWordOpacity = getPastWordOpacity(config)`. Change the opacity calculation from `const wordOpacity = isTokenPast ? PAST_OPACITY : 1` to `const wordOpacity = isTokenPast ? pastWordOpacity : 1`.

5. **BarLayout.tsx**: In BarWord component, add a `pastWordOpacity` prop (number). When `wasActive && !isActive`, apply `opacity: pastWordOpacity` to the span style. In BarPage, read `const pastWordOpacity = getPastWordOpacity(config)` and pass it to each BarWord.

6. **KaraokeLayout.tsx**: In KaraokeWord component, add a `pastWordOpacity` prop (number). For the baseline (inactive) layer span, when `wasActive` is true, apply `opacity: pastWordOpacity` to the baseline span style. The active fill layer is unaffected (it clips on top). In KaraokePage, read `const pastWordOpacity = getPastWordOpacity(config)` and pass it to each KaraokeWord.

7. **Root.tsx** — three changes:
   a. Add `pastWordOpacity: subtitleConfig?.pastWordOpacity ?? DEFAULT_SUBTITLE_CONFIG.pastWordOpacity` to the SubtitledVideo component's merged config object. This ensures the field flows through to layout components.
   b. Add `export` keyword to the `SubtitledVideo` component declaration. Change `const SubtitledVideo: React.FC<RemotionProps> = (...)` to `export const SubtitledVideo: React.FC<RemotionProps> = (...)`. This enables the preview page in Plan 02 to import SubtitledVideo directly for use with @remotion/player.
   c. Modify the `<OffthreadVideo>` element inside SubtitledVideo to support the `rawVideoSrc` prop. Add `rawVideoSrc?: string` to the RemotionProps interface. Change the video source from `<OffthreadVideo src={staticFile(videoSrc)} />` to `<OffthreadVideo src={rawVideoSrc ?? staticFile(videoSrc)} />`. When `rawVideoSrc` is provided (by the preview Player), the video loads from a direct URL path like `/sample-video.mp4`, bypassing `staticFile()`. When `rawVideoSrc` is undefined (production rendering), `staticFile(videoSrc)` is used as before. This fixes the `staticFile()` context mismatch for @remotion/player.
  </action>
  <verify>
    <automated>cd services/remotion-renderer && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>
    - SubtitleConfig interface has pastWordOpacity?: number field
    - DEFAULT_SUBTITLE_CONFIG.pastWordOpacity === 0.4
    - validatePipelineConfig checks pastWordOpacity is 0-1 if provided
    - getPastWordOpacity() helper in shared-styles.ts
    - TikTokLayout CaptionWord applies opacity to wasActive words via pastWordOpacity
    - SentenceLayout uses config.pastWordOpacity instead of hardcoded PAST_OPACITY = 0.5
    - BarLayout BarWord applies opacity to wasActive words
    - KaraokeLayout KaraokeWord applies opacity to wasActive baseline layer
    - Root.tsx SubtitledVideo merges pastWordOpacity from config into defaults
    - SubtitledVideo is exported from Root.tsx (export keyword added)
    - RemotionProps interface has rawVideoSrc?: string field
    - OffthreadVideo src uses rawVideoSrc ?? staticFile(videoSrc) to support Player context
    - TypeScript compilation succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend StyleControls with lineHeight, pastWordOpacity, and bottomOffset sliders</name>
  <files>services/remotion-studio/src/editor/components/StyleControls.tsx, services/remotion-studio/src/editor/App.tsx</files>
  <read_first>services/remotion-studio/src/editor/components/StyleControls.tsx, services/remotion-studio/src/editor/App.tsx, services/remotion-renderer/src/pipeline-config.ts</read_first>
  <action>
Per D-09 and D-03 (PREV-03): Extend StyleControls.tsx with three new controls that are missing from the current component: `lineHeight`, `pastWordOpacity`, and `bottomOffset`. These are SubtitleConfig fields needed for the preview page and also useful in the editor. bottomOffset is explicitly required by PREV-03 as a real-time adjustable parameter.

1. **StyleControls.tsx**: Add three new slider controls after the letter-spacing section:

   a. **Line Height slider**: Range 0.8 to 3.0, step 0.1, default 1.3 (matching DEFAULT_SUBTITLE_CONFIG.lineHeight). Label: "Line Height: {value}". Uses `onChange({ lineHeight: Number(e.target.value) })`.

   b. **Past Word Opacity slider**: Range 0 to 1, step 0.05, default 0.4 (matching DEFAULT_SUBTITLE_CONFIG.pastWordOpacity). Label: "Past Word Opacity: {value}". Uses `onChange({ pastWordOpacity: Number(e.target.value) })`.

   c. **Bottom Offset slider**: Range 0 to 960, step 10, default 250 (matching DEFAULT_SUBTITLE_CONFIG.bottomOffset). Label: "Bottom Offset: {value}px". Uses `onChange({ bottomOffset: Number(e.target.value) })`. This controls the vertical distance from the bottom edge where subtitle text anchors, critical for positioning subtitles within the 9:16 safe zone.

   Place all three sliders between the letter-spacing section and the background-highlight section, matching the existing slider pattern (label with bold current value, range input with accentColor, min/max display).

2. **App.tsx**: No changes needed — the existing `updateSubtitle(partial: Partial<SubtitleConfig>)` callback already handles any partial SubtitleConfig field, including the new lineHeight, pastWordOpacity, and bottomOffset. The StyleControls component reads all three from the same `config: SubtitleConfig` prop.

3. **Verify** that the TypeScript types are correct: StyleControls imports `SubtitleConfig` from `../../pipeline-config.js`, which now includes the `pastWordOpacity` field added in Task 1. The `bottomOffset` field already exists in SubtitleConfig with a default of 250.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - StyleControls.tsx has a "Line Height" slider with range 0.8–3.0, step 0.1, default 1.3
    - StyleControls.tsx has a "Past Word Opacity" slider with range 0–1, step 0.05, default 0.4
    - StyleControls.tsx has a "Bottom Offset" slider with range 0–960, step 10, default 250
    - All three sliders call onChange with the correct SubtitleConfig partial
    - TypeScript compilation succeeds for remotion-studio
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→API | SubtitleConfig values come from client-side state — no auth required (internal Docker network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | StyleControls pastWordOpacity | mitigate | Validate pastWordOpacity is 0–1 in validatePipelineConfig — prevents values >1 or <0 from causing rendering issues |
| T-12-02 | Tampering | SubtitleConfig lineHeight | mitigate | Validate lineHeight is positive number in validatePipelineConfig — prevents 0 or negative values from breaking layout |
| T-12-SC | Tampering | npm install (remotion-studio) | accept | No new packages installed in this plan — only extending existing code |
</threat_model>

<verification>
1. TypeScript compilation succeeds in remotion-renderer: `cd services/remotion-renderer && npx tsc --noEmit`
2. TypeScript compilation succeeds in remotion-studio: `cd services/remotion-studio && npx tsc --noEmit`
3. Existing tests pass: `cd services/remotion-renderer && npx vitest run`
4. Grep for `pastWordOpacity` in all 4 layout files confirms presence
5. Grep for `PAST_OPACITY` in SentenceLayout confirms it's removed (replaced with config-driven value)
6. StyleControls.tsx contains "Line Height", "Past Word Opacity", and "Bottom Offset" slider labels
</verification>

<success_criteria>
- SubtitleConfig.pastWordOpacity exists with default 0.4 and validation (0–1 range)
- All 4 layout components (TikTok, Sentence, Bar, Karaoke) apply pastWordOpacity to was-active words
- SentenceLayout no longer has hardcoded PAST_OPACITY = 0.5
- StyleControls has lineHeight slider (0.8–3.0), pastWordOpacity slider (0–1), and bottomOffset slider (0–960)
- bottomOffset slider allows real-time vertical positioning of subtitles
- Root.tsx merges pastWordOpacity into config defaults
- TypeScript compiles without errors in both remotion-renderer and remotion-studio
</success_criteria>

<output>
Create `.planning/phases/12-subtitle-preview-lab/12-01-SUMMARY.md` when done
</output>