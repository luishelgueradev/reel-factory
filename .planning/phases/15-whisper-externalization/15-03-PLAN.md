---
phase: 15-whisper-externalization
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - /home/luis/proyectos/whisper/app/transcript.py
  - services/whisper/
  - docker-compose.yml
autonomous: false

must_haves:
  truths:
    - "The external whisper-api Transcript model emits timeline: \"original\" in its profile=reels body (1-line additive in /home/luis/proyectos/whisper/app/transcript.py)"
    - "A full pipeline run on a clip WITH mid-speech pauses shows highlight-vs-audio sync on the back half (the deferred Spike 001 drift repro is closed) — human-verified"
    - "A parity test transcribes the same clip via old vs new path and confirms word count + timings within tolerance + no_speech_prob present (model value + minor alignment deltas allowed)"
    - "services/whisper/ is deleted and its old compose service is gone; the pipeline still runs end-to-end with the HTTP step"
  artifacts:
    - path: "/home/luis/proyectos/whisper/app/transcript.py"
      provides: "timeline marker on the reels contract"
      contains: "timeline"
  key_links:
    - from: "/home/luis/proyectos/whisper/app/transcript.py Transcript model"
      to: "reels body via filtered.model_dump() (inference.py:395)"
      via: "pydantic default field serialized by model_dump"
      pattern: "timeline"
    - from: "transcript.json timeline=original"
      to: "renderer deterministic remap (captions.ts)"
      via: "shouldSkipSilenceRemap reads timeline marker"
      pattern: "timeline"
---

<objective>
Close out the externalization: add the explicit `timeline: "original"` marker to the external whisper-api contract, validate the full pipeline against a real mid-speech-cut clip (closing the deferred Spike 001 highlight-drift repro), run a parity test old-vs-new, and retire the embedded `services/whisper/` service. Wave 3 — depends on 15-02 having wired the HTTP step.

Purpose: Prove the externalized path is a true drop-in (parity + visual sync) and that the timeline marker fires the renderer's deterministic remap rather than the legacy heuristic, then remove the dead GPU service. This plan has a human-verify checkpoint (needs the external service live + visual judgment), so it is NOT autonomous.

Output: timeline marker added to whisper-api; drift repro + parity verified; services/whisper/ and its compose service deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/contracts/whisper-service-integration.md
@.planning/phases/15-whisper-externalization/15-CONTEXT.md
@.planning/phases/15-whisper-externalization/15-PATTERNS.md
@.planning/phases/15-whisper-externalization/15-01-SUMMARY.md
@.planning/phases/15-whisper-externalization/15-02-SUMMARY.md

<interfaces>
External whisper-api Transcript model (/home/luis/proyectos/whisper/app/transcript.py:39-67):
  fields language, model, segments, words, duration — LACKS timeline.
Reels body assembly (/home/luis/proyectos/whisper/app/inference.py:377-395):
  profile=="reels" -> filter_hallucinations(...).model_dump() — a pydantic default field on Transcript serializes automatically.
reel-factory side already has the marker: services/whisper/src/schema.py:94 (timeline: str = "original") and the renderer reads it (captions.ts:shouldSkipSilenceRemap, contract §5/§7).

Old whisper service to retire (15-PATTERNS.md DELETE section):
  services/whisper/{Dockerfile,main.py,requirements.txt}, services/whisper/src/*.py, services/whisper/tests/*.py, services/whisper/.pytest_cache/
  compose: the old whisper build service was already replaced in 15-02; this plan only confirms no stale ./services/whisper build context remains.
Drift bug context (contract §5): captions.ts maxWordEnd heuristic mis-fires on mid-speech cuts; explicit timeline=original makes the remap deterministic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeline marker to the external whisper-api contract + parity test</name>
  <read_first>
    - /home/luis/proyectos/whisper/app/transcript.py (Transcript model 39-67 — add the field after duration)
    - /home/luis/proyectos/whisper/app/inference.py (377-395 — confirm reels body is filtered.model_dump())
    - services/whisper/src/schema.py (the reel-factory-side reference: timeline: str = "original" at line 94 — match it exactly)
    - .planning/contracts/whisper-service-integration.md (§5 timeline marker, §6 validation plan items 1 and 3)
    - .planning/phases/15-whisper-externalization/15-01-SUMMARY.md (the NO_AUDIO_STREAM behavior-change flag — parity test must account for it)
  </read_first>
  <action>
    NOTE: this task touches a DIFFERENT repo (/home/luis/proyectos/whisper). Make the edit there; it is intentionally outside reel-factory's tree.

    1) In /home/luis/proyectos/whisper/app/transcript.py, add to the Transcript model a one-line additive field: `timeline: str = "original"` immediately after `duration: float` (line 67). Match the reel-factory reference in services/whisper/src/schema.py:94 (same name, same default, same semantics — word timestamps are on the ORIGINAL uncut audio). Because the reels body is `filtered.model_dump()` (inference.py:395), this field serializes into the bare body automatically — no inference.py change needed. Add a docstring line mirroring the reel-factory schema explaining why (renderer's deterministic remap; legacy maxWordEnd heuristic is fallback only).

    2) Run the external repo's existing contract/shape tests to confirm the field is now emitted and nothing else changed: from /home/luis/proyectos/whisper, run its test suite (e.g. pytest tests/test_contract_shape.py and any test_transcript). If a contract test asserts the exact key set {language,model,segments,words,duration}, update it to include timeline (the reel-factory side already expects/tolerates it). Do NOT alter the reels invariants (no LLM, no remap).

    3) Parity test (reel-factory side, contract §6 item 3): write a small test/script under services/whisper-http-step/tests/ (or a standalone scripts/ helper) that, given a transcript.json produced by the OLD path (a saved fixture from services/whisper, or whisper/sync.json) and one produced by the NEW path for the same clip, asserts: identical word count (within a small tolerance if alignment differs), per-word start/end within a tolerance (e.g. ±0.15s), no_speech_prob present on every word, and that the model value is ALLOWED to differ. Account for the NO_AUDIO_STREAM behavior change flagged in 15-01-SUMMARY (a no-audio clip now fails instead of producing an empty transcript — exclude that case from parity or assert the new fail-step behavior explicitly). If a live new-path transcript is not available until Task 2's run, structure this as a reusable comparator function with a unit test over two fixtures, and invoke it for real in Task 2.
  </action>
  <verify>
    <automated>grep -q "timeline" /home/luis/proyectos/whisper/app/transcript.py && cd /home/luis/proyectos/whisper && python -m pytest -q -k "contract or transcript or shape" 2>&1 | tail -5</automated>
  </verify>
  <done>The external whisper-api Transcript model has timeline: str = "original"; the external repo's contract tests pass with the new field emitted; a parity comparator exists (with a fixture-based unit test) asserting word count + timing tolerance + no_speech_prob, allowing the model value to differ and accounting for the NO_AUDIO_STREAM change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full externalized pipeline: reel-factory's whisper step now POSTs to the live external whisper-api (GPU, WhisperX large-v3), which emits timeline="original"; the renderer consumes the marker for a deterministic silence remap. This checkpoint validates the deferred Spike 001 highlight-drift repro that was previously impossible (no GPU for whisper in-pipeline).
  </what-built>
  <how-to-verify>
    1. Ensure the external whisper-api is running and healthy: `curl -s http://localhost:8000/health` returns `{status:"ok",...}`. Ensure WHISPER_API_KEY is set in reel-factory's .env (matching the whisper-api's API_KEY).
    2. Rebuild + bring up reel-factory: `docker compose build whisper api-server` then `docker compose up -d`.
    3. Process a clip that has MID-SPEECH pauses (NOT just trailing silence) — this is the case that triggers the legacy maxWordEnd heuristic mis-fire. Submit it via the API (POST /process) or the normal pipeline entry.
    4. Open the rendered output and watch the BACK HALF of the video: confirm each highlighted word lights up in sync with the spoken audio — no progressive drift where the highlight runs ahead of/behind the voice.
    5. Inspect pipeline/{jobId}/whisper/transcript.json and confirm it contains "timeline": "original" and per-word no_speech_prob.
    6. Run the parity comparator from Task 1 against this run's transcript.json vs an old-path fixture for the same clip; confirm it passes (word count + timing tolerance + no_speech_prob).
    Expected: highlights stay locked to audio through the back half; transcript.json has the timeline marker; parity holds.
  </how-to-verify>
  <resume-signal>Type "approved" if back-half highlight sync is correct and the timeline marker + parity hold; otherwise describe the drift/discrepancy observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Retire services/whisper/ + confirm no stale references</name>
  <read_first>
    - .planning/phases/15-whisper-externalization/15-PATTERNS.md (DELETE section — full file list + the grep-verified "retirement is clean" notes)
    - docker-compose.yml (confirm 15-02 already swapped the whisper service to ./services/whisper-http-step; this task verifies no ./services/whisper build context remains)
    - services/whisper/ (the tree to delete)
  </read_first>
  <action>
    GATED: only run after the Task 2 human checkpoint is approved (parity + drift repro pass). Per D-5.

    1) Confirm the contract is preserved as reference: the field-for-field schema lives in the new step's services/whisper-http-step/src/schema.py (ported in 15-01) and the contract doc in .planning/contracts/. If the parity comparator from Task 1 still imports anything from services/whisper/, move that logic into services/whisper-http-step/ first so deletion does not break it.

    2) Delete the entire services/whisper/ tree (Dockerfile, main.py, requirements.txt, src/*.py, tests/*.py, .pytest_cache/). Use git rm -r so the removal is staged.

    3) Confirm docker-compose.yml has NO remaining build context or image referencing ./services/whisper (the OLD path); the only whisper service must be the 15-02 HTTP-step one pointing at ./services/whisper-http-step. Grep the whole repo for "services/whisper" word-boundary (NOT whisper-http-step) and "reel-factory-whisper:latest" to ensure no stale references remain in compose, scripts, or docs that would break a build.

    EXPECTED/BENIGN references that survive deletion (do NOT treat as failures — they are pure doc comments, zero build impact): `services/srt-exporter/src/types.ts` lines ~24/33/42 ("Mirror of services/whisper/src/schema.py ...") and `services/silence-cutter/src/config.py` line ~21 ("Matches NO_SPEECH_THRESHOLD from whisper/src/config.py ..."). Optionally re-point these comments to `services/whisper-http-step/src/schema.py` / `.planning/contracts/whisper-service-integration.md` in this same pass for cleanliness, but leaving them is acceptable — they do not break any build.

    5) No renderer-sync triggered by this phase (CLAUDE.md convention): the consumer-side `timeline` marker support (`shouldSkipSilenceRemap`) already ships in BOTH `services/remotion-studio/src/captions.ts` and `services/remotion-renderer/src/captions.ts` (landed during the v1.1 sync fix). This phase only adds the producer-side marker (external whisper-api) — no `compositions/` or shared renderer module is modified, so the studio→renderer copy step does NOT need to run.

    4) Verify the pipeline still composes: docker compose config parses; the whisper service resolves to the HTTP step image.
  </action>
  <verify>
    <automated>test ! -d services/whisper && ! grep -rn "context: ./services/whisper$" docker-compose.yml && ! grep -rn "reel-factory-whisper:latest" docker-compose.yml && grep -q "reel-factory-whisper-http-step" docker-compose.yml && docker compose config >/dev/null</automated>
  </verify>
  <done>services/whisper/ no longer exists; docker-compose.yml references only the HTTP-step whisper service (no ./services/whisper build context, no reel-factory-whisper:latest image); compose config parses; the contract reference survives in services/whisper-http-step/src/schema.py + .planning/contracts/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reel-factory pipeline → live external whisper-api | Real transcription over HTTP during the e2e run |
| repo state → git rm of services/whisper | Destructive deletion gated behind verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Repudiation/Loss | Deleting services/whisper before contract is preserved | mitigate | Deletion gated behind the human checkpoint + Task 3 step 1 confirms schema.py + contract doc preserve the reference |
| T-15-09 | Tampering | Editing a different repo (/home/luis/proyectos/whisper) | mitigate | One-line additive default field only; run that repo's contract tests to confirm reels invariants untouched |
| T-15-10 | Denial of service | e2e run against a down whisper-api | accept | Checkpoint instructs verifying /health first; step fails with a clear PipelineStepError otherwise |
</threat_model>

<verification>
- `grep -q timeline /home/luis/proyectos/whisper/app/transcript.py` — marker added.
- External whisper-api contract tests pass with the new field.
- transcript.json from a live run contains "timeline": "original" + per-word no_speech_prob.
- Parity comparator passes (word count + timing tolerance, model value allowed to differ).
- Human checkpoint approved: back-half highlight sync correct on a mid-speech-cut clip.
- `test ! -d services/whisper` and `docker compose config` clean.
</verification>

<success_criteria>
- The external whisper-api emits timeline="original" so the renderer's deterministic remap fires (not the legacy heuristic).
- The deferred Spike 001 drift repro is closed: highlights stay synced through the back half of a mid-speech-cut clip (human-verified).
- Parity old-vs-new confirmed within tolerance.
- services/whisper/ retired; pipeline runs end-to-end on the HTTP step only.
</success_criteria>

<output>
Create `.planning/phases/15-whisper-externalization/15-03-SUMMARY.md` when done. Record the drift-repro outcome (clip used, observed sync), the parity result (tolerances + word counts), and confirm the timeline marker fired the deterministic path. Note that the old whisper service is retired.
</output>
