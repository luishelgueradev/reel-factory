---
phase: 24-named-config-profiles
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - services/remotion-studio/src/preview/PreviewApp.tsx
  - services/remotion-studio/src/preview/ProfilesMenu.tsx
  - services/remotion-studio/src/preview/profiles-menu.test.tsx
autonomous: true
requirements: [PROFILE-01, PROFILE-02, PROFILE-03]
must_haves:
  truths:
    - "A 'Perfiles' control sits in the header action zone (next to Guardar config), opening an inline popover — no modal (D-09)"
    - "Saving the current Studio config under a name creates a profile that appears in the list immediately (PROFILE-01)"
    - "Selecting a profile applies it (PUT /api/profiles/:slug/apply) and the Studio state (subtitle, titles, overlays) refreshes from the response (PROFILE-02)"
    - "The list supports inline rename and delete with persistence and immediate reflection (PROFILE-03)"
    - "Green discipline holds: 'Guardar config' is the only --action green; profile actions use --accent/--danger (D-10)"
  artifacts:
    - "services/remotion-studio/src/preview/ProfilesMenu.tsx"
    - "services/remotion-studio/src/preview/profiles-menu.test.tsx"
  key_links:
    - "ProfilesMenu calls GET/POST /api/profiles, PUT /api/profiles/:slug/apply, PATCH, DELETE (24-02)"
    - "PreviewApp passes current config (subtitle+liveTitles+liveOverlays) to ProfilesMenu and applies returned config to state"
---

<objective>
Build the Studio UI for named profiles: a header "Perfiles ▾" inline popover that
saves the current config under a name, lists profiles, applies one (restoring full
Studio state), and renames/deletes inline. Honors the 24-UI-SPEC.md contract.
</objective>

<context>
@.planning/phases/24-named-config-profiles/24-CONTEXT.md
@.planning/phases/24-named-config-profiles/24-UI-SPEC.md
- PreviewApp.tsx state + save handler: subtitleConfig, titles/liveTitles, overlays/liveOverlays (Phase 22/23). Saving current config = { subtitle: subtitleConfig, titles: liveTitles, overlays: liveOverlays } — same payload shape PUT /api/config uses.
- Apply response = a PipelineConfig; refresh Studio state from it (mirror the load-on-mount fetch path that sets subtitle/titles/overlays + sync live*).
- Reuse existing OKLCH tokens and the "✓ Guardado recién"-style transient chip pattern.
- Reuse the render-state machine (23-04) to disable apply/save while a render is submitting|running.
</context>

<ui_quality_mandate>
NON-NEGOTIABLE (AGENTS.md / CLAUDE.md): this is frontend work. At the START of implementation you MUST:
1. Invoke the `impeccable` skill.
2. Use the `frontend-design` plugin for the visual design of the popover.
3. Read the project skill `sketch-findings-reel-factory` (SKILL.md) — follow the validated `named style presets` + `header action zone` + inline-first / modal-stack-law + green-discipline grammar. Do NOT improvise UI.
Match the existing header/control visual language exactly (tokens, density, motion).
</ui_quality_mandate>

<tasks>

<task type="auto">
  <name>Task 1: ProfilesMenu component (inline popover) + tests</name>
  <files>services/remotion-studio/src/preview/ProfilesMenu.tsx, services/remotion-studio/src/preview/profiles-menu.test.tsx</files>
  <action>
    Create ProfilesMenu.tsx — a self-contained inline popover per 24-UI-SPEC.md:
    - Props: `{ getCurrentConfig: () => PipelineConfig, onApplied: (config: PipelineConfig) => void, disabled?: boolean }`.
    - On open: GET /api/profiles → list (sorted by updatedAt desc). Loading + empty states per spec.
    - Save-as: text input + "Guardar actual" (--accent). POST /api/profiles { name, config: getCurrentConfig() } → prepend/refresh list; "✓ Perfil guardado" chip. If name matches existing → label "Actualizar" + inline confirm. (PROFILE-01)
    - Row click → PUT /api/profiles/:slug/apply → call onApplied(responseConfig); mark active row; "✓ Perfil aplicado" chip. (PROFILE-02)
    - Row rename (pencil) → inline editable input, Enter commits via PATCH, Esc cancels; list reflects new name immediately. (PROFILE-03)
    - Row delete (trash, --danger) → inline "¿Borrar? Sí/No" confirm → DELETE → row removed immediately. (PROFILE-03)
    - Error handling: inline danger line with cause on any failed call; never silent.
    - Accessibility: trigger toggles; Esc closes; focus the save input on open; return focus to trigger on close; rows keyboard-navigable.
    - Tokens only; motion respects prefers-reduced-motion. Green discipline: NO --action green inside the menu.
    Tests (profiles-menu.test.tsx, vitest + @testing-library/react, mock fetch):
    - Renders trigger; opening fetches + lists profiles; empty state when none.
    - Save-as POSTs { name, config } and shows the new profile + chip. (PROFILE-01)
    - Clicking a row PUTs .../apply and calls onApplied with the returned config. (PROFILE-02)
    - Rename PATCHes and updates the row; delete (after confirm) DELETEs and removes the row. (PROFILE-03)
    - Assert no element carries the --action (green) token class inside the menu (green discipline).
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx vitest run src/preview/profiles-menu.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ProfilesMenu implements save/list/apply/rename/delete against the 24-02 API.
    - Tests green; green-discipline assertion passes.
  </acceptance_criteria>
  <done>Inline profiles popover fully built + tested against the API contract.</done>
</task>

<task type="auto">
  <name>Task 2: Mount ProfilesMenu in PreviewApp header + wire apply→state</name>
  <files>services/remotion-studio/src/preview/PreviewApp.tsx</files>
  <action>
    - Render <ProfilesMenu> in the header action zone, immediately left of "Guardar config".
    - getCurrentConfig() returns { subtitle: subtitleConfig, titles: liveTitles, overlays: liveOverlays } (+ visualEffects if PreviewApp tracks it) — the same shape PUT /api/config sends.
    - onApplied(config): set subtitleConfig, titles+liveTitles, overlays+liveOverlays from the applied config (reuse/extract the same state-population logic used on load-on-mount so titles/overlays + their live mirrors stay in sync). The preview must reflect the applied profile immediately. (PROFILE-02)
    - Pass disabled = renderState is "submitting" || "running" (guard mid-render config swap).
    - Keep "Guardar config" as the only green CTA (D-10).
    - Extend/adjust render-overlay/PreviewApp tests only as needed so the suite stays green; do not weaken existing assertions.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ProfilesMenu mounted in the header; applying a profile updates subtitle+titles+overlays in the live preview.
    - Full studio suite green; build (npm run build:editor) succeeds.
  </acceptance_criteria>
  <done>Profiles control is live in the Studio header and applying restores full config into the editor + preview.</done>
</task>

</tasks>

<verification>
- `npx vitest run` (studio) all green; `npm run build:editor` succeeds.
- Visual: one green on screen; inline popover (no modal); empty/loading/error states designed.
</verification>

<success_criteria>
- From the Studio header a user saves the current config as a named profile, sees it listed, applies one to restore the full config into the preview, and renames/deletes inline — all without leaving the page.
</success_criteria>

<output>
Create `.planning/phases/24-named-config-profiles/24-03-SUMMARY.md` when done.
</output>
