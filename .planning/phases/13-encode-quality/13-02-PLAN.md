---
phase: 13-encode-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ffmpeg-finalizer/src/config.py
  - services/ffmpeg-finalizer/src/crop.py
  - services/ffmpeg-finalizer/src/validate.py
  - services/ffmpeg-finalizer/tests/test_finalizer.py
autonomous: true
requirements:
  - ENC-02
  - ENC-03
  - ENC-04

must_haves:
  truths:
    - "ffmpeg-finalizer encodes at CRF 18 (was 20), landing in the 5,000–8,000 kbps band for a 60s talking-head fixture (D-03)"
    - "H264_PRESET stays 'medium' — only CRF moves; slow-preset choices belong to Phase 14's Remotion encode (D-04)"
    - "Every scale= invocation in the finalizer filter chain carries flags=lanczos — both crop-applied and no-crop branches (D-08)"
    - "The filter chain ends with unsharp=5:5:0.5:5:5:0.3 after setsar=1 and before the encode flags — applied unconditionally to both crop-applied and no-crop branches (D-05, D-06, D-07)"
    - "The finalizer ffmpeg cmd carries -colorspace bt709 -color_primaries bt709 -color_trc bt709 immediately after -pix_fmt yuv420p — metadata tags only, NOT the colorspace filter (D-09)"
    - "The no-audio (-an) path inherits all four changes without duplication, because they live in the shared filter_chain and cmd list (D-13)"
    - "validate.py exposes validate_color_tags, validate_bitrate_range, and validate_duration_parity (ffprobe asserts color_space/color_primaries/color_transfer = bt709, bitrate band, duration delta ≤ ±33ms); the existing CRF check is updated to enforce CRF 18 under ENC-02 (D-10, D-11)"
    - "test_finalizer.py CRF fixtures are swept end-to-end from 20 → 18 (all 8 occurrences); the suite is GREEN at the end of Plan 02 (no half-passing state, no spurious ENC-02 noise)"
  artifacts:
    - path: "services/ffmpeg-finalizer/src/config.py"
      provides: "H264_CRF = 18 constant (was 20)"
      contains: "H264_CRF = 18"
    - path: "services/ffmpeg-finalizer/src/crop.py"
      provides: "Lanczos-flagged scale, unsharp filter, BT.709 metadata tags, manifest fields for new flags"
      contains: "unsharp=5:5:0.5:5:5:0.3"
    - path: "services/ffmpeg-finalizer/src/validate.py"
      provides: "validate_color_tags, validate_bitrate_range, validate_duration_parity ffprobe-based validators"
      contains: "def validate_color_tags"
    - path: "services/ffmpeg-finalizer/tests/test_finalizer.py"
      provides: "CRF-18-aware fixtures + ENC-02 assertion; suite is green under the new validator"
      contains: "config.H264_CRF == 18"
  key_links:
    - from: "services/ffmpeg-finalizer/src/crop.py::apply_finalizer"
      to: "config.H264_CRF"
      via: "str(config.H264_CRF) in the cmd list (already wired; only the constant value changes)"
      pattern: "str\\(config\\.H264_CRF\\)"
    - from: "services/ffmpeg-finalizer/src/crop.py::apply_finalizer"
      to: "ffmpeg argv color tags"
      via: "cmd list contains '-colorspace', 'bt709' immediately after '-pix_fmt', 'yuv420p'"
      pattern: '"-colorspace",\\s*"bt709"'
    - from: "services/ffmpeg-finalizer/src/validate.py::validate_color_tags"
      to: "ffprobe -show_entries stream=color_space,color_primaries,color_transfer"
      via: "shared ffprobe JSON pattern from crop.py::probe_video"
      pattern: "color_space,color_primaries,color_transfer"
    - from: "services/ffmpeg-finalizer/tests/test_finalizer.py::TestConfig::test_encoding_params"
      to: "config.H264_CRF"
      via: "assert config.H264_CRF == 18"
      pattern: "assert\\s+config\\.H264_CRF\\s*==\\s*18"
---

<objective>
Apply the three core encode-quality changes to the ffmpeg-finalizer service: drop CRF 20 → 18 (ENC-02), add `flags=lanczos` + `unsharp=5:5:0.5:5:5:0.3` to the filter chain in both branches (ENC-04), and tag the encode with BT.709 metadata flags (ENC-03). Extend `validate.py` with three new ffprobe-based validators (color tags, bitrate range, duration parity) so the new invariants are test-enforceable. Sweep the existing `test_finalizer.py` CRF fixtures from 20 → 18 in the same plan so the test suite is GREEN at the end of Plan 02 — no half-passing state hand-off to Plan 03.

Purpose: Closes ENC-02 (sharper finalizer encode within target bitrate band), ENC-03 (correct color tagging — eliminates "lavado de color" on Instagram), and ENC-04 (Lanczos scaling + mild unsharp without halos). Together with Plan 01 these are the entire phase scope before adding new tests/UAT.

Output: Modified `config.py` (one constant), `crop.py` (filter chain + cmd list + manifest dict), `validate.py` (three new validators + updated CRF check), and `tests/test_finalizer.py` (CRF fixture sweep + updated TestConfig assertion). No new modules, no new env vars, no Docker Compose changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-encode-quality/13-CONTEXT.md
@.planning/phases/13-encode-quality/13-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@.planning/phases/04-9-16-vertical-output/04-CONTEXT.md
@services/ffmpeg-finalizer/src/config.py
@services/ffmpeg-finalizer/src/crop.py
@services/ffmpeg-finalizer/src/validate.py
@services/ffmpeg-finalizer/tests/test_finalizer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop H264_CRF 20 → 18 in config.py (ENC-02, D-03)</name>
  <files>services/ffmpeg-finalizer/src/config.py</files>
  <read_first>
    - services/ffmpeg-finalizer/src/config.py (lines 24-30 — the existing H264_CRF + H264_PRESET block, lines 7-22 — the surrounding Phase 4 D-XX comment convention to match)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-03 CRF 18 decision + rationale; D-04 preset unchanged)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"services/ffmpeg-finalizer/src/config.py" target shape with the dual D-08/D-03 reference comment)
    - .planning/phases/04-9-16-vertical-output/04-CONTEXT.md (original D-08 CRF 20 rationale being superseded)
  </read_first>
  <behavior>
    - `python -c "from src import config; print(config.H264_CRF)"` from `services/ffmpeg-finalizer/` prints `18`.
    - `python -c "from src import config; print(config.H264_PRESET)"` from `services/ffmpeg-finalizer/` still prints `medium` (D-04 invariant).
    - No `H264_CRF_ENV` constant introduced (CONTEXT.md "Deferred Ideas" — constants-over-env-vars convention preserved).
  </behavior>
  <action>
    In services/ffmpeg-finalizer/src/config.py, in the H264_CRF block at lines 24-26:
    1. REPLACE the value `H264_CRF = 20` with `H264_CRF = 18` (single integer literal).
    2. REPLACE the leading two-line comment (currently `# D-08: H.264 CRF 20 for quality/filesize balance.` + `# Good visual quality for ...`) with a four-line comment that references BOTH the superseded Phase 4 D-08 AND the new Phase 13 D-03 / ENC-02. The comment must include: "Phase 4 D-08 (superseded by Phase 13 D-03 / ENC-02)", a one-sentence rationale on the 5,000–8,000 kbps target band for the 60s talking-head fixture, and a one-line note that CRF 17 was considered and rejected (research-validated; marginal gain given Remotion re-encodes in Phase 14).
    3. KEEP `H264_PRESET = "medium"` on line 30 and its D-09 comment exactly as-is (D-04 explicit preservation).
    4. KEEP `H264_PROFILE = "high"` on line 33 unchanged.
    5. DO NOT introduce `H264_CRF_ENV` (no env-var surface — CONTEXT.md D-03).
    Do not touch any other constant in the file (LOUDNORM_*, SAFE_ZONE_*, FPS_OUTPUT, CROP_STRATEGY, etc.).
  </action>
  <verify>
    <automated>cd services/ffmpeg-finalizer &amp;&amp; grep -cE '^H264_CRF\s*=\s*18\s*$' src/config.py | grep -q '^1$' &amp;&amp; grep -cE '^H264_CRF\s*=\s*20' src/config.py | grep -q '^0$' &amp;&amp; grep -cE '^H264_PRESET\s*=\s*"medium"\s*$' src/config.py | grep -q '^1$' &amp;&amp; grep -cE '^H264_CRF_ENV' src/config.py | grep -q '^0$' &amp;&amp; grep -cF 'ENC-02' src/config.py | grep -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E '^H264_CRF\s*=\s*18\s*$' services/ffmpeg-finalizer/src/config.py` returns exactly one match.
    - Source assertion: `grep -E '^H264_CRF\s*=\s*20' services/ffmpeg-finalizer/src/config.py` returns zero matches.
    - Source assertion: `grep -E '^H264_PRESET\s*=\s*"medium"\s*$' services/ffmpeg-finalizer/src/config.py` returns exactly one match (D-04 invariant).
    - Source assertion: `grep -E '^H264_CRF_ENV' services/ffmpeg-finalizer/src/config.py` returns zero matches (no env-var surface).
    - Source assertion: `grep -F 'ENC-02' services/ffmpeg-finalizer/src/config.py` returns at least one match (new traceability comment).
    - Source assertion: `grep -F 'D-03' services/ffmpeg-finalizer/src/config.py` returns at least one match (Phase 13 decision referenced).
    - Behavior assertion: `cd services/ffmpeg-finalizer && python -c "from src import config; assert config.H264_CRF == 18 and config.H264_PRESET == 'medium'"` exits 0.
  </acceptance_criteria>
  <done>H264_CRF is 18 with dual D-08/D-03 trailing comment; H264_PRESET is unchanged; no env-var surface added; downstream consumers (`crop.py`'s `str(config.H264_CRF)` in the cmd list) automatically pick up the new value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Lanczos + unsharp + BT.709 to crop.py filter chain and cmd (ENC-03, ENC-04, D-05..D-09, D-13)</name>
  <files>services/ffmpeg-finalizer/src/crop.py</files>
  <read_first>
    - services/ffmpeg-finalizer/src/crop.py (lines 107-202 entire `apply_finalizer` function — both filter-chain branches at lines 126-135, encode cmd at lines 137-150, no-audio branch at 163-164, return dict at 180-202)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-05 unsharp values, D-06 placement after setsar=1, D-07 unconditional, D-08 lanczos on every scale=, D-09 BT.709 metadata tags only — NOT the colorspace filter, D-13 no-audio inheritance)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"services/ffmpeg-finalizer/src/crop.py" — both target shapes; "No-audio branch inheritance" note that the -an branch does NOT re-emit cmd; "Return-dict tracing pattern" for new manifest fields)
    - .planning/research/PITFALLS.md (A-6: BT.709 metadata vs filter distinction)
    - .planning/research/FEATURES.md ("Anti-Features" — unsharp luma > 0.8 produces halos; 0.5 is the safe ceiling)
  </read_first>
  <behavior>
    - Both filter-chain branches end with `unsharp=5:5:0.5:5:5:0.3` and have `flags=lanczos` on every `scale=`.
    - The encode cmd contains `-colorspace bt709`, `-color_primaries bt709`, `-color_trc bt709` immediately after `-pix_fmt yuv420p` and before `-r`.
    - No `colorspace=` filter is added to the filter chain (PITFALLS.md A-6 — metadata only).
    - The no-audio (`-an`) branch is NOT modified — it inherits all changes via the shared cmd/filter_chain.
    - The return dict from `apply_finalizer` includes new fields `lanczos_scaling=True`, `unsharp_filter="5:5:0.5:5:5:0.3"`, `color_space="bt709"`, `color_primaries="bt709"`, `color_transfer="bt709"` so validators can assert against the manifest without re-probing the MP4.
    - probe_video and compute_crop functions are untouched.
  </behavior>
  <action>
    In services/ffmpeg-finalizer/src/crop.py, modify only the `apply_finalizer` function (lines 107-202):

    PART A — filter_chain (lines 126-135):
    1. In the `if crop_applied:` branch at line 126-132: change the `scale=` line so it reads `scale={target_width}:{target_height}:force_original_aspect_ratio=increase:flags=lanczos`. After the `setsar=1` line, append a fourth filter line `unsharp=5:5:0.5:5:5:0.3`. The filter_chain is a Python f-string assembled from four comma-joined parts; preserve the f-string assembly pattern.
    2. In the `else:` branch at line 133-135 (already-9:16 input): change `scale={target_width}:{target_height}` to `scale={target_width}:{target_height}:flags=lanczos`, and append `,unsharp=5:5:0.5:5:5:0.3` after `setsar=1`. Result is a three-part comma-joined filter string.
    3. Add explanatory comments above EACH branch referencing ENC-04 / D-05 / D-06 / D-08 / D-13 — matching the existing D-03 inline-comment style at line 134.

    PART B — cmd list (lines 137-150):
    1. INSERT the three BT.709 metadata args `"-colorspace", "bt709",` then `"-color_primaries", "bt709",` then `"-color_trc", "bt709",` between the existing `"-pix_fmt", "yuv420p",` line (line 145) and the existing `"-r", str(config.FPS_OUTPUT),` line (line 146).
    2. Add a multi-line comment immediately above these new args referencing ENC-03 / D-09 and explicitly stating "metadata tags ONLY (not the colorspace filter — PITFALLS.md A-6)".
    3. DO NOT modify any other cmd entry: `-i`, `-vf`, `-c:v libx264`, `-crf str(config.H264_CRF)`, `-preset config.H264_PRESET`, `-profile:v config.H264_PROFILE`, `-pix_fmt yuv420p`, `-r str(config.FPS_OUTPUT)`, `-movflags +faststart`, `-map_metadata -1`, `-map 0:v:0` all remain in place and in their existing order (apart from the three new inserted args after `-pix_fmt`).

    PART C — no-audio branch (lines 152-166):
    1. DO NOT touch the `if probe_info["has_audio"]:` / `else: cmd.append("-an")` block. The new filter_chain and color-tag args are already in the shared cmd list above this branch — D-13 inheritance is automatic. PATTERNS.md flags this explicitly: "Do not duplicate logic in the -an branch."

    PART D — return dict (lines 180-202):
    1. Add five new key/value pairs to the returned dict, immediately after `"h264_preset": config.H264_PRESET,`:
       - `"lanczos_scaling": True,`
       - `"unsharp_filter": "5:5:0.5:5:5:0.3",`
       - `"color_space": "bt709",`
       - `"color_primaries": "bt709",`
       - `"color_transfer": "bt709",`
    2. Keep all existing dict keys unchanged (`input_width`, `input_height`, `input_aspect_ratio`, `output_width`, `output_height`, `output_aspect_ratio`, `crop_strategy`, `crop_applied`, `crop_x`, `crop_y`, `crop_width`, `crop_height`, `h264_crf`, `h264_preset`, `audio_normalization`, `safe_zone`).

    Do NOT modify `is_already_target_ratio`, `probe_video`, or `compute_crop` (other functions in the same file).
  </action>
  <verify>
    <automated>cd services/ffmpeg-finalizer &amp;&amp; grep -cE 'scale=.*:flags=lanczos' src/crop.py | grep -q '^2$' &amp;&amp; grep -cE 'unsharp=5:5:0\.5:5:5:0\.3' src/crop.py | grep -q '^2$' &amp;&amp; grep -cE '"-colorspace",\s*"bt709"' src/crop.py | grep -q '^1$' &amp;&amp; grep -cE '"-color_primaries",\s*"bt709"' src/crop.py | grep -q '^1$' &amp;&amp; grep -cE '"-color_trc",\s*"bt709"' src/crop.py | grep -q '^1$' &amp;&amp; grep -cE '"colorspace=bt709"' src/crop.py | grep -q '^0$' &amp;&amp; python -c "from src.crop import apply_finalizer, probe_video, compute_crop; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE 'scale=.*:flags=lanczos' services/ffmpeg-finalizer/src/crop.py` returns exactly `2` (both filter-chain branches carry the flag).
    - Source assertion: `grep -cE 'unsharp=5:5:0\.5:5:5:0\.3' services/ffmpeg-finalizer/src/crop.py` returns exactly `2` (one per branch).
    - Source assertion: `grep -cE '"-colorspace",\s*"bt709"' services/ffmpeg-finalizer/src/crop.py` returns exactly `1` (in the cmd list).
    - Source assertion: `grep -cE '"-color_primaries",\s*"bt709"' services/ffmpeg-finalizer/src/crop.py` returns exactly `1`.
    - Source assertion: `grep -cE '"-color_trc",\s*"bt709"' services/ffmpeg-finalizer/src/crop.py` returns exactly `1`.
    - Source assertion: `grep -cE '"colorspace=bt709"' services/ffmpeg-finalizer/src/crop.py` returns `0` (the colorspace FILTER must not appear — PITFALLS.md A-6).
    - Source assertion: `grep -cF '"lanczos_scaling": True' services/ffmpeg-finalizer/src/crop.py` returns exactly `1` (manifest field).
    - Source assertion: `grep -cF '"unsharp_filter": "5:5:0.5:5:5:0.3"' services/ffmpeg-finalizer/src/crop.py` returns exactly `1`.
    - Source assertion: `grep -cE '"color_space":\s*"bt709"' services/ffmpeg-finalizer/src/crop.py` returns exactly `1`.
    - Source assertion: `grep -F 'ENC-03' services/ffmpeg-finalizer/src/crop.py` returns at least one match.
    - Source assertion: `grep -F 'ENC-04' services/ffmpeg-finalizer/src/crop.py` returns at least one match.
    - Behavior assertion: `cd services/ffmpeg-finalizer && python -c "from src.crop import apply_finalizer, probe_video, compute_crop"` exits 0 (file is still importable).
    - Behavior assertion: the -an branch was NOT touched: `grep -A3 'else:' services/ffmpeg-finalizer/src/crop.py | grep -c 'cmd.append("-an")'` returns exactly `1`, and that block contains no scale/unsharp/colorspace strings.
  </acceptance_criteria>
  <done>Both filter-chain branches carry Lanczos + unsharp; the cmd list carries the three BT.709 metadata args after -pix_fmt; the return dict exposes five new manifest fields; the no-audio branch is untouched and inherits all changes via the shared cmd. crop.py remains importable; probe_video and compute_crop are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update validate.py with ffprobe validators + CRF check AND sweep test_finalizer.py CRF fixtures end-to-end (ENC-02, ENC-03, ENC-05, D-03, D-10, D-11, D-14)</name>
  <files>
    services/ffmpeg-finalizer/src/validate.py
    services/ffmpeg-finalizer/tests/test_finalizer.py
  </files>
  <read_first>
    - services/ffmpeg-finalizer/src/validate.py (full file — header at lines 1-6, validate_finalizer_info at lines 11-101, the existing D-08 CRF check at lines 87-91 that must be updated)
    - services/ffmpeg-finalizer/src/crop.py (lines 35-71 — `probe_video` ffprobe JSON pattern to copy; lines 256-272 of services/silence-cutter/src/cut_video.py for the simpler csv=p=0 duration pattern)
    - services/ffmpeg-finalizer/tests/test_finalizer.py (lines 1-19 module docstring; lines 38-91 valid_finalizer_info_dict + no_crop_info_dict fixtures with h264_crf at lines 54, 82; lines 98-121 TestConfig with test_encoding_params docstring at lines 113-116; lines 196-272 TestSchema with h264_crf at lines 211, 234, 268; lines 279-376 TestValidation with h264_crf at lines 294, 308, 322)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-10 color-tag enforcement, D-11 verification protocol — color tags + 5000-8000 kbps band + ±33ms duration parity, D-14 -c copy edge cases)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"services/ffmpeg-finalizer/src/validate.py" — three suggested validators with their assertion targets; §"Fixture-update locations" — the exhaustive line-number sweep list; §"ffprobe JSON parsing" shared pattern; §"Validator error-string convention")
  </read_first>
  <behavior>
    - The existing D-08 CRF check at lines 87-91 of validate.py is updated to enforce `h264_crf == 18` and the error strings reference `ENC-02` and `(D-03)`. The D-08 reference is updated to ENC-02/D-03 (Phase 4 D-08 is now superseded).
    - New validator `validate_color_tags(output_mp4_path: str) -> List[str]` invokes ffprobe with `-show_entries stream=color_space,color_primaries,color_transfer -of json` and returns `[]` when all three fields equal `"bt709"`, else returns error strings prefixed `ENC-03:` with `(D-10)`.
    - New validator `validate_bitrate_range(output_mp4_path: str, min_kbps: int = 5000, max_kbps: int = 8000) -> List[str]` invokes ffprobe with `-show_entries format=bit_rate -of json`, divides by 1000, returns `[]` when the value is in `[min_kbps, max_kbps]`, else returns an error prefixed `ENC-02:` with `(D-11)`.
    - New validator `validate_duration_parity(silence_cutter_output: str, finalizer_output: str, tolerance_ms: int = 33) -> List[str]` invokes `ffprobe -show_entries format=duration -of csv=p=0` on both files, returns `[]` when `abs(d1 - d2) * 1000 <= tolerance_ms`, else returns an error prefixed `ENC-05:` with `(D-11, D-14)`.
    - All three validators handle ffprobe failure by returning a single error string prefixed with the relevant REQ-ID (no exception escapes).
    - Existing `validate_finalizer_info` and `validate_crop_logic` continue to work; only the inner CRF check is updated.
    - All 8 occurrences of `h264_crf: 20` / `h264_crf=20` in test_finalizer.py are swept to 18 — exhaustive, not partial. After the sweep: `pytest tests/test_finalizer.py -x -q` exits 0 with ZERO spurious ENC-02 errors in the errors list.
    - TestConfig.test_encoding_params asserts `config.H264_CRF == 18` with comment `(ENC-02 / D-03)`.
  </behavior>
  <action>
    PART 1 — services/ffmpeg-finalizer/src/validate.py:

    PART 1A — update header (lines 1-6):
    1. Extend the module docstring to list `ENC-02`, `ENC-03`, `ENC-05` alongside the existing `VERT-XX` references. Keep the one-paragraph convention.

    PART 1B — add imports (after line 8 `from typing import List`):
    1. Add `import subprocess`.
    2. Add `import json`.

    PART 1C — update the D-08 CRF check (lines 87-91 inside validate_finalizer_info):
    1. Replace the comment `# D-08: H264 CRF must be 20` with `# ENC-02 / D-03: H264 CRF must be 18 (Phase 4 D-08 superseded; tightened for sharper output)`.
    2. Replace the literal `20` in the `elif info_data["h264_crf"] != 20:` check with `18`.
    3. Replace the error string prefix `D-08:` in BOTH error appends (lines 89 and 91) with `ENC-02:`, and append `(D-03)` to the "expected 18" error message.
    4. DO NOT remove any other check inside validate_finalizer_info (VERT-01/02/03, D-03 crop_applied, D-04 dimensions, D-06 safe zone, D-10 audio_normalization all stay).

    PART 1D — append three new validators at the bottom of the file (after validate_crop_logic):

    1. `validate_color_tags(output_mp4_path: str) -> List[str]` — docstring referencing ENC-03 / D-10. Build cmd `["ffprobe", "-v", "quiet", "-select_streams", "v:0", "-show_entries", "stream=color_space,color_primaries,color_transfer", "-of", "json", output_mp4_path]`. Run via `subprocess.run(..., capture_output=True, text=True, timeout=30)`. On non-zero returncode, return `[f"ENC-03: ffprobe failed: {result.stderr.strip()}"]`. Parse JSON, pull the first video stream, check each of the three fields equals `"bt709"`. For each mismatch append `f"ENC-03: {field} is '{actual}', expected 'bt709' (D-10)"` (matches existing error-string format from PATTERNS.md §"Validator error-string convention").

    2. `validate_bitrate_range(output_mp4_path: str, min_kbps: int = 5000, max_kbps: int = 8000) -> List[str]` — docstring referencing ENC-02 / D-11. Build cmd `["ffprobe", "-v", "quiet", "-show_entries", "format=bit_rate", "-of", "json", output_mp4_path]`. Same subprocess pattern. Parse `data["format"]["bit_rate"]` as int, divide by 1000 for kbps. If outside `[min_kbps, max_kbps]`, append `f"ENC-02: bitrate is {kbps} kbps, expected [{min_kbps}, {max_kbps}] (D-11)"`. On missing field, append `"ENC-02: ffprobe did not report format.bit_rate"`.

    3. `validate_duration_parity(silence_cutter_output: str, finalizer_output: str, tolerance_ms: int = 33) -> List[str]` — docstring referencing ENC-05 / D-11 / D-14. Build cmd template `["ffprobe", "-v", "quiet", "-show_entries", "format=duration", "-of", "csv=p=0", path]` (csv pattern matches silence-cutter's get_video_duration). Call twice (one per path). Parse `float(result.stdout.strip())`. Compute `delta_ms = abs(d1 - d2) * 1000`. If `delta_ms > tolerance_ms`, append `f"ENC-05: duration delta {delta_ms:.1f}ms exceeds tolerance {tolerance_ms}ms (D-11, D-14)"`. Each ffprobe failure returns a single error string `"ENC-05: ffprobe failed on {path}: {stderr}"`.

    All three validators must use `from typing import List` (already imported) and return `List[str]`. Empty list = passed. No exceptions escape — ffprobe errors are converted to error-list entries.

    Do NOT modify validate_crop_logic. Do NOT remove or rename validate_finalizer_info.

    PART 2 — services/ffmpeg-finalizer/tests/test_finalizer.py (MANDATORY EXHAUSTIVE SWEEP):

    PART 2A — update module docstring (lines 1-19):
    1. Append `ENC-01`, `ENC-02`, `ENC-03`, `ENC-04`, `ENC-05` to the "Referenced requirements:" list.
    2. Update the existing `- D-08: H.264 CRF 20` line to `- D-08 (superseded): originally H.264 CRF 20 — see ENC-02 / D-03 for Phase 13 update to 18`.
    3. Add a `- D-03 (Phase 13): H.264 CRF 18` line (note: D-03 is reused between phases — this is the Phase 13 D-03, not the Phase 4 D-03; the Phase 4 D-03 entry at line 12 stays).

    PART 2B — sweep ALL 8 occurrences (mandatory, no exceptions):
    1. Line 54 inside `valid_finalizer_info_dict`: change `"h264_crf": 20,` to `"h264_crf": 18,`.
    2. Line 82 inside `no_crop_info_dict`: change `"h264_crf": 20,` to `"h264_crf": 18,`.
    3. Line 211 inside `test_finalizer_info_with_crop`: change `h264_crf=20,` to `h264_crf=18,`.
    4. Line 234 inside `test_finalizer_info_without_crop`: change `h264_crf=20,` to `h264_crf=18,`.
    5. Line 268 inside `test_crop_applied_field_exists`: change `h264_crf=20,` to `h264_crf=18,`.
    6. Line 294 inside `test_validate_wrong_dimensions`: change `"h264_crf": 20,` to `"h264_crf": 18,`.
    7. Line 308 inside `test_validate_crop_not_center`: change `"h264_crf": 20,` to `"h264_crf": 18,` (MANDATORY — leaving at 20 would emit a spurious ENC-02 error noise that masks real regressions; W1 fix).
    8. Line 322 inside `test_validate_missing_safe_zone`: change `"h264_crf": 20,` to `"h264_crf": 18,` (MANDATORY — same rationale as line 308).

    NO planner discretion. NO leave-at-20-for-uniformity caveat. ALL 8 lines must be swept.

    PART 2C — update TestConfig.test_encoding_params (lines 113-116):
    1. Change docstring from `"""H264_CRF=20 (D-08), H264_PRESET='medium' (D-09)."""` to `"""H264_CRF=18 (ENC-02 / D-03), H264_PRESET='medium' (D-04 / Phase 4 D-09 preserved)."""`.
    2. Change `assert config.H264_CRF == 20  # D-08` to `assert config.H264_CRF == 18  # ENC-02 / D-03`.
    3. Keep `assert config.H264_PRESET == "medium"  # D-09` (still valid — Phase 4 D-09 is preserved by Phase 13 D-04).

    Do not modify TestComputeCrop class (D-01/D-03/VERT-02 — unrelated to Phase 13).
    Do not modify the imports at lines 21-31.

    PART 3 — verify the suite is GREEN after PART 1 + PART 2:
    1. Run `cd services/ffmpeg-finalizer && pytest tests/test_finalizer.py -x -q`. It MUST exit 0.
    2. If any test fails, the most likely cause is an incidental fixture overlooked in PART 2B — re-grep the file for `'\"h264_crf\": 20'` or `'h264_crf=20'` and sweep any stragglers. The expected total of `"h264_crf"` dict literals (lines 54, 82, 294, 308, 322 — 5 occurrences) plus `h264_crf=` kwarg literals (lines 211, 234, 268 — 3 occurrences) is exactly 8.
  </action>
  <verify>
    <automated>cd services/ffmpeg-finalizer &amp;&amp; grep -cE 'def validate_color_tags' src/validate.py | grep -q '^1$' &amp;&amp; grep -cE 'def validate_bitrate_range' src/validate.py | grep -q '^1$' &amp;&amp; grep -cE 'def validate_duration_parity' src/validate.py | grep -q '^1$' &amp;&amp; grep -cE 'info_data\["h264_crf"\]\s*!=\s*18' src/validate.py | grep -q '^1$' &amp;&amp; grep -cE 'info_data\["h264_crf"\]\s*!=\s*20' src/validate.py | grep -q '^0$' &amp;&amp; grep -cF 'ENC-03' src/validate.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'ENC-02' src/validate.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'ENC-05' src/validate.py | grep -q '^[1-9]' &amp;&amp; grep -v '^#' tests/test_finalizer.py | grep -cE '"h264_crf":\s*18|h264_crf=18' | grep -qx '8' &amp;&amp; grep -v '^#' tests/test_finalizer.py | grep -cE '"h264_crf":\s*20|h264_crf=20' | grep -qx '0' &amp;&amp; grep -cE 'assert\s+config\.H264_CRF\s*==\s*18' tests/test_finalizer.py | grep -q '^1$' &amp;&amp; grep -cE 'assert\s+config\.H264_CRF\s*==\s*20' tests/test_finalizer.py | grep -q '^0$' &amp;&amp; python -c "from src.validate import validate_color_tags, validate_bitrate_range, validate_duration_parity, validate_finalizer_info, validate_crop_logic; print('ok')" &amp;&amp; python -m pytest tests/test_finalizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE 'def validate_color_tags' services/ffmpeg-finalizer/src/validate.py` returns `1`.
    - Source assertion: `grep -cE 'def validate_bitrate_range' services/ffmpeg-finalizer/src/validate.py` returns `1`.
    - Source assertion: `grep -cE 'def validate_duration_parity' services/ffmpeg-finalizer/src/validate.py` returns `1`.
    - Source assertion: `grep -cE 'info_data\["h264_crf"\]\s*!=\s*18' services/ffmpeg-finalizer/src/validate.py` returns `1`.
    - Source assertion: `grep -cE 'info_data\["h264_crf"\]\s*!=\s*20' services/ffmpeg-finalizer/src/validate.py` returns `0` (old CRF check fully replaced).
    - Source assertion: `grep -F 'ENC-02' services/ffmpeg-finalizer/src/validate.py` returns at least one match.
    - Source assertion: `grep -F 'ENC-03' services/ffmpeg-finalizer/src/validate.py` returns at least one match.
    - Source assertion: `grep -F 'ENC-05' services/ffmpeg-finalizer/src/validate.py` returns at least one match.
    - Source assertion: `grep -E 'import subprocess' services/ffmpeg-finalizer/src/validate.py` returns `1`.
    - Source assertion: `grep -E 'import json' services/ffmpeg-finalizer/src/validate.py` returns `1`.
    - Source assertion (sweep — filter comments first to avoid self-invalidation): `grep -v '^#' services/ffmpeg-finalizer/tests/test_finalizer.py | grep -cE '"h264_crf":\s*18|h264_crf=18'` returns exactly `8` (all 8 occurrences swept).
    - Source assertion (sweep — filter comments first): `grep -v '^#' services/ffmpeg-finalizer/tests/test_finalizer.py | grep -cE '"h264_crf":\s*20|h264_crf=20'` returns exactly `0` (no stragglers in code).
    - Source assertion: `grep -cE 'assert\s+config\.H264_CRF\s*==\s*18' services/ffmpeg-finalizer/tests/test_finalizer.py` returns exactly `1` (TestConfig assertion updated).
    - Source assertion: `grep -cE 'assert\s+config\.H264_CRF\s*==\s*20' services/ffmpeg-finalizer/tests/test_finalizer.py` returns `0` (old assertion removed).
    - Source assertion: `grep -F 'ENC-02' services/ffmpeg-finalizer/tests/test_finalizer.py` returns at least one match (traceability comment).
    - Behavior assertion: `cd services/ffmpeg-finalizer && python -c "from src.validate import validate_color_tags, validate_bitrate_range, validate_duration_parity, validate_finalizer_info, validate_crop_logic"` exits 0.
    - Behavior assertion: `cd services/ffmpeg-finalizer && pytest tests/test_finalizer.py -x -q` exits 0 (GREEN — no half-passing handoff to Plan 03).
  </acceptance_criteria>
  <done>The CRF check inside validate_finalizer_info enforces 18 under ENC-02/D-03; three new ffprobe-based validators exist with the documented signatures, error-string conventions, and graceful ffprobe-failure handling; test_finalizer.py has all 8 CRF fixtures swept to 18; TestConfig asserts H264_CRF == 18 with ENC-02/D-03 traceability; the full test_finalizer.py suite exits 0 — Plan 02 leaves the suite green and Plan 03 starts from a clean baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| silence-cutter output → ffmpeg-finalizer input | Already enforced by Phase 1/4 step contract (probe_video at crop.py:35-71 validates dimensions/duration). Phase 13 does not change this boundary. |
| ffmpeg-finalizer argv → ffmpeg subprocess | argv is a Python list (no shell string interpolation). Phase 13 inserts new constant args (`-colorspace`, `bt709`, etc.) into the same list pattern. |
| ffprobe stderr → validator error strings | Local-only surface (tests, logs). No network egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.02-01 | Tampering | ffmpeg argv construction in crop.py::apply_finalizer | mitigate | Preserve list-based argv pattern; new args are static string literals (`"-colorspace"`, `"bt709"`, etc.) with no user-input interpolation. Filter chain is built from `target_width`/`target_height`/`config.*` integers, not user strings. |
| T-13.02-02 | Denial of Service | CRF 18 + unsharp filter produces larger files than CRF 20 | mitigate | The 5,000–8,000 kbps band is bounded by validate_bitrate_range (Task 3). Downstream consumers (Remotion renderer) already handle this bitrate envelope in v1.0 — no resource cliff. If bitrate exceeds the band on a real fixture, the validator fails fast, surfacing the regression before it ships. |
| T-13.02-03 | Information Disclosure | ffprobe stderr captured into validator error strings (validate_color_tags, validate_bitrate_range, validate_duration_parity) | accept | Same exposure pattern as existing cut_video.py::get_video_duration (line 269) and crop.py::probe_video (line 45). Local test/log surface only; no network exposure. No new risk surface introduced. |
| T-13.02-04 | Elevation of Privilege | Unsharp filter strength chosen by config (hardcoded `5:5:0.5:5:5:0.3`) | mitigate | Strength is locked to research-validated safe ceiling (PITFALLS.md A-6 / FEATURES.md Anti-Features — luma > 0.8 produces halos). Constant, not env-var, so a future operator can't accidentally raise it past the halo threshold without a code review. |
| T-13.02-05 | Repudiation | Encode parameter drift (e.g., a future regression accidentally restores CRF 20 or drops BT.709 tags) | mitigate | Three new ffprobe-based validators (Task 3) plus the updated CRF check enforce all four invariants test-side. test_finalizer.py is GREEN at the end of Plan 02 (no half-passing handoff). Plan 03 wires the validators into a new test module that exercises them against a real fixture. |
| T-13.02-SC | Supply Chain | npm/pip/cargo installs | not_applicable | Phase 13 adds no dependencies. `subprocess` and `json` are stdlib; FFmpeg 7.1.1 is already pinned in the base images (Phase 1 PIPE-05). No package-legitimacy gate needed. |

</threat_model>

<verification>
1. `python -c "from src import config; assert config.H264_CRF == 18 and config.H264_PRESET == 'medium'"` from `services/ffmpeg-finalizer/` exits 0.
2. `python -c "from src.crop import apply_finalizer, probe_video, compute_crop"` from `services/ffmpeg-finalizer/` exits 0.
3. `python -c "from src.validate import validate_color_tags, validate_bitrate_range, validate_duration_parity, validate_finalizer_info, validate_crop_logic"` from `services/ffmpeg-finalizer/` exits 0.
4. `cd services/ffmpeg-finalizer && pytest tests/test_finalizer.py -x -q` exits 0 (the suite is GREEN at the end of Plan 02).
5. All source-grep assertions in the three tasks' acceptance criteria pass.
6. The -an branch in crop.py was NOT touched.
</verification>

<success_criteria>
- ENC-02: CRF constant is 18; validator enforces 18 under ENC-02/D-03; bitrate-range validator (5000-8000 kbps) is in place.
- ENC-03: Three BT.709 metadata args present in cmd after -pix_fmt; NO colorspace filter; manifest exposes color_space/color_primaries/color_transfer; validate_color_tags asserts via ffprobe.
- ENC-04: `flags=lanczos` on both `scale=` invocations; `unsharp=5:5:0.5:5:5:0.3` appended to both filter-chain branches after `setsar=1`; manifest exposes lanczos_scaling and unsharp_filter.
- ENC-05: Duration parity validator in place with ±33ms tolerance (will be exercised in Plan 03).
- test_finalizer.py is GREEN — all 8 CRF fixtures swept to 18, TestConfig updated, no spurious ENC-02 noise in any test's error list.
- D-04 invariant (H264_PRESET="medium") preserved; D-13 inheritance (no-audio branch) automatic; no env-var surface added.
</success_criteria>

<output>
Create `.planning/phases/13-encode-quality/13-02-SUMMARY.md` when done. Include:
- The diff hunks applied to config.py, crop.py, validate.py, and test_finalizer.py (file:line ranges, before/after for the cmd list, filter chain, and all 8 fixture lines).
- Confirmation of every source-grep assertion in the three tasks.
- Confirmation that crop.py and validate.py remain importable.
- Confirmation that `pytest tests/test_finalizer.py -x -q` exits 0 — Plan 02 hands off a green suite to Plan 03 (no half-passing state).
</output>
</output>
