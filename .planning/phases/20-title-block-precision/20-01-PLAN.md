---
phase: 20-title-block-precision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-renderer/src/pipeline-config.test.ts
  - services/remotion-studio/src/pipeline-config.ts
autonomous: true
requirements: [TITLE-01, TITLE-02, TITLE-03]

must_haves:
  truths:
    - "TitleStyleProps no longer contains topOffset, subtitleFontSize, subtitleColor, or subtitleFontFamily"
    - "TitleStyleProps contains x?: number, y?: number, borderRadius?: number"
    - "TitleConfig no longer contains subtitle?: string"
    - "validatePipelineConfig accepts x/y/borderRadius and rejects negative values"
    - "Existing tests pass; new Phase 20 tests pass"
  artifacts:
    - path: "services/remotion-studio/src/pipeline-config.ts"
      provides: "Updated TitleStyleProps, TitleConfig, validatePipelineConfig"
      contains: "x?: number"
    - path: "services/remotion-renderer/src/pipeline-config.test.ts"
      provides: "Phase 20 test cases for x/y/borderRadius and subtitle removal"
      contains: "accepts x and y fields in title style"
  key_links:
    - from: "services/remotion-studio/src/pipeline-config.ts"
      to: "services/remotion-studio/src/compositions/TitleOverlay.tsx"
      via: "TitleStyleProps import"
      pattern: "import.*TitleStyleProps.*pipeline-config"
    - from: "services/remotion-renderer/src/pipeline-config.test.ts"
      to: "services/remotion-renderer/src/pipeline-config.ts"
      via: "vitest import of validatePipelineConfig"
      pattern: "validatePipelineConfig"
---

<objective>
Update the schema (pipeline-config.ts in studio) and the renderer test file to implement the three Phase 20 changes at the type/validation layer. Schema is the authoritative source — TitleOverlay.tsx and TitleEditor.tsx depend on TitleStyleProps being correct before they are edited.

Purpose: Establishes the type contract all downstream tasks build against. A TypeScript compilation failure here will propagate to every consumer.
Output: Updated pipeline-config.ts with new TitleStyleProps/TitleConfig; updated test file with Phase 20 test cases; vitest suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

IMPORTANT (AGENTS.md requirement): This phase touches the studio frontend (TitleEditor) and Remotion compositions (TitleOverlay). Invoke the `impeccable` skill and the `frontend-design` plugin at the START of this execute session before any edits.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-title-block-precision/20-CONTEXT.md
@.planning/phases/20-title-block-precision/20-RESEARCH.md
@.planning/phases/20-title-block-precision/20-PATTERNS.md
@.planning/phases/20-title-block-precision/20-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update pipeline-config.test.ts — Phase 20 test cases (Wave 0 gap)</name>
  <files>services/remotion-renderer/src/pipeline-config.test.ts</files>
  <read_first>
    - services/remotion-renderer/src/pipeline-config.test.ts (full file — understand all existing describe blocks and the line-57 subtitle fixture)
    - .planning/phases/20-title-block-precision/20-PATTERNS.md §"pipeline-config.test.ts" (new test cases to add verbatim)
    - .planning/phases/20-title-block-precision/20-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - Existing test "accepts full config with all fields" at line 54–67 contains `subtitle: "Episode 1"` in the titles fixture — this MUST be removed from the fixture (the field is being deleted from TitleConfig)
    - New valid-config test: `accepts x and y fields in title style` — config with `style: { x: 200, y: 960 }` → `result.valid` toBe true
    - New valid-config test: `accepts borderRadius field in title style` — config with `style: { borderRadius: 24 }` → `result.valid` toBe true
    - New valid-config test: `accepts borderRadius = 0 (sharp corners)` — config with `style: { borderRadius: 0 }` → `result.valid` toBe true
    - New invalid-config test: `rejects negative x in title style` — config with `style: { x: -10 }` → `result.valid` toBe false AND `result.errors.some(e => e.includes(".style.x"))` toBe true
    - New invalid-config test: `rejects negative y in title style` — config with `style: { y: -1 }` → same error-includes pattern for ".style.y"
    - New invalid-config test: `rejects negative borderRadius in title style` — config with `style: { borderRadius: -5 }` → same for ".style.borderRadius"
    - Tests run RED now (schema not yet updated) — that is expected and correct
  </behavior>
  <action>
    In `services/remotion-renderer/src/pipeline-config.test.ts`:
    1. Line 57: remove `subtitle: "Episode 1",` from the titles fixture in "accepts full config with all fields" test. The TitleConfig type no longer has subtitle; leaving it causes a TypeScript error once schema is updated.
    2. At the end of `describe("valid configs", ...)`: add three new it() cases for x/y accepted and borderRadius accepted (including borderRadius=0), using the exact patterns from 20-PATTERNS.md §"Test cases to ADD to describe('valid configs')".
    3. At the end of `describe("invalid configs", ...)`: add three new it() cases for negative x, negative y, and negative borderRadius rejected, using the patterns from 20-PATTERNS.md §"Test cases to ADD to describe('invalid configs')". Each uses `result.errors.some((e) => e.includes(".style.x"))` / ".style.y" / ".style.borderRadius" as the error-message assertion.
    Do NOT modify any other existing tests. Do NOT add vitest imports (already present). The test file imports `TitleConfig` and `TitleStyleProps` from "./pipeline-config" — these types will be updated in Task 2.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-renderer && npx vitest run src/pipeline-config.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Test file compiles (no import errors). The 6 new test cases appear. Existing tests still pass. The 3 "rejects negative" tests FAIL (expected RED — schema not yet updated); the "accepts" tests may pass or fail depending on current schema state. Zero TypeScript errors in the test file itself.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema migration — TitleStyleProps, TitleConfig, validatePipelineConfig in pipeline-config.ts</name>
  <files>services/remotion-studio/src/pipeline-config.ts</files>
  <read_first>
    - services/remotion-studio/src/pipeline-config.ts (lines 66–100 for TitleStyleProps + TitleConfig; lines 400–420 for validatePipelineConfig title-style validation blocks)
    - .planning/phases/20-title-block-precision/20-PATTERNS.md §"pipeline-config.ts" (exact before/after interface shapes, validation blocks to remove and add)
    - .planning/phases/20-title-block-precision/20-RESEARCH.md §"Code Examples — Before/After: TitleStyleProps schema" and §"Before/After: TitleConfig schema"
  </read_first>
  <behavior>
    - After edit: `TitleStyleProps` does NOT contain `topOffset`, `subtitleFontSize`, `subtitleColor`, `subtitleFontFamily`
    - After edit: `TitleStyleProps` DOES contain `x?: number`, `y?: number`, `borderRadius?: number`
    - After edit: `TitleConfig` does NOT contain `subtitle?: string`
    - After edit: `validatePipelineConfig` does NOT contain validation blocks for `s.subtitleFontSize` or `s.topOffset`
    - After edit: `validatePipelineConfig` DOES contain validation blocks for `s.x`, `s.y`, `s.borderRadius` (non-negative number pattern)
    - vitest run exits 0 (all tests green, including the 6 new ones added in Task 1)
  </behavior>
  <action>
    In `services/remotion-studio/src/pipeline-config.ts`:

    1. In `TitleStyleProps` interface (around lines 72–88): remove `subtitleFontSize?: number`, `subtitleColor?: string`, `subtitleFontFamily?: string`, `topOffset?: number`. Add after `titleFontFamily?: string`: `x?: number;  // pixel x from top-left of 1080×1920 frame (D-05, D-06)`, `y?: number;  // pixel y from top-left (D-05, D-06)`, `borderRadius?: number;  // container border-radius in px, default 12 (D-09)`.

    2. In `TitleConfig` interface (around lines 91–97): remove `subtitle?: string;`.

    3. In `validatePipelineConfig` (search for the validation loop over `titles`): remove both blocks — the one checking `s.subtitleFontSize` (rejects < 8 or > 200) and the one checking `s.topOffset` (rejects < 0 or > 100). Add three new validation blocks after the `titleFontSize` check, using the `< 0` only pattern (no upper bound): x, y, and borderRadius each emit an error containing `titles[${index}].style.x`, `.style.y`, `.style.borderRadius` respectively (per 20-PATTERNS.md §"Validation pattern to ADD").

    Do NOT touch any other interface or validation block. `textColor` field stays in `TitleStyleProps` — it is a fallback and is not being removed.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-renderer && npx vitest run src/pipeline-config.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests green (vitest exits 0). `TitleStyleProps` contains `x?: number`, `y?: number`, `borderRadius?: number` and does NOT contain `topOffset`, `subtitleFontSize`, `subtitleColor`, `subtitleFontFamily`. `TitleConfig` does NOT contain `subtitle`. Validation blocks for x/y/borderRadius present with non-negative-number checks. The renderer test runs against the renderer's pipeline-config.ts which is NOT yet synced — tests run against the studio copy after renderer sync in Plan 04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline-config.json → validatePipelineConfig | Config read from disk (trusted local file); validated before use in renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | pipeline-config.ts schema fields | accept | Schema fields are local TypeScript types; no network surface; consumer is a local Remotion render only |
| T-20-02 | Information Disclosure | validatePipelineConfig error messages | accept | Error messages name field paths (e.g. "titles[0].style.x") — these are developer-facing in a local tool; no PII |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | Phase 20 installs zero new packages (verified in RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

<verification>
After both tasks complete:
- `cd services/remotion-renderer && npx vitest run src/pipeline-config.test.ts` exits 0
- `grep -n "topOffset\|subtitleFontSize\|subtitleColor\|subtitleFontFamily" services/remotion-studio/src/pipeline-config.ts | grep -v "^.*//.*remove\|validatePipelineConfig"` — should show zero matches in interface declarations (only in validation removal comments if any)
- `grep -c "x?: number" services/remotion-studio/src/pipeline-config.ts` — returns at least 1
- `grep -c "subtitle?: string" services/remotion-studio/src/pipeline-config.ts` — returns 0
</verification>

<success_criteria>
- TitleStyleProps: contains x/y/borderRadius, does NOT contain topOffset/subtitleFontSize/subtitleColor/subtitleFontFamily
- TitleConfig: does NOT contain subtitle
- validatePipelineConfig: rejects x < 0, y < 0, borderRadius < 0; does NOT reject topOffset or subtitleFontSize
- All existing vitest tests pass; 6 new Phase 20 tests pass
</success_criteria>

<output>
Create `.planning/phases/20-title-block-precision/20-01-SUMMARY.md` when done
</output>
