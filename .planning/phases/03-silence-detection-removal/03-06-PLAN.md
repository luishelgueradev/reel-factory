---
phase: 03-silence-detection-removal
plan: 06
subsystem: testing
tags: [pytest, bugfix, test-coverage, import-fix]
type: gap_closure

# Dependency graph
requires:
  - phase: 03-silence-detection-removal
    provides: All existing source modules (cross_reference.py, validate.py, cut_video.py, etc.)
provides:
  - Fixed test_silence_cutter.py with correct imports and rewritten TestCrossReference
  - All 38 unit tests passing
affects: [silence-cutter-tests]

# UAT Gap Reference
gap_test: 9
gap_truth: "All 38 unit tests in test_silence_cutter.py run and pass with no import errors"
root_cause: "test_silence_cutter.py imports _check_whisper_confirmation and _times_overlap from cross_reference.py, but these were renamed/removed during implementation: _check_whisper_confirmation → _check_silence (different signature), _times_overlap removed (logic inlined)"

---

# Phase 3 Plan 6: Fix Unit Test Import Errors [GAP CLOSURE]

## Objective

Fix the import errors in `test_silence_cutter.py` so that all 38 unit tests can run and pass. The test file was written against plan-specified function names that changed during implementation.

## Tasks

### T-06-01: Fix imports and rewrite TestCrossReference class

**What:** Update line 26 import and rewrite the TestCrossReference test class to use the actual function signatures from `cross_reference.py`.

**Why:** The imports reference `_check_whisper_confirmation` and `_times_overlap` which don't exist. The actual function is `_check_silence(silence_start, silence_end, words, original_duration)` returning `(cut_start, cut_end, SilenceSource)` or `None`.

**Changes:**
1. Line 26: Replace `from src.cross_reference import _check_whisper_confirmation, _times_overlap` with `from src.cross_reference import _check_silence`
2. Rewrite `test_whisper_confirms_silence` → test `_check_silence` with high no_speech_prob word → returns `(start, end, SilenceSource.BOTH)`
3. Rewrite `test_whisper_rejects_non_silence` → test `_check_silence` with low no_speech_prob word + speech_ratio > 0.5 → returns `None`
4. Rewrite `test_any_word_threshold` → test `_check_silence` with one high no_speech_prob word → returns `(start, end, SilenceSource.BOTH)`
5. Rewrite `test_no_overlapping_words` → test `_check_silence` with no words in region → returns `(start, end, SilenceSource.BOTH)`
6. Replace `test_times_overlapping` → `test_edge_overlap_rejected` → test that sparse edge overlap (<50% speech) returns `(start, end, SilenceSource.FFMPEG)`
7. Remove `test_times_non_overlapping` and `test_times_overlap_edge` (overlap logic is internal to `_check_silence`, tested implicitly)
8. Replace `test_times_overlap_unknown_end` → `test_dense_speech_rejected` → test that >50% speech overlap returns `None`
9. Keep `test_config_no_speech_threshold` unchanged (tests config, not cross_reference)

**Verify:** Run `cd services/silence-cutter && python3 -m pytest tests/ -v` — all tests pass.

---

## Verification

- All 38 unit tests pass without import errors
- `python3 -m pytest tests/ -v` exits with 0