---
phase: 24-named-config-profiles
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - services/remotion-studio/src/server.ts
  - services/remotion-studio/src/server.test.ts
autonomous: true
requirements: [PROFILE-01, PROFILE-02, PROFILE-03, PROFILE-04]
must_haves:
  truths:
    - "The Studio server exposes profiles CRUD over HTTP: list, save, get, apply, rename, delete (D-05)"
    - "Profiles persist under dirname(ACTIVE_PIPELINE_CONFIG_PATH)/profiles — the same bind mount as the active config, so they survive docker rebuild (D-01, PROFILE-04)"
    - "Applying a profile atomically writes its config to ACTIVE_PIPELINE_CONFIG_PATH and returns the applied PipelineConfig (D-05)"
    - "All profile routes are registered BEFORE the static serveSpa catch-all and validate slugs/names (path-traversal safe)"
    - "Integration tests (supertest) cover save→list→get→apply→rename→delete and the error paths (404/409/422)"
  artifacts:
    - "services/remotion-studio/src/server.ts"
    - "services/remotion-studio/src/server.test.ts"
  key_links:
    - "server.ts imports list/save/read/rename/remove from ./profiles.js (24-01)"
---

<objective>
Expose the 24-01 profiles core over the Studio Express server as the public profiles
API, and make "load" a server-side apply that atomically updates the active config.
Wire startup to ensure the profiles directory exists. Prove every route with supertest.
</objective>

<context>
@.planning/phases/24-named-config-profiles/24-CONTEXT.md
- Import the core from ./profiles.js (24-01). Do NOT reimplement CRUD in the route layer.
- Reuse from server.ts: ACTIVE_PIPELINE_CONFIG_PATH, validatePipelineConfig, the atomic-write helper used by PUT /api/config (extract/share it if needed), and the JOB_ID_REGEX validation style.
- PROFILES_DIR = path.join(path.dirname(ACTIVE_PIPELINE_CONFIG_PATH), "profiles") (D-01).
- ROUTES MUST be added before app.get("/{*splat}", serveSpa) (server.ts ~L347, T-18-03-01).
- Test harness already established in server.test.ts: NODE_ENV=test prevents listen(); import app after env setup; supertest. Use a temp PROFILES_DIR via env override so tests never touch ./pipeline.
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROFILES_DIR + startup ensure + helper wiring</name>
  <files>services/remotion-studio/src/server.ts</files>
  <action>
    - Compute PROFILES_DIR from dirname(ACTIVE_PIPELINE_CONFIG_PATH)+"/profiles". Allow override via env PROFILES_DIR for tests (D-08).
    - On startup (same place the server resolves config / before listen, but also safe under NODE_ENV=test as a one-time mkdir), ensure PROFILES_DIR exists (fs.mkdirSync recursive, idempotent). Must not throw if it already exists.
    - Import { listProfiles, readProfile, saveProfile, renameProfile, removeProfile, isValidSlug, ProfileConflictError, ProfileValidationError } from "./profiles.js".
  </action>
  <verify>
    <automated>cd services/remotion-studio && grep -n "PROFILES_DIR" src/server.ts && npx tsc --noEmit 2>&1 | grep -i "server.ts" || echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - PROFILES_DIR derived from ACTIVE_PIPELINE_CONFIG_PATH dirname, env-overridable.
    - Profiles dir ensured at startup, idempotent.
  </acceptance_criteria>
  <done>Server knows where profiles live (bind-mounted) and the dir always exists.</done>
</task>

<task type="auto">
  <name>Task 2: profiles CRUD + apply routes</name>
  <files>services/remotion-studio/src/server.ts</files>
  <action>
    Register BEFORE the serveSpa catch-all:
    - `GET /api/profiles` → `{ profiles: ProfileSummary[] }` (200). (PROFILE-03 list)
    - `POST /api/profiles` → body `{ name: string, config: PipelineConfig }`. validatePipelineConfig(config) first (400 on invalid). saveProfile(PROFILES_DIR, name, config). 201 with the ProfileFile. Map ProfileValidationError→400. (PROFILE-01)
    - `GET /api/profiles/:slug` → 404 if missing, else the ProfileFile. Reject invalid slug → 400. (PROFILE-02 read)
    - `PUT /api/profiles/:slug/apply` → read the profile (404 if missing); validatePipelineConfig (422 if the stored config is invalid, D-07); atomically write its config to ACTIVE_PIPELINE_CONFIG_PATH (reuse the PUT /api/config atomic write); respond 200 with `{ ...applied config, _meta:{ source:"profile", slug } }`. (PROFILE-02)
    - `PATCH /api/profiles/:slug` → body `{ name }`. renameProfile; 404 if missing, 409 on ProfileConflictError, 400 on invalid name. 200 with the new ProfileFile. (PROFILE-03 rename)
    - `DELETE /api/profiles/:slug` → removeProfile; 200 `{ deleted:true }` if existed, 404 if not. Reject invalid slug → 400. (PROFILE-03 delete)
    All handlers: validate slug param with isValidSlug before any fs touch; wrap in try/catch returning JSON errors (mirror existing route error shape). Loopback/auth behavior inherits the existing server middleware.
  </action>
  <verify>
    <automated>cd services/remotion-studio && grep -nE "/api/profiles" src/server.ts | head</automated>
  </verify>
  <acceptance_criteria>
    - All six routes present, before serveSpa, slug-validated.
    - apply writes to ACTIVE_PIPELINE_CONFIG_PATH atomically and returns the config.
  </acceptance_criteria>
  <done>Profiles API is the single public surface for save/load/list/rename/delete + apply.</done>
</task>

<task type="auto">
  <name>Task 3: server.test.ts integration tests</name>
  <files>services/remotion-studio/src/server.test.ts</files>
  <action>
    Add a "profiles API" describe block. Set process.env.PROFILES_DIR and process.env.ACTIVE_PIPELINE_CONFIG_PATH to temp paths (mkdtemp) BEFORE importing app (the existing suite imports app once after env setup — coordinate so the temp dirs are set in time; if needed use a dedicated test file profiles-api.test.ts that sets env then imports app). Cover with supertest:
    - POST /api/profiles (valid) → 201, then GET /api/profiles lists it. (PROFILE-01)
    - POST with invalid config → 400.
    - GET /api/profiles/:slug → returns verbatim config; unknown slug → 404; "../x" → 400.
    - PUT /api/profiles/:slug/apply → 200, and the active config file on disk now equals the profile's config (read the temp ACTIVE_PIPELINE_CONFIG_PATH and deep-equal). (PROFILE-02 + active-sync)
    - PATCH rename → 200 + new slug listed, old gone; rename onto existing slug → 409. (PROFILE-03)
    - DELETE → 200 then GET list no longer contains it; delete missing → 404. (PROFILE-03)
    - Persistence note (PROFILE-04): assert profile files are written under PROFILES_DIR (the temp stand-in for the bind-mounted ./pipeline/profiles), proving location; the real docker-rebuild survival is verified in phase verification.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx vitest run src/server.test.ts src/profiles-api.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All profiles API tests green; existing server tests still pass.
    - apply test proves the active config file content changed to the profile's config.
  </acceptance_criteria>
  <done>Every profiles route + error path proven by supertest; active-config sync proven on disk.</done>
</task>

</tasks>

<verification>
- `npx vitest run` (studio) all green.
- `grep /api/profiles src/server.ts` shows 6 routes before serveSpa.
</verification>

<success_criteria>
- Profiles API persists under the active-config bind mount, applies atomically to the active config, and is fully integration-tested.
</success_criteria>

<output>
Create `.planning/phases/24-named-config-profiles/24-02-SUMMARY.md` when done.
</output>
