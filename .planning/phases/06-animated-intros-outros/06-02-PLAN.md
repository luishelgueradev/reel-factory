---
phase: 06-animated-intros-outros
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - services/remotion-renderer/src/compositions/Subtitles.tsx
  - services/remotion-renderer/src/compositions/TikTokLayout.tsx
  - services/remotion-renderer/src/compositions/SentenceLayout.tsx
  - services/remotion-renderer/src/compositions/BarLayout.tsx
  - services/remotion-renderer/src/compositions/KaraokeLayout.tsx
  - services/remotion-renderer/src/compositions/LayoutDispatcher.tsx
  - services/remotion-renderer/src/Root.tsx
autonomous: true
requirements: [VISU-01, VISU-02]

must_haves:
  truths:
    - "Users can select TikTok, Sentence, Bar, or Karaoke subtitle layout via pipeline-config.json"
    - "TikTok word-by-word layout renders identically to current Subtitles.tsx behavior (backward compatible)"
    - "Sentence layout shows full sentences with current sentence highlighted"
    - "Bar layout shows word-by-word fill on a colored background bar"
    - "Karaoke layout shows text with progressive color fill as words are spoken"
    - "Background highlight renders a colored rectangle behind subtitle text (D-08)"
    - "Position presets (bottom-center, top-center, center-screen) work for all layouts (D-09)"
    - "SubtitleConfig default values match current hardcoded behavior when config is missing"
  artifacts:
    - path: "services/remotion-renderer/src/compositions/TikTokLayout.tsx"
      provides: "TikTok word-by-word subtitle layout (extracted from Subtitles.tsx)"
      exports: ["TikTokLayout"]
    - path: "services/remotion-renderer/src/compositions/SentenceLayout.tsx"
      provides: "Sentence-at-a-time subtitle layout"
      exports: ["SentenceLayout"]
    - path: "services/remotion-renderer/src/compositions/BarLayout.tsx"
      provides: "Full-width bar subtitle layout with word-by-word fill"
      exports: ["BarLayout"]
    - path: "services/remotion-renderer/src/compositions/KaraokeLayout.tsx"
      provides: "Karaoke-style fill subtitle layout"
      exports: ["KaraokeLayout"]
    - path: "services/remotion-renderer/src/compositions/LayoutDispatcher.tsx"
      provides: "Layout mode dispatcher that selects the correct component"
      exports: ["SubtitleLayoutRenderer"]
    - path: "services/remotion-renderer/src/Root.tsx"
      provides: "Updated composition using LayoutDispatcher instead of Subtitles.tsx directly"
  key_links:
    - from: "services/remotion-renderer/src/compositions/LayoutDispatcher.tsx"
      to: "TikTokLayout|SentenceLayout|BarLayout|KaraokeLayout"
      via: "conditional render based on subtitleLayout prop from PipelineConfig"
      pattern: "subtitleLayout.*===.*tiktok|sentence|bar|karaoke"
    - from: "services/remotion-renderer/src/compositions/TikTokLayout.tsx"
      to: "@remotion/captions"
      via: "TikTokPage token rendering"
      pattern: "import.*TikTokPage.*from.*@remotion/captions"
    - from: "services/remotion-renderer/src/Root.tsx"
      to: "LayoutDispatcher"
      via: "renders SubtitleLayoutRenderer with config props"
      pattern: "SubtitleLayoutRenderer|LayoutDispatcher"
---

<objective>
Implement four subtitle layout modes (TikTok, Sentence, Bar, Karaoke) and a layout dispatcher, refactoring the existing Subtitles.tsx into the TikTok layout component.

Purpose: Deliver the core subtitle enhancement feature (D-04, D-05, D-06, D-08, D-09) — users see different subtitle styles based on their pipeline-config.json setting. The TikTok layout must be pixel-identical to current behavior for backward compatibility.

Output: Four layout mode components, LayoutDispatcher, and Root.tsx wiring.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-animated-intros-outros/06-01-SUMMARY.md
@.planning/phases/06-animated-intros-outros/06-CONTEXT.md

@services/remotion-renderer/src/compositions/Subtitles.tsx
@services/remotion-renderer/src/Root.tsx
@services/remotion-renderer/src/pipeline-config.ts
@services/remotion-renderer/src/captions.ts
</context>

<interfaces>
<!-- From Plan 01 — the PipelineConfig types the executor needs -->

From services/remotion-renderer/src/pipeline-config.ts (Plan 01 creates this):
```typescript
export type SubtitleLayoutMode = 'tiktok' | 'sentence' | 'bar' | 'karaoke';
export type SubtitlePosition = 'bottom-center' | 'top-center' | 'center-screen';
export interface BackgroundHighlight {
  enabled: boolean;
  color: string;
  padding: number;
  borderRadius: number;
}
export interface SubtitleConfig {
  layout?: SubtitleLayoutMode;
  fontFamily?: string;
  fontSize?: number;
  activeColor?: string;
  inactiveColor?: string;
  outlineColor?: string;
  outlineWidth?: number;
  backgroundHighlight?: BackgroundHighlight;
  textShadow?: { enabled: boolean; color?: string; blur?: number; offsetX?: number; offsetY?: number };
  letterSpacing?: number;
  position?: SubtitlePosition;
}
```

From services/remotion-renderer/src/Root.tsx (Plan 01 extends this):
```typescript
// RemotionProps now includes:
// subtitleLayout?: SubtitleLayoutMode
// subtitleConfig?: SubtitleConfig
// titles?: TitleConfig[]
```

From @remotion/captions:
```typescript
export interface TikTokPage {
  startMs: number;
  durationMs: number;
  text: string;
  tokens: Caption[];
}
export interface Caption {
  text: string;
  fromMs: number;
  toMs: number;
  timestampMs?: number;
  confidence?: number;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract TikTokLayout, create LayoutDispatcher, SentenceLayout, and BarLayout</name>
  <files>services/remotion-renderer/src/compositions/TikTokLayout.tsx, services/remotion-renderer/src/compositions/LayoutDispatcher.tsx, services/remotion-renderer/src/compositions/SentenceLayout.tsx, services/remotion-renderer/src/compositions/BarLayout.tsx, services/remotion-renderer/src/Root.tsx</files>
  <read_first>
    services/remotion-renderer/src/compositions/Subtitles.tsx
    services/remotion-renderer/src/pipeline-config.ts
    services/remotion-renderer/src/Root.tsx
  </read_first>
  <acceptance_criteria>
    - "TikTokLayout.tsx renders identically to Subtitles.tsx caption rendering (word-by-word with active highlight)"
    - "LayoutDispatcher.tsx imports all 4 layout components"
    - "LayoutDispatcher renders TikTokLayout when subtitleLayout is tiktok or undefined"
    - "LayoutDispatcher renders SentenceLayout when subtitleLayout is sentence"
    - "LayoutDispatcher renders BarLayout when subtitleLayout is bar"
    - "Root.tsx renders SubtitleLayoutRenderer instead of direct Subtitles component"
    - "SentenceLayout groups tokens by sentence (period, question mark, exclamation) and highlights current sentence"
    - "BarLayout renders a colored background bar with word-by-word fill progression"
    - "Position presets apply bottomOffset for bottom-center from finalizer-info safe_zone"
  </acceptance_criteria>
  <action>
    Per D-04, D-05, D-06, D-08, D-09:

    **1. Create TikTokLayout.tsx** — Extract the existing caption rendering logic from Subtitles.tsx into TikTokLayout. This component:
    - Receives props: `captionPages: TikTokPage[]`, plus all SubtitleConfig style props (fontSize, activeColor, inactiveColor, outlineColor, outlineWidth, bottomOffset, backgroundHighlight, position)
    - Renders word-by-word with active word highlighting — pixel-identical to current Subtitles.tsx behavior
    - Uses `<Sequence>` for each caption page with fade-in/fade-out (preserve existing FADE_IN_MS=100, FADE_OUT_MS=300, PAGE_OVERLAP_GUARD_MS=100)
    - Add background highlight support per D-08: when `backgroundHighlight.enabled` is true, render a colored rectangle behind the text with configurable `color`, `padding`, `borderRadius`
    - Add position preset support per D-09: `bottom-center` uses bottomOffset (from safe zone or default 250px), `top-center` uses top=100px, `center-screen` uses top=50% minus half text height

    **2. Create LayoutDispatcher.tsx** — A wrapper that reads `subtitleLayout` from the config prop and renders the appropriate layout component:
    ```typescript
    // Pseudocode:
    switch (subtitleLayout) {
      case 'tiktok': return <TikTokLayout {...props} />;
      case 'sentence': return <SentenceLayout {...props} />;
      case 'bar': return <BarLayout {...props} />;
      case 'karaoke': return <KaraokeLayout {...props} />;
      default: return <TikTokLayout {...props} />; // backward compatible default
    }
    ```

    **3. Create SentenceLayout.tsx** — Sentence-at-a-time layout per D-04:
    - Groups tokens into sentences (split at `.`, `?`, `!` punctuation)
    - Each sentence appears as a group with the current sentence highlighted (brighter color, larger weight) and past sentences dimmed
    - Uses `<Sequence>` for timing, same fade transitions as TikTok
    - Same position and background highlight support as TikTok
    - Tokens within a sentence are rendered inline, active token highlighted differently

    **4. Create BarLayout.tsx** — Full-width bar layout per D-04:
    - Renders a colored background bar (full width minus safe margins) behind subtitle text
    - Word-by-word fill: as each word is spoken, it transitions to activeColor
    - Previous words stay activeColor, upcoming words in inactiveColor
    - Bar background color uses `backgroundHighlight.color` or defaults to semi-transparent black
    - Bar padding and border radius from backgroundHighlight config
    - Position at bottom-center with safe zone awareness

    **5. Update Root.tsx** — Replace direct `<SubtitledVideo>` reference with `<SubtitleLayoutRenderer>` that uses the LayoutDispatcher. The SubtitledVideo composition now renders: OffthreadVideo + SubtitleLayoutRenderer (instead of direct Subtitles component). Keep Video as background, Overlay layout on top.

    **Important:** Subtitles.tsx is preserved but refactored — TikTokLayout is the extracted version. The original Subtitles.tsx can be simplified to just re-export TikTokLayout for backward compatibility, or kept as-is while TikTokLayout duplicates its logic. Prefer extraction (move logic to TikTokLayout, Subtitles.tsx becomes a thin wrapper or is unused).
  </action>
  <verify>
    <automated>cd services/remotion-renderer && grep -c "TikTokLayout" src/compositions/TikTokLayout.tsx && grep -c "LayoutDispatcher\|SubtitleLayoutRenderer" src/compositions/LayoutDispatcher.tsx && grep -c "SubtitleLayoutRenderer" src/Root.tsx && ls src/compositions/SentenceLayout.tsx src/compositions/BarLayout.tsx</automated>
  </verify>
  <done>TikTokLayout extracts existing behavior. LayoutDispatcher selects layout by config. SentenceLayout and BarLayout render distinct visual styles. Root.tsx uses LayoutDispatcher. Background highlight and position presets implemented for TikTok, Sentence, Bar layouts.</done>
</task>

<task type="auto">
  <name>Task 2: Create KaraokeLayout and complete style configuration wiring</name>
  <files>services/remotion-renderer/src/compositions/KaraokeLayout.tsx, services/remotion-renderer/src/captions.ts</files>
  <read_first>
    services/remotion-renderer/src/compositions/TikTokLayout.tsx
    services/remotion-renderer/src/compositions/LayoutDispatcher.tsx
    services/remotion-renderer/src/pipeline-config.ts
    services/remotion-renderer/src/captions.ts
  </read_first>
  <acceptance_criteria>
    - "KaraokeLayout.tsx renders text with progressive color fill as words are spoken"
    - "KaraokeLayout uses inactiveColor as baseline and fills with activeColor progressively"
    - "captions.ts transcriptToCaptionPages remains unchanged — all layout modes consume TikTokPage[] data"
    - "LayoutDispatcher includes karaoke case in its switch statement"
    - "All 4 layout modes render correctly when selected in pipeline-config.json"
  </acceptance_criteria>
  <action>
    Per D-04 and D-06:

    **1. Create KaraokeLayout.tsx** — Karaoke-style fill layout per D-04:
    - Text renders at full opacity in inactiveColor as a baseline
    - As each word is spoken, a fill overlay transitions to activeColor from left to right within the word
    - Use Remotion's `interpolate()` to create a smooth fill effect — each token has `fromMs` and `toMs`, and within that range, the fill progresses from 0% to 100% width
    - Implementation approach: render the baseline text in one layer, then render a clip-masked active text layer on top. The clip width animates from 0 to full width as the word is spoken. Use `overflow: hidden` + explicit `width` animation on the active layer.
    - Same timing logic as TikTok: `<Sequence>` per caption page, fade-in/fade-out transitions
    - Same background highlight and position support as other layouts (D-08, D-09)

    **2. Verify captions.ts is unchanged** — The TikTokPage[] data structure from `transcriptToCaptionPages()` is layout-mode-agnostic. All four layout modes consume the same TikTokPage[] data. No changes needed to captions.ts — the data layer is shared, only rendering changes per mode.

    **3. Update LayoutDispatcher if needed** — Ensure the `karaoke` case is in the switch statement from Task 1. If it was already added as a stub, replace with the actual import.
  </action>
  <verify>
    <automated>cd services/remotion-renderer && ls src/compositions/KaraokeLayout.tsx && grep -c "subtitleLayout.*karaoke\|case.*karaoke" src/compositions/LayoutDispatcher.tsx</automated>
  </verify>
  <done>KaraokeLayout renders progressive fill. All 4 layout modes (TikTok, Sentence, Bar, Karaoke) work via LayoutDispatcher selection. captions.ts data layer is untouched — all layout modes consume same TikTokPage[] data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline-config.json → LayoutDispatcher | Layout mode string selects rendering component |
| LayoutDispatcher → Layout Components | Style config props passed to layout components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-04 | Tampering | LayoutDispatcher | mitigate | Unknown layout mode falls back to TikTok (safe default per D-05) |
| T-06-05 | Information Disclosure | Layout Components | accept | Subtitle text content comes from user's own video — not external input |

</threat_model>

<verification>
1. All layout components exist: `ls services/remotion-renderer/src/compositions/{TikTokLayout,SentenceLayout,BarLayout,KaraokeLayout,LayoutDispatcher}.tsx`
2. Root.tsx uses LayoutDispatcher: `grep "SubtitleLayoutRenderer\|LayoutDispatcher" services/remotion-renderer/src/Root.tsx`
3. All 4 layout modes in dispatcher: `grep -c "tiktok\|sentence\|bar\|karaoke" services/remotion-renderer/src/compositions/LayoutDispatcher.tsx`
4. Render with TikTok layout (default) produces identical output to current Subtitles.tsx
</verification>

<success_criteria>
- Four layout mode components render distinctly different visual styles
- TikTokLayout is pixel-identical to current Subtitles.tsx behavior
- LayoutDispatcher selects layout based on pipeline-config.json subtitleLayout field
- Background highlight effect works for bar mode (D-08)
- Position presets (bottom-center, top-center, center-screen) work for all layouts (D-09)
- captions.ts data layer unchanged — all layouts consume same TikTokPage[]
</success_criteria>

<output>
After completion, create `.planning/phases/06-animated-intros-outros/06-02-SUMMARY.md`
</output>