---
phase: 13-encode-quality
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - services/ffmpeg-finalizer/tests/test_encode_quality.py
  - services/silence-cutter/tests/test_silence_cutter.py
autonomous: true
requirements:
  - ENC-01
  - ENC-02
  - ENC-03
  - ENC-04
  - ENC-05

must_haves:
  truths:
    - "A new test_encode_quality.py drives an in-process finalizer run on a synthetic NO-AUDIO lavfi clip and asserts: bitrate band, color_space/primaries/transfer all bt709, manifest fields, duration parity within tolerance — and exercises D-13 (no-audio branch inheritance) for real, not just code-path-shared"
    - "test_silence_cutter.py exercises validate_concat_mode against a real stream-copy concat output (positive case) and a real re-encode concat output (negative case) under TestConcatMode, with graceful skip when ffmpeg/ffprobe missing"
    - "test_silence_cutter.py adds D-14 edge-case coverage under TestConcatEdgeCases: variable-keyframe concat parity, and audio-shorter-than-video concat parity — both using -c copy"
    - "All pytest suites for ffmpeg-finalizer and silence-cutter exit 0 after this plan (Plan 02 already left test_finalizer.py green — Plan 03 is pure new-test-creation)"
  artifacts:
    - path: "services/ffmpeg-finalizer/tests/test_encode_quality.py"
      provides: "End-to-end ffprobe-based encode-quality assertions on a no-audio lavfi fixture (also exercises D-13)"
      contains: "validate_color_tags"
    - path: "services/silence-cutter/tests/test_silence_cutter.py"
      provides: "validate_concat_mode exercised against stream-copy + re-encode outputs; D-14 edge-case parity tests"
      contains: "validate_concat_mode"
  key_links:
    - from: "services/ffmpeg-finalizer/tests/test_encode_quality.py"
      to: "services/ffmpeg-finalizer/src/validate.py"
      via: "imports validate_color_tags, validate_bitrate_range, validate_duration_parity"
      pattern: "from src.validate import"
    - from: "services/ffmpeg-finalizer/tests/test_encode_quality.py"
      to: "ffmpeg binary"
      via: "subprocess.run(['ffmpeg', ...]) to synthesize a tiny no-audio fixture clip"
      pattern: "subprocess.run"
    - from: "services/silence-cutter/tests/test_silence_cutter.py::TestConcatEdgeCases"
      to: "cut_video.py::_concatenate_segments (post Plan 01 — stream-copy)"
      via: "same -c copy argv shape exercised against variable-keyframe + audio-shorter-than-video synthesized clips"
      pattern: '"-c",\\s*"copy"'
---

<objective>
Make the Phase 13 source-side changes test-enforceable BEYOND what Plan 02 already did. Plan 02 swept test_finalizer.py CRF fixtures end-to-end and left that suite green — Plan 03 does NOT re-sweep. Plan 03 adds:
1. A new pytest module `test_encode_quality.py` that synthesizes a tiny NO-AUDIO lavfi fixture (intentionally exercising the `-an` branch — D-13) and uses the three new ffprobe-based validators to assert color tags, bitrate band, manifest fields, and duration parity.
2. Wires `validate_concat_mode` into `test_silence_cutter.py` with positive (stream-copy) and negative (re-encode) real-ffmpeg fixtures under TestConcatMode.
3. Adds D-14 edge-case coverage under TestConcatEdgeCases — variable-keyframe concat parity and audio-shorter-than-video concat parity, both via `-c copy`.

Purpose: Closes the verification half of every ENC requirement AND the D-14 -c-copy edge-case coverage. Without these tests the source-side changes from Plans 01 and 02 are unverified on edge cases — a future regression could silently restore CRF 20, drop BT.709 tags, revert stream-copy, or break parity on variable keyframe / mismatched-A-V-duration inputs.

Output: One new pytest module + an extension to the silence-cutter test module. No source-code changes outside `tests/` directories. No re-sweep of test_finalizer.py (already done in Plan 02 Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-encode-quality/13-CONTEXT.md
@.planning/phases/13-encode-quality/13-PATTERNS.md
@services/ffmpeg-finalizer/tests/test_finalizer.py
@services/silence-cutter/tests/test_silence_cutter.py
@services/ffmpeg-finalizer/src/validate.py
@services/ffmpeg-finalizer/src/crop.py
@services/silence-cutter/src/validate.py
@services/silence-cutter/src/cut_video.py

<interfaces>
<!-- Key validator signatures Plan 03 consumes (introduced by Plan 01 + Plan 02) -->

From services/ffmpeg-finalizer/src/validate.py (post Plan 02):
- validate_finalizer_info(info_data: dict) -> List[str]  (existing, CRF check updated to 18 / ENC-02)
- validate_crop_logic(info_data: dict) -> List[str]      (existing, unchanged)
- validate_color_tags(output_mp4_path: str) -> List[str]                                   (NEW — ENC-03/D-10)
- validate_bitrate_range(output_mp4_path: str, min_kbps: int = 5000, max_kbps: int = 8000) -> List[str]  (NEW — ENC-02/D-11)
- validate_duration_parity(silence_cutter_output: str, finalizer_output: str, tolerance_ms: int = 33) -> List[str]  (NEW — ENC-05/D-11/D-14)

From services/silence-cutter/src/validate.py (post Plan 01):
- validate_silence_cuts(cut_list_data: dict) -> List[str]       (existing, unchanged)
- validate_cross_reference_logic(...) -> List[str]              (existing, unchanged)
- validate_concat_mode(concat_output_path: str) -> List[str]    (NEW — ENC-01/D-01)

From services/ffmpeg-finalizer/src/crop.py (post Plan 02):
- probe_video(input_path: str) -> dict                                          (existing, unchanged)
- compute_crop(input_width, input_height, target_width, target_height) -> Tuple (existing, unchanged)
- apply_finalizer(input_path, output_path, target_width, target_height, probe_info: dict) -> dict
  (existing; return dict now includes lanczos_scaling, unsharp_filter, color_space, color_primaries, color_transfer.
   When probe_info["has_audio"] is False, apply_finalizer follows the -an branch — the SAME shared filter_chain and cmd
   are applied; only audio args are skipped. This is what D-13 specifies and what Task 1 exercises.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_encode_quality.py — end-to-end ffprobe assertions on a NO-AUDIO lavfi fixture (ENC-02, ENC-03, ENC-05, D-13)</name>
  <files>services/ffmpeg-finalizer/tests/test_encode_quality.py</files>
  <read_first>
    - services/ffmpeg-finalizer/tests/test_finalizer.py (lines 1-31 — module docstring + sys.path insertion + imports — copy the pattern for the new module; also lines 38-91 for the conftest/fixture style)
    - services/ffmpeg-finalizer/src/validate.py (post Plan 02 — the three new validator signatures and their return contracts)
    - services/ffmpeg-finalizer/src/crop.py (lines 35-71 probe_video; lines 107-202 apply_finalizer — especially the `if probe_info["has_audio"]:` / `else: cmd.append("-an")` branch at lines 155-164 — the no-audio branch this task is exercising)
    - services/ffmpeg-finalizer/src/config.py (lines 36-50 — confirm AUDIO_BITRATE / AUDIO_SAMPLE_RATE / LOUDNORM_TARGET / LOUDNORM_TP / LOUDNORM_LRA exist for the audio branch; Task 1 SIDESTEPS this branch by using a no-audio fixture, so these constants are NOT required for the synthesized lavfi clip)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-11 verification protocol; D-13 no-audio inheritance — this task makes D-13 a REAL test, not just a code-path-shared claim)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"E2E ffprobe test pattern" — planner picks pytest module OR shell-script approach; this plan picks pytest module per CONTEXT.md D-11 Claude's Discretion)
    - scripts/test-ffmpeg-finalizer.sh (lines 36-67 — the pattern for synthesizing tiny ffmpeg test fixtures via `ffmpeg -f lavfi -i testsrc=...`)
  </read_first>
  <behavior>
    - A new pytest module `tests/test_encode_quality.py` exists in `services/ffmpeg-finalizer/`.
    - The module defines a session-scoped pytest fixture that uses `subprocess.run(["ffmpeg", "-f", "lavfi", ...])` to synthesize a 5-second 1920×1080 NO-AUDIO test source MP4 in a tempdir. The fixture intentionally omits the `-f lavfi -i sine=...` second input AND omits `-c:a aac` from the encode argv. Result: `probe_info["has_audio"]` is False when crop.py processes it, routing the run through the `-an` branch (D-13). If `ffmpeg` is not on PATH, the fixture calls `pytest.skip("ffmpeg not available on host")`.
    - Test 1 (`test_finalizer_color_tags_are_bt709`): runs `apply_finalizer` against the synthesized no-audio source, then calls `validate_color_tags(output_path)` and asserts `errors == []`. Maps to ENC-03 / D-10. Implicitly also asserts D-13 — color tags appear on the no-audio branch's output.
    - Test 2 (`test_finalizer_bitrate_in_band`): same finalizer run; calls `validate_bitrate_range(output_path, min_kbps=2000, max_kbps=15000)` and asserts `errors == []`. The band is wider than the production 5000-8000 (the production band targets a 60s talking-head fixture; the synthetic 5s lavfi testsrc has different content statistics, so the test uses a relaxed band that is still tight enough to catch a misconfigured encode that would produce e.g. <500 kbps or >50000 kbps). The production 5000-8000 band is exercised as a hard gate in Plan 04 Task 3 against the real talking-head clip.
    - Test 3 (`test_finalizer_unsharp_and_lanczos_in_manifest`): inspects the dict returned by `apply_finalizer` directly and asserts `result["lanczos_scaling"] is True`, `result["unsharp_filter"] == "5:5:0.5:5:5:0.3"`, `result["color_space"] == "bt709"`, `result["color_primaries"] == "bt709"`, `result["color_transfer"] == "bt709"`. Maps to ENC-04 manifest fields. Implicitly asserts D-13 — manifest fields are emitted on the no-audio branch too.
    - Test 4 (`test_finalizer_crf_is_18`): `_, result = finalizer_output; assert result["h264_crf"] == 18`. Maps to ENC-02.
    - Test 5 (`test_duration_parity_after_finalizer`): calls `validate_duration_parity(source_mp4, finalizer_output, tolerance_ms=100)` (relaxed tolerance for the synthetic fixture because lavfi testsrc duration rounding can differ by a few frames vs the production 33ms). Asserts `errors == []`. Maps to ENC-05 / D-11 / D-14.
    - Test 6 (`test_finalizer_no_audio_branch_executed`): `_, result = finalizer_output; assert "audio_normalization" in result and result.get("audio_normalization") is True or result.get("audio_normalization") is False` — confirms the manifest field is present even on the no-audio branch (existing pipeline contract); AND uses ffprobe to confirm the output has NO audio stream (`ffprobe -select_streams a -show_entries stream=codec_type` returns empty), proving the `-an` branch executed. Maps to D-13 explicitly.
    - All tests skip gracefully if `ffmpeg` or `ffprobe` are missing from PATH (use `shutil.which` checks).
    - `pytest tests/test_encode_quality.py -x -q` exits 0 (passing or all-skipped) on a host with ffmpeg available.
  </behavior>
  <action>
    Create services/ffmpeg-finalizer/tests/test_encode_quality.py with this structure:

    1. Module docstring matching test_finalizer.py style (lines 1-19): list referenced requirements ENC-02, ENC-03, ENC-04, ENC-05 and the D-XX decisions enforced (D-03, D-05, D-06, D-08, D-09, D-10, D-11, D-13, D-14). One paragraph noting this module exercises the post-Plan-02 source changes end-to-end with a synthetic NO-AUDIO lavfi fixture — intentionally exercising the -an branch (D-13) so a real test backs the "no-audio inheritance" claim instead of just code-path-shared assertion.

    2. Imports block matching test_finalizer.py lines 21-31:
       - `import pytest`
       - `import subprocess`
       - `import shutil`
       - `import sys, os` + `sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))`
       - `from src.crop import apply_finalizer, probe_video`
       - `from src.validate import validate_color_tags, validate_bitrate_range, validate_duration_parity, validate_finalizer_info`
       - `from src import config`

    3. Fixture `synth_source_mp4(tmp_path_factory)` — session-scoped, NO AUDIO:
       - If `shutil.which("ffmpeg") is None: pytest.skip("ffmpeg not on PATH")`.
       - Builds path `tmp_path_factory.mktemp("encode-quality") / "source.mp4"`.
       - `subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i", "testsrc=duration=5:size=1920x1080:rate=30", "-c:v", "libx264", "-pix_fmt", "yuv420p", "-an", str(path)], capture_output=True, text=True, timeout=60)`. Note: NO `-f lavfi -i sine=...` second input, NO `-c:a aac`, EXPLICIT `-an`. Assert returncode 0.
       - Yields the path as a string.
       - Docstring on the fixture: "Synthesize a NO-AUDIO 5-second lavfi clip. The `-an` flag and absence of any audio input ensure probe_video reports has_audio=False, routing apply_finalizer through the `-an` branch (D-13). This sidesteps the loudnorm/audio-bitrate config constants entirely — those are exercised by integration tests in scripts/test-ffmpeg-finalizer.sh, not here."

    4. Fixture `finalizer_output(synth_source_mp4, tmp_path)` — function-scoped:
       - If `shutil.which("ffprobe") is None: pytest.skip("ffprobe not on PATH")`.
       - Computes `probe_info = probe_video(synth_source_mp4)`.
       - Sanity assert: `assert probe_info["has_audio"] is False, "Fixture must be no-audio to exercise D-13"`.
       - Builds `output_path = str(tmp_path / "finalized.mp4")`.
       - Calls `result = apply_finalizer(synth_source_mp4, output_path, config.VERTICAL_WIDTH, config.VERTICAL_HEIGHT, probe_info)`.
       - Yields `(output_path, result)`.

    5. Class TestEncodeQuality (matches the TestConfig/TestValidation class style):
       - `test_finalizer_color_tags_are_bt709(self, finalizer_output)`: `output_path, _ = finalizer_output; errors = validate_color_tags(output_path); assert errors == [], errors`. Docstring: "ENC-03 / D-10 / D-13: ffprobe confirms color_space, color_primaries, color_transfer all equal bt709 even on the no-audio branch."
       - `test_finalizer_bitrate_in_band(self, finalizer_output)`: `output_path, _ = finalizer_output; errors = validate_bitrate_range(output_path, min_kbps=2000, max_kbps=15000); assert errors == [], errors`. Docstring: "ENC-02 / D-11: synthetic-fixture bitrate falls in a relaxed band (2000-15000 kbps); the production 5000-8000 band is exercised in Plan 04 Task 3 against a real talking-head clip."
       - `test_finalizer_unsharp_and_lanczos_in_manifest(self, finalizer_output)`: `_, result = finalizer_output; assert result["lanczos_scaling"] is True; assert result["unsharp_filter"] == "5:5:0.5:5:5:0.3"; assert result["color_space"] == "bt709"; assert result["color_primaries"] == "bt709"; assert result["color_transfer"] == "bt709"`. Docstring: "ENC-04 / D-05, D-06, D-08, D-13: manifest fields confirm Lanczos + unsharp + BT.709 were applied on the no-audio branch."
       - `test_finalizer_crf_is_18(self, finalizer_output)`: `_, result = finalizer_output; assert result["h264_crf"] == 18`. Docstring: "ENC-02 / D-03: manifest reports CRF 18 (was 20 in Phase 4)."
       - `test_duration_parity_after_finalizer(self, synth_source_mp4, finalizer_output)`: `output_path, _ = finalizer_output; errors = validate_duration_parity(synth_source_mp4, output_path, tolerance_ms=100); assert errors == [], errors`. Docstring: "ENC-05 / D-11, D-14: source-to-finalizer duration delta within tolerance on the no-audio branch."
       - `test_finalizer_no_audio_branch_executed(self, finalizer_output)`: `output_path, _ = finalizer_output; probe_cmd = ["ffprobe", "-v", "quiet", "-select_streams", "a", "-show_entries", "stream=codec_type", "-of", "csv=p=0", output_path]; r = subprocess.run(probe_cmd, capture_output=True, text=True, timeout=30); assert r.stdout.strip() == "", f"Expected no audio stream, got: {r.stdout!r}"`. Docstring: "D-13: ffprobe confirms the output has NO audio stream, proving the `-an` branch executed and that all Phase 13 changes (Lanczos, unsharp, BT.709, CRF 18) apply equally to no-audio inputs."

    Do NOT add any test that depends on the silence-cutter container being running. Do NOT shell out to docker compose. All tests run in-process against the synthesized fixture.
  </action>
  <verify>
    <automated>cd services/ffmpeg-finalizer &amp;&amp; test -f tests/test_encode_quality.py &amp;&amp; grep -cE 'def test_' tests/test_encode_quality.py | grep -qE '^[6-9]$|^[1-9][0-9]+$' &amp;&amp; grep -cF 'validate_color_tags' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'validate_bitrate_range' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'validate_duration_parity' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'D-13' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; grep -cE '"-an"' tests/test_encode_quality.py | grep -q '^[1-9]' &amp;&amp; python -m pytest tests/test_encode_quality.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -f services/ffmpeg-finalizer/tests/test_encode_quality.py` exits 0.
    - Source assertion: `grep -cE 'def test_' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least `6` (one per test method).
    - Source assertion: `grep -F 'validate_color_tags' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'validate_bitrate_range' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'validate_duration_parity' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'shutil.which' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match (ffmpeg/ffprobe availability gate present).
    - Source assertion: `grep -F 'ENC-03' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'ENC-05' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match.
    - Source assertion: `grep -F 'D-13' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match (no-audio inheritance documented).
    - Source assertion: `grep -E '"-an"' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match (the explicit no-audio flag in the lavfi synthesis argv).
    - Source assertion: `grep -F 'has_audio' services/ffmpeg-finalizer/tests/test_encode_quality.py` returns at least one match (the sanity assertion in finalizer_output fixture).
    - Test command: `pytest services/ffmpeg-finalizer/tests/test_encode_quality.py -x -q` exits 0 (passing on hosts with ffmpeg, all-skipped otherwise).
    - Test command: `pytest services/ffmpeg-finalizer/tests/ -x -q` exits 0 (both test_finalizer.py and test_encode_quality.py pass together).
  </acceptance_criteria>
  <done>test_encode_quality.py exists with six focused tests exercising each ENC requirement (plus an explicit D-13 verification) against a real synthesized NO-AUDIO fixture; tests gracefully skip when ffmpeg/ffprobe are unavailable; the full ffmpeg-finalizer test suite passes. The lavfi fixture is deliberately no-audio so it sidesteps the loudnorm-branch config constants AND makes D-13 a real assertion instead of just a code-path-shared claim.</done>
</task>

<task type="auto">
  <name>Task 2: Wire validate_concat_mode into test_silence_cutter.py with real-ffmpeg fixture (ENC-01)</name>
  <files>services/silence-cutter/tests/test_silence_cutter.py</files>
  <read_first>
    - services/silence-cutter/tests/test_silence_cutter.py (lines 1-29 module docstring + imports; existing TestValidation class style for adding new tests)
    - services/silence-cutter/src/validate.py (post Plan 01 — validate_concat_mode signature and ENC-01 error format)
    - services/silence-cutter/src/cut_video.py (lines 200-237 — the post-Plan-01 stream-copy concat command; lines 240-273 get_video_duration ffprobe pattern)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"services/silence-cutter/src/validate.py" — the validator's behavior and error format)
  </read_first>
  <behavior>
    - Import for `validate_concat_mode` added to test_silence_cutter.py (extending the existing `from src.validate import ...` line).
    - A new test class `TestConcatMode` contains at minimum two tests:
      - `test_validate_concat_mode_stream_copy`: synthesizes two 2-second clips with ffmpeg lavfi, concats them via the SAME ffmpeg command shape Plan 01's `_concatenate_segments` uses (`-c copy` + `-reset_timestamps 1`), then calls `validate_concat_mode(output_path)` and asserts `errors == []`. Skips if ffmpeg/ffprobe missing.
      - `test_validate_concat_mode_detects_reencode`: same synthesis, but concats with `-c:v libx264 -c:a aac` (the pre-Phase-13 shape — to confirm the validator flags re-encode). Asserts `any("ENC-01" in e for e in errors)`. Skips if ffmpeg/ffprobe missing.
    - The existing tests in test_silence_cutter.py continue to pass.
    - `pytest services/silence-cutter/tests/test_silence_cutter.py -x -q` exits 0.
  </behavior>
  <action>
    In services/silence-cutter/tests/test_silence_cutter.py:

    PART A — extend imports (line 28):
    1. Change `from src.validate import validate_silence_cuts, validate_cross_reference_logic` to `from src.validate import validate_silence_cuts, validate_cross_reference_logic, validate_concat_mode`.
    2. Add `import subprocess` and `import shutil` to the top imports (after the existing `import json` at line 17, if not already present — check existing imports first).

    PART B — extend module docstring (lines around 1-15):
    1. Add `ENC-01` and `D-01` (Phase 13) to the referenced-requirements list. Keep the existing SILC-XX references.

    PART C — add new test class TestConcatMode at the bottom of the file:

    1. Class docstring: "Verify validate_concat_mode against real ffmpeg stream-copy and re-encode outputs (ENC-01 / D-01)."

    2. Helper method or module-level helper `_synth_clip(path, duration=2, seed=0)`:
       - Calls `subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i", f"testsrc=duration={duration}:size=320x240:rate=30:decimals=2", "-f", "lavfi", "-i", f"sine=frequency={440+seed*55}:duration={duration}", "-c:v", "libx264", "-pix_fmt", "yuv420p", "-c:a", "aac", str(path)], capture_output=True, text=True, timeout=30)`. Asserts returncode 0.
       - Small clips (320×240) keep the test fast.

    3. Test `test_validate_concat_mode_stream_copy(self, tmp_path)`:
       - Skip-guards: `if shutil.which("ffmpeg") is None or shutil.which("ffprobe") is None: pytest.skip("ffmpeg/ffprobe not on PATH")`.
       - Use the helper to synthesize two clips at `tmp_path / "a.mp4"` and `tmp_path / "b.mp4"`.
       - Write a concat list file at `tmp_path / "list.txt"` containing the two `file '...'` lines.
       - subprocess.run the EXACT ffmpeg shape from cut_video.py::_concatenate_segments (post Plan 01): `["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", list_path, "-map", "0:v:0", "-map", "0:a:0?", "-c", "copy", "-reset_timestamps", "1", str(out_path)]`. Assert returncode 0.
       - Call `errors = validate_concat_mode(str(out_path))`. Assert `errors == []`, attach the list as the assertion message for debuggability.
       - Docstring: "ENC-01 / D-01: stream-copied concat output has no fresh libx264/Lavc encoder tag, so the validator passes."

    4. Test `test_validate_concat_mode_detects_reencode(self, tmp_path)`:
       - Same skip-guards.
       - Same two synthesized clips + concat list.
       - subprocess.run a re-encode concat: `["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", list_path, "-map", "0:v:0", "-map", "0:a:0?", "-c:v", "libx264", "-c:a", "aac", "-reset_timestamps", "1", str(out_path)]`. Assert returncode 0.
       - Call `errors = validate_concat_mode(str(out_path))`. Assert `any("ENC-01" in e for e in errors)`.
       - Docstring: "ENC-01: a re-encoded concat output carries a fresh encoder tag, so the validator flags it."

    Do not touch any other test class. Do not modify imports beyond adding `validate_concat_mode`, `subprocess`, `shutil`. (Task 3 adds a SECOND new class — TestConcatEdgeCases — peer to TestConcatMode.)
  </action>
  <verify>
    <automated>cd services/silence-cutter &amp;&amp; grep -cF 'validate_concat_mode' tests/test_silence_cutter.py | grep -qE '^[3-9]$|^[1-9][0-9]+$' &amp;&amp; grep -cE 'class TestConcatMode' tests/test_silence_cutter.py | grep -q '^1$' &amp;&amp; grep -cF 'shutil.which' tests/test_silence_cutter.py | grep -q '^[1-9]' &amp;&amp; python -m pytest tests/test_silence_cutter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cF 'validate_concat_mode' services/silence-cutter/tests/test_silence_cutter.py` returns at least `3` (one import + at least two test usages).
    - Source assertion: `grep -cE 'class TestConcatMode' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -cE 'def test_validate_concat_mode_stream_copy' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -cE 'def test_validate_concat_mode_detects_reencode' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -F '"-c", "copy"' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (positive case argv mirrors Plan 01).
    - Source assertion: `grep -F '"-c:v", "libx264"' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (negative case argv).
    - Source assertion: `grep -F 'shutil.which' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (skip gate present).
    - Test command: `pytest services/silence-cutter/tests/test_silence_cutter.py -x -q` exits 0.
  </acceptance_criteria>
  <done>validate_concat_mode is exercised by two real-ffmpeg-driven tests (positive + negative) under TestConcatMode; tests skip gracefully when ffmpeg/ffprobe are unavailable; the full silence-cutter test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add D-14 -c copy edge-case coverage under TestConcatEdgeCases (ENC-05, D-14)</name>
  <files>services/silence-cutter/tests/test_silence_cutter.py</files>
  <read_first>
    - services/silence-cutter/tests/test_silence_cutter.py (the file as extended by Task 2 — TestConcatMode now exists; add TestConcatEdgeCases as a peer class)
    - services/silence-cutter/src/cut_video.py (lines 200-237 — the post-Plan-01 stream-copy concat command shape that the edge-case tests must replicate)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-14 — "Phase 13 test suite includes a clip with variable keyframe spacing and a clip with audio-shorter-than-video to exercise -c copy edge cases. Duration parity stays within ±33ms.")
    - services/ffmpeg-finalizer/src/validate.py (post Plan 02 — validate_duration_parity signature; TestConcatEdgeCases uses ffprobe duration calls directly rather than importing the validator across services, but mirrors the same ±33ms tolerance)
  </read_first>
  <behavior>
    - A new test class `TestConcatEdgeCases` is added to services/silence-cutter/tests/test_silence_cutter.py (peer to TestConcatMode from Task 2).
    - Class contains at minimum two tests:
      - `test_concat_variable_keyframe_spacing_preserves_duration`: synthesizes two clips with intentionally different keyframe intervals (e.g., `-g 15` on clip A, `-g 60` on clip B); concats them via the SAME `-c copy` argv shape as `cut_video.py::_concatenate_segments`; ffprobes the concat output duration and asserts it is within ±33ms of the sum of the two source durations. Skips if ffmpeg/ffprobe missing.
      - `test_concat_audio_shorter_than_video_preserves_duration`: synthesizes a clip with `-t 5` video + `-t 4` audio (audio one second shorter than video — emulates a tail-trimmed clip from Whisper); concats two such clips via `-c copy`; ffprobes the concat output duration and asserts it is within ±100ms of the sum of source durations (relaxed because mismatched A/V can yield small extra rounding; the production ±33ms gate runs against the real talking-head clip in Plan 04). Skips if ffmpeg/ffprobe missing.
    - Existing tests in test_silence_cutter.py continue to pass.
    - `pytest services/silence-cutter/tests/test_silence_cutter.py -x -q` exits 0.
  </behavior>
  <action>
    In services/silence-cutter/tests/test_silence_cutter.py, add a new test class `TestConcatEdgeCases` AFTER the TestConcatMode class created by Task 2:

    1. Class docstring: "Verify -c copy concat preserves duration parity on edge cases: variable keyframe spacing and audio-shorter-than-video (D-14 / ENC-05)."

    2. Module-level helper (re-use the existing `_synth_clip` helper from Task 2 where possible; if `_synth_clip` is class-private in Task 2, promote it to module level or define a parallel helper inside TestConcatEdgeCases). The helpers must accept extra kwargs for keyframe interval and audio duration.

    3. Helper `_synth_clip_with_gop(path, duration=3, gop=30, seed=0)`:
       - Calls `subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i", f"testsrc=duration={duration}:size=320x240:rate=30", "-f", "lavfi", "-i", f"sine=frequency={440+seed*55}:duration={duration}", "-c:v", "libx264", "-g", str(gop), "-pix_fmt", "yuv420p", "-c:a", "aac", str(path)], capture_output=True, text=True, timeout=30)`. Asserts returncode 0.
       - The `-g <gop>` flag sets the keyframe interval; clip A uses `-g 15` and clip B uses `-g 60` to deliberately mismatch.

    4. Helper `_synth_clip_audio_short(path, video_duration=5, audio_duration=4, seed=0)`:
       - Calls `subprocess.run(["ffmpeg", "-y", "-f", "lavfi", "-i", f"testsrc=duration={video_duration}:size=320x240:rate=30", "-f", "lavfi", "-i", f"sine=frequency={440+seed*55}:duration={audio_duration}", "-c:v", "libx264", "-pix_fmt", "yuv420p", "-c:a", "aac", "-shortest", "0", str(path)], capture_output=True, text=True, timeout=30)`. (The `-shortest 0` disables the default "stop at shortest stream" — we WANT a clip where audio is shorter than video so the concat exercises the audio-tail edge case.) Asserts returncode 0.

    5. Helper `_ffprobe_duration(path) -> float`:
       - Calls `subprocess.run(["ffprobe", "-v", "quiet", "-show_entries", "format=duration", "-of", "csv=p=0", str(path)], capture_output=True, text=True, timeout=30)`. Asserts returncode 0. Returns `float(result.stdout.strip())`.

    6. Helper `_concat_stream_copy(list_path, out_path)`:
       - subprocess.run the EXACT ffmpeg shape from cut_video.py::_concatenate_segments (post Plan 01): `["ffmpeg", "-y", "-f", "concat", "-safe", "0", "-i", str(list_path), "-map", "0:v:0", "-map", "0:a:0?", "-c", "copy", "-reset_timestamps", "1", str(out_path)]`. Asserts returncode 0.

    7. Test `test_concat_variable_keyframe_spacing_preserves_duration(self, tmp_path)`:
       - Skip-guards: `if shutil.which("ffmpeg") is None or shutil.which("ffprobe") is None: pytest.skip("ffmpeg/ffprobe not on PATH")`.
       - Synthesize `clip_a = tmp_path / "a.mp4"` with `gop=15`, `clip_b = tmp_path / "b.mp4"` with `gop=60`, both `duration=3`.
       - ffprobe both source durations.
       - Write concat list `tmp_path / "list.txt"` with `file 'a.mp4'` + `file 'b.mp4'`.
       - Concat via `_concat_stream_copy`.
       - ffprobe concat output duration; assert `abs(concat_dur - (dur_a + dur_b)) * 1000 <= 33`, message includes all three durations on failure.
       - Docstring: "D-14 / ENC-05: -c copy concat preserves duration even when source clips have different GOP/keyframe intervals (`-g 15` vs `-g 60`). Tolerance: ±33ms (one frame at 30fps)."

    8. Test `test_concat_audio_shorter_than_video_preserves_duration(self, tmp_path)`:
       - Same skip-guards.
       - Synthesize `clip_a` and `clip_b` via `_synth_clip_audio_short(path, video_duration=5, audio_duration=4, seed=...)`. Two clips each ~5s video / ~4s audio.
       - ffprobe both source video durations.
       - Write concat list.
       - Concat via `_concat_stream_copy`.
       - ffprobe concat output duration; assert `abs(concat_dur - (dur_a + dur_b)) * 1000 <= 100`, message includes all three durations.
       - Docstring: "D-14 / ENC-05: -c copy concat preserves video duration even when each source clip has audio one second shorter than video (the Whisper-cut-silent-tail shape). Tolerance: ±100ms — relaxed slightly because mismatched A/V can produce small concat-rounding artifacts; the production ±33ms gate runs against the real talking-head clip in Plan 04."

    Do not touch any class other than TestConcatMode and the new TestConcatEdgeCases.
  </action>
  <verify>
    <automated>cd services/silence-cutter &amp;&amp; grep -cE 'class TestConcatEdgeCases' tests/test_silence_cutter.py | grep -q '^1$' &amp;&amp; grep -cE 'def test_concat_variable_keyframe_spacing_preserves_duration' tests/test_silence_cutter.py | grep -q '^1$' &amp;&amp; grep -cE 'def test_concat_audio_shorter_than_video_preserves_duration' tests/test_silence_cutter.py | grep -q '^1$' &amp;&amp; grep -cF '"-g"' tests/test_silence_cutter.py | grep -q '^[1-9]' &amp;&amp; grep -cF 'D-14' tests/test_silence_cutter.py | grep -q '^[1-9]' &amp;&amp; python -m pytest tests/test_silence_cutter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE 'class TestConcatEdgeCases' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -cE 'def test_concat_variable_keyframe_spacing_preserves_duration' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -cE 'def test_concat_audio_shorter_than_video_preserves_duration' services/silence-cutter/tests/test_silence_cutter.py` returns exactly `1`.
    - Source assertion: `grep -F '"-g"' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (the variable-keyframe-interval helper sets explicit GOP).
    - Source assertion: `grep -F 'D-14' services/silence-cutter/tests/test_silence_cutter.py` returns at least one match (traceability comment).
    - Source assertion: `grep -F '"-c", "copy"' services/silence-cutter/tests/test_silence_cutter.py` returns at least two matches (TestConcatMode + the edge-case concat helper).
    - Test command: `pytest services/silence-cutter/tests/test_silence_cutter.py -x -q` exits 0.
  </acceptance_criteria>
  <done>TestConcatEdgeCases exists with two D-14 edge-case tests (variable keyframe interval + audio-shorter-than-video), both using the post-Plan-01 -c copy concat argv shape; tests skip gracefully when ffmpeg/ffprobe are unavailable; the full silence-cutter test suite passes. D-14 coverage is no longer "claimed" — it is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → ffmpeg subprocess | Tests synthesize fixtures via `subprocess.run(["ffmpeg", ...])` with hardcoded argv (no user-input interpolation). |
| test process → tempfile filesystem | All artifacts under `tmp_path_factory.mktemp(...)` or pytest's `tmp_path`; auto-cleaned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.03-01 | Tampering | test argv construction | mitigate | Tests use the same list-based argv pattern as production. No shell strings. Argv values are literals or pytest tmp_path Path objects. |
| T-13.03-02 | Denial of Service | tests synthesize 3-5 second clips that consume disk space | accept | Tests use 320×240 (concat tests) and 1920×1080×5s no-audio (encode-quality tests) — fixtures stay under 10 MB total per session and live in pytest tmpdir (auto-cleaned). No risk to long-lived storage. |
| T-13.03-03 | Tampering | future regression that removes the ENC checks | mitigate | test_encode_quality.py exercises every ENC requirement via real ffprobe output on the no-audio branch (D-13); TestConfig (already updated in Plan 02 Task 3) asserts CRF == 18; TestConcatMode + TestConcatEdgeCases make stream-copy and -c copy edge-case behavior fail CI if regressed. Phase 13 invariants survive future refactors. |
| T-13.03-04 | Information Disclosure | ffmpeg stderr captured into assertion messages | accept | Same surface as Phase 4 / Phase 3 test patterns; local-only. |
| T-13.03-SC | Supply Chain | npm/pip/cargo installs | not_applicable | Phase 13 plan adds no new dependencies. `pytest`, `subprocess`, `shutil`, `json` are all pre-existing or stdlib. |

</threat_model>

<verification>
1. `pytest services/ffmpeg-finalizer/tests/ -x -q` exits 0.
2. `pytest services/silence-cutter/tests/ -x -q` exits 0.
3. All source greps in the three tasks' acceptance criteria pass.
4. Source code in services/ffmpeg-finalizer/src/ and services/silence-cutter/src/ is unchanged (this plan touches only `tests/`).
5. test_finalizer.py was NOT modified by Plan 03 (Plan 02 Task 3 already swept it green).
</verification>

<success_criteria>
- ENC-01 verified by test_validate_concat_mode_stream_copy / test_validate_concat_mode_detects_reencode against real ffmpeg argv shapes.
- ENC-02 verified by test_finalizer_crf_is_18 (manifest field) + test_finalizer_bitrate_in_band (ffprobe) — note: TestConfig.test_encoding_params (constant) was updated by Plan 02 Task 3, not Plan 03.
- ENC-03 verified by test_finalizer_color_tags_are_bt709 (ffprobe) + test_finalizer_unsharp_and_lanczos_in_manifest (manifest fields).
- ENC-04 verified by test_finalizer_unsharp_and_lanczos_in_manifest (manifest fields) — the perceptual halo-free claim is exercised in Plan 04's visual A/B.
- ENC-05 verified by test_duration_parity_after_finalizer (finalizer pass) + test_concat_variable_keyframe_spacing_preserves_duration + test_concat_audio_shorter_than_video_preserves_duration (concat edge cases — D-14).
- D-13 (no-audio inheritance) verified for real by test_finalizer_no_audio_branch_executed + the no-audio fixture used by every TestEncodeQuality test.
- D-14 (-c copy edge cases) verified by the two TestConcatEdgeCases tests — no longer a documentation-only claim.
- Tests degrade gracefully (skip, do not fail) when ffmpeg/ffprobe are absent — CI without those binaries doesn't false-fail.
</success_criteria>

<output>
Create `.planning/phases/13-encode-quality/13-03-SUMMARY.md` when done. Include:
- Full path of the new test_encode_quality.py with method list (6 tests).
- Diff hunks applied to test_silence_cutter.py (imports + TestConcatMode + TestConcatEdgeCases classes).
- Confirmation that both `pytest services/ffmpeg-finalizer/tests/ -x -q` and `pytest services/silence-cutter/tests/ -x -q` exit 0.
- Confirmation that all source-grep acceptance criteria pass.
- Note that the no-audio lavfi fixture intentionally exercises the -an branch (D-13) so the "no-audio inheritance" claim has a real test backing it.
- Note that D-14 -c copy edge-case coverage is now enforced by TestConcatEdgeCases (was a documentation-only claim before this plan).
- Note that the perceptual ENC-04 "sin halos" claim is NOT covered here by design — Plan 04's visual A/B is the gate.
</output>
</output>
