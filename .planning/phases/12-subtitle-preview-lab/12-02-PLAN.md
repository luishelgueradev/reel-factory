---
phase: 12-subtitle-preview-lab
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - services/remotion-studio/package.json
  - services/remotion-studio/vite.config.ts
  - services/remotion-studio/src/editor/App.tsx
  - services/remotion-studio/src/editor/index.html
  - services/remotion-studio/src/editor/index.tsx
  - services/remotion-studio/src/server.ts
  - services/remotion-studio/src/preview/PreviewApp.tsx
  - services/remotion-studio/src/preview/PreviewPlayer.tsx
  - services/remotion-studio/src/preview/TextareaInput.tsx
  - services/remotion-studio/src/preview/textToCaptions.ts
  - services/remotion-studio/src/preview/FontGridPage.tsx
  - services/remotion-studio/public/sample-video.mp4
  - services/remotion-studio/Dockerfile
autonomous: false
requirements:
  - PREV-01
  - PREV-02
  - PREV-03

user_setup:
  - service: sample-video
    why: "A sample MP4 video file is needed for the preview page background"
    env_vars: []
    dashboard_config:
      - task: "Provide a short 5-10 second Spanish talking-head MP4 video as services/remotion-studio/public/sample-video.mp4"
        location: "Place file manually in the project directory"

must_haves:
  truths:
    - "A web page at /preview shows a 9:16 viewport with subtitle text rendered over a sample background"
    - "The preview uses @remotion/player to render SubtitledVideo — the same composition used in production"
    - "All 18 available fonts load correctly in the preview (matching production font loading)"
    - "Every SubtitleConfig parameter is adjustable in real-time: layout, fontFamily, fontSize, activeColor, inactiveColor, letterSpacing, lineHeight, backgroundHighlight, outlineColor, outlineWidth, position, bottomOffset, pastWordOpacity"
    - "A sample Spanish paragraph cycles word-by-word when the user plays the Remotion Player"
    - "Font grid at /preview/fonts shows all 18 fonts simultaneously with sample text"
    - "Changes to any parameter update the preview instantly without page reload"
  artifacts:
    - path: "services/remotion-studio/src/preview/PreviewApp.tsx"
      provides: "Main preview page component with 9:16 viewport and collapsible controls"
    - path: "services/remotion-studio/src/preview/PreviewPlayer.tsx"
      provides: "Remotion Player wrapper rendering SubtitledVideo at 1080x1920"
    - path: "services/remotion-studio/src/preview/TextareaInput.tsx"
      provides: "Editable textarea with Spanish default text"
    - path: "services/remotion-studio/src/preview/textToCaptions.ts"
      provides: "textToCaptionPages() converting arbitrary text to TikTokPage[]"
    - path: "services/remotion-studio/src/preview/FontGridPage.tsx"
      provides: "Font grid view showing all 18 fonts with sample text"
    - path: "services/remotion-studio/src/editor/App.tsx"
      provides: "Updated SPA with React Router routing /editor and /preview"
    - path: "services/remotion-studio/src/server.ts"
      provides: "Express serving /preview SPA static files"
    - path: "services/remotion-studio/src/editor/index.tsx"
      provides: "Updated entry point using React Router"
  key_links:
    - from: "PreviewPlayer.tsx"
      to: "remotion-renderer/src/Root.tsx"
      via: "import SubtitledVideo component for @remotion/player"
      pattern: "component={SubtitledVideo}"
    - from: "PreviewApp.tsx"
      to: "pipeline-config.ts"
      via: "SubtitleConfig local state drives Player inputProps"
      pattern: "inputProps=.*subtitleConfig"
    - from: "textToCaptions.ts"
      to: "@remotion/captions"
      via: "createTikTokStyleCaptions for TikTokPage[] generation"
      pattern: "createTikTokStyleCaptions"
    - from: "server.ts"
      to: "dist/editor/"
      via: "Express static serving for /preview route"
      pattern: "/preview"
---

<objective>
Build the /preview SPA page with @remotion/player integration, real-time SubtitleConfig controls, text-to-captions conversion, font grid, and Express routing.

Purpose: Per PREV-01 and PREV-02, users need a pixel-accurate preview page that renders subtitles using the production SubtitledVideo composition via @remotion/player, with all 18 fonts available and every SubtitleConfig parameter adjustable in real-time. Per PREV-03, changes must reflect instantly without page reload. Per D-04, D-05, D-06, D-08, D-10, D-11, D-12, D-13.

Output: Working /preview and /preview/fonts pages, Remotion Player rendering, textToCaptionPages utility, Express /preview route, SPA routing with React Router.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-subtitle-preview-lab/12-CONTEXT.md
@.planning/phases/12-subtitle-preview-lab/12-RESEARCH.md
@.planning/phases/12-subtitle-preview-lab/12-01-SUMMARY.md
@services/remotion-studio/src/server.ts
@services/remotion-studio/src/editor/App.tsx
@services/remotion-studio/src/editor/index.tsx
@services/remotion-studio/src/editor/index.html
@services/remotion-studio/src/editor/components/StyleControls.tsx
@services/remotion-studio/src/editor/components/LayoutSelector.tsx
@services/remotion-studio/vite.config.ts
@services/remotion-studio/package.json
@services/remotion-studio/Dockerfile
@services/remotion-renderer/src/Root.tsx
@services/remotion-renderer/src/fonts.ts
@services/remotion-renderer/src/pipeline-config.ts
@services/remotion-renderer/src/compositions/LayoutDispatcher.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From services/remotion-renderer/src/pipeline-config.ts (AFTER Plan 01):
```typescript
export interface SubtitleConfig {
  layout: SubtitleLayoutMode;
  fontFamily?: string;
  fontSize?: number;
  activeColor?: string;
  inactiveColor?: string;
  outlineColor?: string;
  outlineWidth?: number;
  backgroundHighlight?: BackgroundHighlight;
  textShadow?: TextShadow;
  letterSpacing?: number;
  position?: SubtitlePosition;
  lineHeight?: number;
  bottomOffset?: number;
  pastWordOpacity?: number;  // NEW from Plan 01
}

export const DEFAULT_SUBTITLE_CONFIG = {
  fontSize: 58,
  activeColor: "#FFFF00",
  inactiveColor: "#FFFFFF",
  outlineColor: "#000000",
  outlineWidth: 3,
  position: "bottom-center",
  lineHeight: 1.3,
  bottomOffset: 250,
  pastWordOpacity: 0.4,  // NEW from Plan 01
};
```

From services/remotion-renderer/src/Root.tsx:
```typescript
export interface RemotionProps {
  videoSrc: string;
  captionPages: TikTokPage[];
  videoWidth?: number;
  videoHeight?: number;
  totalDurationMs?: number;
  subtitleLayout?: SubtitleLayoutMode;
  subtitleConfig?: SubtitleConfig;
  titles?: TitleConfig[];
  zoomEvents?: ZoomEvent[];
  transitionEvents?: TransitionEvent[];
}
```

From services/remotion-renderer/src/fonts.ts:
```typescript
export const AVAILABLE_FONTS = [
  "Inter", "Roboto", "Montserrat", "Oswald", "Poppins", "BebasNeue", "Antonio",
  "Raleway", "Ubuntu", "Nunito", "SpaceGrotesk", "Rubik", "SourceSans3",
  "Outfit", "PlayfairDisplay", "LexendDeca", "Signika", "Lato", "monospace",
] as const;

export async function loadFont(fontFamily: string): Promise<string>;
```

From services/remotion-renderer/src/compositions/LayoutDispatcher.tsx:
```typescript
export interface SubtitleLayoutProps {
  captionPages: TikTokPage[];
  config: SubtitleConfig;
  totalDurationMs?: number;
}
```

From @remotion/player:
```typescript
import { Player } from '@remotion/player';
<Player
  component={SubtitledVideo}
  durationInFrames={number}
  compositionWidth={1080}
  compositionHeight={1920}
  fps={30}
  controls={true}
  loop={true}
  inputProps={RemotionProps}
  style={{ width: '100%' }}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create SPA routing, Express /preview route, and textToCaptionPages utility</name>
  <files>services/remotion-studio/package.json, services/remotion-studio/vite.config.ts, services/remotion-studio/src/editor/App.tsx, services/remotion-studio/src/editor/index.tsx, services/remotion-studio/src/editor/index.html, services/remotion-studio/src/server.ts, services/remotion-studio/src/preview/textToCaptions.ts</files>
  <read_first>services/remotion-studio/package.json, services/remotion-studio/vite.config.ts, services/remotion-studio/src/editor/App.tsx, services/remotion-studio/src/editor/index.tsx, services/remotion-studio/src/editor/index.html, services/remotion-studio/src/server.ts</read_first>
  <action>
Per D-02: Single SPA with routing — /editor and /preview coexist in the same Vite build. Per D-01: /preview route served by existing remotion-studio Express server. Per RESEARCH Pattern 3: convert textarea text to TikTokPage[] using createTikTokStyleCaptions.

1. **package.json**: Add two dependencies:
   - `@remotion/player@4.0.457` (matches Remotion ecosystem)
   - `react-router-dom@^7.15.1` (SPA routing)
   Run `npm install` after editing package.json.

2. **vite.config.ts**: No structural changes needed — the existing Vite config builds from `src/editor` and outputs to `../../dist/editor`. The React Router approach means both /editor and /preview share the same SPA entry point. The Vite config stays as-is since both routes are handled client-side by React Router.

3. **src/editor/index.html**: Update `<title>` from "Remotion Config Editor" to "Remotion Studio" (it serves both editor and preview now).

4. **src/editor/index.tsx**: No changes needed — it already renders `<App />` which now includes React Router.

5. **src/editor/App.tsx**: Wrap with React Router (BrowserRouter). Import react-router-dom Routes, Route, Navigate. Restructure so:
   - `/editor` renders the existing editor content (the current App component body) inside an `EditorApp` component
   - `/preview` renders `PreviewApp` component (created in Task 2)
   - `/preview/fonts` renders `FontGridPage` component (created in Task 2)
   - `/` redirects to `/editor`
   The current App component body (loading config, save handler, etc.) stays as `EditorApp`. The new `App` is just the Router shell.

6. **src/server.ts**: Add Express static serving for the `/preview` route. Below the existing `/editor` SPA fallback, add:
   ```typescript
   // SPA fallback for /preview (shared SPA with /editor per D-02)
   app.get("/preview", (_req, res) => {
     const indexHtml = path.join(EDITOR_DIST, "index.html");
     if (fs.existsSync(indexHtml)) {
       res.sendFile(indexHtml);
     } else {
       res.status(404).json({ error: "Preview SPA not built", message: "Run 'npm run build:editor' to build the SPA" });
     }
   });
   app.get("/preview/{*splat}", (_req, res) => {
     const indexHtml = path.join(EDITOR_DIST, "index.html");
     if (fs.existsSync(indexHtml)) {
       res.sendFile(indexHtml);
     } else {
       res.status(404).json({ error: "Preview SPA not built", message: "Run 'npm run build:editor' to build the SPA" });
     }
   });
   ```
   Add a log line for the /preview route in the startup section: `console.log('  GET  /preview      — Subtitle Preview SPA');`

7. **src/preview/textToCaptions.ts**: Create the text-to-captions conversion utility per RESEARCH Pattern 3. Import `createTikTokStyleCaptions` and `Caption` from `@remotion/captions`. Export function `textToCaptionPages(text: string, options?: { wordsPerSecond?: number }): TikTokPage[]`:
   - Split text on whitespace into words, filter empty strings
   - Generate synthetic `Caption[]` with evenly-distributed timestamps (default 3 words/second, each word gets startMs, endMs, timestampMs)
   - First word has no leading space, subsequent words have leading space space character (per RESEARCH Pattern 3)
   - Call `createTikTokStyleCaptions({ captions, combineTokensWithinMilliseconds: 1500 })` and return `pages`
   - Return empty array for empty/whitespace-only text
   - Default Spanish sample text: "Hoy quiero compartir contigo una reflexión sobre el poder de las palabras y cómo influyen en nuestra vida diaria. Cada frase que pronunciamos tiene el potencial de inspirar, de sanar o de transformar la realidad de quienes nos rodean."
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm ls @remotion/player react-router-dom 2>&1 | grep -E "@remotion/player|react-router-dom"</automated>
  </verify>
  <done>
    - @remotion/player@4.0.457 and react-router-dom are in package.json dependencies
    - App.tsx uses BrowserRouter with Route paths for /editor, /preview, /preview/fonts
    - server.ts serves /preview and /preview/{*splat} with SPA fallback to same index.html
    - textToCaptionPages() produces valid TikTokPage[] from Spanish text input
    - TypeScript compiles without errors in remotion-studio
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PreviewApp, PreviewPlayer, TextareaInput, and FontGridPage components</name>
  <files>services/remotion-studio/src/preview/PreviewApp.tsx, services/remotion-studio/src/preview/PreviewPlayer.tsx, services/remotion-studio/src/preview/TextareaInput.tsx, services/remotion-studio/src/preview/FontGridPage.tsx</files>
  <read_first>services/remotion-renderer/src/Root.tsx, services/remotion-renderer/src/pipeline-config.ts, services/remotion-renderer/src/fonts.ts, services/remotion-studio/src/editor/components/StyleControls.tsx, services/remotion-studio/src/editor/components/LayoutSelector.tsx</read_first>
  <action>
Per D-04, D-05, D-06, D-08, D-10, D-11, D-12, D-13: Create the preview page components that render SubtitledVideo via @remotion/player with real-time SubtitleConfig controls, text-to-captions conversion, and font grid.

1. **src/preview/PreviewPlayer.tsx**: Create the @remotion/player wrapper component per RESEARCH Patterns 1 and 2.

   Import `Player` from `@remotion/player` and `SubtitledVideo` from `../Root` (which is copied from remotion-renderer at Docker build time, same as other shared files). Import `RemotionProps` from `../Root`. Import `loadFont` from `../fonts`.

   Props: `{ subtitleConfig: SubtitleConfig, captionPages: TikTokPage[], videoSrc: string, totalDurationMs: number }`.

   The component must:
   - Call `loadFont()` for the current `subtitleConfig.fontFamily` in a `useEffect` (per Pitfall 3 — pre-load fonts before rendering)
   - Use `useMemo` to compute `durationInFrames` from `totalDurationMs` at 30fps
   - Render the Player inside a container with `aspect-ratio: 1080/1920` that is centered in the left panel
   - Player props: `component={SubtitledVideo}`, `compositionWidth={1080}`, `compositionHeight={1920}`, `fps={30}`, `controls={true}`, `loop={true}`, `inputProps` containing videoSrc, captionPages, subtitleConfig (with layout extracted as subtitleLayout), titles=[], zoomEvents=[], transitionEvents=[], totalDurationMs, totalDurationMs
   - Player `style={{ width: '100%' }}` — the container handles aspect-ratio
   - `initiallyMuted={true}` per RESEARCH open question resolution

2. **src/preview/TextareaInput.tsx**: Create a textarea component per D-11.

   Props: `{ value: string, onChange: (text: string) => void }`.
   - Renders a `<textarea>` with the current text value
   - Dark theme styling matching the editor SPA (background: #1e1e2e, color: #e0e0e0, border: 1px solid #444, borderRadius: 8)
   - Label: "Sample Text" with a subtitle explaining word-by-word cycling
   - The default text is provided by the parent component (from textToCaptions.ts's default Spanish text)
   - On change, calls `onChange` with the new text value
   - Uses `textToCaptionPages()` to convert text to TikTokPages when the user types (debounced)

3. **src/preview/PreviewApp.tsx**: Create the main preview page component per D-08.

   Layout: 9:16 viewport on the left (~40% width), collapsible control panels on the right (~60% width). This mirrors the existing /editor layout pattern.

   State management:
   - `subtitleConfig: SubtitleConfig` — React local state with defaults from DEFAULT_SUBTITLE_CONFIG
   - `sampleText: string` — default Spanish paragraph from textToCaptionPages
   - `captionPages: TikTokPage[]` — derived from sampleText via textToCaptionPages()
   - `videoSrc: string` — defaults to `staticFile('sample-video.mp4')` per D-06
   - `totalDurationMs: number` — derived from captionPages (last token endMs, or default 10000)

   Components in left panel:
   - PreviewPlayer with aspect-ratio container

   Components in right panel (scrollable, collapsible sections):
   - LayoutSelector (reused from editor) for layout mode selection
   - StyleControls (reused from editor, now extended with lineHeight + pastWordOpacity from Plan 01)
   - TextareaInput for sample text editing
   - Font family selector (reuse the dropdown from StyleControls or create a simpler version)
   - Save Config button (PUT /api/config, same pattern as editor App.tsx)

   Update `subtitleConfig` state via `updateSubtitle(partial: Partial<SubtitleConfig>)` callback, which the StyleControls and LayoutSelector already accept.

   When `sampleText` or `subtitleConfig` changes, recalculate captionPages via `textToCaptionPages(sampleText)` and derive totalDurationMs from the last token's endMs.

   Font loading: On component mount, call `loadFont()` for the default font (Inter) and eagerly load all fonts for the font grid.

4. **src/preview/FontGridPage.tsx**: Create the font grid page per D-12 and D-13.

   Props: `{ onSelectFont: (font: string) => void }` (navigates back to preview with selected font).

   Import `AVAILABLE_FONTS` and `loadFont` from `../fonts`.

   Render a grid (responsive, 3-4 columns) of font cards. Each card:
   - Calls `loadFont(fontName)` in a useEffect to load the Google Font
   - Shows loading state until font is loaded
   - Once loaded, renders sample text "Hola mundo ¿Cómo estás?" in that font via CSS `fontFamily`
   - Shows the font name above the sample text
   - On click, navigates back to /preview with the selected font applied (use `useNavigate()` from react-router-dom or a callback)

   The FontGridPage is a separate React Router route at `/preview/fonts`.

   Style: dark theme matching the rest of the SPA. Each card has hover effect, cursor pointer, border styling consistent with LayoutSelector cards.

   **Important anti-pattern (from RESEARCH):** Font grid cards use plain CSS font rendering with fontFamily, NOT a separate Remotion Player for each font. Per D-13, this is lightweight and the font-family strings match production exactly because they come from the same `fonts.ts` file.

5. **Wire routing in App.tsx** (if not already done in Task 1): Import PreviewApp, FontGridPage, and connect React Router routes:
   - `<Route path="/preview" element={<PreviewApp />} />`
   - `<Route path="/preview/fonts" element={<FontGridPage />} />`

6. **Sample video placeholder**: Create a minimal placeholder file at `services/remotion-studio/public/sample-video.mp4`. The actual sample video must be provided by the user (5-10 second Spanish talking-head MP4). Create a 1-byte placeholder and document the requirement. The Dockerfile already has `RUN mkdir -p /app/public` — add a note that in production, a real video file must be placed at this path.

   For the Vite dev server, `staticFile('sample-video.mp4')` expects the file in the `public/` directory relative to the Vite root (`src/editor/`). But for the Remotion Player in the browser, `staticFile()` is designed for Remotion's own static serving. In the SPA context, we need to either:
   - Place the video in the Vite `public/` directory so it's served as a static asset
   - Or use a direct URL path

   Per D-06: The sample video is bundled in the container's `public/` directory. In Docker, this is `/app/public/`. For local dev, create `services/remotion-studio/public/sample-video.mp4` as a placeholder. The Vite dev server automatically serves files from `public/` at the root path.

   **Note on staticFile():** In the Remotion Player context in the browser, `staticFile()` generates a URL path that expects Remotion's static file serving. Since the Player runs in the browser SPA (not Remotion Studio), use a direct `/sample-video.mp4` URL path instead of `staticFile()`. In local dev, Vite serves from `public/`. In Docker, Express serves from `/app/public/`. Add `app.use("/preview/static", express.static(path.join(process.cwd(), "public")))` to server.ts for serving the sample video, or configure Vite's proxy. Actually, the simplest approach: use `/sample-video.mp4` as the videoSrc, and ensure Express serves `public/` as static files. Add `app.use(express.static(path.join(process.cwd(), "public")))` to server.ts BEFORE the SPA fallback routes.

   In the Dockerfile, add a line to COPY the sample video: `COPY public/sample-video.mp4 /app/public/sample-video.mp4` — but only if the file exists. Use a conditional or ensure the file is present.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - PreviewApp.tsx renders 9:16 viewport (left) + collapsible controls (right)
    - PreviewPlayer.tsx renders SubtitledVideo via @remotion/player at 1080x1920 with CSS aspect-ratio scaling
    - PreviewPlayer loads the selected font before rendering (useEffect + loadFont)
    - TextareaInput.tsx provides editable Spanish text with debounced onChange
    - textToCaptionPages() correctly converts text to TikTokPage[] using createTikTokStyleCaptions
    - FontGridPage.tsx shows all 18 fonts (plus monospace) in a responsive grid with CSS rendering
    - All SubtitleConfig parameters are adjustable in real-time via StyleControls and LayoutSelector
    - React Router routes /preview and /preview/fonts work
    - Express serves /preview SPA and public/ static files
    - TypeScript compilation succeeds in remotion-studio
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of preview lab functionality</name>
  <what_built>Subtitle Preview Lab with @remotion/player rendering, real-time controls, and font grid</what_built>
  <how_to-verify>
    1. Start the dev server: `cd services/remotion-studio && npm run dev:editor`
    2. Open `http://localhost:5174/preview` in a browser
    3. Verify the 9:16 viewport renders with a dark background (sample video may show as black if not provided)
    4. Change the layout mode using LayoutSelector — subtitles should update instantly
    5. Adjust fontSize slider — text size should change in real-time
    6. Change activeColor and inactiveColor — colors should update instantly
    7. Adjust lineHeight and pastWordOpacity sliders — layout and opacity should change
    8. Click Play on the Remotion Player — subtitles should cycle word-by-word
    9. Edit the textarea text — captions should regroup based on new text
    10. Navigate to `/preview/fonts` — all 18 fonts should render in a grid
    11. Click a font in the grid — should return to preview with that font selected
    12. Open `/editor` — existing editor should still work unchanged
  </how-to-verify>
  <action>Manual visual and functional verification of the Subtitle Preview Lab in the browser</action>
  <verify>Human clicks through all 12 verification steps listed in how-to-verify</verify>
  <done>All 12 verification steps pass — preview renders correctly, controls work in real-time, font grid loads all 18 fonts, /editor still works</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→SPA | React local state drives all preview updates — no server round-trip for rendering |
| Browser→API | PUT /api/config for saving — same auth model as editor (internal Docker network) |
| Font CDN→Browser | @remotion/google-fonts loads fonts from Google Fonts CDN — no secrets involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Spoofing | Font CDN loading | accept | Fonts loaded from Google Fonts CDN — standard practice, no sensitive data in font requests |
| T-12-04 | Tampering | Textarea input XSS | mitigate | Textarea text converted to TikTokPage tokens — rendered as text spans in React, not innerHTML. No HTML injection vector |
| T-12-05 | Tampering | /preview route path traversal | mitigate | Express serves static files from dist/editor directory — standard express.static mitigations apply |
| T-12-06 | Tampering | SubtitleConfig via Player inputProps | accept | Config values rendered as CSS properties in React components — no HTML injection possible |
| T-12-SC | Tampering | npm install (@remotion/player, react-router-dom) | mitigate | Both packages verified in RESEARCH Package Legitimacy Audit — @remotion/player is official Remotion monorepo package, react-router-dom is industry standard |
</threat_model>

<verification>
1. TypeScript compilation succeeds: `cd services/remotion-studio && npx tsc --noEmit`
2. Dependencies installed: `cd services/remotion-studio && npm ls @remotion/player react-router-dom`
3. All preview files exist: `ls services/remotion-studio/src/preview/`
4. server.ts has /preview route: `grep -c "/preview" services/remotion-studio/src/server.ts`
5. App.tsx has React Router: `grep -c "BrowserRouter" services/remotion-studio/src/editor/App.tsx`
6. All 18 fonts referenced in FontGridPage: `grep -c "AVAILABLE_FONTS" services/remotion-studio/src/preview/FontGridPage.tsx`
7. textToCaptionPages uses createTikTokStyleCaptions: `grep "createTikTokStyleCaptions" services/remotion-studio/src/preview/textToCaptions.ts`
8. PreviewPlayer uses @remotion/player: `grep "@remotion/player" services/remotion-studio/src/preview/PreviewPlayer.tsx`
</verification>

<success_criteria>
- /preview page renders SubtitledVideo via @remotion/player at 1080x1920 with correct 9:16 aspect ratio
- All SubtitleConfig parameters are adjustable in real-time (layout, fontFamily, fontSize, activeColor, inactiveColor, letterSpacing, lineHeight, backgroundHighlight, outlineColor, outlineWidth, position, bottomOffset, pastWordOpacity)
- Sample Spanish text in textarea converts to TikTokPage[] via createTikTokStyleCaptions
- Remotion Player playback controls allow play/pause/scrub (per D-10)
- All 18 fonts load correctly via @remotion/google-fonts loadFont()
- Font grid at /preview/fonts shows all fonts with correct rendering per font
- Changes to any parameter update preview instantly without page reload (per PREV-03)
- /editor route still works unchanged
- Express serves /preview SPA correctly
- TypeScript compiles without errors
</success_criteria>

<output>
Create `.planning/phases/12-subtitle-preview-lab/12-02-SUMMARY.md` when done
</output>