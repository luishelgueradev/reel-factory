---
phase: 21-png-overlays
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - services/remotion-studio/src/editor/components/OverlayEditor.tsx
  - services/remotion-studio/src/preview/PreviewApp.tsx
autonomous: false
requirements:
  - OVERLAY-01
  - OVERLAY-02
  - OVERLAY-03

user_setup: []

must_haves:
  truths:
    - "An 'Overlays' tab appears in the studio tab bar between Titles and Subtitles"
    - "OverlayEditor shows empty state with '+ Add Overlay' button when no overlays are configured"
    - "User can select a PNG file; it is converted to base64 data URL and shown as a 120px thumbnail"
    - "Client-side 5 MB gate rejects oversized files with inline error before FileReader call"
    - "User can configure x (0-1080), y (0-1920), displayWidth (10-1080), opacity (0-1) for each overlay"
    - "Add Overlay / Save Changes button is disabled when no PNG is selected (empty imageData)"
    - "Hard cap: Add Overlay button is disabled with #555 background when overlays.length >= 3"
    - "Live preview in the Remotion Player updates as the user changes overlay fields"
    - "Overlays state persists in PreviewApp and is passed to the Player's inputProps"
    - "OverlayEditor structure, dark-theme colors, and control patterns mirror TitleEditor exactly"
  artifacts:
    - path: "services/remotion-studio/src/editor/components/OverlayEditor.tsx"
      provides: "OverlayEditor React component — list + add/edit/delete form for PNG overlays"
      exports: ["OverlayEditor"]
    - path: "services/remotion-studio/src/preview/PreviewApp.tsx"
      provides: "Updated PreviewApp with Overlays tab and overlays state wired to Player"
      contains: "overlays"
  key_links:
    - from: "services/remotion-studio/src/preview/PreviewApp.tsx"
      to: "OverlayEditor"
      via: "import + Overlays tab JSX"
      pattern: "OverlayEditor"
    - from: "services/remotion-studio/src/preview/PreviewApp.tsx"
      to: "Player inputProps"
      via: "overlays state passed as inputProps.overlays"
      pattern: "inputProps.*overlays\|overlays.*inputProps"
---

<objective>
Build the Studio UI for PNG overlays: create OverlayEditor.tsx (mirroring TitleEditor exactly), add an "Overlays" tab to PreviewApp.tsx (between Titles and Subtitles), wire overlays state to the Remotion Player inputProps for live preview.

Purpose: The final user-facing piece — the studio UI that lets users upload PNGs, configure position/size/opacity, and see the overlay in the live Player preview.
Output: OverlayEditor.tsx, updated PreviewApp.tsx with Overlays tab and live preview wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-png-overlays/21-CONTEXT.md
@.planning/phases/21-png-overlays/21-UI-SPEC.md
@.planning/phases/21-png-overlays/21-RESEARCH.md
@.planning/phases/21-png-overlays/21-02-SUMMARY.md

<interfaces>
<!-- Key contracts from codebase. Executor reads, does not re-discover. -->

From services/remotion-studio/src/preview/PreviewApp.tsx (current TABS array — lines 27-31):
```typescript
const TABS: { id: string; label: string }[] = [
  { id: "titles",    label: "Titles"    },
  { id: "subtitles", label: "Subtitles" },
  { id: "text",      label: "Text"      },
];
// Phase 21 inserts: { id: "overlays", label: "Overlays" } after "titles"
```

From services/remotion-studio/src/editor/components/TitleEditor.tsx (mirror pattern for OverlayEditor):
```typescript
interface TitleEditorProps {
  titles: TitleConfig[];
  onChange: (titles: TitleConfig[]) => void;
  onPreviewChange?: (liveTitles: TitleConfig[]) => void;
}

// handleDraftChange pattern (line 106-110):
const handleDraftChange = (updater: (prev: Partial<TitleConfig>) => Partial<TitleConfig>) => {
  const updated = updater(newTitle);
  setNewTitle(updated);
  onPreviewChange?.(computeLiveTitles(updated));
};

// handleAdd (line 113-127) and handleRemove (line 130-141) patterns
// handleStartEdit (line 144-169): copies existing item to draft state
// handleSaveEdit (line 171-185): writes draft back to array at editingIndex
```

From services/remotion-studio/src/preview/PreviewApp.tsx (titles state + Player wiring pattern — ~lines 8-20):
```typescript
import type { SubtitleConfig, TitleConfig } from "../pipeline-config";
// Phase 21 adds: import type { PngOverlayConfig } from "../pipeline-config";
// Phase 21 adds: import { OverlayEditor } from "../editor/components/OverlayEditor";

// Titles state wiring to Player (existing):
// const [titles, setTitles] = useState<TitleConfig[]>([]);
// ...passed as inputProps.titles to PreviewPlayer
```

PngOverlayConfig shape:
```typescript
export interface PngOverlayConfig {
  imageData: string;       // base64 data URL
  x: number;
  y: number;
  displayWidth: number;
  opacity?: number;
  _resolvedFile?: string;
}
```

UI-SPEC.md color/spacing tokens (non-negotiable):
- Background page: #0f0f1a; tab bar + active card: #16213e; list items + inputs: #1e1e2e / #2a2a3e
- Accent: #4CAF50; delete: #b71c1c; text primary: #e0e0e0; label: #bbb/#ccc; hint: #666
- Card gap: 12px (matches TitleEditor); card padding: 16px; list item padding: 12px 16px
- Tab minHeight: 44px; upload zone minHeight: 44px
- Slider accentColor: #4CAF50
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OverlayEditor.tsx — PNG overlay list + add/edit/delete form</name>
  <files>
    services/remotion-studio/src/editor/components/OverlayEditor.tsx
  </files>
  <read_first>
    - services/remotion-studio/src/editor/components/TitleEditor.tsx — read the ENTIRE file; every structural and style decision mirrors this file exactly. Note: state management (editingIndex, addingNew, draft state), handleDraftChange pattern, handleAdd/handleRemove/handleStartEdit/handleSaveEdit patterns, list item rendering, form card rendering, and all inline style values.
    - .planning/phases/21-png-overlays/21-UI-SPEC.md — read the ENTIRE spec. Authority for: component states (empty, list, form open), upload zone states (no file, loading, file selected), exact copywriting table, color values, spacing scale, interaction contract, form field order, disabled button states.
    - .planning/phases/21-png-overlays/21-CONTEXT.md — D-02 (hard cap at 3, disabled button), D-03 (PngOverlayConfig fields), D-07 (mirrors TitleEditor exactly), D-08 (tab placement), D-09 (5 MB gate), D-10 (body limit already raised in Plan 21-01)
  </read_first>
  <action>
    Create services/remotion-studio/src/editor/components/OverlayEditor.tsx. This file mirrors TitleEditor.tsx in structure, state management patterns, and inline style conventions. Do NOT introduce new patterns.

    Props interface (OverlayEditorProps): overlays: PngOverlayConfig[]; onChange: (overlays: PngOverlayConfig[]) => void; onPreviewChange?: (liveOverlays: PngOverlayConfig[]) => void;

    State: editingIndex: number | null; addingNew: boolean; draft overlay state (parallel to newTitle in TitleEditor); fileError: string | null (inline validation message for upload errors).

    Default values for new overlays (per D-03 and CONTEXT.md Claude's Discretion): x: 40, y: 40, displayWidth: 200, opacity: 1, imageData: "".

    Upload interaction (per UI-SPEC.md Interaction Contract):
    1. Hidden input type="file" accept="image/png"
    2. Styled label as upload zone (dashed border #444, background #2a2a3e, minHeight 44px, borderRadius 6, padding 16px, textAlign center, cursor pointer)
    3. Upload zone hover: border-color #4CAF50 (use onMouseEnter/onMouseLeave state)
    4. On file selection: first check file.size > 5 * 1024 * 1024 → setFileError("File too large. Maximum size is 5 MB.") and return (per D-09)
    5. Check file.type !== "image/png" → setFileError("Please select a PNG file.") and return
    6. Show "Loading..." text at 12px #aaa while FileReader runs
    7. reader.readAsDataURL(file); on load: update draft imageData with the data URL, show 120px thumbnail + filename; clear fileError
    8. On FileReader error: setFileError("Failed to read file. Please try again.")
    9. After file selected: replace upload zone with: 120px wide img tag (preview thumbnail — this is OK since it is outside Remotion, just a browser preview), filename text at 14px #e0e0e0, "Change" link at 12px #90caf9 cursor pointer that re-triggers the file input

    Form field order (per UI-SPEC.md, form sections in order):
    1. PNG Upload section (upload zone or thumbnail state)
    2. X / Y inputs — two input type="number" in a flex row, gap 16px; X label "X (px)", min 0, max 1080, step 1; Y label "Y (px)", min 0, max 1920, step 1; style mirrors TitleEditor x/y inputs (dark #2a2a3e bg, #e0e0e0 text, #333 border, 8px padding, 8px borderRadius)
    3. Width input — single input type="number", label "Width (px)", min 10, max 1080, step 1
    4. Opacity slider — input type="range", min 0, max 1, step 0.05, label "Opacity: {value.toFixed(2)}", accentColor #4CAF50
    5. Form actions row — primary CTA (add mode: "Add Overlay", edit mode: "Save Changes") + "Discard Changes" button

    Primary CTA disabled state: background #555, cursor not-allowed, when draft.imageData is empty string or falsy.
    Add Overlay trigger disabled state (outer list view): background #555, cursor not-allowed, when overlays.length >= 3 (per D-02). Copy: "+ Add Overlay".
    Delete button: background #b71c1c, no confirmation dialog (per UI-SPEC.md Interaction Contract).

    List item rendering (per UI-SPEC.md "State: overlay list"):
    - Background #1e1e2e, border 1px solid #333, borderRadius 8, padding 12px 16px
    - Primary text: "Overlay N" (1-indexed, e.g., "Overlay 1") — do NOT show filename (per CONTEXT.md Claude's Discretion: "Overlay N" for simplicity), fontSize 14, fontWeight 600, color #e0e0e0
    - Secondary text: "x: {x}, y: {y}, width: {displayWidth}px", fontSize 12, color #666, marginTop 4
    - Edit button: padding "4px 10px", background #333, color #ccc, border "1px solid #555"
    - Delete button: background #b71c1c

    Empty state (per UI-SPEC.md and copywriting table):
    - Copy: "No PNG overlays configured. Click "Add Overlay" to add a logo or watermark."
    - Style: fontSize 12, color #666, fontStyle italic

    Active form card (per UI-SPEC.md):
    - Background #16213e, border 1px solid #4CAF50, borderRadius 8, padding 16
    - Heading: "Add Overlay" (adding) / "Edit Overlay" (editing), fontSize 14, fontWeight 600, color #e0e0e0

    Draft change → onPreviewChange: call onPreviewChange with the computed live overlays array on every field change (same as TitleEditor's handleDraftChange calling computeLiveTitles). This drives live preview in the Player.

    IMPORTANT — impeccable skill + frontend-design plugin: Invoke at the start of this task execution before writing any UI code (AGENTS.md non-negotiable requirement for all frontend phases).
  </action>
  <verify>
    <automated>grep -c "FileReader\|file\.size\|5 \* 1024\|5242880" services/remotion-studio/src/editor/components/OverlayEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: services/remotion-studio/src/editor/components/OverlayEditor.tsx
    - `grep -c "FileReader" services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns >= 1
    - `grep "5 \* 1024 \* 1024\|5242880" services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns a match (5 MB gate per D-09)
    - `grep "overlays\.length >= 3\|length.*>=.*3" services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns a match (hard cap per D-02)
    - `grep "onPreviewChange" services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns >= 2 matches (called on draft changes)
    - `grep "image/png" services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns >= 1 match (MIME validation)
    - Copywriting check: `grep '"Discard Changes"' services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns a match (per UI-SPEC.md copywriting table)
    - `grep '"Add Overlay"' services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns >= 2 matches (trigger button + CTA)
    - `grep "#b71c1c" services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns >= 1 (delete button color)
    - `grep "#4CAF50" services/remotion-studio/src/editor/components/OverlayEditor.tsx` returns >= 2 (accent used on add button border and slider)
  </acceptance_criteria>
  <done>OverlayEditor.tsx exported as named export; full list + add/edit/delete form; 5 MB client gate; hard cap at 3; live preview via onPreviewChange; all UI-SPEC.md copywriting, colors, and spacing honored; dark-theme inline styles match TitleEditor</done>
</task>

<task type="auto">
  <name>Task 2: Wire Overlays tab and overlays state into PreviewApp.tsx</name>
  <files>
    services/remotion-studio/src/preview/PreviewApp.tsx
  </files>
  <read_first>
    - services/remotion-studio/src/preview/PreviewApp.tsx — read the FULL file to understand: the TABS array (lines 27-31), how titles state is managed and passed to the Player (useState, passed via PreviewPlayer inputProps), how the tab panel rendering works (the switch/if block rendering different editors per active tab)
    - .planning/phases/21-png-overlays/21-CONTEXT.md — D-08 (tab order: Titles | Overlays | Subtitles | Text), D-11 (Player receives overlays in inputProps; overlays array with imageData used as rawImageSrc via SubtitledVideo)
    - .planning/phases/21-png-overlays/21-UI-SPEC.md — "Tab placement" section; the TabButton style contract (14px, minHeight 44, active underline #90caf9)
  </read_first>
  <action>
    In services/remotion-studio/src/preview/PreviewApp.tsx, make the following changes:

    1. Add to TABS array (per D-08): insert { id: "overlays", label: "Overlays" } after the "titles" entry and before the "subtitles" entry. Final order: Titles | Overlays | Subtitles | Text.

    2. Import PngOverlayConfig type: add `import type { PngOverlayConfig } from "../pipeline-config";` to the import block (alongside the existing TitleConfig import).

    3. Import OverlayEditor: add `import { OverlayEditor } from "../editor/components/OverlayEditor";` to the imports.

    4. Add overlays state: `const [overlays, setOverlays] = useState<PngOverlayConfig[]>([]);`

    5. Add live-preview overlays state: `const [liveOverlays, setLiveOverlays] = useState<PngOverlayConfig[]>([]);` — same pattern as liveTitle tracking in TitleEditor wiring if present, or use a single overlays state for both committed and live (Claude's discretion; simpler is better — match the TitleEditor pattern found in the file).

    6. Load overlays from the API on mount: In the existing useEffect that calls GET /api/config, extract `data.overlays` and call setOverlays with the loaded value (parallel to how titles are loaded).

    7. Save overlays to the API on change: In the handleSaveConfig function (or wherever PUT /api/config is called), include overlays in the config body (parallel to how titles are sent).

    8. Wire overlays to OverlayEditor in the "overlays" tab panel: when activeTab === "overlays", render `<OverlayEditor overlays={overlays} onChange={(updated) => { setOverlays(updated); setLiveOverlays(updated); }} onPreviewChange={setLiveOverlays} />`.

    9. Pass liveOverlays (or overlays) to the Player's inputProps: Find where inputProps is built for the PreviewPlayer component. Add `overlays: liveOverlays` to the inputProps object. This requires SubtitledVideo (the studio's composition) to accept and render overlays, which was done in Plan 21-02.

    Verify that the PreviewApp builds without TypeScript errors by running: `cd services/remotion-studio && npx tsc --noEmit` (if available) or the build command.

    IMPORTANT — impeccable skill + frontend-design plugin: Invoke at the start of this task execution (AGENTS.md requirement).
  </action>
  <verify>
    <automated>grep -n "overlays\|OverlayEditor" services/remotion-studio/src/preview/PreviewApp.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "overlays" services/remotion-studio/src/preview/PreviewApp.tsx` returns >= 5 (state, tab panel, API load, API save, inputProps)
    - `grep "OverlayEditor" services/remotion-studio/src/preview/PreviewApp.tsx` returns >= 2 (import + JSX usage)
    - Tab order check: `grep -A10 "const TABS" services/remotion-studio/src/preview/PreviewApp.tsx` shows "titles" before "overlays" before "subtitles"
    - `grep "liveOverlays\|setLiveOverlays\|overlays.*inputProps\|inputProps.*overlays" services/remotion-studio/src/preview/PreviewApp.tsx` returns >= 1 (overlays wired to Player)
    - `grep "data\.overlays\|overlays.*api\|config\.overlays" services/remotion-studio/src/preview/PreviewApp.tsx` returns >= 1 (overlays loaded from API config on mount)
    - Build check: `cd services/remotion-studio && npm run build:editor` exits 0
  </acceptance_criteria>
  <done>Overlays tab added between Titles and Subtitles; overlays state initialized, loaded from API on mount, saved to API; OverlayEditor rendered in Overlays tab; liveOverlays passed to Player inputProps for live preview; editor build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 21-03 wired the full PNG overlay feature: OverlayEditor UI (list + add/edit form), Overlays tab in the studio, live Player preview, and the render path decode from Plans 21-01 and 21-02.
  </what-built>
  <how-to-verify>
    Start the studio if not already running:
    cd services/remotion-studio && setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor ACTIVE_PIPELINE_CONFIG_PATH=$(pwd)/../../pipeline/pipeline-config.json npx tsx src/server.ts > /tmp/remotion-server.log 2>&1 &

    Then open the studio at http://localhost:3123 and verify:

    1. TAB ORDER: Confirm the tab bar shows "Titles | Overlays | Subtitles | Text" (Overlays is between Titles and Subtitles).

    2. EMPTY STATE: Click the Overlays tab. Confirm the empty state message: "No PNG overlays configured. Click "Add Overlay" to add a logo or watermark." and a dashed green "+ Add Overlay" button.

    3. UPLOAD + LIVE PREVIEW:
       a. Click "+ Add Overlay". The add form opens with the upload zone ("Click to select a PNG file" / "Max 5 MB · PNG only").
       b. Select any transparent PNG file (logo, watermark, etc.).
       c. Confirm: thumbnail appears (120px wide), no white box background.
       d. Set X=540, Y=200 and observe the overlay moves to the top-center of the Player preview (it may be partially off-screen if the PNG is large).
       e. Adjust opacity slider to 0.5 — overlay becomes semi-transparent in the preview.
       f. Click "Add Overlay" — the overlay appears in the list as "Overlay 1" with x/y/width summary.

    4. HARD CAP: Add 2 more overlays (total 3). Confirm the "+ Add Overlay" button turns grey (#555) and is non-clickable.

    5. 5 MB GATE: Create a file > 5 MB or use a dummy large file. Click "+ Add Overlay", select the large file. Confirm the error message "File too large. Maximum size is 5 MB." appears inline without crashing.

    6. DELETE: Delete one overlay from the list. Confirm it disappears immediately (no confirmation dialog).

    7. EDIT: Click "Edit" on an overlay. Confirm the form opens with existing values pre-filled. Change the display width and click "Save Changes". Confirm the list item summary updates.

    8. TRANSPARENCY: Verify the overlay PNG renders with transparency (no white box around it in the Player preview).

    Visual quality bar: overlay should be crisp (not blurry) when the source PNG is larger than displayWidth. If the PNG is upscaled, accept it — that is a user-input issue, not a bug.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 verification steps pass, or describe which step failed and what was observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| studio browser: file input | User selects a local PNG file; FileReader converts to base64 data URL in-browser |
| studio browser → PUT /api/config | base64 data URL embedded in JSON config body; server now accepts up to 10mb |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering | OverlayEditor: FileReader MIME check | mitigate | Check file.type === "image/png" before calling FileReader; show inline error "Please select a PNG file." for non-PNG files |
| T-21-09 | DoS | OverlayEditor: client-side 5 MB gate | mitigate | Check file.size > 5 * 1024 * 1024 before FileReader call; show "File too large. Maximum size is 5 MB." — prevents large uploads from reaching the server |
| T-21-10 | Tampering | OverlayEditor: hard cap at 3 overlays | mitigate | Add Overlay button disabled when overlays.length >= 3 (D-02); prevents unbounded array growth that could produce excessive base64 payload |
| T-21-11 | Information Disclosure | OverlayEditor: imageData in config | accept | base64 data URL is stored in pipeline-config.json (local file, not exposed over network beyond the studio's own API); studio is protected by HTTP Basic Auth when publicly exposed (Phase 16) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 21 |
</threat_model>

<verification>
After human checkpoint approves:
1. Tab order: Titles | Overlays | Subtitles | Text — visually confirmed in studio
2. Upload → thumbnail → live preview: overlay visible in Player with correct position
3. 5 MB client gate: oversized file rejected with inline error message
4. Hard cap: 4th overlay attempt is blocked (disabled button)
5. Delete: immediate, no confirmation dialog
6. Edit: existing values pre-filled, save updates the list
7. Transparency: PNG renders without white box in the Player
8. Build still green: npm run build:editor exits 0
</verification>

<success_criteria>
- OVERLAY-01: User can add a transparent PNG overlay onto the video (visible in Player preview; render path wired in Plan 21-02)
- OVERLAY-02: PNG larger than frame is downscaled by Chromium (CSS displayWidth constraint set by PngOverlay component; upscale warning in render.ts)
- OVERLAY-03: User can position (x/y) and size (displayWidth) the PNG overlay via OverlayEditor controls
- All 3 requirements closed
</success_criteria>

<output>
Create `.planning/phases/21-png-overlays/21-03-SUMMARY.md` when done
</output>
