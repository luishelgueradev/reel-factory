---
phase: 18-studio-ui-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - services/remotion-studio/src/editor/App.tsx
  - services/remotion-studio/src/editor/EditorApp.tsx
  - services/remotion-studio/src/server.ts
autonomous: false
requirements:
  - STUDIO-01
  - STUDIO-02
  - STUDIO-03

must_haves:
  truths:
    - "App.tsx has exactly two routes: path=/ renders PreviewApp, path=* redirects to /"
    - "EditorApp.tsx is deleted"
    - "server.ts root GET / serves the SPA (not redirects to /editor)"
    - "server.ts has 301 redirects for /editor, /preview, /preview/fonts to /"
    - "server.ts API routes (/api/*) are registered before the static middleware that serves /"
    - "Navigating to http://localhost:3123/ in browser shows the unified two-column StudioApp"
    - "Navigating to /editor, /preview, /preview/fonts each redirect to / and show StudioApp"
  artifacts:
    - path: "services/remotion-studio/src/editor/App.tsx"
      provides: "Single-route React Router config"
      contains: "path=\"/\""
    - path: "services/remotion-studio/src/editor/EditorApp.tsx"
      provides: "Deleted — must not exist"
    - path: "services/remotion-studio/src/server.ts"
      provides: "Updated route serving"
      contains: "res.redirect(301"
  key_links:
    - from: "App.tsx"
      to: "PreviewApp.tsx"
      via: "Route path=/ element=<PreviewApp />"
      pattern: "path.*/"
    - from: "server.ts GET /"
      to: "serveSpa"
      via: "app.get('/', serveSpa)"
      pattern: "app.get.*serveSpa"
---

<objective>
Collapse the React Router to a single canonical route (`/` → PreviewApp), delete EditorApp.tsx,
and update server.ts to serve the SPA at `/` with 301 redirects for all old routes.

Purpose: Completes the routing half of STUDIO-03 (removes the separate /editor screen and its
route). Enables the browser to reach the unified StudioApp at the root URL.
Output: App.tsx with single route, EditorApp.tsx deleted, server.ts serving SPA at /.
Ends with a human-verify checkpoint confirming the UI renders in the browser.
</objective>

<execution_context>
MANDATORY FIRST STEP — invoke impeccable skill + frontend-design plugin before touching any file.
Per AGENTS.md §"UI/frontend work — REQUIRED tooling": every UI phase task must start with these
two tools. This is non-negotiable.

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-studio-ui-redesign/18-CONTEXT.md
@.planning/phases/18-studio-ui-redesign/18-RESEARCH.md
@.planning/phases/18-studio-ui-redesign/18-PATTERNS.md
@.planning/phases/18-studio-ui-redesign/18-UI-SPEC.md
@.planning/phases/18-studio-ui-redesign/18-01-SUMMARY.md

<interfaces>
<!-- Current App.tsx (full content) — source: 18-PATTERNS.md §App.tsx -->
Current App.tsx routes (full set):
  Route path="/editor"         → EditorApp
  Route path="/editor/*"       → EditorApp
  Route path="/preview/fonts"  → FontGridPage
  Route path="/preview"        → PreviewApp
  Route path="/"               → Navigate to "/editor" (redirect)
  Route path="*"               → Navigate to "/editor" (redirect)

Target App.tsx routes (after D-02):
  Route path="/"   → PreviewApp  (single canonical screen)
  Route path="*"   → Navigate to "/" replace

Imports to REMOVE from App.tsx:
  - EditorApp (from ./EditorApp.js)
  - FontGridPage (from ../preview/FontGridPage.js)

Imports to KEEP:
  - Navigate (from react-router-dom)
  - PreviewApp (from ../preview/PreviewApp.js)

<!-- Current server.ts static/SPA routes (lines ~228–251) — source: 18-PATTERNS.md §server.ts -->
Current server.ts (lines ~228–251):
  app.use("/editor", express.static(EDITOR_DIST));
  app.use("/assets", express.static(path.join(EDITOR_DIST, "assets")));
  app.get("/editor", serveSpa);
  app.get("/editor/", serveSpa);
  app.get("/editor/{*splat}", serveSpa);
  app.get("/preview", serveSpa);
  app.get("/preview/", serveSpa);
  app.get("/preview/{*splat}", serveSpa);
  app.get("/", (_req, res) => { res.redirect("/editor"); });

Target server.ts (after D-02):
  app.use("/", express.static(EDITOR_DIST));         // serves /assets/... bundle files
  app.get("/", serveSpa);                             // root → unified screen
  app.get("/editor",          (_req, res) => res.redirect(301, "/"));
  app.get("/editor/",         (_req, res) => res.redirect(301, "/"));
  app.get("/editor/{*splat}", (_req, res) => res.redirect(301, "/"));
  app.get("/preview",         (_req, res) => res.redirect(301, "/"));
  app.get("/preview/",        (_req, res) => res.redirect(301, "/"));
  app.get("/preview/{*splat}",(_req, res) => res.redirect(301, "/"));
  app.get("/{*splat}", serveSpa);                     // SPA catch-all

CRITICAL ORDER: /api/* routes must appear BEFORE app.use("/", express.static(EDITOR_DIST)).
Read server.ts to find where /api routes are registered and ensure the new static line goes AFTER.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse App.tsx to single route and delete EditorApp.tsx</name>
  <files>
    services/remotion-studio/src/editor/App.tsx
    services/remotion-studio/src/editor/EditorApp.tsx
  </files>
  <read_first>
    - services/remotion-studio/src/editor/App.tsx — read full file; note all imports and route definitions
    - services/remotion-studio/src/editor/EditorApp.tsx — read to confirm no exports are re-used anywhere outside App.tsx; verify before deleting
  </read_first>
  <action>
    Step A — Rewrite App.tsx:
    Keep only these imports: `React` from "react", `BrowserRouter`, `Routes`, `Route`, `Navigate` from "react-router-dom", `PreviewApp` from "../preview/PreviewApp.js".
    Remove imports of: `EditorApp`, `FontGridPage`.
    The App function body: single BrowserRouter > Routes with exactly two Route elements:
    1. `<Route path="/" element={<PreviewApp />} />`
    2. `<Route path="*" element={<Navigate to="/" replace />} />`

    Step B — Delete EditorApp.tsx:
    Run: `rm /home/luis/proyectos/reel-factory/services/remotion-studio/src/editor/EditorApp.tsx`
    This file is fully absorbed by the unified PreviewApp. No exports from EditorApp are
    referenced anywhere after App.tsx stops importing it.

    Step C — Build:
    `cd services/remotion-studio && npm run build:editor`
    If the build errors on ConfigPreview (imported by the now-deleted EditorApp), those errors
    disappear because EditorApp.tsx is deleted. If plan 18-02 ran in parallel and deleted
    ConfigPreview.tsx + FontGridPage.tsx, the build should be clean. If this plan runs first
    and those files still exist, the build error about missing imports in EditorApp disappears
    because EditorApp.tsx itself is deleted.

    Expected result: build exits 0 with no TypeScript errors.
  </action>
  <verify>
    <automated>test ! -f /home/luis/proyectos/reel-factory/services/remotion-studio/src/editor/EditorApp.tsx && cd /home/luis/proyectos/reel-factory/services/remotion-studio && npm run build:editor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f services/remotion-studio/src/editor/EditorApp.tsx` passes.
    - App.tsx contains exactly: imports for React, react-router-dom (BrowserRouter, Routes, Route, Navigate), and PreviewApp. No EditorApp or FontGridPage imports.
    - `grep -c "EditorApp\|FontGridPage" services/remotion-studio/src/editor/App.tsx` returns 0.
    - App.tsx Route elements: `path="/"` and `path="*"` — exactly two. No `/editor`, `/preview`, `/preview/fonts` routes.
    - `npm run build:editor` exits 0.
  </acceptance_criteria>
  <done>App.tsx has single / route + * catch-all. EditorApp.tsx deleted. Build green.</done>
</task>

<task type="auto">
  <name>Task 2: Update server.ts to serve SPA at / with 301 redirects for old routes</name>
  <files>services/remotion-studio/src/server.ts</files>
  <read_first>
    - services/remotion-studio/src/server.ts — read the ENTIRE file to find: (a) where /api routes are registered relative to static serving, (b) the current app.use("/editor") and app.get("/") redirect lines (around lines 228–251), (c) the serveSpa function, (d) the PUBLIC_DIR static middleware placement
  </read_first>
  <action>
    Update the SPA serving block in server.ts. The EXISTING block (around lines 228–251) must be
    replaced with the following target pattern. Read the file first to find the exact block.

    Replace the entire block that currently has:
      `app.use("/editor", express.static(EDITOR_DIST));`
      `app.use("/assets", ...);`
      `app.get("/editor", serveSpa);` ... (all the /editor and /preview routes)
      `app.get("/", (_req, res) => { res.redirect("/editor"); });`

    With this ordered block:
      1. `app.use("/", express.static(EDITOR_DIST));`  ← serves /assets/... and any static file at root
      2. `app.get("/", serveSpa);`                      ← explicit root → SPA
      3. `app.get("/editor",          (_req, res) => res.redirect(301, "/"));`
      4. `app.get("/editor/",         (_req, res) => res.redirect(301, "/"));`
      5. `app.get("/editor/{*splat}", (_req, res) => res.redirect(301, "/"));`
      6. `app.get("/preview",         (_req, res) => res.redirect(301, "/"));`
      7. `app.get("/preview/",        (_req, res) => res.redirect(301, "/"));`
      8. `app.get("/preview/{*splat}",(_req, res) => res.redirect(301, "/"));`
      9. `app.get("/{*splat}", serveSpa);`              ← SPA catch-all for client-side routing

    CRITICAL: Verify the /api routes (GET /api/config, PUT /api/config) are registered BEFORE
    `app.use("/", express.static(EDITOR_DIST))` in the file. If they are registered after the
    static middleware, static middleware will intercept API calls. If needed, move the static
    middleware block to be after the API routes. The `app.use(express.static(PUBLIC_DIR))` for
    sample-video stays in its current position — do NOT move it.

    The `/assets` explicit route (`app.use("/assets", ...)`) can be kept alongside or removed
    since `app.use("/", express.static(EDITOR_DIST))` already covers it — either is fine.
    If keeping it, place it before the SPA catch-all.

    The `serveSpa` function body is unchanged.

    Start the studio server in background to test, then stop it:
    `cd services/remotion-studio && npm run build:editor`
    Confirm build exits 0 before starting server.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-studio && npm run build:editor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "res.redirect.*301.*/" services/remotion-studio/src/server.ts` returns at least 6 lines (one for each of /editor, /editor/, /editor/*, /preview, /preview/, /preview/*).
    - `grep "app.get.*\"/\".*serveSpa" services/remotion-studio/src/server.ts` matches (root serves SPA).
    - `grep "app.use.*\"/\".*express.static" services/remotion-studio/src/server.ts` matches (static assets served at root).
    - `grep "res.redirect.*\/editor" services/remotion-studio/src/server.ts` returns 0 lines (no redirect to /editor).
    - `npm run build:editor` exits 0.
    - server.ts: API routes (app.get("/api/config"), app.put("/api/config")) appear BEFORE the `app.use("/", express.static(...))`  line (verify with line numbers).
  </acceptance_criteria>
  <done>server.ts serves the SPA at / with 301 redirects for all old paths. API routes precede static middleware. Build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 18 redesign is complete across all three plans:
    - Plan 18-01: TitleEditor simplified to 2-prop interface (onChange only, no onPreviewChange/onSave)
    - Plan 18-02: PreviewApp.tsx is the unified StudioApp with TabBar (Titles/Subtitles/Text), Font Grid inline, previewTitles removed, ConfigPreview.tsx + FontGridPage.tsx deleted
    - Plan 18-03: App.tsx single route, EditorApp.tsx deleted, server.ts serves / with 301 redirects

    The studio server must be started before verifying:
    ```
    cd /home/luis/proyectos/reel-factory/services/remotion-studio && setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor ACTIVE_PIPELINE_CONFIG_PATH=$(pwd)/../../pipeline/pipeline-config.json npx tsx src/server.ts > /tmp/remotion-server.log 2>&1 &
    ```
    Wait ~3 seconds for startup, then open http://localhost:3123/ in the browser.
  </what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3123/ — should see the unified two-column StudioApp immediately (no redirect to /editor).
       Expected: Left panel shows the 9:16 video Player. Right panel shows the tab bar with "Titles", "Subtitles", "Text". The "Titles" tab is active (underlined in #90caf9). Header shows "Reel Factory Studio" on the left, "Render Video" (greyed, disabled) + "Save Config" (green) on the right.

    2. Verify the Titles tab content: title overlay list (or "No title overlays. Add one below." if empty). The TitleEditor renders with no console errors about onPreviewChange or onSave.

    3. Click "Subtitles" tab: should show the Layout Selector (TikTok/Bar/Sentence/Karaoke options), StyleControls sliders/selects, and below them the "Browse Fonts" grid showing font cards. Clicking a font card should update the Player subtitle font live.

    4. Click "Text" tab: should show the sample text textarea.

    5. Switch tabs and verify no data loss — any edits in Subtitles are still present when switching to Text and back.

    6. Navigate to http://localhost:3123/editor — should redirect to http://localhost:3123/ (check browser address bar).

    7. Navigate to http://localhost:3123/preview — should redirect to http://localhost:3123/.

    8. Navigate to http://localhost:3123/preview/fonts — should redirect to http://localhost:3123/.

    9. Hover over "Render Video" button — tooltip shows "Coming soon — rendering via pipeline API". The button does not fire any request when clicked (or it is fully disabled and does nothing).

    10. Click "Save Config" — should write config and show "Configuration saved successfully" green banner for ~2s.

    11. Open browser DevTools console — no React errors, no "onPreviewChange is not defined", no failed network requests.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 11 checks pass.
    Or describe which checks failed — e.g. "tabs visible but /editor redirect not working".
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Express routing | All old routes now 301 to /; no new attack surface introduced |
| Static file serving at / | express.static checks file existence before next(); API routes registered first prevent interception |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Spoofing | Express static vs API route ordering | mitigate | API routes registered before app.use("/", express.static(...)); verified in acceptance criteria |
| T-18-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Build gate: `npm run build:editor` exits 0 (covers TypeScript correctness of App.tsx + server.ts).
Route verification (requires running server): curl -I http://localhost:3123/editor should return 301.
Human checkpoint: visual confirmation all tabs render, redirects work, no console errors.
Regression: `cd services/remotion-renderer && npm test` still passes.
</verification>

<success_criteria>
App.tsx has exactly one screen route. EditorApp.tsx is deleted. server.ts serves / → StudioApp
and 301-redirects /editor, /preview, /preview/fonts to /. Human checkpoint: all 11 visual checks
passed and the user has typed "approved".
</success_criteria>

<output>
Create `.planning/phases/18-studio-ui-redesign/18-03-SUMMARY.md` when done.
</output>
