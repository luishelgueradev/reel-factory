---
phase: 20-title-block-precision
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - services/remotion-studio/src/compositions/TitleOverlay.tsx
autonomous: true
requirements: [TITLE-01, TITLE-02, TITLE-03]

must_haves:
  truths:
    - "TitleOverlay renders at left=(x/1080)*100%, top=(y/1920)*100% — no centering transform (D-03, D-04)"
    - "TitleOverlay borderRadius is config-driven: borderRadius ?? 12 px (D-09)"
    - "TitleOverlay does not render a subtitle block (D-07)"
    - "DEFAULT_TITLE_STYLE in TitleOverlay.tsx contains x:200, y:960, borderRadius:12 and does not contain topOffset/subtitleFontSize/subtitleColor/subtitleFontFamily (D-06, D-08, D-09)"
    - "fontsToLoad array contains only titleFontFamily (not subtitleFontFamily) (D-08)"
  artifacts:
    - path: "services/remotion-studio/src/compositions/TitleOverlay.tsx"
      provides: "Updated TitleOverlay with pixel-coordinate positioning and no subtitle"
      contains: "(x / 1080) * 100"
  key_links:
    - from: "services/remotion-studio/src/compositions/TitleOverlay.tsx"
      to: "services/remotion-studio/src/pipeline-config.ts"
      via: "TitleStyleProps import"
      pattern: "TitleStyleProps"
---

<objective>
Update TitleOverlay.tsx to implement pixel-coordinate positioning (TITLE-01), config-driven border-radius (TITLE-02), and subtitle rendering removal (TITLE-03). This is the rendering side of the three schema changes introduced in Plan 01.

Purpose: Without this update, the studio preview still renders using the deprecated topOffset/centering/subtitle logic, and the renderer (after sync in Plan 04) would produce videos with old layout behavior.
Output: TitleOverlay.tsx in remotion-studio with new positioning CSS, no subtitle JSX, updated DEFAULT_TITLE_STYLE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

IMPORTANT (AGENTS.md requirement): Invoke `impeccable` skill + `frontend-design` plugin at start of session if not already active from Plan 01.
</execution_context>

<context>
@.planning/phases/20-title-block-precision/20-CONTEXT.md
@.planning/phases/20-title-block-precision/20-RESEARCH.md
@.planning/phases/20-title-block-precision/20-PATTERNS.md
@.planning/phases/20-title-block-precision/20-UI-SPEC.md
@.planning/phases/20-title-block-precision/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite TitleOverlay.tsx — remove subtitle, pixel positioning, config borderRadius</name>
  <files>services/remotion-studio/src/compositions/TitleOverlay.tsx</files>
  <read_first>
    - services/remotion-studio/src/compositions/TitleOverlay.tsx (full file — understand DEFAULT_TITLE_STYLE, all variable declarations, fontsToLoad, positioning CSS, subtitle JSX block, TitleOverlayProps interface)
    - .planning/phases/20-title-block-precision/20-PATTERNS.md §"TitleOverlay.tsx" (complete before/after for every changed site)
    - .planning/phases/20-title-block-precision/20-RESEARCH.md §"Code Examples — Before/After: TitleOverlay.tsx positioning" and §"Pattern 2: Entrance Animation with translateY"
    - services/remotion-studio/src/pipeline-config.ts (confirm updated TitleStyleProps shape from Plan 01)
  </read_first>
  <action>
    Apply all changes to `services/remotion-studio/src/compositions/TitleOverlay.tsx` in this order (follow atomicity from 20-PATTERNS.md §"Execution Order"):

    1. `TitleOverlayProps` interface: remove `subtitle?: string` field.

    2. Component destructuring: remove `subtitle` from the destructured props parameter.

    3. `DEFAULT_TITLE_STYLE` (typed as `Required<TitleStyleProps>`): remove `subtitleFontSize: 42`, `subtitleColor: "#FFFFFF"`, `subtitleFontFamily: "PlusJakartaSans"`, `topOffset: 50`. Add `x: 200`, `y: 960`, `borderRadius: 12`.

    4. Variable declarations in component body: remove `const subtitleFontSize = ...`, `const subtitleColor = ...`, `const subtitleFontFamily = ...`, `const topOffset = ...`, `const subtitleFontCSS = ...`. Add after `const titleFontCSS = ...`: `const x = style?.x ?? DEFAULT_TITLE_STYLE.x;`, `const y = style?.y ?? DEFAULT_TITLE_STYLE.y;`, `const borderRadius = style?.borderRadius ?? DEFAULT_TITLE_STYLE.borderRadius;`.

    5. `fontsToLoad` array: change `[titleFontFamily, subtitleFontFamily].filter(...)` to `[titleFontFamily].filter(...)`.

    6. `useEffect` dependency array: change `[titleFontFamily, subtitleFontFamily]` to `[titleFontFamily]`.

    7. Positioning CSS on the container div: replace `top: \`${topOffset}%\`` with `top: \`${(y / 1920) * 100}%\``; replace `left: "50%"` with `left: \`${(x / 1080) * 100}%\``; replace `transform: \`translate(-50%, -50%) translateY(${translateY}px)\`` with `transform: \`translateY(${translateY}px)\`` (centering part removed, animation-only offset kept); replace `borderRadius: "12px"` with `borderRadius: \`${borderRadius}px\``; replace `gap: subtitle ? "12px" : "0"` with `gap: "0"`.

    8. Subtitle JSX block: remove the entire `{subtitle && (<span ...>{subtitle}</span>)}` block.

    NOTE on borderRadius: use the local variable `borderRadius` (declared in step 4), not `borderRadius ?? 12` inline — the local variable already handles the default.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-studio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compilation exits 0 (no errors in TitleOverlay.tsx or its imports). File contains `(x / 1080) * 100` and `(y / 1920) * 100`. File does NOT contain `topOffset`, `subtitleFontFamily`, `subtitleFontSize`, `subtitleColor`, `subtitleFontCSS`, or `subtitle &&`. `DEFAULT_TITLE_STYLE` in this file contains `x: 200`, `y: 960`, `borderRadius: 12`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TitleConfig.style → TitleOverlay CSS | Config-driven CSS values (x, y, borderRadius) originate from pipeline-config.json on disk; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | TitleOverlay CSS positioning | accept | Values are local config, no network surface; CSS values are strings derived from numbers; no injection vector |
| T-20-SC | Tampering | npm installs | accept | No new packages in this task |
</threat_model>

<verification>
After task completes:
- `grep -n "topOffset\|subtitleFontFamily\|subtitleFontCSS\|subtitleColor\|subtitleFontSize" services/remotion-studio/src/compositions/TitleOverlay.tsx` — should return zero matches
- `grep -n "(x / 1080)" services/remotion-studio/src/compositions/TitleOverlay.tsx` — should return at least 1 match
- `grep -n "(y / 1920)" services/remotion-studio/src/compositions/TitleOverlay.tsx` — should return at least 1 match
- `grep -n "borderRadius" services/remotion-studio/src/compositions/TitleOverlay.tsx` — should show the config-driven value (not hardcoded "12px")
- `grep -n "translate(-50%,-50%)\|translate(-50%, -50%)" services/remotion-studio/src/compositions/TitleOverlay.tsx` — should return zero matches
- `cd services/remotion-studio && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- TitleOverlay renders at pixel-coordinate position (D-03, D-04): left=(x/1080)*100%, top=(y/1920)*100%
- No centering transform — anchor is top-left corner (D-04)
- borderRadius is `${borderRadius}px` from config (D-09), default 12
- No subtitle rendering block
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/20-title-block-precision/20-02-SUMMARY.md` when done
</output>
