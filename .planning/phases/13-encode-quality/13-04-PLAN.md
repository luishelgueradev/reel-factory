---
phase: 13-encode-quality
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - .planning/phases/13-encode-quality/uat/README.md
  - .planning/phases/13-encode-quality/uat/baseline.mp4
  - .planning/phases/13-encode-quality/uat/phase-13.mp4
  - .planning/phases/13-encode-quality/13-UAT.md
  - scripts/visual-ab-phase-13.sh
autonomous: false
requirements:
  - ENC-02
  - ENC-03
  - ENC-04
  - ENC-05

must_haves:
  truths:
    - "Two MP4 files exist at .planning/phases/13-encode-quality/uat/baseline.mp4 (v1.0 encode flags: CRF 20, no Lanczos, no unsharp, no BT.709 tags) and .planning/phases/13-encode-quality/uat/phase-13.mp4 (Phase 13 encode flags: CRF 18, Lanczos, unsharp 5:5:0.5:5:5:0.3, BT.709 metadata) (D-11)"
    - "Both MP4s were produced from the same input clip via the same pipeline order (silence-cutter → ffmpeg-finalizer); only the finalizer encode config differs (D-11)"
    - "scripts/visual-ab-phase-13.sh is the repeatable mechanism that produces both MP4s; it is checked in — direct ffmpeg invocation with explicit v1.0 args for the baseline (no git checkout dance) (D-12)"
    - "phase-13.mp4's measured bitrate lands in the production 5000-8000 kbps band — asserted programmatically via ffprobe+awk (hard gate), not just inspected by eye"
    - "A human reviewer opens both videos side-by-side and confirms: (a) phase-13.mp4 looks sharper, (b) no halos around facial features or subtitle-area text, (c) colors match the source (no green tint / wash-out on the baseline that gets fixed in phase-13)"
    - "13-UAT.md records the test entries and the human's pass/fail verdict — the perceptual side of the D-11 verification protocol (ffprobe + visual A/B together)"
  artifacts:
    - path: ".planning/phases/13-encode-quality/uat/baseline.mp4"
      provides: "v1.0 finalizer output (pre-Phase-13 encode flags) on the chosen input fixture"
    - path: ".planning/phases/13-encode-quality/uat/phase-13.mp4"
      provides: "Phase 13 finalizer output (post-Phase-13 encode flags) on the same input fixture"
    - path: ".planning/phases/13-encode-quality/uat/README.md"
      provides: "How to view the A/B, what to look for, ffprobe deltas between the two files"
    - path: ".planning/phases/13-encode-quality/13-UAT.md"
      provides: "Phase 13 UAT log with frontmatter, numbered tests, summary, gaps"
    - path: "scripts/visual-ab-phase-13.sh"
      provides: "Repeatable mechanism for regenerating both MP4s from one source clip"
  key_links:
    - from: "scripts/visual-ab-phase-13.sh"
      to: ".planning/phases/13-encode-quality/uat/{baseline,phase-13}.mp4"
      via: "two finalizer runs against the same source — one with overridden v1.0 encode args via temporary patch, one with the current Phase 13 src/"
      pattern: "ffmpeg.*output\\.mp4"
    - from: ".planning/phases/13-encode-quality/13-UAT.md"
      to: "human reviewer"
      via: "numbered tests + pass/fail entries + summary block"
      pattern: "result: (pass|fail)"
    - from: ".planning/phases/13-encode-quality/uat/phase-13.mp4"
      to: "ROADMAP success criterion #3 (5000-8000 kbps band)"
      via: "ffprobe + awk programmatic assertion in Task 3 acceptance criteria"
      pattern: "ffprobe.*format=bit_rate.*awk"
---

<objective>
Produce the visual A/B render that gates the perceptual ENC-04 success criterion ("sin halos perceptibles") and provides side-by-side evidence for ENC-02, ENC-03, ENC-05. Generate two MP4s from the same input clip — one with v1.0 encode flags (baseline), one with Phase 13 encode flags — store them under `.planning/phases/13-encode-quality/uat/`, document how to view them, programmatically assert the production 5000-8000 kbps bitrate band on phase-13.mp4 (hard gate), and run the human-verify checkpoint that signs off the perceptual claim.

Purpose: ffprobe and pytest can prove most mechanical invariants (CRF=18, BT.709 tags, duration parity, stream-copy concat), but ENC-04's "no halos" is a perceptual judgment. CONTEXT.md D-11 makes the visual A/B a hard gate before marking the phase done. ROADMAP success criterion #3 (5000-8000 kbps band) is a hard gate on phase-13.mp4 — Plan 03's synthetic-fixture band is relaxed (2000-15000); the production band lands here.

Output: Two MP4 fixtures, one README, one UAT log, one checked-in script for repeatability, and a programmatic bitrate-band assertion on phase-13.mp4. No source-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-encode-quality/13-CONTEXT.md
@.planning/phases/13-encode-quality/13-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/12-subtitle-preview-lab/12-UAT.md
@.planning/phases/04-9-16-vertical-output/04-UAT.md
@services/ffmpeg-finalizer/src/crop.py
@services/ffmpeg-finalizer/src/config.py
@scripts/test-ffmpeg-finalizer.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/visual-ab-phase-13.sh — repeatable A/B render driver</name>
  <files>scripts/visual-ab-phase-13.sh</files>
  <read_first>
    - scripts/test-ffmpeg-finalizer.sh (lines 1-80 — bash structure, set -euo pipefail, lavfi synthesis pattern, docker compose run pattern)
    - services/ffmpeg-finalizer/src/crop.py (lines 107-202 apply_finalizer — to understand the production cmd shape both branches produce)
    - services/ffmpeg-finalizer/src/config.py (the constants the script will reference / override)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-12: the A/B baseline is generated by temporarily reverting Phase 13 changes — planner picks the cleanest mechanism. This plan picks: direct ffmpeg invocation with explicit v1.0 args, so the baseline is reproducible without git checkout dance.)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"FFmpeg subprocess invocation" — argv shape; §"Phase artifact layout" — UAT directory convention)
  </read_first>
  <behavior>
    - `scripts/visual-ab-phase-13.sh` is executable (`chmod +x`).
    - When run, the script accepts one positional argument: path to an input MP4 (a real talking-head clip from the user's local fixtures, NOT a lavfi synthetic — see Task 3 checkpoint).
    - If the input argument is absent or the file does not exist, the script prints a usage message and exits 1.
    - If `ffmpeg` is not on PATH, the script prints an error and exits 2.
    - The script invokes ffmpeg TWICE on the same input:
      - Baseline: v1.0 args — `-c:v libx264 -crf 20 -preset medium -profile:v high -pix_fmt yuv420p -r 30 -movflags +faststart -map_metadata -1 -map 0:v:0 -vf "scale=1080:1920:force_original_aspect_ratio=increase,crop=1080:1920,setsar=1"` (Phase 4 shape — no Lanczos, no unsharp, no BT.709). Audio preserved via `-c:a aac -b:a 128k -ar 44100` (loudnorm omitted to keep the script self-contained — the A/B isolates video changes).
      - Phase 13: same args but with the Phase 13 changes — `-crf 18`, `:flags=lanczos` on scale, `,unsharp=5:5:0.5:5:5:0.3` appended, and `-colorspace bt709 -color_primaries bt709 -color_trc bt709` after `-pix_fmt yuv420p`.
    - Output files are written to `.planning/phases/13-encode-quality/uat/baseline.mp4` and `.planning/phases/13-encode-quality/uat/phase-13.mp4`.
    - After both renders, the script invokes ffprobe on each output and prints a small comparison table to stdout: file size, bitrate, color_space, color_primaries, color_transfer, duration. This lets the human see the mechanical deltas before they open the videos.
    - The script exits 0 on success.
    - Script handles aspect ratios other than 16:9 by always applying the scale+crop branch (the no-crop branch is for inputs that are already 9:16; for the A/B it's simpler to always use the canonical crop branch — script documents this trade-off in its header comment).
  </behavior>
  <action>
    Create scripts/visual-ab-phase-13.sh with the following structure:

    1. Shebang: `#!/usr/bin/env bash`.
    2. Set: `set -euo pipefail`.
    3. Header comment block (10-15 lines): purpose ("Phase 13 visual A/B render driver — ENC-02/03/04/05"), usage (`./scripts/visual-ab-phase-13.sh path/to/input.mp4`), what files it produces (`.planning/phases/13-encode-quality/uat/baseline.mp4` and `phase-13.mp4`), and the design note that the script encodes ONLY the video path through a synthetic ffmpeg invocation (no docker, no orchestrator) so the A/B is isolated to the encode-config delta documented in 13-CONTEXT.md D-05..D-09.
    4. SCRIPT_DIR / PROJECT_DIR resolution matching scripts/test-ffmpeg-finalizer.sh lines 8-10.
    5. INPUT path validation: `if [ $# -lt 1 ]; then echo "Usage: $0 path/to/input.mp4" >&2; exit 1; fi`; `INPUT="$1"`; `if [ ! -f "$INPUT" ]; then echo "Input not found: $INPUT" >&2; exit 1; fi`.
    6. ffmpeg availability check: `if ! command -v ffmpeg >/dev/null 2>&1; then echo "ffmpeg not on PATH" >&2; exit 2; fi`.
    7. UAT_DIR resolution + mkdir -p: `UAT_DIR="${PROJECT_DIR}/.planning/phases/13-encode-quality/uat"; mkdir -p "$UAT_DIR"`.
    8. Baseline encode: `ffmpeg -y -i "$INPUT" -vf "scale=1080:1920:force_original_aspect_ratio=increase,crop=1080:1920,setsar=1" -c:v libx264 -crf 20 -preset medium -profile:v high -pix_fmt yuv420p -r 30 -movflags +faststart -map_metadata -1 -map 0:v:0 -map 0:a:0? -c:a aac -b:a 128k -ar 44100 "$UAT_DIR/baseline.mp4"`. Stream into `> /tmp/visual-ab-baseline.log 2>&1` or similar so the script's own stdout isn't drowned. Echo progress: `echo "[1/2] Encoding baseline (Phase 4 encode flags)..."`.
    9. Phase 13 encode: same as baseline but with `-vf "scale=1080:1920:force_original_aspect_ratio=increase:flags=lanczos,crop=1080:1920,setsar=1,unsharp=5:5:0.5:5:5:0.3"`, `-crf 18`, and the three BT.709 args inserted after `-pix_fmt yuv420p`. Echo progress: `echo "[2/2] Encoding phase-13 output (CRF 18, Lanczos, unsharp, BT.709)..."`.
    10. Comparison table: For each output run `ffprobe -v quiet -show_entries stream=color_space,color_primaries,color_transfer -show_entries format=size,bit_rate,duration -of default=noprint_wrappers=1 "$UAT_DIR/<file>.mp4"` and print to stdout. Surround in `printf` block headers so the human sees a clear "BASELINE:" / "PHASE 13:" delineation.
    11. Final echo: `echo "Done. Open both files in mpv or VLC side-by-side: $UAT_DIR/baseline.mp4 vs $UAT_DIR/phase-13.mp4"`.
    12. `chmod +x scripts/visual-ab-phase-13.sh` after writing (use Bash, NOT a write step inside the script itself).

    The script MUST NOT shell-interpolate user input as ffmpeg argv components; the input path is the only user-controlled value and it's passed as a single quoted bash variable (`"$INPUT"`), which is the standard bash-safe pattern.

    Note on grep tolerance: the verify block's greps for `flags=lanczos` and `unsharp=5:5:0.5:5:5:0.3` use `^[1-9]$` (at-least-one), NOT `^1$` (exactly-one). The semantic gate is "the flag is present in the Phase 13 encode invocation". It is acceptable for the same string to appear additionally in a header comment, an echo line, or any documentation block — the script is correct as long as the flag reaches ffmpeg argv at least once.
  </action>
  <verify>
    <automated>test -x /home/luis/proyectos/reel-factory/scripts/visual-ab-phase-13.sh &amp;&amp; bash -n /home/luis/proyectos/reel-factory/scripts/visual-ab-phase-13.sh &amp;&amp; grep -cE 'flags=lanczos' /home/luis/proyectos/reel-factory/scripts/visual-ab-phase-13.sh | grep -q '^[1-9]$' &amp;&amp; grep -cE 'unsharp=5:5:0\.5:5:5:0\.3' /home/luis/proyectos/reel-factory/scripts/visual-ab-phase-13.sh | grep -q '^[1-9]$' &amp;&amp; grep -cE '"-colorspace",?\s*"?bt709"?|colorspace bt709' /home/luis/proyectos/reel-factory/scripts/visual-ab-phase-13.sh | grep -q '^[1-9]' &amp;&amp; grep -cE '\-crf 18' /home/luis/proyectos/reel-factory/scripts/visual-ab-phase-13.sh | grep -q '^[1-9]' &amp;&amp; grep -cE '\-crf 20' /home/luis/proyectos/reel-factory/scripts/visual-ab-phase-13.sh | grep -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test -x scripts/visual-ab-phase-13.sh` exits 0 (script is executable).
    - Source assertion: `bash -n scripts/visual-ab-phase-13.sh` exits 0 (syntactically valid bash).
    - Source assertion: `grep -cE 'flags=lanczos' scripts/visual-ab-phase-13.sh` returns at least `1` (count matches `^[1-9]$` — present at least once; comments/echoes that mention the flag in addition to the ffmpeg argv are tolerated by design).
    - Source assertion: `grep -cE 'unsharp=5:5:0\.5:5:5:0\.3' scripts/visual-ab-phase-13.sh` returns at least `1` (same tolerance — present in ffmpeg argv at least once).
    - Source assertion: `grep -E 'colorspace bt709|"-colorspace"\s*"bt709"' scripts/visual-ab-phase-13.sh` returns at least one match.
    - Source assertion: `grep -E '\-crf 18' scripts/visual-ab-phase-13.sh` returns at least one match (Phase 13 encode).
    - Source assertion: `grep -E '\-crf 20' scripts/visual-ab-phase-13.sh` returns at least one match (baseline encode).
    - Source assertion: `grep -E 'baseline\.mp4' scripts/visual-ab-phase-13.sh` returns at least one match.
    - Source assertion: `grep -E 'phase-13\.mp4' scripts/visual-ab-phase-13.sh` returns at least one match.
  </acceptance_criteria>
  <done>Repeatable, syntactically valid, executable script exists. It produces both MP4s when given a real input. The script encodes argv invariants for both baseline and Phase 13 flags exactly as documented in CONTEXT.md.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human supplies a real talking-head input clip for the A/B</name>
  <files>pipeline/uat-input/talking-head.mp4 (or any path the user provides)</files>
  <read_first>
    - .planning/phases/13-encode-quality/13-CONTEXT.md (D-11 + D-12 — the visual A/B exists because ffprobe can't see halos; the input must be representative of real talking-head content, not lavfi)
    - .planning/research/FEATURES.md ("Anti-Features" — luma > 0.8 halos most visible on faces and subtitle-area text, which is why a real face fixture is needed)
  </read_first>
  <what-needed>
    A real talking-head MP4 clip, 30-90 seconds, ideally containing:
    - A clearly visible face (so the reviewer can spot halos on skin texture).
    - Some visible text or high-contrast edges in the frame (where unsharp halos are most likely to show up).
    - Real camera capture (not lavfi/synthetic) — the unsharp filter's behavior on noise-floor and chroma channels is what's being judged.
    The clip can be one of the user's existing v1.0 outputs (a finished video before being re-processed by Phase 13) OR raw input footage.
  </what-needed>
  <how-to-supply>
    1. Place the chosen MP4 somewhere readable from the repo root. Suggested path: `pipeline/uat-input/talking-head.mp4`.
    2. Reply with the path (relative or absolute) so the next task can run the A/B script against it.
    3. If you'd prefer to skip the human-supplied fixture and use one of the existing v1.0 pipeline outputs (e.g., something under `pipeline/{job-id}/ffmpeg-finalizer/output.mp4` from a prior run), name the path of that file instead.
  </how-to-supply>
  <action>
    Pause execution and wait for the human to supply a real talking-head MP4 path. This task does not perform any automated work — Claude cannot synthesize a representative talking-head fixture (lavfi testsrc has no real face / no real camera noise floor / no real high-contrast edges; using it would invalidate the perceptual halo check in Task 5). The orchestrator MUST block here until the human replies with the resume-signal. Once the human replies, record the supplied path in the conversation so Task 3 can substitute it into `scripts/visual-ab-phase-13.sh <path>`. If the human cannot supply a clip, surface the blocker back to the orchestrator — Phase 13 cannot complete its perceptual gate without it.
  </action>
  <verify>
    <human-check>Human has replied with a readable MP4 path that exists on disk: `test -f "<supplied-path>"` exits 0. The path is recorded in the next task's invocation argv.</human-check>
  </verify>
  <done>A real talking-head MP4 path has been supplied by the human and recorded for use by Task 3's script invocation.</done>
  <resume-signal>Reply with the path of the talking-head MP4 the A/B should use, or "use existing: &lt;path&gt;" pointing at a prior pipeline output.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run scripts/visual-ab-phase-13.sh against the supplied clip + produce comparison ffprobe report + assert production bitrate band (ENC-02 / ROADMAP #3)</name>
  <files>
    .planning/phases/13-encode-quality/uat/baseline.mp4
    .planning/phases/13-encode-quality/uat/phase-13.mp4
    .planning/phases/13-encode-quality/uat/README.md
  </files>
  <read_first>
    - scripts/visual-ab-phase-13.sh (created in Task 1)
    - .planning/phases/13-encode-quality/13-CONTEXT.md (Success criteria 1-5; D-11 verification protocol — production bitrate band 5000-8000 kbps lands here, not on the synthetic fixture)
    - .planning/ROADMAP.md (Phase 13 success criterion #3 — "bitrate in 5,000–8,000 kbps band for a 60s talking-head fixture")
    - .planning/phases/12-subtitle-preview-lab/12-UAT.md (lines 1-9 — frontmatter style for the UAT log)
  </read_first>
  <action>
    1. Substitute the user-supplied input path from Task 2 into the script invocation: `./scripts/visual-ab-phase-13.sh <user-supplied-path>`.
    2. Capture the stdout (the comparison table from the script's ffprobe block) — pipe to a buffer or file.
    3. Verify both output files exist: `test -s .planning/phases/13-encode-quality/uat/baseline.mp4 && test -s .planning/phases/13-encode-quality/uat/phase-13.mp4`.
    4. Run two more focused ffprobe queries (beyond what the script prints) to populate the README's quantitative section:
       - `ffprobe -v quiet -show_entries stream=color_space,color_primaries,color_transfer -of default=noprint_wrappers=1 .planning/phases/13-encode-quality/uat/baseline.mp4` (expected: empty/unspecified or non-bt709 — the baseline lacks the tags).
       - `ffprobe -v quiet -show_entries stream=color_space,color_primaries,color_transfer -of default=noprint_wrappers=1 .planning/phases/13-encode-quality/uat/phase-13.mp4` (expected: all three equal `bt709`).
       - `ffprobe -v quiet -show_entries format=size,bit_rate,duration -of default=noprint_wrappers=1 <each file>` for size/bitrate/duration delta.
    5. PRODUCTION BITRATE BAND HARD GATE (ENC-02 / ROADMAP #3): Run the following programmatic assertion against phase-13.mp4 and confirm it exits 0:
       ```
       ffprobe -v quiet -show_entries format=bit_rate -of default=nw=1:nk=1 .planning/phases/13-encode-quality/uat/phase-13.mp4 | awk '{kbps=$1/1000; printf "phase-13 bitrate: %.0f kbps\n", kbps > "/dev/stderr"; exit !(kbps>=5000 && kbps<=8000)}'
       ```
       If exit code != 0, surface this as a BLOCKER back to the orchestrator before proceeding to Task 4. This is the hard gate for ROADMAP success criterion #3 — Plan 03's synthetic fixture uses a relaxed band (2000-15000), but the production talking-head clip MUST land in 5000-8000.
    6. Write `.planning/phases/13-encode-quality/uat/README.md` with this structure:
       - Title `# Phase 13 — Visual A/B Reference`.
       - One-paragraph purpose: "Side-by-side reference for the perceptual ENC-04 (sin halos) success criterion and visual cross-check for ENC-02 (bitrate band), ENC-03 (color tagging), ENC-05 (duration parity)."
       - `## Files` section listing baseline.mp4 and phase-13.mp4 with the input clip path used as the source.
       - `## How to view` section with two options:
         a. `mpv` side-by-side: `mpv --geometry=50%x100%+0+0 baseline.mp4 & mpv --geometry=50%x100%+50%+0 phase-13.mp4`.
         b. VLC: open both files in two windows manually.
       - `## What to look for` section enumerating:
         - Sharpness of facial detail (skin texture, eye highlights, hair strands) — phase-13 should look sharper but NOT crunchy or haloed.
         - Halo check: look at high-contrast edges (eyeglasses frames, jaw lines, text in the frame if any) — there must be no visible bright rim around dark edges or dark rim around bright edges in phase-13.
         - Color: phase-13's skin tones and background colors should look correct on the monitor; baseline may look slightly green or washed because of the missing BT.709 metadata (browser/player may default to BT.601 interpretation).
         - Duration / sync: when played simultaneously, both videos should remain frame-synchronized — ENC-05 is broken if drift is visible.
       - `## Quantitative deltas` section with a markdown table of the ffprobe output: size, bitrate (kbps), duration (s), color_space, color_primaries, color_transfer — one row per file. INCLUDE a "Production band (5000-8000 kbps)" column with PASS/FAIL for each file based on the awk gate from step 5.
       - `## Mechanism` section noting that both files were produced by `scripts/visual-ab-phase-13.sh <input-path>` and that the encode-config delta is the only variable (everything else — input clip, filter chain shape, audio path — is held constant).
    7. Stage all three files for commit.
  </action>
  <verify>
    <automated>test -s /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/uat/baseline.mp4 &amp;&amp; test -s /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/uat/phase-13.mp4 &amp;&amp; test -s /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/uat/README.md &amp;&amp; ffprobe -v quiet -show_entries stream=color_space -of default=nw=1:nk=1 /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/uat/phase-13.mp4 | grep -qx 'bt709' &amp;&amp; ffprobe -v quiet -show_entries stream=color_primaries -of default=nw=1:nk=1 /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/uat/phase-13.mp4 | grep -qx 'bt709' &amp;&amp; ffprobe -v quiet -show_entries stream=color_transfer -of default=nw=1:nk=1 /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/uat/phase-13.mp4 | grep -qx 'bt709' &amp;&amp; ffprobe -v quiet -show_entries format=bit_rate -of default=nw=1:nk=1 /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/uat/phase-13.mp4 | awk '{kbps=$1/1000; exit !(kbps>=5000 &amp;&amp; kbps<=8000)}'</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `test -s .planning/phases/13-encode-quality/uat/baseline.mp4` exits 0 (file exists and is non-empty).
    - File assertion: `test -s .planning/phases/13-encode-quality/uat/phase-13.mp4` exits 0.
    - File assertion: `test -s .planning/phases/13-encode-quality/uat/README.md` exits 0.
    - CLI assertion: `ffprobe -v quiet -show_entries stream=color_space -of default=nw=1:nk=1 .planning/phases/13-encode-quality/uat/phase-13.mp4` outputs exactly `bt709` (one line).
    - CLI assertion: `ffprobe -v quiet -show_entries stream=color_primaries -of default=nw=1:nk=1 .planning/phases/13-encode-quality/uat/phase-13.mp4` outputs exactly `bt709`.
    - CLI assertion: `ffprobe -v quiet -show_entries stream=color_transfer -of default=nw=1:nk=1 .planning/phases/13-encode-quality/uat/phase-13.mp4` outputs exactly `bt709`.
    - CLI assertion: baseline.mp4's `color_space` is NOT `bt709` (or is empty/unspecified) — confirming the A/B isolates the BT.709 tagging delta.
    - **CLI assertion (HARD GATE — ROADMAP success criterion #3):** `ffprobe -v quiet -show_entries format=bit_rate -of default=nw=1:nk=1 .planning/phases/13-encode-quality/uat/phase-13.mp4 | awk '{kbps=$1/1000; exit !(kbps>=5000 && kbps<=8000)}'` exits 0 (phase-13.mp4 bitrate falls in [5000, 8000] kbps inclusive). If this assertion fails, the phase is NOT done — surface as a blocker before Task 4.
    - Source assertion: `grep -F 'baseline.mp4' .planning/phases/13-encode-quality/uat/README.md` returns at least one match.
    - Source assertion: `grep -F 'phase-13.mp4' .planning/phases/13-encode-quality/uat/README.md` returns at least one match.
    - Source assertion: `grep -F 'halo' .planning/phases/13-encode-quality/uat/README.md` returns at least one match (the perceptual criterion the reviewer is asked to check).
    - Source assertion: `grep -E '5000|5,000' .planning/phases/13-encode-quality/uat/README.md` returns at least one match (the production band is documented in the Quantitative deltas section).
  </acceptance_criteria>
  <done>Both MP4s exist in `.planning/phases/13-encode-quality/uat/`; phase-13.mp4 carries BT.709 tags AND lands in the 5000-8000 kbps production band (hard gate passed); baseline.mp4 lacks the tags (the A/B isolates the delta); README.md explains how to view, what to look for, and records the PASS/FAIL of the production bitrate band per file.</done>
</task>

<task type="auto">
  <name>Task 4: Create 13-UAT.md with frontmatter + test entries (mechanical + perceptual)</name>
  <files>.planning/phases/13-encode-quality/13-UAT.md</files>
  <read_first>
    - .planning/phases/12-subtitle-preview-lab/12-UAT.md (lines 1-9 frontmatter; lines 15-17 test-entry pattern; lines 99-110 summary block)
    - .planning/phases/04-9-16-vertical-output/04-UAT.md (cross-reference for Phase 4 UAT shape — same convention)
    - .planning/phases/13-encode-quality/13-PATTERNS.md (§"Phase artifact layout")
  </read_first>
  <behavior>
    - 13-UAT.md exists at `.planning/phases/13-encode-quality/13-UAT.md`.
    - YAML frontmatter matches the Phase 12 UAT shape: `status: in_progress` (will flip to `complete` after Task 5's checkpoint passes), `phase: 13-encode-quality`, `source: 13-01-SUMMARY.md, 13-02-SUMMARY.md, 13-03-SUMMARY.md`, `started: <ISO timestamp>`.
    - The file enumerates at least 6 test entries — one per ENC requirement plus one per visual claim:
      1. ENC-01 — silence-cutter concat output uses stream copy (no fresh encoder tag) — pytest assertion (test_validate_concat_mode_stream_copy).
      2. ENC-02 — finalizer encodes at CRF 18 (manifest + ffprobe) and lands in 5000-8000 kbps band on real talking-head clip — pytest + Task 3 awk gate.
      3. ENC-03 — finalizer output carries BT.709 metadata tags — pytest + ffprobe (test_finalizer_color_tags_are_bt709) AND visual A/B color claim.
      4. ENC-04 (mechanical) — Lanczos + unsharp present in manifest — pytest assertion (test_finalizer_unsharp_and_lanczos_in_manifest).
      5. ENC-04 (perceptual) — NO visible halos in phase-13.mp4 vs baseline.mp4 — human visual verdict.
      6. ENC-05 — duration parity between source and finalizer output within ±33ms — pytest (test_duration_parity_after_finalizer) + concat edge cases (TestConcatEdgeCases).
    - Each entry follows the `### N. <Title>` + `expected: ...` + `result: pending` pattern (matching 12-UAT.md). Result stays `pending` until Task 5's human-verify checkpoint resolves entry #5; the mechanical entries 1, 2, 3 (ffprobe half), 4, 6 are marked `result: pass` after Plan 03's tests pass.
    - Required `## Summary` block at the bottom: `total: 6`, `passed: <count after Plan 03>`, `issues: 0`, `pending: 1` (the perceptual one), `skipped: 0`, `blocked: 0`. Counts update to fully passed after Task 5.
    - Required `## Gaps` section: `[none]` if all pass; otherwise enumerate the perceptual entry that's pending.
  </behavior>
  <action>
    Create `.planning/phases/13-encode-quality/13-UAT.md` with the following content shape:

    1. YAML frontmatter (between `---` markers):
       - `status: in_progress`
       - `phase: 13-encode-quality`
       - `source: 13-01-SUMMARY.md, 13-02-SUMMARY.md, 13-03-SUMMARY.md`
       - `started: <current ISO timestamp; use date -u +%Y-%m-%dT%H:%M:%SZ when running>`

    2. `## Current Test` section: `pending: visual A/B human verification (entry 5)`.

    3. Six numbered test entries (`### 1. ...` through `### 6. ...`):
       - **1. ENC-01: silence-cutter concat output is stream-copied**
         expected: `pytest services/silence-cutter/tests/test_silence_cutter.py::TestConcatMode::test_validate_concat_mode_stream_copy -x -q` exits 0; validate_concat_mode returns [] on a real stream-copied concat output. Additionally, TestConcatEdgeCases proves -c copy preserves duration on variable-keyframe and audio-shorter-than-video inputs (D-14).
         result: pass (mark pass if Plan 03's pytest run was green; otherwise mark pending and unblock by re-running Plan 03's tests).
       - **2. ENC-02: finalizer encodes at CRF 18 with bitrate in production band**
         expected: `pytest services/ffmpeg-finalizer/tests/test_encode_quality.py::TestEncodeQuality::test_finalizer_crf_is_18 -x -q` and `::test_finalizer_bitrate_in_band -x -q` both exit 0; Plan 04 Task 3's awk gate (`ffprobe ... | awk '{kbps=$1/1000; exit !(kbps>=5000 && kbps<=8000)}'`) exits 0 against `.planning/phases/13-encode-quality/uat/phase-13.mp4`.
         result: pass (predicated on Task 3 awk gate passing; if the gate fails the entry stays pending and the phase blocks).
       - **3. ENC-03: BT.709 metadata tags on finalizer output**
         expected: phase-13.mp4 reports `color_space=bt709`, `color_primaries=bt709`, `color_transfer=bt709` via ffprobe (asserted both by pytest and by the Task 3 CLI checks); baseline.mp4 lacks these tags.
         result: pass.
       - **4. ENC-04 (mechanical): Lanczos + unsharp manifest fields**
         expected: `pytest services/ffmpeg-finalizer/tests/test_encode_quality.py::TestEncodeQuality::test_finalizer_unsharp_and_lanczos_in_manifest -x -q` exits 0.
         result: pass.
       - **5. ENC-04 (perceptual): NO visible halos in phase-13.mp4 vs baseline.mp4**
         expected: A human reviewer opens both files in mpv/VLC side-by-side per `.planning/phases/13-encode-quality/uat/README.md` and confirms: (a) phase-13 looks sharper, (b) no halos on facial features, edges, or text-bearing regions, (c) colors look correct (no green/wash).
         result: pending (resolved by Task 5).
       - **6. ENC-05: duration parity ±33ms**
         expected: `pytest services/ffmpeg-finalizer/tests/test_encode_quality.py::TestEncodeQuality::test_duration_parity_after_finalizer -x -q` exits 0; TestConcatEdgeCases (variable-keyframe + audio-shorter-than-video) exit 0; visual A/B has no frame drift during simultaneous playback.
         result: pass.

    4. `## Summary` block at the bottom:
       - `total: 6`
       - `passed: 5` (mechanical entries 1, 2, 3, 4, 6)
       - `issues: 0`
       - `pending: 1` (entry 5 — perceptual)
       - `skipped: 0`
       - `blocked: 0`

    5. `## Gaps` section: list entry 5 as the gating perceptual test until Task 5 resolves it.
  </action>
  <verify>
    <automated>test -s /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/13-UAT.md &amp;&amp; grep -cE '^status:\s*in_progress$' /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/13-UAT.md | grep -q '^1$' &amp;&amp; grep -cE '^### [1-6]\. ENC' /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/13-UAT.md | grep -q '^6$' &amp;&amp; grep -cE '^total: 6$' /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/13-UAT.md | grep -q '^1$' &amp;&amp; grep -cF 'halo' /home/luis/proyectos/reel-factory/.planning/phases/13-encode-quality/13-UAT.md | grep -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `test -s .planning/phases/13-encode-quality/13-UAT.md` exits 0.
    - Source assertion: `grep -E '^status:\s*in_progress$' .planning/phases/13-encode-quality/13-UAT.md` returns exactly one match.
    - Source assertion: `grep -E '^### [1-6]\. ENC' .planning/phases/13-encode-quality/13-UAT.md` returns exactly 6 matches (one per ENC test entry).
    - Source assertion: `grep -F 'pending' .planning/phases/13-encode-quality/13-UAT.md` returns at least one match (entry 5 result).
    - Source assertion: `grep -E '^total: 6$' .planning/phases/13-encode-quality/13-UAT.md` returns exactly one match.
    - Source assertion: `grep -F 'halo' .planning/phases/13-encode-quality/13-UAT.md` returns at least one match (perceptual criterion documented).
    - Source assertion: `grep -F 'baseline.mp4' .planning/phases/13-encode-quality/13-UAT.md` returns at least one match.
    - Source assertion: `grep -F 'phase-13.mp4' .planning/phases/13-encode-quality/13-UAT.md` returns at least one match.
  </acceptance_criteria>
  <done>13-UAT.md exists with frontmatter, 6 enumerated test entries (5 mechanical pass + 1 perceptual pending), summary block, and gaps section — ready for the human-verify checkpoint in Task 5.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human visual A/B verification — ENC-04 "sin halos" gate</name>
  <files>.planning/phases/13-encode-quality/13-UAT.md</files>
  <read_first>
    - .planning/phases/13-encode-quality/uat/README.md (created by Task 3 — viewing instructions and what to look for)
    - .planning/phases/13-encode-quality/uat/baseline.mp4 (the pre-Phase-13 encode for comparison)
    - .planning/phases/13-encode-quality/uat/phase-13.mp4 (the Phase 13 encode under review)
    - .planning/phases/13-encode-quality/13-UAT.md (created by Task 4 — entry 5 to be resolved)
  </read_first>
  <what-built>
    Two MP4 files have been generated from the same input clip via `scripts/visual-ab-phase-13.sh`:
    - `.planning/phases/13-encode-quality/uat/baseline.mp4` — v1.0 encode flags (CRF 20, no Lanczos, no unsharp, no BT.709 tags).
    - `.planning/phases/13-encode-quality/uat/phase-13.mp4` — Phase 13 encode flags (CRF 18, Lanczos, unsharp 5:5:0.5:5:5:0.3, BT.709 metadata).

    The `.planning/phases/13-encode-quality/uat/README.md` documents how to view the A/B and exactly what to look for. The ffprobe deltas (file size, bitrate, color tags) are pre-computed in the README's "Quantitative deltas" section. Task 3 already programmatically asserted phase-13.mp4 falls in the 5000-8000 kbps production band — that gate has passed if Task 5 is being executed.

    All mechanical assertions (CRF=18, BT.709 tags, duration parity, manifest fields, stream-copy concat, production bitrate band) have already passed in Plan 03's pytest suite and Plan 04 Task 3's awk gate. This checkpoint is solely the perceptual gate for ENC-04 — "sin halos perceptibles" — which ffprobe cannot prove.
  </what-built>
  <how-to-verify>
    1. Open both files side-by-side. Recommended:
       - `mpv .planning/phases/13-encode-quality/uat/baseline.mp4 &` then `mpv .planning/phases/13-encode-quality/uat/phase-13.mp4 &` and arrange the two windows.
       - Or open both in VLC in separate windows.
    2. Play both videos. Pause at a moment with a clearly visible face.
    3. Check (a) — sharpness gain on phase-13:
       - Phase-13 should look perceptibly sharper than baseline on skin texture, hair strands, and eye highlights.
       - If phase-13 looks IDENTICAL to baseline → unsharp didn't apply correctly. Reject.
       - If phase-13 looks oversharpened / crunchy → strength is too high (shouldn't happen at 0.5 luma per research, but report).
    4. Check (b) — no halos on phase-13:
       - Look at high-contrast edges (eyeglass frames, jaw lines against background, any text in the frame).
       - In phase-13: scan for bright rims around dark edges OR dark rims around bright edges. There should be NONE perceptible at normal viewing distance.
       - If halos are visible at any edge → unsharp strength regression. Reject and ask for strength tuning (out of Phase 13 scope — would be a gap-closure plan).
    5. Check (c) — color correctness:
       - Phase-13's skin tones and background colors should look natural / correct on your monitor.
       - Baseline may look slightly green-tinted or washed-out compared to phase-13 (because baseline lacks BT.709 metadata, so the player may default to BT.601 interpretation, which is the bug ENC-03 fixes).
       - If baseline and phase-13 look identical in color → either the player ignores color tags (try a different player) or BT.709 tags aren't actually present (re-run the ffprobe assertion). Report.
    6. Check (d) — duration / sync:
       - When played simultaneously, both videos should remain frame-aligned. If you see one drift behind the other → ENC-05 is broken (shouldn't happen given stream-copy and metadata-only tag changes, but worth a sanity check).
    7. After all four checks: edit `.planning/phases/13-encode-quality/13-UAT.md`:
       - Set entry 5's `result:` from `pending` to `pass` (if all four checks passed) or `fail` with a short note (if any check failed).
       - If pass: update frontmatter `status: complete`, update Summary `passed: 6` / `pending: 0`, set Gaps to `[none]`.
       - If fail: leave `status: in_progress`, list the failing check in `## Gaps`, and surface the issue back to the orchestrator for a gap-closure plan.
  </how-to-verify>
  <action>
    Pause execution and wait for the human reviewer to complete the four-check visual A/B per the how-to-verify steps above. Claude cannot perform this verification: ffprobe / pytest / shell tools cannot perceive halos, sharpness, or color drift — those are perceptual judgments that require a human eye on a real monitor. While paused, Claude must NOT auto-fill 13-UAT.md entry 5; that file is updated either by the human (recommended) or by Claude after the human responds with `approved` (in which case Claude flips entry 5 to `result: pass`, sets `status: complete`, updates Summary to `passed: 6 / pending: 0`, and sets Gaps to `[none]`). If the human responds with any non-approved signal, Claude leaves 13-UAT.md as `in_progress`, records the failure mode under `## Gaps`, and surfaces the issue back to the orchestrator so the appropriate gap-closure plan can be created.
  </action>
  <verify>
    <human-check>Human has replied with one of the documented resume signals. If `approved`, then: `grep -E '^status:\s*complete$' .planning/phases/13-encode-quality/13-UAT.md` returns one match, AND `grep -E '^passed: 6$' .planning/phases/13-encode-quality/13-UAT.md` returns one match, AND entry 5's `result:` line reads `pass`. If non-approved, the failing check is recorded in `## Gaps` and Phase 13 stays `in_progress`.</human-check>
  </verify>
  <done>Human's perceptual verdict on ENC-04 (and the cross-checks on ENC-03 color and ENC-05 sync) is recorded in 13-UAT.md entry 5. Either: (a) approved → 13-UAT.md is `status: complete`, all 6 entries pass, and Phase 13 is ready to mark done in ROADMAP.md, OR (b) rejected → the failing check is enumerated in `## Gaps` and a gap-closure plan path is identified for orchestrator follow-up.</done>
  <resume-signal>
    Reply with one of:
    - `approved` — all four checks passed; 13-UAT.md updated to pass/complete; phase moves to summary commit.
    - `halos at <edge description>` — perceptual fail; need a gap-closure plan to reduce unsharp strength or rethink placement.
    - `no sharpness gain` — unsharp didn't apply; need to debug the filter chain (likely a wiring regression).
    - `color identical` — BT.709 tags ineffective or player ignores them; need to re-verify via ffprobe + try a different player before concluding regression.
    - `duration drift` — ENC-05 regression; need to investigate stream-copy concat output for variable-keyframe edge cases.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied input MP4 → visual-ab script | Script accepts one positional path argument; passes it as a single quoted bash variable to ffmpeg (no shell interpolation of contents). Input is read-only. |
| ffmpeg subprocess → UAT directory | Output paths are constructed by the script from `$PROJECT_DIR`; user cannot influence the output path. |
| baseline.mp4 / phase-13.mp4 → human reviewer | Local file artifacts; no network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.04-01 | Tampering | bash script argv handling | mitigate | The user-supplied input path is the only argv variable and is always quoted (`"$INPUT"`); existence is validated with `[ -f "$INPUT" ]` before invocation. No `eval`, no `sh -c "..."`, no string concatenation into ffmpeg argv. |
| T-13.04-02 | Information Disclosure | UAT MP4s checked into the planning directory | accept | The planning directory is part of the repo; the user explicitly placed the input clip there for human review. The same convention applies to Phase 12's preview lab fixtures. Operator awareness suffices. |
| T-13.04-03 | Denial of Service | encoding a very large input could fill disk | accept | The script runs at the user's request against a user-supplied clip; if disk fills, ffmpeg fails and exits. No automated/recurring trigger. Phase 13 doesn't introduce a server-side workflow that could be abused. |
| T-13.04-04 | Repudiation | the perceptual judgment is unverifiable later | mitigate | Both MP4s plus the README plus the 13-UAT.md entry are checked into git, so the verdict is reproducible: anyone can re-run `scripts/visual-ab-phase-13.sh <same-input>` and recompute the comparison; the perceptual verdict is recorded in 13-UAT.md test entry 5. The production bitrate band is a hard gate (Task 3 awk assertion) — independent of any human's judgment. |
| T-13.04-SC | Supply Chain | npm/pip/cargo installs | not_applicable | Plan 04 adds no dependencies. ffmpeg/ffprobe are FFmpeg 7.1.1 already pinned in Phase 1 base images. |

</threat_model>

<verification>
1. `scripts/visual-ab-phase-13.sh` is executable, syntactically valid bash, and contains both the baseline (`-crf 20`, no Lanczos) and Phase 13 (`-crf 18`, `flags=lanczos`, `unsharp=5:5:0.5:5:5:0.3`, `-colorspace bt709`) argv shapes.
2. After Task 3, both MP4s exist and `ffprobe` confirms phase-13.mp4 has all three BT.709 tags equal to `bt709` and baseline.mp4 does not.
3. After Task 3, the production bitrate band hard gate (`ffprobe ... | awk '{kbps=$1/1000; exit !(kbps>=5000 && kbps<=8000)}'`) exits 0 against phase-13.mp4 — ROADMAP success criterion #3 is enforced.
4. 13-UAT.md exists with frontmatter, 6 enumerated tests, and a Summary block.
5. README.md in `uat/` documents the viewing instructions and references both files plus the production-band PASS/FAIL.
6. The human-verify checkpoint (Task 5) resolves with one of the listed resume signals; if "approved", 13-UAT.md flips to `status: complete`.
</verification>

<success_criteria>
- ENC-04 perceptual claim ("sin halos perceptibles") has a human verdict on record in 13-UAT.md entry 5.
- ENC-03 BT.709 claim has a visible color-correctness gain in phase-13.mp4 vs baseline.mp4 (or a documented player-limitation note if the local player ignores tags).
- ENC-02 production bitrate band (5000-8000 kbps) is a HARD GATE — Task 3's awk assertion enforces ROADMAP success criterion #3 programmatically.
- ENC-02 / ENC-05 mechanical claims have a second confirmation via the real-clip A/B (ffprobe table in README's "Quantitative deltas").
- `scripts/visual-ab-phase-13.sh` is checked in, so the A/B is reproducible by any future maintainer.
- 13-UAT.md follows the Phase 12 convention (frontmatter + numbered entries + Summary + Gaps).
</success_criteria>

<output>
Create `.planning/phases/13-encode-quality/13-04-SUMMARY.md` when done. Include:
- Path of the user-supplied input clip used for the A/B.
- Full ffprobe comparison table (size, bitrate, duration, color tags) between baseline.mp4 and phase-13.mp4.
- The exit code of the Task 3 production-band awk gate against phase-13.mp4 (MUST be 0 for the phase to be done).
- The human reviewer's verdict on entry 5 (approved / specific issue).
- Confirmation that 13-UAT.md frontmatter is `status: complete` and Summary shows `passed: 6` / `pending: 0` (or the gap-closure path forward if not).
- A note that all 5 ENC requirements have been verified, and the phase is ready to mark complete in ROADMAP.md.
</output>
</output>
