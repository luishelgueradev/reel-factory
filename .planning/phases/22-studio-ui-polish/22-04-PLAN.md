---
phase: 22-studio-ui-polish
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - services/remotion-studio/src/SubtitledVideo.tsx
  - services/remotion-renderer/src/Root.tsx
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "Back-layer overlays paint BELOW subtitles and titles; front-layer overlays paint ABOVE titles"
    - "An overlay with no layer field is treated as 'back' (default)"
    - "Within each layer band, array order = paint order (later array entry on top)"
    - "Studio SubtitledVideo and renderer Root.tsx inline SubtitledVideo produce the identical 5-layer paint order"
    - "Back-layer preview dim (opacity 0.85 / saturate 0.8) applies in the studio Player ONLY, never in the renderer"
  artifacts:
    - path: "services/remotion-studio/src/SubtitledVideo.tsx"
      provides: "back/front overlay band split around subtitles+titles"
      contains: "layer"
    - path: "services/remotion-renderer/src/Root.tsx"
      provides: "Mirrored band split in inline SubtitledVideo"
      contains: "layer"
  key_links:
    - from: "services/remotion-studio/src/SubtitledVideo.tsx"
      to: "services/remotion-renderer/src/Root.tsx"
      via: "identical paint order (renderer-sync mirror)"
      pattern: "filter\\([^)]*layer"
---

<objective>
Implement the D-03/D-04 overlay layering model in BOTH the studio
`SubtitledVideo.tsx` and the renderer's inline `SubtitledVideo` (in `Root.tsx`):
split the overlays array into a back band (painted before subtitles/titles,
the new default) and a front band (painted after titles), preserving array
order as paint order within each band. Add the preview-only legibility dim for
back-layer overlays in the studio Player path only. Re-run renderer vitest to
satisfy the renderer-sync clobber hazard.

Purpose: Make overlay decorators render behind text by default with an opt-in
front promotion, consistently across preview and final render.
Output: Correct 5-layer paint order in both services; renderer tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-PATTERNS.md
@.planning/phases/22-studio-ui-polish/22-UI-SPEC.md
@.planning/phases/22-studio-ui-polish/22-01-SUMMARY.md

<interfaces>
<!-- Required paint order, bottom→top (UI-SPEC § Overlay Layering Model) -->
1. ZoomContainer/OffthreadVideo
2. PngOverlay  where (layer ?? "back") === "back"     ← NEW: before subtitles
3. SubtitleLayoutRenderer
4. TitleOverlay sequences
5. PngOverlay  where layer === "front"                ← promoted, after titles

<!-- Studio SubtitledVideo.tsx current overlay block (L109-113) — ALL overlays after titles (wrong for D-03) -->
{overlays.map((ov, i) => (
  <PngOverlay key={`overlay-${i}`} overlay={ov} rawImageSrc={ov.imageData} />
))}

<!-- Renderer Root.tsx inline SubtitledVideo (L121-125) — mirror, BUT: -->
<!--   - omit rawImageSrc (renderer uses staticFile(_resolvedFile) — PngOverlay.tsx L33-41) -->
<!--   - keep hard-coded fps = 30 (Root.tsx L106); studio reads useVideoConfig() — KEEP this divergence -->
{overlays.map((ov, i) => (
  <PngOverlay key={`overlay-${i}`} overlay={ov} />
))}

<!-- Preview-only dim (UI-SPEC) — STUDIO Player path ONLY, must NOT reach renderer -->
back-layer overlay in Player: opacity 0.85; filter saturate(0.8)   (legibility hint, not exported)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split overlays into back/front bands in studio SubtitledVideo.tsx with preview-only dim</name>
  <read_first>
    - services/remotion-studio/src/SubtitledVideo.tsx (full file — render block L85-114, current overlay map L109-113)
    - services/remotion-studio/src/compositions/PngOverlay.tsx (the overlay component + computeOverlaySrc/computeOverlayOpacity helpers; note rawImageSrc is the Player/browser path)
    - .planning/phases/22-studio-ui-polish/22-01-SUMMARY.md (confirms the layer field shape: layer?: "back" | "front")
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Overlay Layering Model"
  </read_first>
  <action>
    In services/remotion-studio/src/SubtitledVideo.tsx, replace the single overlay `.map()` (L109-113) with two filtered passes preserving array order: render `overlays.filter(o => (o.layer ?? "back") === "back")` BEFORE the `<SubtitleLayoutRenderer>` (L94), and `overlays.filter(o => o.layer === "front")` AFTER the title `.map()` (after L108). Keep `rawImageSrc={ov.imageData}` on both passes (Player/browser context). For the BACK band only, apply the preview-only legibility dim by wrapping each back overlay (or passing through) with `opacity: 0.85; filter: saturate(0.8)` — implement as a Player-only style on the back-band PngOverlay container (e.g., an extra `previewDim` flag/prop or a wrapping <div> with that style) so it is unmistakably the studio path. This dim MUST NOT be applied in the renderer (Task 2). Use stable keys: `overlay-back-${i}` / `overlay-front-${i}`. Do NOT change the video/subtitle/title rendering otherwise.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: SubtitledVideo.tsx contains exactly two overlay filter passes — `grep -c 'layer ?? "back"\|=== "back"' SubtitledVideo.tsx` ≥ 1 AND `grep -c '=== "front"' SubtitledVideo.tsx` ≥ 1.
    - Order assertion: the back-band map appears at a LOWER line number than `<SubtitleLayoutRenderer`, and the front-band map appears at a HIGHER line number than the title `.map(` block (verify by reading the render block).
    - Source assertion: the preview dim `saturate(0.8)` + `0.85` appears in SubtitledVideo.tsx (studio) and is attached to the back band.
    - Build: `npm run build:editor` exits 0.
  </acceptance_criteria>
  <done>Studio SubtitledVideo paints back overlays under text, front overlays over text, array order preserved, preview dim on back band; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the band split in renderer Root.tsx inline SubtitledVideo, re-run renderer vitest (no cp)</name>
  <read_first>
    - services/remotion-renderer/src/Root.tsx (inline SubtitledVideo render block L92-126; overlay map L121-125; hard-coded fps=30 at L106; title rendering L104-120)
    - services/remotion-studio/src/SubtitledVideo.tsx (the just-edited studio canonical — mirror its band split)
    - AGENTS.md § "Renderer sync pattern" (Root.tsx is NOT bulk-synced; edit the inline SubtitledVideo by hand; do NOT copy Root.tsx or SubtitledVideo.tsx wholesale; re-run renderer vitest after any compositions/shared sync — the clobber hazard)
    - services/remotion-renderer/src/compositions/PngOverlay.tsx (renderer uses staticFile(_resolvedFile), no rawImageSrc)
  </read_first>
  <action>
    In services/remotion-renderer/src/Root.tsx, replace the single overlay `.map()` (L121-125) with the SAME two filtered passes as studio: back band `overlays.filter(o => (o.layer ?? "back") === "back")` rendered BEFORE `<SubtitleLayoutRenderer>` (L103), front band `overlays.filter(o => o.layer === "front")` AFTER the title `.map()` (after L120). KEEP the renderer divergences: (a) do NOT pass `rawImageSrc` (renderer's PngOverlay resolves via staticFile(_resolvedFile)); (b) keep the hard-coded `const fps = 30` (L106) — do NOT switch to useVideoConfig. Do NOT apply the preview-only dim here (it is studio-only; the renderer must paint back overlays at full opacity/saturation). RENDERER-SYNC — run NO `cp` in this plan: `pipeline-config.ts` was already synced by 22-01, NO composition file (`src/compositions/*`) or shared `src/` module changed in this phase, and the ONLY renderer edit is this hand-applied `Root.tsx` inline band split. A defensive bulk `cp compositions/*` (or any `cp`) would silently clobber renderer-only fixes (the documented clobber hazard in AGENTS.md / MEMORY) — so do not copy anything. After editing Root.tsx by hand, run the FULL renderer vitest suite — that re-run is the acceptance gate (clobber-hazard guard).
  </action>
  <verify>
    <automated>cd services/remotion-renderer && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (mirror): `grep -c '=== "front"' services/remotion-renderer/src/Root.tsx` ≥ 1 AND `grep -c 'layer ?? "back"\|=== "back"' services/remotion-renderer/src/Root.tsx` ≥ 1.
    - Paint-order match: the renderer back-band map precedes `<SubtitleLayoutRenderer` and the front-band map follows the title map — same structure as studio SubtitledVideo (read both render blocks and confirm the 5-layer order is identical).
    - Divergence preserved: Root.tsx still has `const fps = 30` and the overlay passes do NOT pass `rawImageSrc`.
    - No-dim assertion: `grep -c 'saturate(0.8)' services/remotion-renderer/src/Root.tsx` returns 0 (preview dim must NOT reach the renderer).
    - No-cp assertion: this plan ran no `cp` — only Root.tsx was edited (`git diff --name-only` lists only services/remotion-renderer/src/Root.tsx for the renderer side; no composition files reverted).
    - Test command: `cd services/remotion-renderer && npm test` passes the full suite (overlay.test.ts, pipeline-config.test.ts incl. the 22-01 layer cases, all others) — this is the renderer-sync clobber-hazard acceptance gate.
  </acceptance_criteria>
  <done>Renderer Root.tsx inline SubtitledVideo mirrors the studio 5-layer paint order with renderer divergences kept and no preview dim; no cp run; full renderer vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline-config.json overlays → renderer composition | User-authored overlay configs (incl. `layer`) drive paint order in the headless-Chrome render. Existing boundary; this plan only changes ordering logic, not the data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Tampering | layer-driven render order | mitigate | `layer` is enum-validated upstream (22-01); the `(o.layer ?? "back")` default means a malformed/missing value can never crash the render — it falls into the back band. |
| T-22-08 | Information disclosure | preview dim style | mitigate | The `saturate(0.8)/opacity 0.85` hint is confined to the studio Player path; grep-asserted absent from Root.tsx so it cannot alter exported video. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (composition edits only). |
</threat_model>

<verification>
- Studio + renderer paint orders are the identical 5-layer sequence.
- Missing layer → back band (no crash).
- Preview dim studio-only.
- No cp run (clobber hazard avoided); full renderer vitest green (clobber-hazard gate).
</verification>

<success_criteria>
- D-03/D-04 layering implemented consistently in both services; renderer tests pass.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-04-SUMMARY.md` when done.
</output>
