---
phase: 25-ai-social-metadata
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - services/remotion-studio/src/server.ts
  - services/remotion-studio/src/metadata-api.test.ts
  - docker-compose.yml
  - .env.example
autonomous: true
requirements: [META-01, META-02, META-04]
must_haves:
  truths:
    - "POST /api/metadata reads the job's whisper/transcript.json, calls the local-llms router, validates, and returns { title, description, hashtags } (META-01, D-01)"
    - "The router call uses METADATA_API_URL/METADATA_API_KEY/METADATA_MODEL env (default http://host.docker.internal:3210, big-cloud) via the OpenAI surface + response_format json_object (AI-SPEC §3)"
    - "remotion-studio gets extra_hosts host.docker.internal:host-gateway so it can reach the router (and METADATA_* env wired in docker-compose)"
    - "Generated metadata is persisted atomically to {jobId}/metadata.json and re-served on GET (D-05); a missing API key or unreachable router returns a clear JSON error, never a crash (D-07)"
    - "Routes are before the serveSpa catch-all; jobId is validated (UUID) before any fs/network touch"
  artifacts:
    - "services/remotion-studio/src/server.ts"
    - "services/remotion-studio/src/metadata-api.test.ts"
  key_links:
    - "server.ts imports buildSystemPrompt/generateMetadata/extractTranscriptText/sanitize from ./metadata.js (25-01)"
---

<objective>
Expose POST /api/metadata (+ a GET to re-serve persisted metadata) on the Studio server:
read the job transcript, call the local-llms router (the real ChatClient over fetch),
validate via the 25-01 core, persist, and return. Wire the env + extra_hosts in docker-compose.
Prove with supertest using a mocked router (no real network in tests).
</objective>

<context>
@.planning/phases/25-ai-social-metadata/25-CONTEXT.md
@.planning/phases/25-ai-social-metadata/25-AI-SPEC.md
- Import the core from ./metadata.js (25-01). The server provides the REAL ChatClient: a fetch to `${METADATA_API_URL}/v1/chat/completions` with Bearer METADATA_API_KEY, model METADATA_MODEL, response_format json_object, ~90s timeout; returns choices[0].message.content. Log X-Model-Backend + X-Cost-Cents.
- Transcript path: path.join(dirname(ACTIVE_PIPELINE_CONFIG_PATH or pipeline data dir), jobId, "whisper", "transcript.json"). Reuse the same pipeline data dir resolution the server already uses for artifacts (GET /api/result derives from /data/pipeline). Define a PIPELINE_DATA_DIR resolution if not present.
- Persisted metadata path: {jobId}/metadata.json (atomic temp+rename, reuse the existing atomicWriteConfig pattern generalized, or a small local atomic write).
- Routes BEFORE app.get("/{*splat}", serveSpa). Validate jobId with the existing UUID regex.
- Tests: mirror metadata-... in a dedicated metadata-api.test.ts that sets env + temp dirs BEFORE importing app (lazy getters where needed, per the 24-02 module-caching lesson). Mock global fetch so no real router call happens.
</context>

<tasks>

<task type="auto">
  <name>Task 1: real ChatClient + POST /api/metadata + GET /api/metadata/:jobId</name>
  <files>services/remotion-studio/src/server.ts</files>
  <action>
    - Add lazy getters: getMetadataApiUrl() (default http://host.docker.internal:3210), getMetadataApiKey(), getMetadataModel() (default "big-cloud"); and a PIPELINE_DATA_DIR resolver (dir that holds {jobId}/...).
    - Implement `routerChatClient({system,user})`: POST to `${url}/v1/chat/completions` with Authorization Bearer key, body {model, messages:[{role:"system",...},{role:"user",...}], response_format:{type:"json_object"}, temperature:0.7, max_tokens:500}, AbortSignal.timeout(90000). If key empty → throw a typed RouterNotConfiguredError. Return choices[0].message.content; on non-200 throw RouterError(status, body). Log backend/cost headers.
    - `POST /api/metadata` body { jobId, platform, tone }: validate jobId (UUID) + platform/tone (must be known); read {jobId}/whisper/transcript.json (404 if missing); call generateMetadata({transcript, platform, tone, client: routerChatClient}); persist result to {jobId}/metadata.json (atomic); respond 200 { ...metadata, _meta:{ backend, model } }. Map errors: RouterNotConfiguredError→503 "router no configurado", RouterError/timeout→502, EmptyTranscriptError→422, MetadataValidationError→502 with detail. Never throw uncaught.
    - `GET /api/metadata/:jobId` → re-serve persisted {jobId}/metadata.json (200) or 404 if none. (lets the UI restore after reload, D-05)
    - Register both BEFORE serveSpa.
  </action>
  <verify><automated>cd services/remotion-studio && grep -nE "/api/metadata" src/server.ts | head</automated></verify>
  <acceptance_criteria>
    - POST + GET /api/metadata present before serveSpa; jobId/platform/tone validated; all error paths return JSON (no crash); missing key → 503.
  </acceptance_criteria>
  <done>The Studio server is the single surface for metadata generation + retrieval, backed by the router.</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose + .env.example wiring</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
    - On the `remotion-studio` service: add `extra_hosts: ["host.docker.internal:host-gateway"]` and environment:
      METADATA_API_URL=${METADATA_API_URL:-http://host.docker.internal:3210},
      METADATA_API_KEY=${METADATA_API_KEY:-},
      METADATA_MODEL=${METADATA_MODEL:-big-cloud}.
    - Add the three vars to .env.example with a comment: METADATA_API_KEY = your local-llms ROUTER_BEARER_TOKEN; METADATA_MODEL options (big-cloud=gpt-oss:120b cloud, chat-local=qwen local/private).
    - Do NOT hardcode any secret. Leave METADATA_API_KEY empty by default (endpoint returns 503 until set).
  </action>
  <verify><automated>cd /home/luis/proyectos/reel-factory && awk '/^  remotion-studio:/,/^  [a-z].*:$/' docker-compose.yml | grep -E "METADATA_|host.docker" && docker compose config >/dev/null 2>&1 && echo "compose valid"</automated></verify>
  <acceptance_criteria>
    - remotion-studio has extra_hosts + the 3 METADATA_* env; compose config validates; .env.example documents them.
  </acceptance_criteria>
  <done>The Studio container can reach the router and is configured (key left to the user).</done>
</task>

<task type="auto">
  <name>Task 3: metadata-api.test.ts integration tests (mocked router)</name>
  <files>services/remotion-studio/src/metadata-api.test.ts</files>
  <action>
    Supertest + mocked global fetch (the router). Set METADATA_* env + a temp PIPELINE_DATA_DIR with a seeded {jobId}/whisper/transcript.json BEFORE importing app. Cover:
    - POST /api/metadata happy: mock fetch returns a valid chat-completion JSON → 200 with title/description/hashtags; assert {jobId}/metadata.json written.
    - GET /api/metadata/:jobId after a POST returns the persisted metadata; unknown jobId → 404.
    - Missing transcript → 404; "../evil" jobId → 400; unknown platform/tone → 400.
    - METADATA_API_KEY empty → 503 "router no configurado" (fetch not called).
    - Router 500 (mock) → 502; router returns invalid JSON twice → 502 MetadataValidation; (retry path).
    - Existing studio suite still green.
  </action>
  <verify><automated>cd services/remotion-studio && npx vitest run src/metadata-api.test.ts 2>&1 | tail -8</automated></verify>
  <acceptance_criteria>
    - All metadata-api tests green (no real network); persistence + error mapping proven.
  </acceptance_criteria>
  <done>Every metadata route + error path proven; router mocked.</done>
</task>

</tasks>

<verification>
- `npx vitest run` (studio) all green; `docker compose config` valid.
- `grep /api/metadata src/server.ts` shows POST + GET before serveSpa.
</verification>

<success_criteria>
- POST /api/metadata generates from the transcript via the router, persists, and returns validated metadata; resilient to missing key/router; fully integration-tested with a mocked router.
</success_criteria>

<output>
Create `.planning/phases/25-ai-social-metadata/25-02-SUMMARY.md` when done.
</output>
