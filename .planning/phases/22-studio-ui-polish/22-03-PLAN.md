---
phase: 22-studio-ui-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/preview/PreviewApp.tsx
  - services/remotion-studio/src/editor/index.html
autonomous: true
requirements: [D-01, D-02, D-10, D-11]
must_haves:
  truths:
    - "The Studio renders three columns: preview (left), controls (center), metadata placeholder (right)"
    - "Column 3 is an always-visible 320px panel reading 'Metadata de redes' + 'Próximamente…' with no interactive controls"
    - "The standalone Text tab is gone; final tabs are Títulos | Overlays | Subtítulos"
    - "The sample-text TextareaInput sits at the TOP of the Subtítulos tab and still drives the live caption preview"
    - "The default.css OKLCH token set is available as CSS custom properties on the app root"
  artifacts:
    - path: "services/remotion-studio/src/preview/PreviewApp.tsx"
      provides: "3-column shell, tab restructure, metadata placeholder, token-driven theme"
      contains: "Metadata de redes"
    - path: "services/remotion-studio/src/editor/index.html"
      provides: "Inlined default.css token :root block"
      contains: "--accent"
  key_links:
    - from: "PreviewApp Subtítulos tab"
      to: "captionPages preview"
      via: "sampleText state → textToCaptionPages → PreviewPlayer"
      pattern: "TextareaInput[\\s\\S]*sampleText"
---

<objective>
Restructure the Studio shell in `PreviewApp.tsx` from 2-column to the locked
3-column layout (preview · controls · metadata placeholder), remove the
standalone `Text` tab and relocate `<TextareaInput>` to the top of the
Subtítulos tab, relabel tabs to Spanish, build the static column-3 placeholder,
and make the `default.css` OKLCH tokens available as CSS custom properties
(D-01/D-02/D-10/D-11).

Purpose: Lock the final layout so the densification pass (this plan's whole-
panel impeccable treatment) and the per-tab control reordering are not done
against a stale 2-column assumption. Column 3 is presentational only — NO AI,
NO data wiring (D-02, explicitly out of scope).
Output: A 3-column themed shell with Títulos | Overlays | Subtítulos tabs and a
working sample-text-in-Subtítulos preview path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-PATTERNS.md
@.planning/phases/22-studio-ui-polish/22-UI-SPEC.md

<interfaces>
<!-- Current PreviewApp.tsx structure (the file being restructured) -->
TABS array (L28-33): titles | overlays | subtitles | text   → DROP "text"
TabBar (L37-105), TabButton (L70-105): keep; relabel to Spanish
Subtitles panel (L449-459): LayoutSelector + StyleControls + FontGrid
                            → prepend <TextareaInput value={sampleText} onChange={setSampleText} />
Text panel (L461-464): the ONLY home of TextareaInput today → DELETE this panel
Shell container (L398-467): left 40% preview (L400-419) + right flex:1 panel (L422-466)
                            → left flex:0 1 470px, center flex:1, NEW right col width:320px flex:none
sampleText/captionPages/totalDurationMs (L210, L224-228) feed PreviewPlayer — UNCHANGED, must still drive preview
Header (L336-383): Render Video (disabled) + Save Config buttons — keep behavior; relabel to UI-SPEC copy

<!-- Copywriting Contract (UI-SPEC) — Spanish, verbatim -->
Tabs:            Títulos | Overlays | Subtítulos
Col 3 heading:   Metadata de redes
Col 3 body:      Próximamente — descripción, hashtags y más generados a partir de tus subtítulos.
Sample role cue: ● Alimenta los subtítulos · no se exporta
Sample placeholder: Cómo edité este reel en 30 segundos…

<!-- default.css token :root block to inline — verbatim from
     .claude/skills/sketch-findings-reel-factory/sources/themes/default.css L7-68 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke impeccable + frontend-design, inline default.css tokens, build the 3-column shell + col-3 placeholder</name>
  <read_first>
    - AGENTS.md § "UI/frontend work — REQUIRED tooling" (impeccable + frontend-design — invoke FIRST)
    - services/remotion-studio/src/preview/PreviewApp.tsx (the full file — read current shell L333-470, TABS L28-33, tab panels L434-465)
    - services/remotion-studio/src/editor/index.html (the <style> block at L7-11 where the body/root base styles live — tokens go here as a :root block)
    - .claude/skills/sketch-findings-reel-factory/sources/themes/default.css (the canonical token set to inline verbatim)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Layout Contract" + § "Copywriting Contract" + § "Color" (color law)
  </read_first>
  <action>
    FIRST invoke the `impeccable` skill and `frontend-design` plugin. (1) In services/remotion-studio/src/editor/index.html, add the default.css `:root { … }` token block (verbatim from sources/themes/default.css L7-68) inside the existing <style> tag, so `var(--canvas/--surface/--surface-2/--chrome/--stage/--border/--text/--text-2/--text-muted/--accent/--accent-tint/--accent-strong/--action/--danger/--warning/--success/--s-*/--t-*/--r-*/--ease/--dur)` resolve app-wide; also add the `@media (prefers-reduced-motion: reduce)` collapse rule from UI-SPEC § Motion. (2) In PreviewApp.tsx, delete `{ id: "text", label: "Text" }` from the TABS array (L32) and relabel the remaining three to Spanish: `{ id:"titles", label:"Títulos" }`, `{ id:"overlays", label:"Overlays" }`, `{ id:"subtitles", label:"Subtítulos" }`. Delete the Text tab panel (L461-464). Move `<TextareaInput value={sampleText} onChange={setSampleText} />` to the TOP of the Subtítulos panel (above LayoutSelector). Add the role-cue line `● Alimenta los subtítulos · no se exporta` (blue dot = `var(--accent)`) near the textarea per UI-SPEC. (3) Convert the shell (L398-467) from 2-column to 3-column flex: col1 preview `flex: 0 1 470px` bg `var(--stage)`; col2 controls `flex: 1` (existing TabBar + panels); NEW col3 `width: 320px; flex: none` containing a presentational card (heading `Metadata de redes` at `var(--t-base)`, body `Próximamente — descripción, hashtags y más generados a partir de tus subtítulos.` in muted italic `var(--text-muted)`) — NO state, NO fetch, NO controls (D-02). Hide col3 below a 1024px viewport. (4) Migrate the header buttons to the UI-SPEC Header Contract: `▶ Render Video` is the single green CTA (`var(--action)`, stays disabled/coming-soon as today), `Guardar config` becomes an OUTLINE button (never green) — flip its current green fill to outline using `var(--border-strong)`/`var(--text)`. Apply the whole-panel impeccable density pass (UI-SPEC spacing/type tokens) across the shell chrome you touch; per-tab control reordering and PositionPresets wiring are handled in 22-05 (do NOT duplicate that work here). Keep sampleText/captionPages/totalDurationMs plumbing (L210, L224-228, PreviewPlayer props L412-418) UNCHANGED so the preview still drives.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '"text"' services/remotion-studio/src/preview/PreviewApp.tsx` shows the `text` tab id is gone from TABS (no `{ id: "text"`).
    - Source assertion: PreviewApp.tsx contains `Títulos`, `Subtítulos`, `Metadata de redes`, and `Próximamente — descripción, hashtags`.
    - Source assertion (textarea relocation): in PreviewApp.tsx the `<TextareaInput` JSX appears within the `subtitles` panel block and NOT inside any `text` panel (grep shows TextareaInput once, no `activeTab === "text"`).
    - Source assertion: `grep -c 'width: 320\|width:320\|"320px"\|320,' services/remotion-studio/src/preview/PreviewApp.tsx` ≥ 1 (col-3 fixed width).
    - Source assertion: index.html `<style>` contains `--accent` and `--surface` (token block inlined) and a `prefers-reduced-motion` rule.
    - Color law: the `Guardar config` button no longer uses `#4CAF50`/`background: "#4CAF50"`; exactly one green CTA (Render) remains in the header.
    - Build: `npm run build:editor` exits 0.
    - Behavior (preview drives): editing sample text still recomputes captionPages — confirm the `useMemo(() => textToCaptionPages(sampleText), [sampleText])` line (L224) is intact and TextareaInput's onChange is `setSampleText`.
  </acceptance_criteria>
  <done>3-column themed shell renders; Text tab removed; sample text lives atop Subtítulos and drives preview; col-3 placeholder is static; tokens inlined; build green; Guardar is outline, Render is the only green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a presentational restructure of an internal Studio screen. Column 3 is a static placeholder — no data source, no model, no network call (D-02). Config save/load (PUT/GET /api/config) is unchanged from Phase 21. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Information disclosure | column-3 placeholder | accept | Renders only hard-coded Spanish copy; no transcript/title data is read or displayed (D-02 defers all AI/data wiring to a future phase). |
| T-22-06 | Tampering | inlined CSS tokens | accept | Tokens are static CSS custom properties in index.html; no user input flows into them. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (uses existing React + Remotion Player deps). |
</threat_model>

<verification>
- `npm run build:editor` succeeds.
- 3 columns present; Text tab absent; tabs in Spanish; col-3 static placeholder.
- Sample-text→preview path intact.
- One green CTA (Render); Guardar is outline.
</verification>

<success_criteria>
- The Studio shell matches the UI-SPEC Layout Contract (3 columns, tab order, header contract) with default.css tokens available.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-03-SUMMARY.md` when done.
</output>
