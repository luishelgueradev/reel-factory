---
phase: "16-render-config-flicker"
plan: 03
type: execute
wave: 2
depends_on:
  - "16-02"
files_modified:
  - services/remotion-studio/src/compositions/BarLayout.tsx
  - services/remotion-studio/src/compositions/TikTokLayout.tsx
  - services/remotion-renderer/src/compositions/BarLayout.tsx
  - services/remotion-renderer/src/compositions/TikTokLayout.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Non-last caption pages hold their Sequence visible until the next page starts (no blank gap)"
    - "Last caption page fades out naturally after the last token (FADE_OUT_MS=300ms)"
    - "BarLayout and TikTokLayout apply the identical corrected formula"
    - "Renderer copies are byte-for-byte identical to studio source after sync"
    - "Unit test in captions.test.ts (or a layout-duration test file) verifies the no-gap formula"
    - "A real /process render after the fix shows no inter-page subtitle blink"
  artifacts:
    - path: "services/remotion-studio/src/compositions/BarLayout.tsx"
      provides: "Fixed durationInFrames formula (extend to nextPageStartMs for non-last pages)"
      contains: "isLastPage"
    - path: "services/remotion-studio/src/compositions/TikTokLayout.tsx"
      provides: "Fixed durationInFrames formula (identical to BarLayout fix)"
      contains: "isLastPage"
    - path: "services/remotion-renderer/src/compositions/BarLayout.tsx"
      provides: "Sync copy of studio BarLayout.tsx"
      contains: "isLastPage"
    - path: "services/remotion-renderer/src/compositions/TikTokLayout.tsx"
      provides: "Sync copy of studio TikTokLayout.tsx"
      contains: "isLastPage"
  key_links:
    - from: "services/remotion-studio/src/compositions/BarLayout.tsx captionPages.map"
      to: "Sequence durationInFrames"
      via: "isLastPage ? lastTokenEndMs + FADE_OUT_MS : captionPages[i+1].startMs"
      pattern: "isLastPage"
    - from: "services/remotion-renderer/src/compositions/BarLayout.tsx"
      to: "services/remotion-studio/src/compositions/BarLayout.tsx"
      via: "cp sync (CLAUDE.md convention)"
      pattern: "isLastPage"
---

<objective>
**MUST NOT begin execution if Plan 16-02 checkpoint returned failed:loaded=false.** If the resume signal from Plan 16-02 is "failed: loaded=false", the Issue A fix did not propagate — stop here and report to the orchestrator for gap-closure on Plan 16-01.

Fix Issue B: eliminate subtitle flicker by extending each non-last caption page's
Sequence duration to the start of the next page, so the fade-out overlaps with the next
page's Sequence instead of playing into empty air.

This is the Option 1 fix described in RESEARCH.md: replace the safeEndMs/displayEndMs
block in both BarLayout.tsx and TikTokLayout.tsx with isLastPage-conditional logic, then
sync the renderer copies per CLAUDE.md.

**[MANDATORY PRE-EXECUTION GATE]** This plan touches frontend-layout TSX files.
Per CLAUDE.md, before any file edits:
1. Invoke `/impeccable` skill
2. Load the `frontend-design` plugin
Failure to invoke these is a hard stop — do not begin Task 1 without completing this gate.
Verify: both tools acknowledged/loaded before proceeding.

Purpose: The 20–241ms inter-page gaps combined with FADE_OUT_MS=300 cause a visible
blink at every page boundary. Extending each Sequence to the next page's startMs
eliminates the blank gap entirely — the pages are contiguous or overlapping.

Output: Fixed BarLayout.tsx and TikTokLayout.tsx in studio + synced renderer copies +
a unit test guarding the no-gap formula + a passing e2e render with no subtitle blink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-render-config-flicker/16-CONTEXT.md
@.planning/phases/16-render-config-flicker/16-RESEARCH.md
@.planning/phases/16-render-config-flicker/16-PATTERNS.md
@.planning/phases/16-render-config-flicker/16-02-SUMMARY.md

<interfaces>
From services/remotion-studio/src/compositions/shared-styles.ts (constants used by layouts):
  export const FADE_IN_MS = 100;
  export const FADE_OUT_MS = 300;
  export const PAGE_OVERLAP_GUARD_MS = 100;  // NOT subtracted in the new formula for non-last pages

From services/remotion-studio/src/compositions/BarLayout.tsx (current buggy formula, lines 245–255):
  const fromFrame = Math.round(page.startMs * (fps / 1000));
  const lastTokenEndMs = page.tokens.length > 0 ? page.tokens[page.tokens.length - 1].toMs : page.startMs;
  const nextPageStartMs = i + 1 < captionPages.length ? captionPages[i + 1].startMs : Infinity;
  const displayEndMs = lastTokenEndMs + FADE_OUT_MS;
  const safeEndMs = Math.min(displayEndMs, nextPageStartMs - PAGE_OVERLAP_GUARD_MS);
  const clampedEndMs = (i === captionPages.length - 1 && totalDurationMs)
    ? Math.min(safeEndMs, totalDurationMs)
    : safeEndMs;
  const durationInFrames = Math.max(1, Math.ceil((clampedEndMs - page.startMs) * (fps / 1000)));

TikTokLayout.tsx lines 222–231: IDENTICAL formula (substitute CaptionPage for BarPage in JSX).

The Sequence JSX in both files is NOT changed:
  <Sequence key={i} from={fromFrame} durationInFrames={durationInFrames}>
    <BarPage ... />   (BarLayout) / <CaptionPage ... />  (TikTokLayout)
  </Sequence>

From services/remotion-renderer/src/captions.test.ts imports:
  import { transcriptToCaptionPages, ... } from "./captions";
  import type { SilenceCutList, WhisperWord } from "./captions";
  Uses vitest describe/it/expect. Framework: vitest ^4.1.5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix durationInFrames formula in BarLayout.tsx and TikTokLayout.tsx (studio)</name>
  <files>
    services/remotion-studio/src/compositions/BarLayout.tsx,
    services/remotion-studio/src/compositions/TikTokLayout.tsx
  </files>
  <read_first>
    services/remotion-studio/src/compositions/BarLayout.tsx — read lines 240–268 to see the
    complete captionPages.map() block: fromFrame, lastTokenEndMs, the buggy safeEndMs formula
    (lines 249–255), and the Sequence JSX (lines 257–266). Understand which lines to replace.
    services/remotion-studio/src/compositions/TikTokLayout.tsx — read lines 215–244 to see the
    identical structure (lines 222–231 are the buggy formula, lines 233–241 are the Sequence JSX).
    services/remotion-studio/src/compositions/shared-styles.ts — lines 1–9 to confirm
    FADE_OUT_MS=300 and PAGE_OVERLAP_GUARD_MS=100 are the values in scope.
  </read_first>
  <behavior>
    - Test A: non-last page where nextPageStartMs - lastTokenEndMs < FADE_OUT_MS (e.g. gap=40ms):
        old formula: safeEndMs = min(lastToken+300, nextPage-100) = nextPage-100 → gap of 100ms before next page
        new formula: displayEndMs = nextPageStartMs → durationInFrames covers exactly to nextPage start → gap=0
    - Test B: last page:
        new formula: displayEndMs = lastTokenEndMs + FADE_OUT_MS → same as before (last page unchanged)
    - Test C: non-last page where nextPageStartMs - lastTokenEndMs > FADE_OUT_MS (e.g. silence gap, 1200ms):
        old formula: safeEndMs = min(lastToken+300, nextPage-100) = lastToken+300 → page ends early
        new formula: displayEndMs = nextPageStartMs → page lingers until next page (expected trade-off per RESEARCH.md)
    - Test D: PAGE_OVERLAP_GUARD_MS is NOT subtracted in the non-last case → no guard in the new formula
    - Test E: durationInFrames is always Math.max(1, ...) → never zero even for very short pages
  </behavior>
  <action>
    In BOTH BarLayout.tsx and TikTokLayout.tsx, replace ONLY the durationInFrames calculation
    block (the four lines containing safeEndMs, displayEndMs, clampedEndMs, durationInFrames).
    Do NOT change fromFrame, lastTokenEndMs, or the Sequence JSX.

    Replace this block (lines 249–255 in BarLayout, lines 225–231 in TikTokLayout):
      const nextPageStartMs = ...
      const displayEndMs = lastTokenEndMs + FADE_OUT_MS;
      const safeEndMs = Math.min(displayEndMs, nextPageStartMs - PAGE_OVERLAP_GUARD_MS);
      const clampedEndMs = ...
      const durationInFrames = ...

    With this replacement (apply identically to both files):
      const isLastPage = i === captionPages.length - 1;
      const nextPageStartMs = !isLastPage ? captionPages[i + 1].startMs : Infinity;

      // Non-last pages: Sequence runs until next page starts — eliminates inter-page blank gap.
      // PAGE_OVERLAP_GUARD_MS is intentionally NOT subtracted (it was the source of the gap).
      // Last page: fade out naturally after last token (FADE_OUT_MS=300ms).
      const displayEndMs = !isLastPage ? nextPageStartMs : lastTokenEndMs + FADE_OUT_MS;

      const clampedEndMs = (isLastPage && totalDurationMs)
        ? Math.min(displayEndMs, totalDurationMs)
        : displayEndMs;
      const durationInFrames = Math.max(1, Math.ceil((clampedEndMs - page.startMs) * (fps / 1000)));

    The fromFrame declaration on the line before (Math.round(page.startMs * (fps / 1000)))
    stays unchanged. The Sequence JSX after stays unchanged.

    Constraints:
    - PAGE_OVERLAP_GUARD_MS is still imported/present in shared-styles.ts — do NOT remove it
      from shared-styles.ts (other code may reference it). Just stop using it in the formula.
    - totalDurationMs clamping only applies to the last page (isLastPage guard on clampedEndMs).
    - The replacement in TikTokLayout.tsx is byte-for-byte identical to BarLayout.tsx
      (the JSX inner component name BarPage vs CaptionPage is outside the calculation block
      and is not changing).
  </action>
  <verify>
    <automated>grep -c "isLastPage" /home/luis/proyectos/reel-factory/services/remotion-studio/src/compositions/BarLayout.tsx && grep -c "isLastPage" /home/luis/proyectos/reel-factory/services/remotion-studio/src/compositions/TikTokLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BarLayout.tsx contains isLastPage (the new formula is in place)
    - TikTokLayout.tsx contains isLastPage (same)
    - Neither file contains "PAGE_OVERLAP_GUARD_MS" in the captionPages.map block (the guard is no longer used in the duration formula; grep -n "PAGE_OVERLAP_GUARD_MS" on both files should show 0 occurrences INSIDE the map, only in the import if any)
    - Neither file contains "safeEndMs" (old variable is gone)
    - The Sequence JSX in both files is unchanged: <Sequence key={i} from={fromFrame} durationInFrames={durationInFrames}> still present
    - TypeScript compiles without errors: cd services/remotion-studio && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -20 (expect no errors in BarLayout or TikTokLayout)
  </acceptance_criteria>
  <done>BarLayout.tsx and TikTokLayout.tsx use the isLastPage-conditional formula; non-last pages extend Sequence to nextPageStartMs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit test for no-gap formula + renderer sync</name>
  <files>
    services/remotion-renderer/src/captions.test.ts,
    services/remotion-renderer/src/compositions/BarLayout.tsx,
    services/remotion-renderer/src/compositions/TikTokLayout.tsx
  </files>
  <read_first>
    services/remotion-renderer/src/captions.test.ts — read lines 1–50 to see import style,
    describe/it/expect patterns, and how SilenceCutList/WhisperWord are used. The new test
    block will follow the same import/describe style.
    services/remotion-renderer/src/captions.ts — check the transcriptToCaptionPages signature
    and TikTokPage shape ({startMs, durationMs, tokens}) to understand what the layout
    components receive.
    services/remotion-studio/src/compositions/BarLayout.tsx — confirm the fix from Task 1
    is present (isLastPage formula) before running the sync cp.
  </read_first>
  <behavior>
    Unit test cases for the layout durationInFrames formula (pure arithmetic — no React/Remotion runtime):
    - Case 1 "contiguous pages, short gap (40ms)":
        pages = [{startMs:0, tokens:[{toMs:800}]}, {startMs:840, tokens:[{toMs:1600}]}]
        fps=30, totalDurationMs=undefined
        For page i=0 (non-last): durationInFrames = ceil((840 - 0) * 30/1000) = ceil(25.2) = 26
        Expected: 26 frames (not ceil((800+300-100 - 0)*30/1000) = ceil(30) = 30 from old formula)
        Assert: page0Duration === 26 (no gap — ends exactly at next page start frame)
    - Case 2 "last page fades out after final token":
        Same pages, fps=30
        For page i=1 (last): displayEndMs = 1600 + 300 = 1900
        durationInFrames = ceil((1900 - 840) * 30/1000) = ceil(31.8) = 32
        Expected: 32 frames
    - Case 3 "single page (last page when N=1)":
        pages = [{startMs:100, tokens:[{toMs:2000}]}]
        fps=30, totalDurationMs=5000
        durationInFrames = ceil((min(2300,5000) - 100) * 30/1000) = ceil(66) = 66
        Expected: 66 frames
    - Case 4 "isLastPage=true but totalDurationMs clamps":
        pages = [{startMs:0, tokens:[{toMs:4900}]}]
        fps=30, totalDurationMs=5000
        durationInFrames = ceil((min(5200,5000) - 0) * 30/1000) = ceil(150) = 150
        Expected: 150 frames (clamped to totalDurationMs)
  </behavior>
  <action>
    Step 1 — Add unit tests to captions.test.ts:
    Add a new describe block at the bottom of services/remotion-renderer/src/captions.test.ts
    named "layout durationInFrames formula (Issue B fix)". Implement it as a pure arithmetic
    test — define a local helper function computeDuration(pages, i, fps, totalDurationMs) that
    replicates the isLastPage formula from the fixed BarLayout/TikTokLayout. This lets vitest
    test the logic without importing React or Remotion hooks (which are not available in test context).

    The helper must implement exactly:
      const isLastPage = i === pages.length - 1;
      const lastTokenEndMs = pages[i].tokens[pages[i].tokens.length - 1].toMs;
      const nextPageStartMs = !isLastPage ? pages[i + 1].startMs : Infinity;
      const displayEndMs = !isLastPage ? nextPageStartMs : lastTokenEndMs + FADE_OUT_MS;
      const clampedEndMs = (isLastPage && totalDurationMs) ? Math.min(displayEndMs, totalDurationMs) : displayEndMs;
      return Math.max(1, Math.ceil((clampedEndMs - pages[i].startMs) * (fps / 1000)));

    Where FADE_OUT_MS = 300 (inline constant in the helper, matching shared-styles.ts:7).

    Add the 4 test cases from the behavior block above as separate it() assertions.

    Step 2 — Sync renderer copies:
    Run from the repo root (not from services/remotion-studio):
      cp services/remotion-studio/src/compositions/BarLayout.tsx \
         services/remotion-renderer/src/compositions/BarLayout.tsx
      cp services/remotion-studio/src/compositions/TikTokLayout.tsx \
         services/remotion-renderer/src/compositions/TikTokLayout.tsx

    Per CLAUDE.md sync convention: only compositions/* files are synced. Do NOT sync server.ts,
    Root.tsx, or SubtitledVideo.tsx. Do NOT sync shared-styles.ts unless it changed (it did not).

    Step 3 — Run the test suite:
    cd services/remotion-renderer && npm test
    All existing tests must still pass. The 4 new tests must pass.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-renderer && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (all tests pass including the 4 new layout-duration cases)
    - The new describe block "layout durationInFrames formula (Issue B fix)" appears in captions.test.ts
    - services/remotion-renderer/src/compositions/BarLayout.tsx contains "isLastPage" (sync confirmed)
    - services/remotion-renderer/src/compositions/TikTokLayout.tsx contains "isLastPage" (sync confirmed)
    - diff services/remotion-studio/src/compositions/BarLayout.tsx services/remotion-renderer/src/compositions/BarLayout.tsx produces no output (files are identical)
    - diff services/remotion-studio/src/compositions/TikTokLayout.tsx services/remotion-renderer/src/compositions/TikTokLayout.tsx produces no output (files are identical)
  </acceptance_criteria>
  <done>Unit tests pass; renderer BarLayout.tsx and TikTokLayout.tsx are synced with studio and contain the isLastPage fix</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Tasks 1–2 fixed the durationInFrames formula in both layout files (studio + renderer).
    The fix extends each non-last caption page's Sequence to the start of the next page,
    eliminating the blank inter-page gap.
  </what-built>
  <how-to-verify>
    1. If the studio server is still running from Plan 16-02, restart it to pick up the new
       BarLayout/TikTokLayout (tsx will reload on restart):
       pkill -f "tsx src/server.ts" 2>/dev/null || true
       cd services/remotion-studio && setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor npx tsx src/server.ts > /tmp/remotion-server.log 2>&1 &

    2. Confirm the bar-layout config is still active:
       cat pipeline/pipeline-config.json | python3 -m json.tool | grep '"layout"'
       Expected: "layout": "bar"

    3. Submit a new /process render (use the same short test clip as Plan 16-02 or any MP4):
       curl -s -X POST http://localhost:3000/process \
         -F "video=@/path/to/test-clip.mp4" \
         | python3 -m json.tool | grep '"jobId"'

    4. After render completes, verify config loaded:
       cat pipeline/{jobId}/remotion-renderer/remotion-info.json | python3 -m json.tool | grep -A6 '"pipeline_config"'
       Expected: "loaded": true, "subtitle_layout": "bar"

    5. Play or scrub pipeline/{jobId}/remotion-renderer/output.mp4 and observe subtitle behavior.
       PASS: subtitle bar holds visible continuously between words — no blank/black flash between
             caption pages. Words fade smoothly. Bar container stays present.
       FAIL: blank gaps still visible between caption page transitions.
  </how-to-verify>
  <resume-signal>
    Type one of:
      "approved: no flicker, Issue B closed"   — subtitle bar continuous, no blank gaps
      "approved: flicker reduced, acceptable"  — minor residual acceptable for v1.2
      "failed: flicker persists"               — describe what you see; gap-closure plan needed
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio compositions → Renderer sync | Layout files copied from studio; renderer uses them at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | BarLayout/TikTokLayout duration formula | accept | Pure arithmetic change; no external input processed. Sequence durationInFrames is a positive integer clamped by Math.max(1,...) — cannot produce negative or zero frames. |
| T-16-04 | Information Disclosure | Frontend layout TSX | accept | Layout files render only pre-validated caption data from captions.ts (no user-provided raw strings in this path). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages. vitest already present in renderer. |
</threat_model>

<verification>
Full phase verification after this plan:

1. Unit tests pass: cd services/remotion-renderer && npm test
   All tests green including the 4 new "layout durationInFrames formula" cases.

2. Renderer sync confirmed: diff output empty for both BarLayout and TikTokLayout.

3. E2E render with bar layout:
   - pipeline_config.loaded=true in remotion-info.json
   - subtitle_layout=bar in remotion-info.json
   - No visible inter-page subtitle flicker in the output video

4. TypeScript clean:
   cd services/remotion-studio && npx tsc --noEmit 2>&1 | grep -v node_modules
   cd services/remotion-renderer && npx tsc --noEmit 2>&1 | grep -v node_modules
   Both exit 0 with no errors in BarLayout.tsx or TikTokLayout.tsx.
</verification>

<success_criteria>
- BarLayout.tsx and TikTokLayout.tsx (studio + renderer) use the isLastPage formula
- Non-last pages: durationInFrames extends to captionPages[i+1].startMs (no PAGE_OVERLAP_GUARD_MS subtracted)
- Last page: durationInFrames = lastTokenEndMs + FADE_OUT_MS (clamped to totalDurationMs if set)
- Unit tests in captions.test.ts cover 4 formula cases and pass
- Real render confirms no subtitle flicker on bar layout
- Both issues (A and B) closed in the same phase
</success_criteria>

<output>
Create .planning/phases/16-render-config-flicker/16-03-SUMMARY.md when done.
Include: TypeScript compile status, test results, diff output for both layout sync files,
and the human flicker observation from the final checkpoint.
</output>
