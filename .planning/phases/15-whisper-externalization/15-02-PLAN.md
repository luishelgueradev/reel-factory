---
phase: 15-whisper-externalization
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - services/api-server/src/orchestrator.ts
  - services/api-server/src/orchestrator.test.ts
  - docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "The orchestrator's whisper STEP keeps name=\"whisper\" but points at image reel-factory-whisper-http-step, drops HF_HOME, and adds WHISPER_API_URL + WHISPER_API_KEY env"
    - "The whisper-specific NVIDIA DeviceRequests block is removed from createContainer (no other step relied on it)"
    - "orchestrator.test.ts whisper assertions match the new image + env (WHISPER_API_URL/KEY present, HF_HOME absent) and the vitest orchestrator suite is green"
    - "docker-compose.yml whisper service builds ./services/whisper-http-step, drops the GPU deploy stanza + HF_HOME, adds WHISPER_API_URL/KEY env, and adds extra_hosts host.docker.internal:host-gateway"
    - "The compose service KEY stays whisper and OUTPUT_PATH stays .../whisper/transcript.json so srt-exporter.depends_on.whisper + all TRANSCRIPT_PATH references keep resolving"
    - "api-server gains WHISPER_API_KEY env (and WHISPER_API_URL default) so process.env-sourced orchestrator values resolve"
    - "tsc introduces zero NEW errors over the documented api-server baseline"
  artifacts:
    - path: "services/api-server/src/orchestrator.ts"
      provides: "Swapped whisper STEP + removed GPU DeviceRequests"
      contains: "reel-factory-whisper-http-step"
    - path: "docker-compose.yml"
      provides: "HTTP-step whisper service + host.docker.internal reachability + api-server key env"
      contains: "host.docker.internal:host-gateway"
  key_links:
    - from: "services/api-server/src/orchestrator.ts"
      to: "reel-factory-whisper-http-step image"
      via: "STEPS[0].image swap"
      pattern: "reel-factory-whisper-http-step"
    - from: "docker-compose.yml whisper service"
      to: "host whisper-api:8000"
      via: "WHISPER_API_URL + extra_hosts host-gateway"
      pattern: "host.docker.internal"
    - from: "docker-compose.yml api-server"
      to: "orchestrator process.env.WHISPER_API_KEY"
      via: "environment WHISPER_API_KEY"
      pattern: "WHISPER_API_KEY"
---

<objective>
Wire the new whisper-http-step container (built in 15-01) into the running pipeline: swap the orchestrator's whisper STEP image, drop the GPU plumbing, add the whisper-api URL/key env, fix the orchestrator tests, and update docker-compose so the whisper service builds the HTTP step and can reach the external whisper-api on the host.

Purpose: Make the externalized transcription the actual pipeline step while keeping STEP name + output path identical so every downstream step (silence-cutter, renderer, srt-exporter) is untouched. Wave 2 — depends on 15-01's container existing.

Output: Orchestrator + tests + docker-compose updated; the pipeline now calls the external whisper-api. Validation/retirement is 15-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/contracts/whisper-service-integration.md
@.planning/phases/15-whisper-externalization/15-CONTEXT.md
@.planning/phases/15-whisper-externalization/15-PATTERNS.md
@.planning/phases/15-whisper-externalization/15-01-SUMMARY.md

<interfaces>
Current orchestrator whisper STEP (orchestrator.ts:57-66):
  STEPS[0] = { name: "whisper", image: "reel-factory-whisper",
    envVars: { INPUT_PATH, OUTPUT_PATH (.../whisper/transcript.json), PIPELINE_JOB_ID, HF_HOME } }
GPU block to remove (orchestrator.ts:244-253): if (step.name === "whisper") { DeviceRequests = [{Driver:"nvidia",...}] }
resolveEnvVars (orchestrator.ts:168-174) only substitutes {jobId}; process.env values resolve at module load.
createContainer binds NetworkMode: PIPELINE_NETWORK (orchestrator.ts:236).

Tests to update (orchestrator.test.ts:46-56): assert image + INPUT/OUTPUT/PIPELINE_JOB_ID + HF_HOME; line ~235 firstCall.Image.

compose whisper service (docker-compose.yml:42-71): build ./services/whisper, HF_HOME env, deploy.resources GPU stanza, healthcheck start_period 30s.
api-server service (docker-compose.yml:210-240): add WHISPER_API_KEY / WHISPER_API_URL env.
srt-exporter depends_on.whisper (docker-compose.yml:253) + TRANSCRIPT_PATH refs (112, 261) must keep resolving.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the orchestrator whisper STEP + remove GPU DeviceRequests + update tests</name>
  <read_first>
    - services/api-server/src/orchestrator.ts (STEPS[0] at 57-66; GPU DeviceRequests at 244-253; resolveEnvVars 168-174; createContainer 231-255)
    - services/api-server/src/orchestrator.test.ts (whisper assertions 46-56; image assertion near 235)
    - .planning/phases/15-whisper-externalization/15-PATTERNS.md (orchestrator.ts EDIT section — exact before/after)
    - .planning/phases/15-whisper-externalization/15-01-SUMMARY.md (confirm the built image name)
  </read_first>
  <action>
    In services/api-server/src/orchestrator.ts STEPS[0] (the whisper entry): KEEP name: "whisper". Change image from "reel-factory-whisper" to "reel-factory-whisper-http-step". In envVars: KEEP INPUT_PATH (.../input/video.mp4), OUTPUT_PATH (.../whisper/transcript.json), PIPELINE_JOB_ID; REMOVE HF_HOME; ADD WHISPER_API_URL set to process.env.WHISPER_API_URL OR the default "http://host.docker.internal:8000", and WHISPER_API_KEY set to process.env.WHISPER_API_KEY OR "". These resolve at module load via process.env; resolveEnvVars only substitutes {jobId}, which is correct here.

    Remove the whisper-specific GPU block at orchestrator.ts:244-253: the `if (step.name === "whisper") { containerConfig.HostConfig!.DeviceRequests = [...] }` stanza. CONFIRM before deleting that this is the ONLY DeviceRequests assignment and that no other step name triggers it (grep DeviceRequests / nvidia in orchestrator.ts — expect exactly this one block). D-4: the external service owns the GPU; the step needs host.docker.internal reachability instead, provided by the existing NetworkMode + the compose extra_hosts added in Task 2 — no new DeviceRequests.

    Update services/api-server/src/orchestrator.test.ts:
      - The "whisper step should have correct env vars" test (46-56): change the image assertion to "reel-factory-whisper-http-step"; keep INPUT_PATH/OUTPUT_PATH/PIPELINE_JOB_ID assertions; REMOVE the two HF_HOME assertions; ADD assertions that envVars has WHISPER_API_URL and WHISPER_API_KEY, and that it does NOT have HF_HOME (expect(whisper.envVars).not.toHaveProperty("HF_HOME")).
      - The image assertion near line 235 (firstCall.Image === "reel-factory-whisper"): update to "reel-factory-whisper-http-step".
      - If any test asserts the GPU DeviceRequests for whisper, remove/adjust it to assert DeviceRequests is undefined for the whisper container.

    No fenced code in this plan; the env-var names + image string above are the contract.
  </action>
  <verify>
    <automated>cd services/api-server && grep -q "reel-factory-whisper-http-step" src/orchestrator.ts && ! grep -q "HF_HOME" src/orchestrator.ts && grep -q "WHISPER_API_URL" src/orchestrator.ts && ! grep -q "DeviceRequests" src/orchestrator.ts && npx vitest run src/orchestrator.test.ts</automated>
  </verify>
  <done>orchestrator.ts STEPS[0] uses image reel-factory-whisper-http-step with name still "whisper", no HF_HOME, plus WHISPER_API_URL/KEY; the whisper GPU DeviceRequests block is gone; orchestrator.test.ts assertions updated and the vitest orchestrator suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update docker-compose.yml — whisper HTTP-step service, host reachability, api-server key env</name>
  <read_first>
    - docker-compose.yml (whisper service 42-71; api-server 210-240; srt-exporter depends_on.whisper 245-262; x-pipeline-common 1-9)
    - docker-compose.yml silence-cutter / quality-finalizer blocks (clean non-GPU pipeline-common + healthcheck shape)
    - .planning/phases/15-whisper-externalization/15-PATTERNS.md (docker-compose.yml EDIT section — exact target block)
  </read_first>
  <action>
    Edit the docker-compose.yml whisper service block (42-71). KEEP the service KEY as whisper and KEEP the pipeline-common anchor merge. Change:
      - image: reel-factory-whisper-http-step:latest
      - build.context: ./services/whisper-http-step (drop the explicit dockerfile line; the step Dockerfile sits at the context root)
      - depends_on: base-python service_completed_successfully (unchanged — the HTTP step is still base-python)
      - environment: KEEP INPUT_PATH (.../input/video.mp4), OUTPUT_PATH (.../whisper/transcript.json), PIPELINE_JOB_ID; REMOVE HF_HOME; ADD WHISPER_API_URL with default http://host.docker.internal:8000 and WHISPER_API_KEY with empty default (use the ${VAR:-default} compose syntax)
      - REMOVE the entire deploy.resources.reservations.devices GPU stanza (59-65)
      - ADD extra_hosts with "host.docker.internal:host-gateway" (D-3 reachability to the external whisper-api on the host; required on Linux/WSL2, harmless on Docker Desktop)
      - healthcheck: keep the manifest.json -f test; drop start_period from 30s to 15s (no model load)
    Confirm OUTPUT_PATH dir stays .../whisper/ so srt-exporter.depends_on.whisper (253) + TRANSCRIPT_PATH refs (112, 261) keep resolving.

    Edit the api-server service block (210-240) environment list: ADD WHISPER_API_KEY (empty default) and WHISPER_API_URL (default http://host.docker.internal:8000). The orchestrator reads these from process.env at module load (Task 1), so the api-server container must carry them; otherwise process.env.WHISPER_API_KEY is undefined when STEPS is constructed.

    Run docker compose config to confirm the file parses and the whisper service resolves with the new env + extra_hosts.
  </action>
  <verify>
    <automated>grep -q "reel-factory-whisper-http-step:latest" docker-compose.yml && grep -q "host.docker.internal:host-gateway" docker-compose.yml && grep -q "WHISPER_API_KEY" docker-compose.yml && docker compose config >/dev/null</automated>
  </verify>
  <done>docker-compose.yml whisper service builds ./services/whisper-http-step, has no HF_HOME and no GPU stanza, carries WHISPER_API_URL/KEY env + extra_hosts host.docker.internal:host-gateway; api-server carries WHISPER_API_KEY/URL; OUTPUT_PATH dir unchanged; docker compose config parses cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reel-factory whisper container → host whisper-api:8000 | Cross-stack HTTP via host-gateway; carries the API key |
| api-server env → orchestrator process.env | WHISPER_API_KEY flows from compose env into the spawned step's env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information disclosure | WHISPER_API_KEY in compose/env | mitigate | Sourced from ${WHISPER_API_KEY} (env/.env), empty default; never hardcoded in the committed compose file |
| T-15-06 | Spoofing | host.docker.internal target | accept | Self-hosted host-gateway on the user's WSL2/Docker Desktop; trusted local boundary (D-3) |
| T-15-07 | Elevation of privilege | Removing GPU DeviceRequests | mitigate | Confirm grep shows the nvidia block is whisper-only before deletion; no other step affected |
</threat_model>

<verification>
- `npx vitest run src/orchestrator.test.ts` (from services/api-server) — orchestrator suite green with updated whisper assertions.
- `grep -q "reel-factory-whisper-http-step" services/api-server/src/orchestrator.ts` and `! grep -q DeviceRequests services/api-server/src/orchestrator.ts`.
- `docker compose config` parses; whisper service shows the new image, no GPU stanza, extra_hosts host-gateway, WHISPER_API_URL/KEY.
- `npx tsc --noEmit` from services/api-server introduces zero NEW errors over the documented api-server baseline (compare against the baseline noted in prior phase summaries).
</verification>

<success_criteria>
- The pipeline's whisper step now runs the HTTP-step image and reaches the external whisper-api; STEP name and transcript.json path are unchanged so downstream steps need no edits.
- GPU plumbing fully removed (orchestrator DeviceRequests + compose deploy stanza).
- Orchestrator tests green; compose parses; tsc clean against baseline.
</success_criteria>

<output>
Create `.planning/phases/15-whisper-externalization/15-02-SUMMARY.md` when done. Record the tsc baseline delta (expect zero new errors) and confirm host.docker.internal reachability assumptions for 15-03's live e2e run.
</output>
