---
context: phase
phase: 20-title-block-precision
task: 4
total_tasks: 4
status: phase_complete
last_updated: "2026-05-29T21:37:54.792Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. Phase 20 is fully complete with post-UAT fixes applied._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Spread-and-forget stale style fields | `{ ...title.style }` copies all JSON fields including deprecated ones not in TitleStyleProps. Fixed in handleStartEdit via explicit field pick. | advisory | Always do explicit property pick when loading config into form state |
| GET/PUT config path divergence | resolveConfigPath() (GET) and ACTIVE_PIPELINE_CONFIG_PATH (PUT) pointed to different files. Changes saved in-session were lost on reload. Fixed in server.ts. | advisory | Verify GET and PUT use the same path before trusting persistence |

<current_state>
Phase 20 is COMPLETE. Two full playwright UAT passes, all 5 tests green. Three post-UAT bugs found and fixed (commit fbf6802). STATE.md shows Phase 20 complete.

The next work is **Phase 21 — PNG overlays**.
</current_state>

<completed_work>

**Session work (2026-05-29, second pass):**

- Plan 20-04: Renderer sync, vitest 277/277, build:editor clean (original)
- QA UAT (playwright-testing) — 1st pass: 5/5 PASS. Found 3 bugs:
  - BUG-1: handleStartEdit spread stale deprecated fields (subtitleFontSize, topOffset, etc.)
  - BUG-2: GET/PUT used different config file paths → changes lost on reload
  - BUG-3: Local pipeline-config.json had pre-Phase-20 stale fields
- Fixes applied (commit fbf6802):
  - TitleEditor.tsx: explicit TitleStyleProps field pick in handleStartEdit
  - server.ts: resolveConfigPath() fallback → ACTIVE_PIPELINE_CONFIG_PATH
  - pipeline-config.json (local): stale fields removed
- QA UAT (playwright-testing) — 2nd pass: 7 tests, all PASS (including persistence, cancel, delete, regression of Subtitles tab)
- 20-UAT.md committed with full results

**Prior session work (2026-05-29, first pass):**

- Plan 20-04 complete (vitest 277/277, build:editor clean, human UAT 6/6)
- VERIFICATION.md: 12/12 must-haves verified
- Code review fixes: CR-01 delayRender leak, CR-02 Number.isFinite, WR-01 stale test, WR-02 lineHeight, WR-03 fps
- NaN guard fix in TitleEditor X/Y onChange
</completed_work>

<remaining_work>
- [ ] Commit STATE.md (modified, minor)
- [ ] Phase 21: PNG overlays — start with `/gsd-plan-phase 21`
</remaining_work>

<decisions_made>

- BUG-2 was previously accepted as "pre-existing architecture, not a Phase 20 bug" — UAT proved it was user-impacting and fixed it
- BUG-1 was previously accepted as "expected behavior" — UAT showed it pollutes the config on round-trip → fixed
- `textColor` IS a valid TitleStyleProps field; do not remove it from configs
- GET/PUT now both point to ACTIVE_PIPELINE_CONFIG_PATH (pipeline/pipeline-config.json)
- Local services/remotion-studio/pipeline-config.json cleaned and is the fallback when ACTIVE is unset
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/20-title-block-precision/20-UAT.md` — full results + 3 bugs fixed
2. `.planning/ROADMAP.md` → Phase 21 section — PNG overlay spec (OVERLAY-01/02/03)
3. `services/remotion-studio/src/server.ts` lines 259-280 — resolveConfigPath() as fixed

## Infrastructure State
- Studio server: **running on port 3123**
- Start command (from `services/remotion-studio/`):
  ```
  setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor ACTIVE_PIPELINE_CONFIG_PATH=$(pwd)/../../pipeline/pipeline-config.json npx tsx src/server.ts > /tmp/remotion-server.log 2>&1 &
  ```
- GET and PUT now use same file (pipeline/pipeline-config.json). Edit → Save → Reload round-trips correctly.
- Renderer vitest: 277/277 passing
- Editor build: clean (dist/editor up to date)

<context>
Phase 20 fully complete after two UAT passes. Second pass added playwright autonomous regression testing — found 3 bugs, fixed all 3, verified fixes. Milestone v1.3 at 95% (18/19 phases). Phase 21 (PNG overlays) is the last planned phase of v1.3.
</context>

<next_action>
Start with: `/gsd-plan-phase 21` — plan PNG overlays (final phase of milestone v1.3).
First commit STATE.md: `git add .planning/STATE.md && git commit -m "chore(state): update state after phase 20 UAT fixes"`
</next_action>
