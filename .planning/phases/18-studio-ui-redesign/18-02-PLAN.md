---
phase: 18-studio-ui-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - services/remotion-studio/src/preview/PreviewApp.tsx
  - services/remotion-studio/src/editor/components/ConfigPreview.tsx
  - services/remotion-studio/src/preview/FontGridPage.tsx
autonomous: true
requirements:
  - STUDIO-01
  - STUDIO-02
  - STUDIO-03

must_haves:
  truths:
    - "PreviewApp renders two columns: left 40% player, right flex-1 with tab bar + tab content"
    - "Tab bar has three tabs: Titles (default active), Subtitles, Text"
    - "Titles tab shows TitleEditor(titles, onChange); Subtitles tab shows LayoutSelector + StyleControls + FontGrid inline; Text tab shows TextareaInput"
    - "previewTitles state and setPreviewTitles are entirely removed; PreviewPlayer receives titles prop"
    - "handleSave takes no parameters — always uses titles and subtitleConfig from state"
    - "useSearchParams and fontFromUrl are removed"
    - "CollapsibleSection component is deleted from the file"
    - "ConfigPreview.tsx and FontGridPage.tsx are deleted"
    - "build:editor exits 0 after all changes"
  artifacts:
    - path: "services/remotion-studio/src/preview/PreviewApp.tsx"
      provides: "Unified StudioApp — two-column layout with TabBar + three tab panels"
      contains: "TABS"
    - path: "services/remotion-studio/src/editor/components/ConfigPreview.tsx"
      provides: "Deleted file — must not exist"
    - path: "services/remotion-studio/src/preview/FontGridPage.tsx"
      provides: "Deleted file — must not exist"
  key_links:
    - from: "PreviewApp.tsx"
      to: "TitleEditor.tsx"
      via: "onChange={setTitles} — single prop"
      pattern: "onChange.*setTitles"
    - from: "PreviewApp.tsx"
      to: "PreviewPlayer.tsx"
      via: "titles={titles} — live state, not previewTitles"
      pattern: "titles=\\{titles\\}"
    - from: "FontCard (inline)"
      to: "loadFont / getFontFamilyCSS"
      via: "imported from ../fonts"
      pattern: "loadFont.*fontName"
---

<objective>
Convert PreviewApp.tsx into the unified StudioApp: replace CollapsibleSection groups with an
array-driven TabBar (three tabs — Titles default, Subtitles, Text), fold Font Grid inline into
the Subtitles tab, unify title state (remove previewTitles), clean up dead SearchParams wiring,
and delete ConfigPreview.tsx and FontGridPage.tsx.

Purpose: Delivers STUDIO-01 (two-column), STUDIO-02 (tabs), STUDIO-03 (consolidation).
Output: Single PreviewApp.tsx containing StudioApp, TabBar, FontCard, FontGrid. Two files deleted.
</objective>

<execution_context>
MANDATORY FIRST STEP — invoke impeccable skill + frontend-design plugin before touching any file.
Per AGENTS.md §"UI/frontend work — REQUIRED tooling": every UI phase task must start with these
two tools. This is non-negotiable.

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-studio-ui-redesign/18-CONTEXT.md
@.planning/phases/18-studio-ui-redesign/18-RESEARCH.md
@.planning/phases/18-studio-ui-redesign/18-PATTERNS.md
@.planning/phases/18-studio-ui-redesign/18-UI-SPEC.md
@.planning/phases/18-studio-ui-redesign/18-01-SUMMARY.md

<interfaces>
<!-- PreviewPlayer.tsx — unchanged; executor must wire it correctly -->
<!-- Source: 18-PATTERNS.md §PreviewPlayer -->
interface PreviewPlayerProps {
  subtitleConfig: SubtitleConfig;
  captionPages: TikTokPage[];
  totalDurationMs: number;
  titles?: TitleConfig[];
}
<!-- After D-10: caller uses titles={titles}, NOT titles={previewTitles} -->

<!-- LayoutSelector.tsx — unchanged prop interface -->
interface LayoutSelectorProps {
  value: SubtitleLayoutMode;
  onChange: (layout: SubtitleLayoutMode) => void;
}

<!-- StyleControls.tsx — unchanged prop interface -->
interface StyleControlsProps {
  config: SubtitleConfig;
  onChange: (partial: Partial<SubtitleConfig>) => void;
}

<!-- TitleEditor.tsx — simplified by Plan 18-01 -->
interface TitleEditorProps {
  titles: TitleConfig[];
  onChange: (titles: TitleConfig[]) => void;
}

<!-- TextareaInput.tsx — unchanged -->
interface TextareaInputProps {
  value: string;
  onChange: (text: string) => void;
}

<!-- fonts.ts exports needed for inline FontGrid -->
<!-- AVAILABLE_FONTS: string[] — all available font names (includes "monospace" at end) -->
<!-- getFontFamilyCSS(fontName: string): string — resolves CSS font-family string -->
<!-- loadFont(fontName: string): Promise<void> — triggers Google Fonts load -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PreviewApp.tsx as unified StudioApp with tabs and inline FontGrid</name>
  <files>services/remotion-studio/src/preview/PreviewApp.tsx</files>
  <read_first>
    - services/remotion-studio/src/preview/PreviewApp.tsx — read the ENTIRE file; note exact state declarations, the CollapsibleSection component, the handleSave signature, the config load useEffect, the full JSX tree and current imports
    - services/remotion-studio/src/preview/FontGridPage.tsx — read the FontCard component fully; the inline FontGrid JSX is adapted from it (loadFont pattern, isSelected border, hover handlers, getFontFamilyCSS usage)
    - services/remotion-studio/src/pipeline-config.ts — verify SubtitleConfig shape and INITIAL_SUBTITLE_CONFIG (check actual export name used in PreviewApp — it may be DEFAULT_SUBTITLE_CONFIG)
    - services/remotion-studio/src/fonts.ts — verify AVAILABLE_FONTS export name and getFontFamilyCSS signature
  </read_first>
  <action>
    Rewrite PreviewApp.tsx applying ALL of the following changes. Read the full current file first,
    then apply each change group in order, and run the build after.

    === IMPORTS ===
    Remove: `Link` and `useSearchParams` from "react-router-dom" (keep `Navigate` if used — it won't
    be; only import what is used after the rewrite).
    Add: `AVAILABLE_FONTS` and `getFontFamilyCSS` to the import from `../fonts`.
    Keep: all other existing imports unchanged.

    === STATE (after all changes, state block must be exactly this) ===
    - `subtitleConfig` / `setSubtitleConfig` — keep, but initialize WITHOUT the fontFromUrl conditional:
      `useState<SubtitleConfig>(() => ({ ...DEFAULT_SUBTITLE_CONFIG }))` (or whatever the
      actual export name for the initial config is — read pipeline-config.ts to confirm).
    - `sampleText` / `setSampleText` — keep unchanged.
    - `titles` / `setTitles` — keep (single source of truth per D-10).
    - REMOVE: `previewTitles` / `setPreviewTitles` — deleted entirely.
    - `saving` / `setSaving` — keep.
    - `saveSuccess` / `setSaveSuccess` — keep.
    - `saveError` / `setSaveError` — keep.
    - ADD: `activeTab` / `setActiveTab` — `useState<string>("titles")` (D-04: Titles tab is default).
    - REMOVE: `searchParams` from `useSearchParams()` — delete this destructure entirely.

    === CONFIG LOAD useEffect ===
    Keep the `fetch("/api/config")` useEffect. Inside the `.then`, remove the
    `setPreviewTitles(data.titles)` line. Keep `setTitles(data.titles)`.

    === handleSave ===
    Simplify signature to `const handleSave = useCallback(async () => { ... }, [subtitleConfig, titles])`.
    Remove the `updatedTitles` parameter. The payload is always
    `JSON.stringify({ subtitle: subtitleConfig, titles })` using state directly.
    Remove the `if (updatedTitles) { setTitles(...); setPreviewTitles(...); }` block.

    === HELPER COMPONENTS (add these inside the file, before the PreviewApp function) ===
    Define these as module-level functions (not inside PreviewApp, to avoid recreation on render):

    1. TABS constant:
       ```
       const TABS: { id: string; label: string }[] = [
         { id: "titles",    label: "Titles"    },
         { id: "subtitles", label: "Subtitles" },
         { id: "text",      label: "Text"      },
       ];
       ```

    2. TabBar function:
       Props: `{ activeTab: string; onTabChange: (id: string) => void }`
       Renders a `div` with `display: flex`, `borderBottom: "1px solid #333"`,
       `background: "#16213e"`, `padding: "0 24px"`.
       Maps TABS to `<button>` elements with:
       - `key={tab.id}`, `onClick={() => onTabChange(tab.id)}`
       - `padding: "12px 16px"`, `minHeight: 44`, `border: "none"`, `background: "transparent"`, `cursor: "pointer"`, `fontSize: 14`
       - Active: `borderBottom: "2px solid #90caf9"`, `color: "#90caf9"`, `fontWeight: 600`
       - Inactive: `borderBottom: "2px solid transparent"`, `color: "#aaa"`, `fontWeight: 400`
       - Hover on inactive: inline onMouseEnter sets `color: "#e0e0e0"`, `background: "rgba(255,255,255,0.04)"`;
         onMouseLeave restores inactive state.

    3. FontCard function:
       Props: `{ fontName: string; isSelected: boolean; onSelect: (font: string) => void }`
       Internal state: `const [loaded, setLoaded] = useState(false)`.
       useEffect: calls `loadFont(fontName).then(() => setLoaded(true)).catch(() => setLoaded(true))`.
       Renders a `div` with `onClick={() => onSelect(fontName)`, `padding: 16`,
       `background: "#1e1e2e"`, `borderRadius: 8`, `cursor: "pointer"`,
       border: `isSelected ? "2px solid #90caf9" : "1px solid #333"`,
       `transition: "border-color 0.2s, background 0.2s"`.
       onMouseEnter: `borderColor: "#4CAF50"`, `background: "rgba(76, 175, 80, 0.08)"`.
       onMouseLeave: restores `borderColor: isSelected ? "#90caf9" : "#333"`, `background: "#1e1e2e"`.
       Inside: font name `div` (`fontSize: 14, fontWeight: 600, color: "#90caf9", marginBottom: 8`).
       Then: if loaded → `div` with `fontSize: 24, fontFamily: getFontFamilyCSS(fontName), color: "#e0e0e0"` showing "Hola mundo".
       Else → `div` with `fontSize: 24, fontFamily: "monospace", color: "#666"` showing "Loading...".

    4. FontGrid function:
       Props: `{ selectedFont: string | undefined; onSelect: (font: string) => void }`
       Renders a React fragment containing:
       - Section heading `div`: `fontSize: 12, fontWeight: 600, color: "#90caf9", marginBottom: 8, marginTop: 16` with text "Browse Fonts".
       - Grid `div`: `display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(200px, 1fr))", gap: 12`.
       - Maps `AVAILABLE_FONTS.filter(f => f !== "monospace")` to `<FontCard>` with `isSelected={selectedFont === fontName}` and `onSelect`.

    === HEADER (JSX) ===
    Replace the current header with the UI-SPEC header contract:
    - `background: "#16213e"`, `padding: "12px 24px"`, `borderBottom: "1px solid #333"`,
      `display: "flex"`, `justifyContent: "space-between"`, `alignItems: "center"`.
    - Left: `<h1>` with text "Reel Factory Studio" — `fontSize: 20, fontWeight: 600, color: "#fff", margin: 0`.
      Remove any `<Link>` elements (← Editor link is gone per D-01/D-02).
    - Right: `div` with `display: "flex"`, `gap: 12` containing:
      1. Render Video button: `disabled` attribute, `title="Coming soon — rendering via pipeline API"`,
         `background: "#333"`, `color: "#777"`, `border: "1px solid #444"`, `padding: "8px 16px"`,
         `borderRadius: 6`, `fontSize: 14`, `cursor: "not-allowed"`, `opacity: 0.6`. Text: "Render Video".
         No onClick handler.
      2. Save Config button: `onClick={handleSave}`, `disabled={saving}`,
         `background: saving ? "#555" : "#4CAF50"`, `color: "#fff"`, `border: "none"`,
         `padding: "8px 20px"`, `borderRadius: 6`, `cursor: saving ? "wait" : "pointer"`,
         `fontSize: 14`, `fontWeight: 600`. Text: `saving ? "Saving..." : "Save Config"`.

    === STATUS BANNERS ===
    Keep the saveSuccess and saveError banner blocks exactly as they are (full-width, below header).

    === TWO-COLUMN LAYOUT SHELL ===
    Outer `div`: `display: "flex"`, `flex: 1`, `overflow: "hidden"`.

    Left panel (40%):
    `width: "40%"`, `minWidth: 300`, `display: "flex"`, `alignItems: "center"`,
    `justifyContent: "center"`, `padding: 16` (not 32 — check current value and retain),
    `background: "#111"`, `borderRight: "1px solid #333"`.
    Contains `<PreviewPlayer subtitleConfig={subtitleConfig} captionPages={captionPages} totalDurationMs={totalDurationMs} titles={titles} />`.
    The `titles` prop uses `titles` state directly — NOT `previewTitles` (per D-10).

    Right panel:
    `flex: 1`, `display: "flex"`, `flexDirection: "column"`, `overflow: "hidden"`.
    Contains in order:
    1. `<TabBar activeTab={activeTab} onTabChange={setActiveTab} />`
    2. Tab content wrapper `div`: `flex: 1`, `overflowY: "auto"`, `padding: 24`.
       Inside: three sibling `div`s using `display: activeTab === "X" ? "block" : "none"` pattern
       (all mounted, visibility toggled — preserves TitleEditor draft state on tab switch per RESEARCH Pitfall 5):
       - `display: activeTab === "titles" ? "block" : "none"`:
         Contains `<TitleEditor titles={titles} onChange={setTitles} />` (2-prop interface from Plan 18-01).
       - `display: activeTab === "subtitles" ? "block" : "none"`:
         Contains `<LayoutSelector value={subtitleConfig.layout} onChange={(layout) => updateSubtitle({ layout })} />`,
         then `<StyleControls config={subtitleConfig} onChange={updateSubtitle} />`,
         then `<FontGrid selectedFont={subtitleConfig.fontFamily} onSelect={(font) => updateSubtitle({ fontFamily: font })} />`.
       - `display: activeTab === "text" ? "block" : "none"`:
         Contains `<TextareaInput value={sampleText} onChange={setSampleText} />`.

    === DELETE: CollapsibleSection component ===
    Remove the entire `CollapsibleSection` component definition from the file (it was defined at the
    bottom of PreviewApp.tsx). It is replaced by the tab panel pattern above.

    === NO RENDERER SYNC ===
    This phase is studio-frontend-only. Verify: no files in src/compositions/, src/fonts.ts,
    src/captions.ts, src/zoom-detection.ts, or src/pipeline-config.ts are modified.
    If any of those were accidentally touched, stop and report before proceeding.

    Run build after all changes: `cd services/remotion-studio && npm run build:editor`
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-studio && npm run build:editor 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "previewTitles" services/remotion-studio/src/preview/PreviewApp.tsx` returns 0.
    - `grep -c "useSearchParams\|fontFromUrl" services/remotion-studio/src/preview/PreviewApp.tsx` returns 0.
    - `grep -c "CollapsibleSection" services/remotion-studio/src/preview/PreviewApp.tsx` returns 0.
    - `grep -c "activeTab" services/remotion-studio/src/preview/PreviewApp.tsx` is at least 4 (state declaration + TabBar prop + three tab panel conditionals).
    - `grep -c 'TABS' services/remotion-studio/src/preview/PreviewApp.tsx` is at least 1.
    - `grep "titles={titles}" services/remotion-studio/src/preview/PreviewApp.tsx` matches the PreviewPlayer usage (not previewTitles).
    - `grep "Reel Factory Studio" services/remotion-studio/src/preview/PreviewApp.tsx` matches (page title updated).
    - `grep "Render Video" services/remotion-studio/src/preview/PreviewApp.tsx` matches (disabled placeholder present).
    - `grep "Coming soon" services/remotion-studio/src/preview/PreviewApp.tsx` matches (tooltip on Render Video button).
    - `npm run build:editor` exits 0 with no TypeScript errors.
  </acceptance_criteria>
  <done>PreviewApp.tsx is the unified StudioApp: two-column layout, TabBar with Titles/Subtitles/Text tabs (Titles default), Font Grid inline in Subtitles tab, title state unified, no previewTitles, no SearchParams, CollapsibleSection gone. Build green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ConfigPreview.tsx and FontGridPage.tsx</name>
  <files>
    services/remotion-studio/src/editor/components/ConfigPreview.tsx
    services/remotion-studio/src/preview/FontGridPage.tsx
  </files>
  <read_first>
    - services/remotion-studio/src/editor/EditorApp.tsx — verify ConfigPreview import and usage; confirm EditorApp is the only consumer (EditorApp.tsx will be deleted in Plan 18-03, but any import of ConfigPreview must be gone by that deletion)
    - services/remotion-studio/src/editor/App.tsx — verify FontGridPage is imported there and that Plan 18-03 will handle that deletion; this task only deletes the source files
    - services/remotion-studio/src/preview/PreviewApp.tsx — after Task 1, verify it does NOT import FontGridPage (it uses the inline FontGrid component instead)
  </read_first>
  <action>
    Delete both files:
    - `services/remotion-studio/src/editor/components/ConfigPreview.tsx` — raw-JSON config panel, dropped per D-06. Only consumer was EditorApp.tsx (being deleted in Plan 18-03). PreviewApp never imported it.
    - `services/remotion-studio/src/preview/FontGridPage.tsx` — standalone font grid page, absorbed inline into Subtitles tab by Task 1. The route `/preview/fonts` is removed in Plan 18-03.

    Use shell `rm` for each file:
    `rm /home/luis/proyectos/reel-factory/services/remotion-studio/src/editor/components/ConfigPreview.tsx`
    `rm /home/luis/proyectos/reel-factory/services/remotion-studio/src/preview/FontGridPage.tsx`

    After deletion, run build again to confirm no dangling imports from the files touched in Task 1:
    `cd services/remotion-studio && npm run build:editor`

    If the build errors on "cannot find module ConfigPreview" or "cannot find module FontGridPage":
    these imports are still present in EditorApp.tsx or App.tsx — those files are handled in
    Plan 18-03. The build WILL fail here if EditorApp.tsx still imports ConfigPreview.
    That is acceptable and expected — Plan 18-03 runs in parallel (Wave 2) and the two plans
    together produce a green build. Within this plan, run the build and note the errors for the
    SUMMARY; do NOT fix App.tsx or EditorApp.tsx imports here (that is Plan 18-03's scope).

    If the build is green here (because EditorApp's ConfigPreview import doesn't block PreviewApp's
    compilation due to Vite's lazy import graph), note the green build in the SUMMARY.
  </action>
  <verify>
    <automated>test ! -f /home/luis/proyectos/reel-factory/services/remotion-studio/src/editor/components/ConfigPreview.tsx && test ! -f /home/luis/proyectos/reel-factory/services/remotion-studio/src/preview/FontGridPage.tsx && echo "BOTH_DELETED"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f services/remotion-studio/src/editor/components/ConfigPreview.tsx` passes.
    - `test ! -f services/remotion-studio/src/preview/FontGridPage.tsx` passes.
    - PreviewApp.tsx does not import FontGridPage (`grep "FontGridPage" services/remotion-studio/src/preview/PreviewApp.tsx` returns empty).
  </acceptance_criteria>
  <done>ConfigPreview.tsx and FontGridPage.tsx are deleted. The inline FontGrid in PreviewApp.tsx replaces FontGridPage. ConfigPreview is gone entirely per D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Express API | PUT /api/config — user-controlled title text; sanitizeTitles() in server.ts handles this; unchanged in this phase |
| Font loading | loadFont() fetches from Google CDN via @remotion/google-fonts — same as before, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | PUT /api/config title text | accept | server.ts sanitizeTitles() unchanged; client is a dev tool, not public-facing |
| T-18-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — pure React refactor |
</threat_model>

<verification>
Primary gate: `cd services/remotion-studio && npm run build:editor` exits 0.
Secondary: `cd services/remotion-renderer && npm test` passes (shared composition logic regression).
Visual: Studio at port 3123 shows two-column layout, Titles tab open by default, Save Config + disabled Render Video in header.
</verification>

<success_criteria>
PreviewApp.tsx is the unified StudioApp. TabBar renders Titles/Subtitles/Text with Titles active by default.
PreviewPlayer receives `titles={titles}` (live state). FontGrid is inline in Subtitles tab.
ConfigPreview.tsx and FontGridPage.tsx are deleted. Build exits 0.
</success_criteria>

<output>
Create `.planning/phases/18-studio-ui-redesign/18-02-SUMMARY.md` when done.
</output>
