---
phase: 23-render-execution-progress
plan: 05
type: execute
wave: 4
depends_on: ["23-03", "23-04"]
files_modified: []
autonomous: false
requirements: [RENDER-01, RENDER-02, RENDER-03, RENDER-04, RENDER-05]
must_haves:
  truths:
    - "The renderer Docker image is rebuilt so vendored public/fonts/*.woff2 are baked in (RENDER-05 takes effect only after rebuild)"
    - "A real short-clip render through the Studio lands a playable 9:16 inline with working download (RENDER-01..04 end-to-end)"
    - "A gstatic-blocked render still completes with a non-monospace font (the literal RENDER-05 proof)"
    - "The four inline Studio surfaces pass the impeccable + frontend-design visual quality bar against the sketch grammar"
  artifacts: []
  key_links: []
---

<objective>
Close the phase with the verifications that cannot be automated in CI: the renderer image rebuild
(without which vendored fonts never take effect — RESEARCH Runtime State Inventory), a real
end-to-end render smoke through the Studio, the gstatic-blocked render that proves RENDER-05, and the
mandatory impeccable + frontend-design visual sign-off on the four inline surfaces.

These are the phase-gate verifications named in 23-VALIDATION.md "Manual-Only Verifications" and the
"Phase gate" sampling row. Everything automatable was proven in Plans 01-04; this plan proves the
Docker-runtime + visual behaviors a unit test cannot.

Purpose: prove the full pipeline works from the Studio and that the font-resilience guarantee holds
against a real network failure.
Output: human-verified sign-off (no code files; a SUMMARY recording the results + any follow-up todos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-render-execution-progress/23-VALIDATION.md
@.planning/phases/23-render-execution-progress/23-UI-SPEC.md
@.planning/phases/23-render-execution-progress/23-RESEARCH.md

<interfaces>
Renderer-sync clobber hazard (AGENTS.md / MEMORY): after any studio→renderer `cp` of fonts.ts or
  compositions, RE-RUN renderer vitest. Plan 03 changed fonts.ts in both services — confirm parity
  survived before the image build.
remotion-studio port is ALWAYS 3123 (AGENTS.md). Start command (AGENTS.md):
  cd services/remotion-studio && setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor \
    ACTIVE_PIPELINE_CONFIG_PATH=$(pwd)/../../pipeline/pipeline-config.json npx tsx src/server.ts \
    > /tmp/remotion-server.log 2>&1 &
MAX_CONCURRENT_JOBS=1 (never raise); never run two remotion-renderer containers at once.
Finished MP4 served via the Studio proxy GET /api/result/:jobId (quality-finalizer/output.mp4 upstream).
gstatic-block techniques (RESEARCH Wave-0 / VALIDATION): `--add-host fonts.gstatic.com:127.0.0.1`
  on the renderer container, or run the render with the renderer's network egress to gstatic blocked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-rebuild parity check + renderer image rebuild</name>
  <files>services/remotion-renderer/Dockerfile, services/remotion-renderer/public/fonts/</files>
  <read_first>
    - services/remotion-renderer/src/fonts.ts and services/remotion-studio/src/fonts.ts (confirm Plan 03 parity survived — both have the withTimeout chain, no degraded monospace return)
    - services/remotion-renderer/Dockerfile (confirm COPY public/ is present from Plan 03)
    - docker-compose.yml (remotion-renderer build context + image name)
    - .planning/phases/23-render-execution-progress/23-RESEARCH.md (Runtime State Inventory: "a stale image still hits gstatic")
  </read_first>
  <action>
    First, re-run the renderer vitest suite to confirm the Plan 03 font rewrite is intact and the
    clobber hazard did not revert it: `cd services/remotion-renderer && npx vitest run`. Confirm
    services/remotion-renderer/public/fonts/ contains at least PlusJakartaSans-Regular.woff2 and
    Inter-Regular.woff2, and that the Dockerfile has `COPY public/`. Then rebuild ONLY the renderer
    image so the vendored fonts are baked in: `docker compose build remotion-renderer` (the build
    context + image name are in docker-compose.yml). Do NOT rebuild unrelated services. This step is
    required because vendored fonts in public/ only take effect inside the running image after a build
    (RESEARCH: a stale image still hits gstatic).
  </action>
  <verify>
    <automated>cd services/remotion-renderer && npx vitest run && grep -c "COPY public/" Dockerfile && ls public/fonts/PlusJakartaSans-Regular.woff2 public/fonts/Inter-Regular.woff2</automated>
  </verify>
  <acceptance_criteria>
    - Renderer vitest exits 0 (font chain intact, clobber hazard did not revert it).
    - `public/fonts/PlusJakartaSans-Regular.woff2` and `public/fonts/Inter-Regular.woff2` exist.
    - `docker compose build remotion-renderer` completes successfully (image rebuilt with public/fonts baked in).
  </acceptance_criteria>
  <done>Renderer font tests green, parity confirmed, and the renderer image is rebuilt with vendored fonts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real render smoke through the Studio (RENDER-01..04)</name>
  <files>(none — runtime verification of the deployed Studio + pipeline)</files>
  <what-built>
    The Studio render flow: POST /api/render → POST /batch (queue) → live /api/status polling →
    inline progress (step name + honest %) → inline "Reel listo" with proxied play + download, or
    inline failure with the cause line. Plus the @remotion/player background driven by the uploaded MP4.
  </what-built>
  <action>
    Pause for human verification of the full render flow against a live stack. Do not auto-advance —
    this gate confirms RENDER-01..04 end-to-end behavior that no unit test covers. Bring up the stack,
    open the Studio on port 3123, run a real short-clip render, and confirm each behavior in
    how-to-verify. Record the result in the SUMMARY.
  </action>
  <how-to-verify>
    1. Bring up the stack: `docker compose up -d` (Redis, api-server on 3000, remotion-renderer image
       from Task 1). Confirm api-server is reachable internally and the Studio is on port 3123.
    2. Open the Studio at http://localhost:3123 (basic-auth if configured).
    3. Upload a SHORT MP4 (a talking-head clip, ideally < ~30s to keep the render quick). Confirm the
       uploaded video becomes the live preview background (D-02) and the preview-sync note is shown.
    4. Click "▶ Render Video". Confirm:
       - the stage dims, controls disable, a second submit is blocked;
       - the progress surface shows the Spanish step name (Transcribiendo → Cortando silencios →
         Formato vertical 9:16 → Renderizando → Afinando calidad → Exportando subtítulos) and an
         honest % + "paso n/6";
       - on the long "Renderizando" step, the "este paso toma más tiempo" hint + shimmer appear (no faked movement).
    5. On success: confirm "✓ Reel listo", the finished 9:16 plays inline (seek works — Range), and
       "⤓ Descargar" (accent/blue, NOT green) downloads the MP4. Confirm "Renderizar de nuevo" is the green CTA.
    6. (Optional) Force a failure (e.g. an OOM-prone large clip) and confirm the inline danger surface
       shows "No se pudo generar el reel" + the muted-mono cause line (e.g. `remotion-renderer · exit
       137 — sin memoria`) + "↻ Reintentar".
  </how-to-verify>
  <verify>
    <human-check>The full upload → live progress → inline play+download flow works on port 3123.</human-check>
  </verify>
  <resume-signal>Type "approved" if the full flow works (upload → live progress → inline play+download), or describe the failures observed.</resume-signal>
  <done>Human confirms the Studio renders a real clip end-to-end with live progress and inline play+download.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: gstatic-blocked render proof (RENDER-05) + impeccable visual sign-off</name>
  <files>(none — runtime + visual verification)</files>
  <what-built>
    Font resilience: the renderer now loads vendored woff2 first, bounds each font load with a ~10s
    timeout, and falls back to a bundled sans (Plus Jakarta Sans) — never monospace. Plus the four
    inline Studio surfaces built through the impeccable + frontend-design gate.
  </what-built>
  <action>
    Pause for human verification of the RENDER-05 guarantee against a real network failure and the
    mandatory visual quality bar. Do not auto-advance. Block gstatic for the renderer, run a render,
    and confirm completion with a non-monospace font; then review the four inline surfaces against
    23-UI-SPEC.md and the sketch grammar. Record results + any gap-closure todos in the SUMMARY.
  </action>
  <how-to-verify>
    RENDER-05 proof:
    1. Block gstatic for the renderer (e.g. recreate the renderer container with
       `--add-host fonts.gstatic.com:127.0.0.1`, or run the render with the renderer's egress to
       fonts.gstatic.com blocked). Use a config whose font is one of the VENDORED fonts.
    2. Run a render of a short clip. Confirm the render COMPLETES (does not hang on the font fetch,
       does not abort) and the output title/subtitle font is a real sans — NOT monospace.
    3. Repeat with a config whose font is NOT vendored: confirm it either loads via gstatic-retry (if
       reachable) or falls back to the bundled sans — and in NO case renders monospace.
    Visual sign-off (impeccable + frontend-design, AGENTS.md non-negotiable):
    4. Review all four inline surfaces (upload dropzone, live progress, inline success, inline failure)
       against 23-UI-SPEC.md + the sketch grammar: inline-first (no modal), green discipline (Render
       the only green; Descargar accent; failure low-chroma danger), compact `--s-*`/`--t-*` token use,
       calm motion, prefers-reduced-motion collapse. Confirm no improvised colors/sizes.
  </how-to-verify>
  <verify>
    <human-check>gstatic-blocked render completes with a non-monospace font AND the four surfaces pass the visual bar.</human-check>
  </verify>
  <resume-signal>Type "approved" if the gstatic-blocked render completes with a non-monospace font AND the four surfaces pass the visual bar, or describe the gaps (which become gap-closure todos).</resume-signal>
  <done>Human confirms the offline-font guarantee (non-monospace, no hang) and the four surfaces meet the visual quality bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer container → gstatic | Deliberately severed in Task 3 to prove the offline-first guarantee |
| operator → running stack | Manual verification of the deployed behavior; no new code paths introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | Denial of service | gstatic blip aborting/hanging a render | mitigate (verified here) | Task 3 is the live proof that the Plan 03 vendored-first + 10s-timeout chain holds against a real network failure — RENDER-05 closed empirically, not just by unit test. |
| T-23-05-02 | Denial of service | two renderer containers at once | accept (by-design constraint) | Verification runs a single render at a time; `MAX_CONCURRENT_JOBS=1` enforced; never run two remotion-renderer containers simultaneously (AGENTS.md / MEMORY render-parallelism-forbidden). |
| T-23-SC | Tampering | image build pulling deps | mitigate | The build uses the pinned base image + the already-vetted @remotion/fonts@4.0.457 from Plan 03; no new package introduced at this stage. |
</threat_model>

<verification>
- Renderer vitest green post-parity-check (Task 1).
- `docker compose build remotion-renderer` succeeds (Task 1).
- Human-verified: real render smoke lands a playable+downloadable 9:16 (Task 2).
- Human-verified: gstatic-blocked render completes with a non-monospace font (Task 3, RENDER-05).
- Human-verified: four inline surfaces pass the impeccable + frontend-design bar (Task 3).
</verification>

<success_criteria>
- Renderer image rebuilt with vendored fonts; font tests green and parity intact.
- End-to-end Studio render works (upload → live progress → inline play + download).
- RENDER-05 proven against a real gstatic block (non-monospace completion, no hang).
- The four inline surfaces meet the project visual quality bar.
</success_criteria>

<output>
Create `.planning/phases/23-render-execution-progress/23-05-SUMMARY.md` when done — record the smoke
results, the gstatic-blocked render outcome, the visual sign-off, and any gap-closure todos surfaced.
</output>
