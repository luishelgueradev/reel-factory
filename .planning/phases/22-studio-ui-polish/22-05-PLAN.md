---
phase: 22-studio-ui-polish
plan: 05
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - services/remotion-studio/src/editor/components/StyleControls.tsx
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
  - services/remotion-studio/src/editor/components/OverlayEditor.tsx
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-11, D-03]
must_haves:
  truths:
    - "All three editors (Subtitles, Titles, Overlays) consume the shared PositionPresets component (D-03, D-05, D-06, D-07, D-08, D-09, D-11)"
    - "Subtitles consume PositionPresets in ENUM mode (mode=\"enum\"): only bottom-center/top-center/center cells are enabled and map to the SubtitlePosition enum via onApplyAnchor; the old 3-button presets are removed without regressing the bottom-center/top-center/center-screen behavior"
    - "Titles and Overlays consume PositionPresets in PX mode (full 9-cell grid) writing x/y through their existing handleDraftChange path"
    - "OverlayEditor has a Capa (Detrás | Delante) segmented control writing the layer field, default Detrás"
    - "Each editor orders controls Posición → Estilo → Avanzado as always-open titled sections (per UI-SPEC Layout Contract) with the established dark-theme tokens; selection states are blue (green is Render-only)"
    - "Icon-only delete buttons carry Spanish aria-labels and 44px hit areas"
  artifacts:
    - path: "services/remotion-studio/src/editor/components/StyleControls.tsx"
      provides: "PositionPresets (enum mode) in Posición section; old 3-button presets removed; subtitle position path preserved"
      contains: "PositionPresets"
    - path: "services/remotion-studio/src/editor/components/TitleEditor.tsx"
      provides: "PositionPresets (px mode) in Posición; Posición→Estilo→Avanzado ordering; aria-labeled delete"
      contains: "PositionPresets"
    - path: "services/remotion-studio/src/editor/components/OverlayEditor.tsx"
      provides: "PositionPresets (px mode) + Capa layer control; aria-labeled delete"
      contains: "layer"
  key_links:
    - from: "StyleControls (subtitles)"
      to: "PositionPresets.onApplyAnchor → onChange({ position })"
      via: "enum-mode mapping bottom-center/top-center/center-screen"
      pattern: "onApplyAnchor[\\s\\S]*position"
    - from: "TitleEditor / OverlayEditor"
      to: "PositionPresets.onApply"
      via: "px-mode handleDraftChange writing x/y"
      pattern: "PositionPresets[\\s\\S]*onApply"
    - from: "OverlayEditor Capa control"
      to: "PngOverlayConfig.layer"
      via: "handleDraftChange((prev) => ({...prev, layer}))"
      pattern: "layer: \"(back|front)\""
---

<objective>
Wire the shared `PositionPresets` (from 22-02) into all three editors, run the
full impeccable densification + Posición→Estilo→Avanzado reordering pass over
each editor (D-05/D-06/D-11), migrate the subtitle 3-button presets onto the
shared grid in its ENUM mode without regression (D-08), and add the
OverlayEditor `Capa` (Detrás | Delante) layer control that writes the `layer`
field from 22-01 (D-03). Flip selection states from green to blue and add the
required accessible names (UI-SPEC).

Subtitles use the component's enum mode (3 reachable cells → SubtitlePosition);
Titles + Overlays use the full px-grid mode. This is the SINGLE decided path —
the 22-02 contract already reconciled the 9-point grid with the 3-enum model, so
there is no either/or to resolve at build time.

Purpose: Deliver the dense, prioritized, deliberate control panel across the
whole right column — not one component — with the unified position affordance
and the overlay layer toggle.
Output: Three densified editors consuming PositionPresets (subtitles in enum
mode, titles/overlays in px mode); subtitle preset path not regressed; overlay
layer control live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-PATTERNS.md
@.planning/phases/22-studio-ui-polish/22-UI-SPEC.md
@.planning/phases/22-studio-ui-polish/22-02-SUMMARY.md
@.planning/phases/22-studio-ui-polish/22-03-SUMMARY.md

<interfaces>
<!-- PositionPresets (built in 22-02) — the LOCKED dual-mode prop signature to consume -->
<!-- px mode (Titles, Overlays): -->
<PositionPresets
  elementWidth={number} elementHeight={number}
  frameWidth?={1080} frameHeight?={1920}
  onApply={(x, y) => void}
  activeAnchor?={string}
/>
<!-- enum mode (Subtitles) — the DECIDED subtitle migration path: -->
<PositionPresets
  mode="enum"
  anchorToValue={{ "center-bottom": "bottom-center", "center-top": "top-center", "center-center": "center-screen" }}
  onApplyAnchor={(value: SubtitlePosition) => void}
  activeAnchor={config.position ?? "bottom-center"}
/>

<!-- StyleControls.tsx — DECIDED PATH (no either/or): remove the old 3-button block, mount
     <PositionPresets mode="enum">. The subtitle model is the SubtitlePosition enum
     (bottom-center | top-center | center-screen), NOT free x/y. The 9-grid's 3 reachable cells
     map to the enum; the other 6 are disabled by 22-02's enum mode. onApplyAnchor writes the
     enum through the EXISTING onChange({ position }) path — same writes as the old 3 buttons,
     so the working subtitle preview cannot regress. The `anchorToValue` keys are
     `${anchorX}-${anchorY}`: center-bottom→bottom-center, center-top→top-center,
     center-center→center-screen. -->
POSITION_OPTIONS array (L17-21): bottom-center | top-center | center-screen  → REMOVE (replaced by anchorToValue map)
Position selector block (L185-213): the 3 green buttons writing onChange({ position })  → REPLACE with <PositionPresets mode="enum">

<!-- TitleEditor.tsx — px mode. Posición section is the X/Y inputs (L295-341); handleDraftChange L106-110 -->
onApply: (x,y) => handleDraftChange((prev) => ({ ...prev, style: { ...prev.style!, x, y } }))
title size input for elementWidth/Height estimate: style.titleFontSize (L426 default 72) + padding (L495 default 40)
existing x/y writers: L307-311 (x), L329-333 (y)
delete button needing aria-label: L228-233 (✕ → aria-label="Eliminar título")

<!-- OverlayEditor.tsx — px mode. X/Y inputs L300-338; handleDraftChange L72-76; DEFAULT_OVERLAY L27-33 -->
onApply: (x,y) => handleDraftChange((prev) => ({ ...prev, x, y }))
elementWidth = draft.displayWidth (L31 default 200); height auto → nominal estimate
DEFAULT_OVERLAY (L27-33): add `layer: "back"`
Capa control writes: handleDraftChange((prev) => ({ ...prev, layer: "back" | "front" }))
delete button needing aria-label: L220-225 (Delete → aria-label="Eliminar overlay")

<!-- Color law (UI-SPEC): selection/active = var(--accent) BLUE. Green (#4CAF50/var(--action)) is
     Render-CTA ONLY. Existing editors use green for selected segmented buttons — flip to blue. -->
<!-- Section ordering (D-11): Posición → Estilo → Avanzado, consistent across all three.
     DISCLOSURE RESOLUTION: D-11/ROADMAP Crit 2 literally say "rarely-used collapsed/disclosed",
     but the UI-SPEC Layout Contract (L236-243) resolves disclosure to ALWAYS-OPEN titled sections
     — "no collapsible sections, no accordion" (D-06 authorizes the design pass to decide this).
     The UI-SPEC ALWAYS-OPEN rule OVERRIDES the literal "collapsed" wording in D-11/ROADMAP Crit 2.
     Avanzado is a titled, always-visible section — do NOT build an accordion/disclosure widget. -->
<!-- Overlays tab direction: 019-C list-forward inline cards (UI-SPEC § Overlays). Fallback 019-A
     (list + single-column form) IS permitted without further discussion if inline cards read
     off-pattern at build review (UI-SPEC Implementation Note 7). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke impeccable + frontend-design; migrate StyleControls subtitle presets to PositionPresets ENUM mode and reorder Posición→Estilo→Avanzado</name>
  <read_first>
    - AGENTS.md § "UI/frontend work — REQUIRED tooling" (impeccable + frontend-design — invoke FIRST)
    - services/remotion-studio/src/editor/components/StyleControls.tsx (full file — POSITION_OPTIONS L17-21, position block L185-213, all control rows)
    - services/remotion-studio/src/editor/components/PositionPresets.tsx (the dual-mode component built in 22-02 — especially the ENUM mode props: `mode`, `anchorToValue`, `onApplyAnchor`)
    - services/remotion-studio/src/pipeline-config.ts (L12 SubtitlePosition enum — the values the enum mode maps to)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Subtítulos tab" + § "Position Presets" + § "Color" + § "Layout Contract" L236-243 (always-open sections)
    - .planning/phases/22-studio-ui-polish/22-02-SUMMARY.md (PositionPresets contract as actually built, incl. enum mode)
  </read_first>
  <action>
    FIRST invoke `impeccable` + `frontend-design`. In StyleControls.tsx: remove the 3-button Position selector block (L185-213) and the now-unused `POSITION_OPTIONS` array (L17-21), and mount the SHARED `<PositionPresets>` in ENUM mode inside a `Posición` section. This is the SINGLE decided subtitle migration path (D-08) — no either/or. Wire it as:
      `<PositionPresets mode="enum" anchorToValue={{ "center-bottom": "bottom-center", "center-top": "top-center", "center-center": "center-screen" }} activeAnchor={config.position ?? "bottom-center"} onApplyAnchor={(value) => onChange({ position: value })} />`
    The enum mode (built in 22-02) enables exactly the 3 reachable cells and disables the other 6; `onApplyAnchor` writes the SubtitlePosition value through the EXISTING `onChange({ position })` path — i.e. the same writes the old 3 buttons produced — so the working subtitle preview MUST NOT regress (the three working anchors still produce identical `SubtitlePosition` writes through the shared component). Do NOT pass x/y or `onApply` for subtitles. Reorder the StyleControls fields into Posición → Estilo → Avanzado ALWAYS-OPEN titled sections per UI-SPEC Layout Contract (L236-243): Avanzado is an always-open titled section — this OVERRIDES the literal "collapsed" wording in D-11/ROADMAP Crit 2; do NOT build an accordion/disclosure widget. (Posición: PositionPresets + bottomOffset; Estilo: font family, size, weight, color matrix, glow; Avanzado: letterSpacing, lineHeight, pastWordOpacity, highlightDuration/transition, subtitleWidth, outlineWidth). Flip ALL selected/active segmented-button states from green (`#4CAF50` / `rgba(76,175,80,...)` / `#a5d6a7`) to the blue tokens (`var(--accent-tint)` bg, `var(--accent-strong)` border, `var(--accent)` text); slider `accentColor` may stay neutral/accent but NOT action-green. Use default.css tokens for surfaces/text/spacing. Keep all existing `onChange` field writes intact.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'PositionPresets' services/remotion-studio/src/editor/components/StyleControls.tsx` ≥ 1 and it is mounted with `mode="enum"` and `anchorToValue`; `grep -c 'POSITION_OPTIONS' StyleControls.tsx` returns 0 (old array removed).
    - Regression guard (D-08, single path): the enum preset writes the subtitle position via `onChange({ position` (the working `config.position` path is preserved) — grep confirms `onApplyAnchor` is wired to an `onChange({ position` write, so the 3 working anchors produce the same SubtitlePosition writes through the shared component. The old 3-button JSX block (L185-213) is gone.
    - Color law: `grep -c '#4CAF50\|rgba(76, 175, 80' StyleControls.tsx` for SELECTED-state styling is 0 (selection flipped to blue); any remaining green is not a selection state.
    - Section order: the file contains `Posición`/`Estilo`/`Avanzado` section markers in that source order, rendered as always-open titled sections (NO accordion/collapsible/disclosure widget — `grep -ci 'accordion\|collaps' StyleControls.tsx` returns 0 for new disclosure controls).
    - Build: `npm run build:editor` exits 0.
    - Behavioral smoke (preview not regressed): selecting each of the 3 enabled enum cells still drives the live subtitle position — covered by the 22-06 human-verify checklist item "subtitle 9-grid: bottom/top/center cells move subtitles, other 6 cells inert". (No inline render test exists for StyleControls; this is the explicit cross-reference.)
  </acceptance_criteria>
  <done>StyleControls uses the shared PositionPresets in enum mode, old 3-button block + array gone, subtitle position writes preserved through onChange({ position }), sections reordered as always-open, selection blue; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add PositionPresets (px mode) + Posición→Estilo→Avanzado + aria-labels to TitleEditor</name>
  <read_first>
    - services/remotion-studio/src/editor/components/TitleEditor.tsx (full file — X/Y inputs L295-341, handleDraftChange L106-110, delete button L228-233, entrance/size/glow blocks)
    - services/remotion-studio/src/editor/components/PositionPresets.tsx (prop signature — px mode)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Títulos tab" + § "Accessible Name Contract" + § "Color" + § "Layout Contract" L236-243 (always-open sections)
  </read_first>
  <action>
    Mount `<PositionPresets>` (px mode — full 9-cell grid) adjacent to the X/Y inputs (L295-341) inside a `Posición` section, wired `onApply={(x, y) => handleDraftChange((prev) => ({ ...prev, style: { ...prev.style!, x, y } }))}`. Estimate `elementWidth`/`elementHeight` from `newTitle.style?.titleFontSize` (default 72) + `padding` (default 40) — a rendered-box estimate is acceptable. Reorder fields into Posición → Estilo → Avanzado ALWAYS-OPEN titled sections (UI-SPEC Layout Contract L236-243; this overrides the literal "collapsed" wording in D-11 — Avanzado is always-open, NO accordion): Posición = X/Y inputs + PositionPresets; Estilo = title text, font, size, weight/style, color pair, entrance animation, glow; Avanzado = borderRadius, lineHeight, padding, timing (start/duration). Add `aria-label="Eliminar título"` to the ✕/Delete button (L228-233) and ensure a 44×44px hit area; if a drag handle is introduced add `aria-label="Reordenar título"` + `role="button"` (drag reorder is optional this phase — only add the handle if you implement reordering, otherwise skip it). Flip selected segmented states (entrance animation buttons L349-372, weight/style toggles) from green to blue tokens. Keep all existing `handleDraftChange` field writes intact.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'PositionPresets' TitleEditor.tsx` ≥ 1 and it is wired in px mode with `onApply` writing `style: { ...prev.style!, x, y }`.
    - Source assertion: `grep -c 'aria-label="Eliminar título"' TitleEditor.tsx` returns 1.
    - Color law: SELECTED segmented states no longer use `#4CAF50`/`rgba(76, 175, 80`/`#a5d6a7` (flipped to blue var(--accent) tokens).
    - Section order: `Posición`/`Estilo`/`Avanzado` markers present in source order, rendered always-open (no accordion/disclosure widget added).
    - Build: `npm run build:editor` exits 0.
  </acceptance_criteria>
  <done>TitleEditor has a working 9-point px preset writing style.x/y, reordered always-open sections, aria-labeled delete, blue selection; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Add PositionPresets (px mode) + Capa layer control + aria-labels to OverlayEditor</name>
  <read_first>
    - services/remotion-studio/src/editor/components/OverlayEditor.tsx (full file — DEFAULT_OVERLAY L27-33, handleDraftChange L72-76, X/Y inputs L300-338, delete button L220-225, MAX_OVERLAYS cap L22/L167/L416-439)
    - services/remotion-studio/src/editor/components/PositionPresets.tsx (prop signature — px mode)
    - .planning/phases/22-studio-ui-polish/22-01-SUMMARY.md (layer field shape) and 22-04-SUMMARY.md if present (how layer drives paint order — for the badge semantics)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Overlays tab" + § "Accessible Name Contract" + § "Color" + § "Layout Contract" L236-243 (always-open sections)
  </read_first>
  <action>
    Add `layer: "back"` to `DEFAULT_OVERLAY` (L27-33). Mount `<PositionPresets>` (px mode) in the overlay form's position area, `elementWidth={draft.displayWidth}` (height auto → nominal estimate), `onApply={(x, y) => handleDraftChange((prev) => ({ ...prev, x, y }))}`. Add a `Capa` segmented control (2-way `Detrás | Delante`) mirroring the existing segmented-button pattern, writing `handleDraftChange((prev) => ({ ...prev, layer: "back" | "front" }))`; default selection reflects `draft.layer ?? "back"`. On each overlay list card, show a read-only layer badge: `Detrás` muted (`var(--text-muted)` / `var(--surface-2)`), `Delante` blue (`var(--accent)` / `var(--accent-tint)`). Add `aria-label="Eliminar overlay"` to the Delete button (L220-225) with a 44px hit area; if a drag handle is added for reorder, `aria-label="Reordenar overlay"` + `role="button"` (reorder optional this phase). Apply the impeccable density pass with Posición → Estilo → Avanzado as ALWAYS-OPEN titled sections (UI-SPEC Layout Contract L236-243; overrides the literal "collapsed" wording in D-11 — NO accordion). Prefer the UI-SPEC 019-C list-forward inline-card direction, but the 019-A fallback (list + single-column form) is explicitly permitted if inline cards read off-pattern (UI-SPEC Note 7) — pick one and keep it coherent with Titles/Subtitles. Flip selected segmented states green→blue. Preserve the 3-overlay cap, 5MB/PNG file gate, and all existing field writes.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm run build:editor 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'layer: "back"' OverlayEditor.tsx` ≥ 1 (DEFAULT_OVERLAY) and a Capa control writes both `layer: "back"` and `layer: "front"`.
    - Source assertion: `grep -c 'PositionPresets' OverlayEditor.tsx` ≥ 1 wired in px mode with `onApply` writing `{ ...prev, x, y }`.
    - Source assertion: `grep -c 'aria-label="Eliminar overlay"' OverlayEditor.tsx` returns 1.
    - Source assertion: a `Capa` label and `Detrás`/`Delante` strings are present.
    - Color law: SELECTED segmented states (incl. Capa) use blue tokens, not `#4CAF50`/green selection.
    - Section order: always-open Posición/Estilo/Avanzado (no accordion/disclosure widget added).
    - Cap preserved: `MAX_OVERLAYS = 3` and the `atCap` disabled add path are intact.
    - Build: `npm run build:editor` exits 0.
  </acceptance_criteria>
  <done>OverlayEditor has a 9-point px preset, a Capa Detrás|Delante control writing layer (default Detrás), layer badges, aria-labeled delete, cap+file-gate preserved, always-open sections, blue selection; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → editor draft → PUT /api/config | Title/overlay/subtitle config edits (incl. new layer + preset-driven x/y + enum-mapped subtitle position) persist via the existing config save path. No new network surface; same boundary as Phase 20/21. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Tampering | preset-written x/y + layer + subtitle position | mitigate | x/y are bounded integers (existing number inputs clamp 0–1080/0–1920); subtitle enum mode emits only the 3 mapped SubtitlePosition values (validated at config load, VALID_POSITIONS); `layer` is enum-validated at 22-01's validator before persist. |
| T-22-10 | Repudiation/Accessibility gap | icon-only delete/drag controls | mitigate | Required aria-labels added (Eliminar/Reordenar) per UI-SPEC Accessible Name Contract — keyboard/SR users get named controls. |
| T-22-11 | Information disclosure | editor UI | accept | Internal Studio screen behind existing basic auth; edits are local config only, no PII. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (consumes existing PositionPresets + React deps). |
</threat_model>

<verification>
- All three editors consume PositionPresets (subtitles enum mode, titles/overlays px mode); subtitle path not regressed.
- OverlayEditor writes layer (default back) via Capa control; badges correct.
- Selection states blue; Render remains the only green.
- Avanzado is always-open (no accordion); aria-labels present; build green.
</verification>

<success_criteria>
- The whole right-panel control surface is densified, reordered (Posición→Estilo→Avanzado, always-open), uses the shared dual-mode PositionPresets (enum for subtitles, px for titles/overlays), and the overlay layer toggle is wired — matching the UI-SPEC per-tab contracts. D-07 (9-point grid canonical for titles/overlays) AND D-08 (subtitles migrated onto the same shared component) are both satisfied with no contradiction.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-05-SUMMARY.md` when done.
</output>
