---
phase: 06-animated-intros-outros
plan: 05
type: execute
wave: 4
depends_on: [06-03, 06-04]
files_modified:
  - services/remotion-studio/src/editor/App.tsx
  - services/remotion-studio/src/editor/components/LayoutSelector.tsx
  - services/remotion-studio/src/editor/components/StyleControls.tsx
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
  - services/remotion-studio/src/editor/components/ConfigPreview.tsx
  - services/remotion-studio/src/editor/index.html
  - services/remotion-studio/src/editor/index.tsx
  - services/remotion-studio/src/server.ts
  - services/remotion-renderer/src/validate.ts
  - scripts/test-remotion-studio.sh
autonomous: true
requirements: [VISU-01, VISU-02]

must_haves:
  truths:
    - "Config editor React SPA allows selecting subtitle layout mode (D-16)"
    - "Config editor allows changing subtitle style parameters (colors, fonts, position) (D-16)"
    - "Config editor allows adding/editing/removing title overlays (D-16)"
    - "Config changes are written to pipeline-config.json via PUT /api/config (D-16)"
    - "Remotion Studio shows live preview of config changes (D-15)"
    - "Render trigger button sends request to start production render (D-20 scaffolding)"
    - "Validation module checks all Phase 6 requirements"
  artifacts:
    - path: "services/remotion-studio/src/editor/App.tsx"
      provides: "Main config editor React SPA component"
      exports: ["App"]
    - path: "services/remotion-studio/src/editor/components/LayoutSelector.tsx"
      provides: "Layout mode selection UI (TikTok, Sentence, Bar, Karaoke)"
    - path: "services/remotion-studio/src/editor/components/StyleControls.tsx"
      provides: "Subtitle style controls (colors, font, size, position, background highlight)"
    - path: "services/remotion-studio/src/editor/components/TitleEditor.tsx"
      provides: "Title overlay editor (add/edit/remove titles with timing)"
    - path: "services/remotion-studio/src/editor/components/ConfigPreview.tsx"
      provides: "Config JSON preview showing current pipeline-config.json"
    - path: "services/remotion-renderer/src/validate.ts"
      provides: "Updated validation module checking VISU-01, VISU-02 requirements"
      contains: "VISU-01\|VISU-02"
    - path: "scripts/test-remotion-studio.sh"
      provides: "E2E Docker test for studio container"
  key_links:
    - from: "services/remotion-studio/src/editor/App.tsx"
      to: "PUT /api/config"
      via: "fetch call to save config changes"
      pattern: "fetch.*api/config"
    - from: "services/remotion-studio/src/editor/components/LayoutSelector.tsx"
      to: "pipeline-config.ts"
      via: "SubtitleLayoutMode values"
      pattern: "tiktok|sentence|bar|karaoke"
    - from: "services/remotion-studio/src/editor/components/TitleEditor.tsx"
      to: "pipeline-config.ts"
      via: "TitleConfig schema"
      pattern: "startTimeMs|durationMs"
---

<objective>
Build the config editor React SPA that provides the web UI for selecting subtitle layouts, styling, and title overlays, and update the validation module for Phase 6 requirements.

Purpose: Deliver the web UI (D-16) that lets users interactively configure subtitle styles and title parameters, see live preview via Remotion Studio, and trigger renders. Also deliver E2E validation (VISU-01, VISU-02) confirming the entire config-driven pipeline works end-to-end.

Output: Config editor SPA, render trigger, validation module updates, E2E test.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-animated-intros-outros/06-03-SUMMARY.md
@.planning/phases/06-animated-intros-outros/06-04-SUMMARY.md
@.planning/phases/06-animated-intros-outros/06-CONTEXT.md

@services/remotion-studio/src/server.ts
@services/remotion-renderer/src/validate.ts
@services/remotion-renderer/src/pipeline-config.ts
</context>

<interfaces>
<!-- From Plan 01 — PipelineConfig schema -->

From services/remotion-renderer/src/pipeline-config.ts:
```typescript
export type SubtitleLayoutMode = 'tiktok' | 'sentence' | 'bar' | 'karaoke';
export type SubtitlePosition = 'bottom-center' | 'top-center' | 'center-screen';
export interface BackgroundHighlight {
  enabled: boolean; color: string; padding: number; borderRadius: number;
}
export interface SubtitleConfig {
  layout?: SubtitleLayoutMode;
  fontFamily?: string;
  fontSize?: number;
  activeColor?: string;
  inactiveColor?: string;
  outlineColor?: string;
  outlineWidth?: number;
  backgroundHighlight?: BackgroundHighlight;
  textShadow?: { enabled: boolean; color?: string; blur?: number; offsetX?: number; offsetY?: number };
  letterSpacing?: number;
  position?: SubtitlePosition;
}
export interface TitleStyleProps {
  entranceAnimation: 'slide-up' | 'fade-in' | 'none';
  backgroundColor: string;
  textColor: string;
}
export interface TitleConfig {
  text: string; subtitle?: string; startTimeMs: number; durationMs: number; style?: TitleStyleProps;
}
export interface PipelineConfig {
  subtitle: SubtitleConfig;
  titles?: TitleConfig[];
}
```

<!-- From Plan 04 — Studio server endpoints -->
From services/remotion-studio/src/server.ts:
```typescript
// GET /api/config — reads pipeline-config.json, returns JSON
// PUT /api/config — validates and writes pipeline-config.json
// POST /api/render — placeholder for render trigger (returns 501)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build config editor React SPA with layout selector, style controls, and title editor (D-16, D-20)</name>
  <files>services/remotion-studio/src/editor/App.tsx, services/remotion-studio/src/editor/components/LayoutSelector.tsx, services/remotion-studio/src/editor/components/StyleControls.tsx, services/remotion-studio/src/editor/components/TitleEditor.tsx, services/remotion-studio/src/editor/components/ConfigPreview.tsx, services/remotion-studio/src/editor/index.html, services/remotion-studio/src/editor/index.tsx, services/remotion-studio/src/server.ts</files>
  <read_first>
    services/remotion-studio/src/server.ts
    services/remotion-renderer/src/pipeline-config.ts
  </read_first>
  <acceptance_criteria>
    - "Config editor SPA loads in browser at the studio's /editor endpoint"
    - "LayoutSelector shows 4 options: TikTok, Sentence, Bar, Karaoke with radio or dropdown (D-04)"
    - "StyleControls shows color pickers for activeColor/inactiveColor/outlineColor, font selector from AVAILABLE_FONTS, fontSize slider, position selector"
    - "TitleEditor allows adding titles with text, subtitle, startTimeMs, durationMs, and style (D-12)"
    - "TitleEditor allows removing and editing existing titles"
    - "ConfigPreview shows current pipeline-config.json as formatted JSON"
    - "Save button calls PUT /api/config with validated config"
    - "Config changes are reflected in Remotion Studio preview via inputProps"
  </acceptance_criteria>
  <action>
    Per D-16 and D-20:

    **1. Create the config editor SPA as a React app served by the studio's Express server.**

    Architecture: The editor is a separate React SPA that the studio Express server serves at `/editor`. It communicates with the server via the `/api/config` endpoints. Remotion Studio runs at the root `/` path.

    **2. Create index.html and index.tsx entry points:**
    - `index.html`: Standard React SPA HTML shell with div#root, minimal CSS for layout
    - `index.tsx`: React 18 createRoot startup

    **3. Create App.tsx:**
    - Main layout: sidebar with config controls, main area with JSON preview
    - State: full PipelineConfig object (subtitle config + titles array)
    - On mount: `GET /api/config` to load current config
    - On save: `PUT /api/config` with validated config
    - Organized in tabs or sections: Layout, Style, Titles

    **4. Create LayoutSelector.tsx:**
    - Radio button group or segmented control for 4 layout modes: TikTok, Sentence, Bar, Karaoke
    - Each option shows layout name and brief description
    - Selection updates `config.subtitle.layout` in state
    - Default: 'tiktok'

    **5. Create StyleControls.tsx:**
    - Active color picker (`<input type="color">` for activeColor)
    - Inactive color picker for inactiveColor
    - Outline color picker for outlineColor
    - Font selector dropdown populated from AVAILABLE_FONTS (Inter, Roboto, Montserrat, Oswald, monospace)
    - Font size slider or number input (range: 24-120, default: 58)
    - Position selector: bottom-center, top-center, center-screen (D-09)
    - Background highlight toggle: enabled/disabled, color picker, padding, borderRadius (D-08)
    - All changes update PipelineConfig state reactively

    **6. Create TitleEditor.tsx:**
    - Add Title button: opens form with text, subtitle (optional), startTimeMs, durationMs fields
    - Title style section: entranceAnimation dropdown (slide-up, fade-in, none), backgroundColor picker, textColor picker
    - Edit existing title: click to expand, update fields
    - Remove title: delete button with confirmation
    - Duration default: 3000ms for intro titles, configurable
    - Times displayed as seconds (user-friendly) but stored as milliseconds in config

    **7. Create ConfigPreview.tsx:**
    - Read-only JSON view of current PipelineConfig
    - Copy button to copy JSON to clipboard
    - Syntax highlighted or formatted JSON display

    **8. Update server.ts to serve the editor SPA:**
    - Serve static files from `/editor` path pointing to the built editor SPA
    - The editor SPA is built with a simple bundler (esbuild or Vite)
    - Add build script to package.json: `"build:editor": "vite build src/editor --outDir dist/editor"` (or esbuild equivalent)
    - Express static middleware: `app.use('/editor', express.static('dist/editor'))`
  </action>
  <verify>
    <automated>ls services/remotion-studio/src/editor/App.tsx services/remotion-studio/src/editor/components/LayoutSelector.tsx services/remotion-studio/src/editor/components/StyleControls.tsx services/remotion-studio/src/editor/components/TitleEditor.tsx && grep -c "api/config" services/remotion-studio/src/editor/App.tsx && grep -c "tiktok.*sentence.*bar.*karaoke" services/remotion-studio/src/editor/components/LayoutSelector.tsx</automated>
  </verify>
  <done>Config editor SPA with layout mode selection, style controls, title editor, and config preview. Saves config via PUT /api/config. Served at /editor path in studio container.</done>
</task>

<task type="auto">
  <name>Task 2: Update validation module + E2E Docker test for studio (VISU-01, VISU-02)</name>
  <files>services/remotion-renderer/src/validate.ts, scripts/test-remotion-studio.sh</files>
  <read_first>
    services/remotion-renderer/src/validate.ts
    services/remotion-studio/src/server.ts
    services/remotion-renderer/src/pipeline-config.ts
  </read_first>
  <acceptance_criteria>
    - "validate.ts includes VISU-01 check: output video has intro title when pipeline-config has titles with startTimeMs=0"
    - "validate.ts includes VISU-02 check: output video has outro title when pipeline-config has titles near video end"
    - "validate.ts checks LayoutDispatcher renders all 4 layout modes without error"
    - "validate.ts checks PipelineConfig validation (title timing, layout mode values)"
    - "validate.ts checks font loading function is importable"
    - "test-remotion-studio.sh exercises the studio container end-to-end: starts container, writes config, reads config"
    - "test-remotion-studio.sh starts remotion-renderer with pipeline-config.json and produces output"
  </acceptance_criteria>
  <action>
    Per VISU-01, VISU-02, and established validation patterns from prior phases:

    **1. Update validate.ts to add Phase 6 requirement checks:**

    Add validation functions following the established pattern (individual validate* functions returning string[] of errors with requirement IDs):

    - `validatePipelineConfig(outputDir: string): string[]` — checks that pipeline-config.json exists and is valid JSON when expected (VISU-01, VISU-02)
    - `validateLayoutModes(outputDir: string): string[]` — checks that all layout mode components (TikTokLayout, SentenceLayout, BarLayout, KaraokeLayout) are importable from the composition files. This validates that the LayoutDispatcher can render each mode.
    - `validateTitleOverlays(outputDir: string): string[]` — checks that TitleOverlay component exists and that intro/outro titles work:
      - VISU-01: "Video starts with animated intro title card" — validate that pipeline-config.json with a title at startTimeMs=0 produces a manifest indicating the render succeeded with title overlays
      - VISU-02: "Video ends with animated outro title card" — validate that pipeline-config.json titles array includes an entry with startTimeMs near video end
    - `validateFontInfrastructure(outputDir: string): string[]` — checks that fonts.ts exports AVAILABLE_FONTS with 5 entries and loadFont function exists

    Add these to the main `validate()` export function, returning all errors from each validation function.

    **2. Create test-remotion-studio.sh:**
    Following the established E2E test pattern from test-remotion-renderer.sh and test-ffmpeg-finalizer.sh:

    ```bash
    #!/bin/bash
    # test-remotion-studio.sh — E2E test for remotion-studio container
    set -e
    JOB_ID="test-studio-$(date +%s)"
    PIPELINE_DIR="/data/pipeline/$JOB_ID"
    # ... setup test data, create pipeline-config.json, run containers, validate
    ```

    Test steps:
    1. Create test job directory structure
    2. Create a test pipeline-config.json with: subtitle layout mode "tiktok", title at startTimeMs=0 with 3s duration, and style parameters
    3. Run the remotion-renderer container with the test pipeline-config.json
    4. Check output: manifest.json status="success", output video exists
    5. Run the remotion-studio container and verify /api/config GET returns the config
    6. PUT a new config to /api/config and verify GET returns updated config
    7. Print TEST_PASSED/TEST_FAILED counter
  </action>
  <verify>
    <automated>grep -c "VISU-01\|VISU-02" services/remotion-renderer/src/validate.ts && ls scripts/test-remotion-studio.sh && grep -c "pipeline-config" scripts/test-remotion-studio.sh</automated>
  </verify>
  <done>Validation module checks VISU-01 (intro title), VISU-02 (outro title), LayoutDispatcher modes, PipelineConfig, and font infrastructure. E2E test validates studio container config API and renderer with pipeline-config.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Config editor SPA | User input for subtitle styling and title configuration |
| Config editor → PUT /api/config | Config write to shared volume |
| Config editor → Studio preview | Remotion Studio reads inputProps from config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-12 | Cross-Site Scripting | Config editor SPA | mitigate | React defaults prevent XSS; sanitize title text before rendering |
| T-06-13 | Injection | PUT /api/config | mitigate | Validate JSON against PipelineConfig schema with zod before writing |

</threat_model>

<verification>
1. Editor SPA components exist: `ls services/remotion-studio/src/editor/{App.tsx,components/LayoutSelector.tsx,components/StyleControls.tsx,components/TitleEditor.tsx}`
2. Config API calls from editor: `grep "api/config" services/remotion-studio/src/editor/App.tsx`
3. Layout mode selection: `grep "tiktok" services/remotion-studio/src/editor/components/LayoutSelector.tsx`
4. Validation checks VISU-01/02: `grep "VISU-01\|VISU-02" services/remotion-renderer/src/validate.ts`
5. E2E test exists: `ls scripts/test-remotion-studio.sh`
</verification>

<success_criteria>
- Config editor SPA allows selecting layout modes (TikTok, Sentence, Bar, Karaoke) (D-16)
- Config editor allows changing style parameters (colors, fonts, position) (D-16)
- Config editor allows adding/editing/removing title overlays (D-16)
- Config changes persist via PUT /api/config to pipeline-config.json (D-16)
- Remotion Studio shows live preview of config changes (D-15)
- Validation module checks VISU-01 and VISU-02 (VISU-01, VISU-02)
- E2E test validates studio container and config API (VISU-01, VISU-02)
</success_criteria>

<output>
After completion, create `.planning/phases/06-animated-intros-outros/06-05-SUMMARY.md`
</output>