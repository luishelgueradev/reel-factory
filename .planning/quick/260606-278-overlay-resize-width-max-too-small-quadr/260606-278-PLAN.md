# Quick 260606-278: Quadruple overlay resize width max

**Problem:** The "Ancho px" slider for resizing PNG overlays in the Studio caps at
540px — too small to scale overlays large enough on the 1080-wide frame.

**Change:** `RangeRow label="Ancho px"` max 540 → 2160 (×4) in OverlayEditor.tsx.

## Task
- **File:** services/remotion-studio/src/editor/components/OverlayEditor.tsx (line ~392)
- **Action:** `max={540}` → `max={2160}`
- **Verify:** `displayWidth` validator only requires `> 0` (no upper bound) — 2160 valid; no test asserts 540 for overlay width; OverlayEditor is studio-only (no renderer sync). `npx vitest run src/editor/components` green.
- **Done:** slider allows widths up to 2160px.
