---
phase: 26-ui-convergence
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - services/remotion-studio/src/editor/index.html
  - services/remotion-studio/src/editor/components/StyleControls.tsx
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
  - services/remotion-studio/src/editor/components/OverlayEditor.tsx
autonomous: true
requirements: [UICONV-01]
must_haves:
  truths:
    - "At a narrow width (~360px) the tab form 2-col grids collapse to 1-col and the preset-card rows reflow (sketch 018-B), without horizontal overflow"
    - "The reflow uses CSS @media (in the studio stylesheet) targeting form-grid/card-grid classes the form sections carry — not JS width hacks"
    - "Desktop layout (~1280px) is unchanged; metadata column hides <1024px (confirm existing behavior is intact)"
    - "prefers-reduced-motion remains honored"
  artifacts: []
  key_links: []
---

<objective>
Close the responsive gap (sketch 018-B): the editor tab forms reflow gracefully at narrow
widths — 2-col → 1-col, preset-card rows wrap to a compact grid — with no horizontal overflow,
while desktop is untouched. CSS-only (no behavior change).
</objective>

<context>
@.planning/phases/26-ui-convergence/26-CONTEXT.md
@.planning/phases/26-ui-convergence/26-UI-SPEC.md
@.planning/phases/26-ui-convergence/26-RESEARCH.md
- Sketch `.planning/sketches/018-dense-tabs-at-breakpoint/` and `.planning/sketches/047-shell-responsive-ladder/` define the reflow rule.
- The studio stylesheet lives in services/remotion-studio/src/editor/index.html (<style> block, where col3-metadata visibility is handled). Add @media rules there.
- The form sections use inline style objects today; to drive @media, give the form grids + card grids stable className hooks (e.g. className="rf-form-grid" / "rf-card-grid") and add the @media rules in the stylesheet to override grid-template-columns at the breakpoint. Keep the inline styles as the desktop default.
</context>

<ui_quality_mandate>
Invoke `impeccable`; use `frontend-design`; follow sketch 018-B / 047 reflow ladder. Converge, don't redesign.
</ui_quality_mandate>

<tasks>

<task type="auto">
  <name>Task 1: form/card grid className hooks</name>
  <files>services/remotion-studio/src/editor/components/StyleControls.tsx, services/remotion-studio/src/editor/components/TitleEditor.tsx, services/remotion-studio/src/editor/components/OverlayEditor.tsx</files>
  <action>
    Add stable className hooks to the 2-col form grids (e.g. `rf-form-grid`) and the preset-card grids from 26-02 (e.g. `rf-card-grid`) across the three editors. Keep the existing inline grid styles as the desktop default (the className just gives @media something to target). No layout change at desktop.
  </action>
  <verify><automated>cd services/remotion-studio && grep -rn "rf-form-grid\|rf-card-grid" src/editor/components/ | head</automated></verify>
  <acceptance_criteria>
    - The form + card grids carry stable class hooks; desktop rendering unchanged.
  </acceptance_criteria>
  <done>Grids are addressable by CSS.</done>
</task>

<task type="auto">
  <name>Task 2: @media reflow rules + verify</name>
  <files>services/remotion-studio/src/editor/index.html</files>
  <action>
    In the studio <style> block add `@media (max-width: 380px)` rules: `.rf-form-grid { grid-template-columns: 1fr !important; }` and `.rf-card-grid { grid-template-columns: repeat(2, 1fr) !important; }` (cards to 2-up), plus any needed gap tightening with --s-* tokens. Confirm the existing `.col3-metadata` hide-below-1024px rule is intact. Ensure no horizontal overflow at 360px.
  </action>
  <verify><automated>cd services/remotion-studio && grep -nE "@media|rf-form-grid|rf-card-grid|col3-metadata" src/editor/index.html | head && npm run build:editor 2>&1 | tail -2 && npx vitest run 2>&1 | tail -4</automated></verify>
  <acceptance_criteria>
    - @media reflow present; build OK; full suite green; col3 hide rule intact.
  </acceptance_criteria>
  <done>Forms reflow to 1-col + cards to 2-up at ~360px; desktop unchanged.</done>
</task>

</tasks>

<verification>
- `npm run build:editor` OK; `npx vitest run` green; @media rules present; no overflow at 360px (confirm via the orchestrator's Playwright screenshot at narrow width).
</verification>

<success_criteria>
- The editor tab forms reflow cleanly at ~360px (2-col→1-col, cards 2-up) per 018-B, with desktop untouched and the metadata column hiding <1024px.
</success_criteria>

<output>
Create `.planning/phases/26-ui-convergence/26-03-SUMMARY.md` when done.
</output>
