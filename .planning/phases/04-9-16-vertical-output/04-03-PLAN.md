---
phase: 04-9-16-vertical-output
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - scripts/test-ffmpeg-finalizer.sh
  - docker-compose.yml
requirements: [VERT-01, VERT-02, VERT-03]
autonomous: false

must_haves:
  truths:
    - "An MP4 processed through the complete ffmpeg-finalizer Docker container produces a 1080x1920 output (VERT-01)"
    - "Wide input videos get center-cropped in the Docker pipeline (VERT-02)"
    - "finalizer-info.json contains safe zone metadata for Phase 5 subtitle positioning (VERT-03)"
    - "manifest.json is written with correct status and output paths following step contract"
  artifacts:
    - path: "scripts/test-ffmpeg-finalizer.sh"
      provides: "E2E Docker test script for ffmpeg-finalizer step contract"
      contains: "docker compose run"
    - path: "docker-compose.yml"
      provides: "Updated ffmpeg-finalizer service health check and dependencies"
  key_links:
    - from: "scripts/test-ffmpeg-finalizer.sh"
      to: "services/ffmpeg-finalizer/"
      via: "docker compose build + run"
      pattern: "docker compose (build|run).*ffmpeg-finalizer"

---

<objective>
End-to-end Docker validation of the complete FFmpeg finalizer pipeline — from MP4 input to 9:16 output with safe zone metadata — verifying all step contract requirements and VERT success criteria.

Purpose: VERT-01, VERT-02, and VERT-03 success criteria must be TRUE after this plan. The E2E test proves the Docker container produces correct output with manifest, finalizer-info.json, and proper 9:16 dimensions.

Output: E2E test script, updated docker-compose.yml with health check, verified Docker pipeline.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-9-16-vertical-output/04-CONTEXT.md

<!-- Prior plan context -->
@.planning/phases/04-9-16-vertical-output/04-01-SUMMARY.md
@.planning/phases/04-9-16-vertical-output/04-02-SUMMARY.md

<!-- Reference E2E pattern from phase 3 -->
@.planning/phases/03-silence-detection-removal/03-03-SUMMARY.md

<!-- Existing services -->

From docker-compose.yml (ffmpeg-finalizer service):
```yaml
ffmpeg-finalizer:
  <<: *pipeline-common
  build:
    context: ./services/ffmpeg-finalizer
  depends_on:
    base-python:
      condition: service_completed_successfully
  environment:
    - INPUT_PATH=${FINALIZER_INPUT_PATH:-/data/pipeline/${PIPELINE_JOB_ID}/silence-cutter/output.mp4}
    - OUTPUT_PATH=/data/pipeline/${PIPELINE_JOB_ID}/ffmpeg-finalizer/output.mp4
    - PIPELINE_JOB_ID=${PIPELINE_JOB_ID}
    - VERTICAL_WIDTH=${VERTICAL_WIDTH:-1080}
    - VERTICAL_HEIGHT=${VERTICAL_HEIGHT:-1920}
    - CROP_STRATEGY=${CROP_STRATEGY:-center}
```

From services/ffmpeg-finalizer/Dockerfile:
```dockerfile
FROM video-pipeline-base-python:latest
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
COPY src/ src/
CMD ["python", "main.py"]
```

Step contract (from Phase 1 CONTEXT.md, D-05/D-06/D-07):
- INPUT_PATH env var for input file
- OUTPUT_PATH env var for output directory
- manifest.json in output directory with status/output_files/duration
- Exit codes: 0=success, 1=error, 2+=partial
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add health check to docker-compose.yml and update finalizer service</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml, .planning/phases/04-9-16-vertical-output/04-CONTEXT.md</read_first>
  <action>
Update the `ffmpeg-finalizer` service in `docker-compose.yml`:

1. Add a health check that verifies manifest.json exists in the output directory:
```yaml
healthcheck:
  test: ["CMD", "test", "-f", "/data/pipeline/${PIPELINE_JOB_ID}/ffmpeg-finalizer/manifest.json"]
  interval: 5s
  timeout: 10s
  retries: 3
  start_period: 15s
```

2. Verify the `INPUT_PATH` default points to silence-cutter output (it already does: `${FINALIZER_INPUT_PATH:-/data/pipeline/${PIPELINE_JOB_ID}/silence-cutter/output.mp4}`).

3. Keep the existing environment variables (VERTICAL_WIDTH, VERTICAL_HEIGHT, CROP_STRATEGY) as-is since they already have defaults.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory && grep -A5 "healthcheck" docker-compose.yml | grep -c "ffmpeg-finalizer\|manifest.json" && grep -c "ffmpeg-finalizer" docker-compose.yml</automated>
  </verify>
  <done>docker-compose.yml has health check for ffmpeg-finalizer service that checks manifest.json existence</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E Docker test script for FFmpeg finalizer step contract</name>
  <files>scripts/test-ffmpeg-finalizer.sh</files>
  <read_first>docker-compose.yml, services/ffmpeg-finalizer/src/config.py, services/ffmpeg-finalizer/main.py, .planning/phases/04-9-16-vertical-output/04-CONTEXT.md</read_first>
  <action>
Create `scripts/test-ffmpeg-finalizer.sh` following the pattern from the silence-cutter E2E test (referenced in 03-03-SUMMARY.md).

The script must:

1. Set `PIPELINE_JOB_ID=test-finalizer-$(date +%s)` for unique job ID
2. Create test input directory: `mkdir -p pipeline/${PIPELINE_JOB_ID}/{input,ffmpeg-finalizer}`
3. Create a synthetic test video using FFmpeg (wider than 9:16 to test the crop path):
   ```bash
   ffmpeg -y -f lavfi -i testsrc=duration=5:size=1920x1080:rate=30 \
     -f lavfi -i sine=frequency=440:duration=5 \
     -c:v libx264 -pix_fmt yuv420p -c:a aac \
     pipeline/${PIPELINE_JOB_ID}/input/video.mp4
   ```
4. Build the Docker container: `docker compose build ffmpeg-finalizer`
5. Run the container: `docker compose run --rm -e PIPELINE_JOB_ID=${PIPELINE_JOB_ID} ffmpeg-finalizer`
6. Verify all step contract outputs:
   - Output file exists: `pipeline/${PIPELINE_JOB_ID}/ffmpeg-finalizer/output.mp4`
   - Manifest exists: `pipeline/${PIPELINE_JOB_ID}/ffmpeg-finalizer/manifest.json`
   - Finalizer info exists: `pipeline/${PIPELINE_JOB_ID}/ffmpeg-finalizer/finalizer-info.json`
7. Verify VERT success criteria using ffprobe and jq:
   - VERT-01: `ffprobe -v quiet -show_entries stream=width,height -of csv=p=0 output.mp4` returns `1080,1920`
   - VERT-02: Check `finalizer-info.json` has `crop_strategy: "center"`
   - VERT-03: Check `finalizer-info.json` has `safe_zone` with values `top:100, bottom:230, left:54, right:54`
8. Also test with a second video that's already 9:16 to verify D-03 conditional crop:
   ```bash
   ffmpeg -y -f lavfi -i testsrc=duration=5:size=1080x1920:rate=30 \
     -f lavfi -i sine=frequency=440:duration=5 \
     -c:v libx264 -pix_fmt yuv420p -c:a aac \
     pipeline/${PIPELINE_JOB_ID}/input/video_9_16.mp4
   ```
   Run with INPUT_PATH pointing to this video and verify `crop_applied: false` in finalizer-info.json.
9. Print summary: PASSED/FAILED for each VERT requirement
10. Make the script executable with `chmod +x`
  </action>
  <verify>
    <automated>test -x /home/luis/proyectos/reel-factory/scripts/test-ffmpeg-finalizer.sh && grep -c "VERT-01\|VERT-02\|VERT-03" /home/luis/proyectos/reel-factory/scripts/test-ffmpeg-finalizer.sh && grep -c "1080,1920\|crop_applied\|safe_zone" /home/luis/proyectos/reel-factory/scripts/test-ffmpeg-finalizer.sh</automated>
  </verify>
  <done>E2E test script creates synthetic videos, runs Docker container, verifies output dimensions (VERT-01), crop strategy (VERT-02), safe zones (VERT-03), and conditional crop (D-03)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify E2E FFmpeg finalizer Docker pipeline output</name>
  <files>scripts/test-ffmpeg-finalizer.sh</files>
  <action>Automated E2E test already ran in Task 2. This checkpoint pauses for human visual inspection of the FFmpeg finalizer output.</action>
  <what-built>End-to-end FFmpeg finalizer Docker pipeline: synthetic wide video → 9:16 output with safe zone metadata</what-built>
  <how-to-verify>
    1. Run the E2E test: `cd /home/luis/proyectos/reel-factory && bash scripts/test-ffmpeg-finalizer.sh`
    2. Verify output shows:
       - VERT-01: ✅ Output video is 1080x1920
       - VERT-02: ✅ Crop strategy is "center"
       - VERT-03: ✅ Safe zone metadata present
       - D-03: ✅ 9:16 input gets no crop (crop_applied: false)
    3. Inspect `pipeline/${JOB_ID}/ffmpeg-finalizer/finalizer-info.json` to confirm safe zone values
    4. Play the output video to verify visual quality (no distortion, center crop)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host filesystem → Docker | Test script writes synthetic video to shared volume; Docker container reads it |
| Docker output → Host | Container writes output.mp4 and metadata; host script reads and validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | S | E2E test script | accept | Test script generates synthetic video locally — no external input |
| T-04-06 | D | ffprobe output | accept | ffprobe returns structured data; script validates dimensions numerically |

</threat_model>

<verification>
1. `bash scripts/test-ffmpeg-finalizer.sh` runs without errors
2. Output video is 1080x1920 (VERT-01)
3. finalizer-info.json contains crop_strategy="center" (VERT-02)
4. finalizer-info.json contains safe_zone with correct values (VERT-03)
5. 9:16 input produces crop_applied=false (D-03)
6. manifest.json has status="success"
7. Health check in docker-compose.yml verifies manifest.json
</verification>

<success_criteria>
- E2E test script verifies all VERT success criteria from ROADMAP
- 1080x1920 output confirmed (VERT-01)
- Center-crop strategy confirmed (VERT-02)
- Safe zone metadata confirmed for Phase 5 consumption (VERT-03)
- Step contract fulfilled: manifest.json, finalizer-info.json, output.mp4
- Conditional crop path verified: 9:16 input produces crop_applied=false (D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-9-16-vertical-output/04-03-SUMMARY.md`
</output>