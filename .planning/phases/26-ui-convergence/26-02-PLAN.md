---
phase: 26-ui-convergence
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - services/remotion-studio/src/editor/components/StyleControls.tsx
  - services/remotion-studio/src/editor/components/LayoutSelector.tsx
  - services/remotion-studio/src/editor/components/TitleEditor.tsx
  - services/remotion-studio/src/editor/components/layout-mode-cards.test.tsx
autonomous: true
requirements: [UICONV-01]
must_haves:
  truths:
    - "Subtitles layout modes are presented as blue-active preset cards leading the form (TabLead, sketch 011-C) — not a green/radio LayoutSelector"
    - "Titles entrance animation is presented as preset cards (sketch 014-C) leading/above the timing inputs"
    - "Cards use --accent active state (blue), token spacing/type, and preserve ALL existing config fields + behavior (layout mode value, entrance value)"
    - "Cards are STATIC (no live animation — deferred per D-03); selecting a card sets the same config it set before"
  artifacts:
    - "services/remotion-studio/src/editor/components/layout-mode-cards.test.tsx"
  key_links: []
---

<objective>
Deepen north-star convergence: turn the Subtitles layout-mode control (currently a radio-style
LayoutSelector) and the Titles entrance-animation control (currently flat inputs) into the
validated **preset-card** pattern that leads the form (sketches 011-C and 014-C), with blue
active state. STATIC cards (no live animation — out of scope). No config/behavior change —
same values set, just the validated presentation.
</objective>

<context>
@.planning/phases/26-ui-convergence/26-CONTEXT.md
@.planning/phases/26-ui-convergence/26-UI-SPEC.md
@.planning/phases/26-ui-convergence/26-RESEARCH.md
- Read the sketches for the exact card grammar: `.planning/sketches/011-subtitle-style-density/` (layout-mode cards) and `.planning/sketches/014-title-style-density/` (entrance cards). Match their card layout (label + tiny glyph/specimen + selected ring), promoted ABOVE the 2-col form.
- StyleControls.tsx renders LayoutSelector + the style form; TitleEditor.tsx renders the entrance/timing inputs. Preserve the underlying config fields exactly (subtitle.layout enum; title entranceAnimation/timing).
- Active state = --accent (blue) per 26-01's color law. Reuse the existing segmented/card visual language already in the editors (e.g. mode preset cards used elsewhere) for consistency.
</context>

<ui_quality_mandate>
Invoke `impeccable`; use `frontend-design`; read `sketch-findings-reel-factory` (TabLead/TabForm skeleton, specimen mode cards). Follow 011-C / 014-C exactly — do NOT improvise a new card design. Static cards only.
</ui_quality_mandate>

<tasks>

<task type="auto">
  <name>Task 1: Subtitles layout-mode preset cards (011-C)</name>
  <files>services/remotion-studio/src/editor/components/LayoutSelector.tsx, services/remotion-studio/src/editor/components/StyleControls.tsx</files>
  <action>
    Refactor LayoutSelector from radio rows into a row/grid of preset cards (TikTok / Sentence / Bar / Karaoke) per sketch 011-C: each card = mode label + a small static glyph/specimen hint + a blue selected ring (--accent). Promote it to lead the Subtitles form (TabLead position) in StyleControls. Keep the exact `subtitle.layout` value contract + onChange. No live animation. Preserve keyboard a11y (rad:group semantics or buttons with aria-pressed).
  </action>
  <verify><automated>cd services/remotion-studio && npx vitest run 2>&1 | tail -4</automated></verify>
  <acceptance_criteria>
    - Layout modes render as blue-active cards leading the form; selecting sets subtitle.layout as before; no green.
  </acceptance_criteria>
  <done>Subtitles layout modes match the 011-C card grammar.</done>
</task>

<task type="auto">
  <name>Task 2: Titles entrance preset cards (014-C) + tests</name>
  <files>services/remotion-studio/src/editor/components/TitleEditor.tsx, services/remotion-studio/src/editor/components/layout-mode-cards.test.tsx</files>
  <action>
    Present the title entrance animation as preset cards (e.g. Slide↑ / Slide↓ / Fade / Ninguna per the title config's entrance options) leading/above the timing inputs (Aparece/Dura/Velocidad), per sketch 014-C, blue active. Preserve the exact entrance config field + the timing inputs/behavior. Static cards.
    Tests (layout-mode-cards.test.tsx): render StyleControls + TitleEditor; assert the layout-mode cards and entrance cards render, selecting a card calls onChange with the correct config value, the active card uses the accent (not green), and all prior config fields still render.
  </action>
  <verify><automated>cd services/remotion-studio && npx vitest run src/editor/components/layout-mode-cards.test.tsx 2>&1 | tail -6 && npm run build:editor 2>&1 | tail -2</automated></verify>
  <acceptance_criteria>
    - Entrance presents as blue-active cards; config values unchanged; tests green; build OK.
  </acceptance_criteria>
  <done>Titles entrance matches the 014-C card grammar; behavior preserved.</done>
</task>

</tasks>

<verification>
- `npx vitest run` (studio) green; `npm run build:editor` OK; no green on active cards.
</verification>

<success_criteria>
- Subtitles layout modes and Titles entrance render as the validated blue-active preset cards leading the form (011-C/014-C), with identical config behavior — converging control density to north-star.
</success_criteria>

<output>
Create `.planning/phases/26-ui-convergence/26-02-SUMMARY.md` when done.
</output>
