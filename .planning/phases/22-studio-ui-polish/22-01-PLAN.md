---
phase: 22-studio-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/pipeline-config.ts
  - services/remotion-renderer/src/pipeline-config.ts
  - services/remotion-renderer/src/pipeline-config.test.ts
  - services/remotion-renderer/src/compositions/overlay.test.ts
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "PngOverlayConfig carries an optional layer field constrained to 'back' | 'front'"
    - "An overlay with no layer field still validates (back is the implied default)"
    - "An overlay with layer not in {back, front} produces a validation error"
    - "The studio and renderer pipeline-config schemas are byte-identical for PngOverlayConfig + its validator (D-03, D-04)"
  artifacts:
    - path: "services/remotion-studio/src/pipeline-config.ts"
      provides: "PngOverlayConfig.layer field + validator branch"
      contains: "layer?: \"back\" | \"front\""
    - path: "services/remotion-renderer/src/pipeline-config.ts"
      provides: "Mirrored schema + validator"
      contains: "layer?: \"back\" | \"front\""
    - path: "services/remotion-renderer/src/pipeline-config.test.ts"
      provides: "layer validation test cases"
      contains: "layer"
  key_links:
    - from: "services/remotion-studio/src/pipeline-config.ts"
      to: "services/remotion-renderer/src/pipeline-config.ts"
      via: "renderer sync (identical PngOverlayConfig + validator)"
      pattern: "layer\\?: \"back\" \\| \"front\""
---

<objective>
Add the per-overlay `layer: "back" | "front"` field (default `"back"`) to the
`PngOverlayConfig` schema and its validator, in BOTH the studio and renderer
copies of `pipeline-config.ts`, with mirrored validation test cases in the
renderer suite. This is the foundation the layering implementation (22-04) and
the OverlayEditor `Capa` control (22-03) consume (D-03/D-04).

Purpose: Establish the data contract before any consumer reads it, so the
layering split (back-band vs front-band) has a typed field to filter on.
Output: A new optional schema field, validated identically in both services,
covered by renderer vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-PATTERNS.md

<interfaces>
<!-- Current PngOverlayConfig — studio src/pipeline-config.ts L98-105 (renderer L98-104 identical) -->
PngOverlayConfig {
  imageData: string;       // base64 data URL
  x: number;               // px from left of 1080 frame
  y: number;               // px from top of 1920 frame
  displayWidth: number;    // CSS display width px
  opacity?: number;        // 0–1, default 1   ← the exact optional-field precedent
  _resolvedFile?: string;  // runtime-only, NOT persisted
}

<!-- Validator opacity branch — studio + renderer pipeline-config.ts L494-499 (the analog to copy) -->
if (ov.opacity !== undefined) {
  if (typeof ov.opacity !== "number" || ov.opacity < 0 || ov.opacity > 1) {
    errors.push(`overlays[${index}].opacity must be a number between 0 and 1`);
  }
}
<!-- _resolvedFile note follows at L501 (runtime-only, NOT validated) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add layer field + validator to studio pipeline-config.ts, mirror to renderer, add renderer tests</name>
  <read_first>
    - services/remotion-studio/src/pipeline-config.ts (read L90-106 for PngOverlayConfig, L462-502 for the overlays validator block)
    - services/remotion-renderer/src/pipeline-config.ts (confirm L98-104 schema + L462-501 validator are the byte-identical mirror)
    - services/remotion-renderer/src/pipeline-config.test.ts (read the existing overlay validation tests to match style/structure — 23 overlay/layer matches exist)
    - AGENTS.md § "Renderer sync pattern" (pipeline-config.ts is a shared module synced studio→renderer)
  </read_first>
  <behavior>
    - Test: a config with `overlays: [{imageData:"data:...", x:0, y:0, displayWidth:200}]` (no layer) → validatePipelineConfig returns no overlay errors (back is implied).
    - Test: `{...validOverlay, layer: "back"}` → no errors.
    - Test: `{...validOverlay, layer: "front"}` → no errors.
    - Test: `{...validOverlay, layer: "middle"}` → error string `overlays[0].layer must be "back" or "front"`.
    - Test: `{...validOverlay, layer: 3}` (wrong type) → same error.
  </behavior>
  <action>
    In services/remotion-studio/src/pipeline-config.ts, add `layer?: "back" | "front";` to the `PngOverlayConfig` interface (after `opacity?` at L103, before `_resolvedFile?` at L104) with a comment `// D-03: default "back" — decorators behind text`. In the overlays validator block, after the optional-`opacity` branch (L494-499) and before the `_resolvedFile` note (L501), add an analogous optional branch: `if (ov.layer !== undefined) { if (ov.layer !== "back" && ov.layer !== "front") { errors.push(\`overlays[${index}].layer must be "back" or "front"\`); } }`. The field is OPTIONAL — a missing `layer` MUST NOT error (back is the runtime default, applied by consumers in 22-04, not by the validator). Then sync the identical change to services/remotion-renderer/src/pipeline-config.ts (same interface line, same validator branch — the file is the renderer mirror per AGENTS.md). Add the 5 behavior test cases to services/remotion-renderer/src/pipeline-config.test.ts following the existing overlay-test structure. Do NOT add the test to a studio test file — studio has no pipeline-config.test.ts (renderer vitest is the suite of record per CLAUDE.md renderer-sync convention).
  </action>
  <verify>
    <automated>cd services/remotion-renderer && npm test -- pipeline-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'layer?: "back" | "front"' services/remotion-studio/src/pipeline-config.ts` returns 1 AND same grep on the renderer copy returns 1.
    - Source assertion: `grep -F 'overlays[${index}].layer must be "back" or "front"' services/remotion-studio/src/pipeline-config.ts` and the renderer copy both match.
    - Test command: `cd services/remotion-renderer && npm test -- pipeline-config.test.ts` passes including the 5 new layer cases.
    - Behavior: a missing-layer overlay validates with zero overlay errors (regression guard — Phase 21 configs must keep validating).
    - Diff parity: `diff <(grep -A8 'interface PngOverlayConfig' services/remotion-studio/src/pipeline-config.ts) <(grep -A8 'interface PngOverlayConfig' services/remotion-renderer/src/pipeline-config.ts)` is empty.
  </acceptance_criteria>
  <done>layer field present + validated identically in both pipeline-config.ts copies; renderer vitest green with 5 new layer cases; missing-layer overlays still validate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| studio UI → PUT /api/config → pipeline-config.json | User-authored overlay config (incl. new `layer`) is persisted as JSON and later read by the renderer. Existing boundary; this plan only widens the accepted schema by one enum field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `layer` field in pipeline-config.json | mitigate | Validator rejects any `layer` not in {back, front}; field is enum-only, no free-text, no path/HTML interpolation downstream. |
| T-22-02 | Information disclosure | overlay config | accept | Config is local Studio state behind the existing Cloudflare-tunnel basic auth; no new network/PII surface introduced by an enum field. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (schema + test edit only). |
</threat_model>

<verification>
- Both pipeline-config.ts copies carry the layer field + validator branch identically.
- Renderer vitest passes (`npm test` full suite in services/remotion-renderer).
- No Phase 21 overlay regression: missing-layer overlays validate clean.
</verification>

<success_criteria>
- `PngOverlayConfig.layer?: "back" | "front"` exists in studio + renderer schemas.
- Validator errors only on out-of-enum values; missing field is valid.
- 5 new renderer test cases pass.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-01-SUMMARY.md` when done.
</output>
