---
phase: 01-pipeline-infrastructure
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - services/base-python/Dockerfile
  - services/base-node/Dockerfile
  - scripts/smoke-test.sh
autonomous: true
requirements: [PIPE-05, PIPE-01, PIPE-02, PIPE-03, PIPE-04]
gap_closure: true
gap_sources:
  - truth: "FFmpeg --version returns the same pinned version (7.1.1) across all containers"
    test: 5
  - truth: "scripts/smoke-test.sh exists and validates all 5 PIPE requirements"
    test: 7

must_haves:
  truths:
    - "FFmpeg --version returns 7.1.1 in both base-python and base-node containers"
    - "scripts/smoke-test.sh exists and validates all PIPE requirements with pass/fail output"
  artifacts:
    - path: "services/base-python/Dockerfile"
      provides: "FFmpeg 7.1.1 pinned download URL"
      contains: "ffmpeg-7.1.1-amd64-static.tar.xz"
    - path: "services/base-node/Dockerfile"
      provides: "FFmpeg 7.1.1 pinned download URL"
      contains: "ffmpeg-7.1.1-amd64-static.tar.xz"
    - path: "scripts/smoke-test.sh"
      provides: "E2E smoke test script for bind-mount pipeline setup"
      min_lines: 50
  key_links:
    - from: "scripts/smoke-test.sh"
      to: "services/base-python/Dockerfile"
      via: "docker compose build base-python"
      pattern: "docker compose build"
    - from: "scripts/smoke-test.sh"
      to: "docker-compose.yml"
      via: "docker compose run smoke-test"
      pattern: "docker compose run.*smoke-test"
---

<objective>
Fix two UAT gaps from Phase 01: (1) Pin FFmpeg to version 7.1.1 in both base Dockerfiles — the generic release URL downloads 7.0.2 instead of the declared 7.1.1. (2) Restore the deleted scripts/smoke-test.sh adapted for the current bind-mount setup.

Purpose: Phase 01 UAT found these two blockers — FFmpeg version mismatch undermines PIPE-05 and the missing smoke-test script means no automated validation of the pipeline infrastructure.

Output: Both Dockerfiles pinned to FFmpeg 7.1.1, restored smoke-test.sh script.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-infrastructure/01-UAT.md
@.planning/phases/01-pipeline-infrastructure/01-04-SUMMARY.md

@services/base-python/Dockerfile
@services/base-node/Dockerfile
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin FFmpeg 7.1.1 download URL in both base Dockerfiles</name>
  <files>services/base-python/Dockerfile, services/base-node/Dockerfile</files>
  <action>
In BOTH `services/base-python/Dockerfile` and `services/base-node/Dockerfile`, replace the generic FFmpeg download URL with the version-pinned URL:

BEFORE: `"https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz"`
AFTER:  `"https://johnvansickle.com/ffmpeg/releases/ffmpeg-7.1.1-amd64-static.tar.xz"`

This pins FFmpeg to exactly 7.1.1 (matching `FFMPEG_VERSION=7.1.1` in `.env.example`). The generic "release" URL currently resolves to 7.0.2 which is the root cause of the UAT failure.

Both Dockerfiles have identical curl lines — fix both. No other changes needed. The `FFDIR` wildcard extraction pattern (`/tmp/ffmpeg-*-amd64-static`) still works because the pinned tarball extracts to `ffmpeg-7.1.1-amd64-static/`.
  </action>
  <verify>
    <automated>grep -c "ffmpeg-7.1.1-amd64-static.tar.xz" services/base-python/Dockerfile services/base-node/Dockerfile | grep -v ":0$"</automated>
  </verify>
  <done>Both Dockerfiles download FFmpeg 7.1.1 explicitly. The generic "ffmpeg-release-amd64-static.tar.xz" URL appears zero times across both files.</done>
</task>

<task type="auto">
  <name>Task 2: Restore and adapt scripts/smoke-test.sh for bind-mount setup</name>
  <files>scripts/smoke-test.sh</files>
  <action>
Create `scripts/smoke-test.sh` — a restored and adapted version of the original smoke test script (deleted in commit c5f9a0a). The script must work with the current bind-mount `./pipeline/` setup (not named Docker volumes).

Key adaptations from the original:
1. **Host-local file operations instead of docker cp**: The current `docker-compose.yml` uses bind mounts (`./pipeline:/data/pipeline`), so test files can be placed directly on the host filesystem under `./pipeline/${JOB_ID}/input/` — no `docker cp` needed.
2. **Source file generation stays the same**: Use `ffmpeg -f lavfi` for test MP4 if ffmpeg is available on host, otherwise create a minimal binary file.
3. **FFmpeg version check stays the same**: `docker compose run --rm --no-deps base-python ffmpeg -version` and same for `base-node`, grep for `7.1.1`.
4. **Smoke-test container invocation**: `docker compose run --rm smoke-test` — unchanged.
5. **Artifact verification**: Use bind mount path `./pipeline/${JOB_ID}/smoke-test/` directly on host filesystem (no `docker compose run --rm` needed for file checks).
6. **Cleanup**: Remove the `./pipeline/${JOB_ID}/` directory after test.

The script must validate all 5 PIPE requirements:
- PIPE-01: Shared volume I/O — input file accessible by container, output written to bind mount
- PIPE-02: Step contract — smoke-test container reads INPUT_PATH, writes OUTPUT_PATH + manifest.json
- PIPE-03: Intermediate artifacts inspectable — analysis.json exists on host
- PIPE-04: Pipeline extensibility — smoke-test service exists in docker-compose.yml
- PIPE-05: FFmpeg version consistency — both containers report 7.1.1

Make it executable (`chmod +x`). Use `set -euo pipefail`. Print pass/fail per check with summary at end. Exit 0 on all pass, exit 1 on any failure.
  </action>
  <verify>
    <automated>test -x scripts/smoke-test.sh && grep -c "PIPE-0[1-5]" scripts/smoke-test.sh | grep -v "^0$"</automated>
  </verify>
  <done>scripts/smoke-test.sh exists, is executable, validates all 5 PIPE requirements, and works with the bind-mount ./pipeline/ directory setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker build | FFmpeg binary downloaded from external URL during build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | FFmpeg download URL | accept | URL pinned to specific version tarball from well-known FFmpeg static builds site; supply chain risk accepted for pinned release |
| T-01-02 | T | FFmpeg binary integrity | accept | No checksum verification in Dockerfile — same as original design, accepted for dev tooling |
</threat_model>

<verification>
1. Rebuild both base images: `docker compose build base-python base-node`
2. Verify FFmpeg version: `docker compose run --rm --no-deps base-python ffmpeg -version` reports 7.1.1
3. Verify FFmpeg version: `docker compose run --rm --no-deps base-node ffmpeg -version` reports 7.1.1
4. Run smoke test: `./scripts/smoke-test.sh` — all 5 PIPE checks pass
</verification>

<success_criteria>
- `docker compose run --rm --no-deps base-python ffmpeg -version 2>&1 | head -1` contains "7.1.1"
- `docker compose run --rm --no-deps base-node ffmpeg -version 2>&1 | head -1` contains "7.1.1"
- `scripts/smoke-test.sh` is executable and validates all 5 PIPE requirements
- Phase 01 UAT gaps (test 5 and test 7) are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-infrastructure/01-05-SUMMARY.md`
</output>