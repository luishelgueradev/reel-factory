---
phase: 23-render-execution-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/preview/render-status.ts
  - services/remotion-studio/src/preview/render-status.test.ts
  - services/remotion-studio/package.json
autonomous: true
requirements: [RENDER-02, RENDER-03]
must_haves:
  truths:
    - "A client can map any of the 6 real pipeline currentStep values + queued/completed/timeout to a Spanish label (RENDER-02)"
    - "An exitCode of 137 produces the cause line `<step> · exit 137 — sin memoria`; other exit codes produce `<step> · exit <code>` (RENDER-03, D-09)"
    - "Studio vitest can run integration tests against an in-process Express app (harness ready for Plan 02 + Plan 04)"
  artifacts:
    - path: "services/remotion-studio/src/preview/render-status.ts"
      provides: "STEP_LABELS map, stepLabel(), causeLine(), parseStatus terminal-state helper — pure, unit-testable"
      exports: ["STEP_LABELS", "stepLabel", "causeLine"]
      min_lines: 30
    - path: "services/remotion-studio/src/preview/render-status.test.ts"
      provides: "Unit coverage for step-label + cause-line mapping (RENDER-02/03)"
      contains: "exit 137"
  key_links:
    - from: "services/remotion-studio/src/preview/render-status.ts"
      to: "orchestrator STEPS (whisper→silence-cutter→ffmpeg-finalizer→remotion-renderer→quality-finalizer→srt-exporter)"
      via: "STEP_LABELS keys exactly match the 6 real step names + queued/completed/timeout"
      pattern: "remotion-renderer"
---

<objective>
Stand up the Wave-0 test infrastructure and the pure, unit-testable mapping layer this phase
depends on, BEFORE any proxy or UI code is written. Two deliverables:

1. `render-status.ts` — the pure functions every later plan consumes: the 6-step Spanish label
   map (`STEP_LABELS`), `stepLabel()`, and the failure `causeLine()` formatter (D-09). These are
   the RENDER-02 (step→label) and RENDER-03 (exitCode→cause line) contracts as testable functions
   with zero React/Express dependency.
2. The studio vitest + supertest harness so Plan 02 (`server.test.ts`) and Plan 04 (component
   tests) have a working integration-test path (studio currently has vitest but NOT supertest).

Purpose: Per 23-VALIDATION.md, every RENDER requirement has a Wave-0 test gap. This plan closes
the mapping-function gaps and the harness gap so downstream tasks have an `<automated>` verify.
Output: `render-status.ts`, `render-status.test.ts`, supertest installed in studio devDeps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-render-execution-progress/23-RESEARCH.md
@.planning/phases/23-render-execution-progress/23-PATTERNS.md
@.planning/phases/23-render-execution-progress/23-VALIDATION.md

<interfaces>
The 6 real pipeline steps (from api-server orchestrator STEPS, verified):
  whisper → silence-cutter → ffmpeg-finalizer → remotion-renderer → quality-finalizer → srt-exporter

`GET /status/:jobId` response shape (api-server status.ts L47-56, Zod-validated):
  { jobId, status, currentStep, progress (0-100 number), stepInfo (e.g. "4/6"), steps (string[]), startedAt, error (string|undefined) }
  - status is one of: queued | active | completed | failed | timeout
  - error on /status is a STRING: "Step remotion-renderer failed (exit 137): <msg>"

`POST /process` / proxy failure body is STRUCTURED: { jobId, error: { step, exitCode, message } }
The client formatter MUST handle BOTH the string and structured shapes (RESEARCH Code Examples / D-09).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install supertest in studio devDeps + confirm vitest harness</name>
  <read_first>
    - services/remotion-studio/package.json (current scripts + devDeps; vitest 4.1.8 present, supertest absent)
    - services/remotion-studio/vitest.config.ts (existing config to confirm it picks up *.test.ts)
    - services/api-server/src/routes/process.test.ts (the supertest + vi.mock harness shape to mirror — L1-55)
  </read_first>
  <behavior>
    - After install, `cd services/remotion-studio && npx vitest run` still passes the existing suite (no regressions).
    - `supertest` + `@types/supertest` resolve as imports in a studio test file.
  </behavior>
  <action>
    Add `supertest` and `@types/supertest` to `devDependencies` in
    services/remotion-studio/package.json and run `npm install` in that service so node_modules
    is populated. Do NOT bump vitest or any @remotion/* version. The studio test script is already
    `"test": "vitest run"` — do not change it. This task only makes the supertest import available
    for Plan 02's server.test.ts; it does not yet write that test. Note the worktree devDep-install
    gotcha (MEMORY): the primary tree needs `npm install` after any worktree merge.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npm install && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `services/remotion-studio/package.json` devDependencies contains both `supertest` and `@types/supertest`.
    - `node -e "require.resolve('supertest')"` run from services/remotion-studio exits 0.
    - `npx vitest run` in services/remotion-studio exits 0 (existing suite green).
  </acceptance_criteria>
  <done>supertest is installed in studio devDeps and the existing studio vitest suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render-status.ts mapping module + unit tests</name>
  <files>services/remotion-studio/src/preview/render-status.ts, services/remotion-studio/src/preview/render-status.test.ts</files>
  <read_first>
    - .planning/phases/23-render-execution-progress/23-RESEARCH.md (Code Examples: STEP_LABELS, causeLine)
    - .planning/phases/23-render-execution-progress/23-PATTERNS.md (PreviewApp section: STEP_LABELS + causeLine excerpt, L145-155)
    - .planning/phases/23-render-execution-progress/23-UI-SPEC.md (§2 the 6-step Spanish label table; §4 cause formatting `exitCode === 137` → "— sin memoria")
    - services/api-server/src/routes/status.ts (status response shape, error-as-string format)
  </read_first>
  <behavior>
    - `stepLabel("whisper")` → "Transcribiendo"; `stepLabel("silence-cutter")` → "Cortando silencios";
      `stepLabel("ffmpeg-finalizer")` → "Formato vertical 9:16"; `stepLabel("remotion-renderer")` → "Renderizando";
      `stepLabel("quality-finalizer")` → "Afinando calidad"; `stepLabel("srt-exporter")` → "Exportando subtítulos";
      `stepLabel("queued")` → "En cola"; `stepLabel("completed")` → "Listo"; `stepLabel("timeout")` → "Tiempo agotado".
    - `stepLabel("<anything-unmapped>")` returns the raw input string (no throw).
    - `causeLine("remotion-renderer", 137)` → "remotion-renderer · exit 137 — sin memoria".
    - `causeLine("remotion-renderer", 1)` → "remotion-renderer · exit 1".
    - `causeLine("remotion-renderer", undefined)` → "remotion-renderer" (no " · exit" tail when no code).
    - A helper that, given a `/status` error STRING like "Step remotion-renderer failed (exit 137): killed", extracts step + exitCode so causeLine can format it identically to the structured `{step,exitCode}` shape.
    - `isLongStep("remotion-renderer")` → true (drives the UI-SPEC §2 "este paso toma más tiempo" hint + shimmer); false for all other steps.
  </behavior>
  <action>
    Create render-status.ts exporting: `STEP_LABELS` (Record string→string with exactly the keys in
    the behavior block — all 6 real steps from orchestrator STEPS plus queued/completed/timeout),
    `stepLabel(currentStep: string): string`, `causeLine(step: string, exitCode?: number): string`,
    `isLongStep(step: string): boolean`, and a `parseStatusError(errStr: string): {step?:string, exitCode?:number}`
    helper that regex-extracts step + exit code from the api-server `/status` error string so the UI
    can feed BOTH the structured `{step,exitCode}` (from a proxy 500) and the `/status` string into
    one formatter (RENDER-03 / D-09 dual-shape requirement). Keep it pure: no React, no fetch, no
    Express imports. Use the exact Spanish strings from 23-UI-SPEC.md §2 verbatim. Honest-% note for
    downstream: progress comes straight from `GET /status` `progress` (the `(completedSteps+1)/6*100`
    value) — this module does NOT recompute it; it only maps labels + cause lines.
    Write render-status.test.ts covering every case in the behavior block, including the unmapped-key
    passthrough and the OOM/non-OOM/undefined-exitCode causeLine branches and the parseStatusError
    extraction of `exit 137` from the string form.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx vitest run src/preview/render-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `render-status.test.ts` asserts all 9 label mappings + the unmapped-passthrough case.
    - `render-status.test.ts` asserts `causeLine("remotion-renderer",137)` === "remotion-renderer · exit 137 — sin memoria".
    - `render-status.test.ts` asserts `parseStatusError("Step remotion-renderer failed (exit 137): killed")` yields `{step:"remotion-renderer", exitCode:137}`.
    - `grep -c "remotion-renderer" services/remotion-studio/src/preview/render-status.ts` ≥ 1 and `STEP_LABELS` contains all 6 real step keys.
    - `npx vitest run src/preview/render-status.test.ts` exits 0.
  </acceptance_criteria>
  <done>render-status.ts exports the label/cause/long-step/parse helpers; every behavior case is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Studio Express | Untrusted UI input; this plan ships no route, only pure functions consumed later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Information disclosure | causeLine / parseStatusError surfacing raw error strings | accept | Cause strings are pipeline step + exit code only (no secrets/paths); api-server already controls the error string content. Low value, internal single-user Studio. |
| T-23-SC | Tampering | npm install of `supertest` / `@types/supertest` | mitigate | Both are mature, high-download, first-party-adjacent test libs; not in RESEARCH Package Legitimacy Audit as [ASSUMED]/[SUS]/[SLOP]. Dev-only dependency, never shipped to the renderer image. |
</threat_model>

<verification>
- `cd services/remotion-studio && npx vitest run` — full studio suite green (existing + new render-status tests).
- `render-status.ts` has no `import ... express` and no `import ... react` (pure module).
</verification>

<success_criteria>
- supertest available in studio devDeps; studio vitest suite green.
- render-status.ts exports STEP_LABELS, stepLabel, causeLine, isLongStep, parseStatusError.
- All RENDER-02/03 mapping behaviors are covered by green unit tests.
</success_criteria>

<output>
Create `.planning/phases/23-render-execution-progress/23-01-SUMMARY.md` when done.
</output>
