---
phase: 20-title-block-precision
plan: 04
type: execute
wave: 3
depends_on: ["20-02", "20-03"]
files_modified:
  - services/remotion-renderer/src/compositions/TitleOverlay.tsx
  - services/remotion-renderer/src/pipeline-config.ts
autonomous: false
requirements: [TITLE-01, TITLE-02, TITLE-03]

must_haves:
  truths:
    - "Renderer TitleOverlay.tsx is identical to studio TitleOverlay.tsx (pixel-coordinate positioning) (D-03, D-04)"
    - "Renderer pipeline-config.ts is identical to studio pipeline-config.ts (updated schema) (D-05, D-09)"
    - "Full vitest suite passes in remotion-renderer"
    - "npm run build:editor exits 0"
    - "Studio preview shows title block at x=0,y=0 flush with top-left (no centering offset) (D-04)"
    - "Border-radius slider changes block corner roundness in live preview (D-09)"
    - "No subtitle field visible in add/edit form (D-07)"
  artifacts:
    - path: "services/remotion-renderer/src/compositions/TitleOverlay.tsx"
      provides: "Renderer sync — pixel-coordinate positioning, no subtitle"
      contains: "(x / 1080) * 100"
    - path: "services/remotion-renderer/src/pipeline-config.ts"
      provides: "Renderer sync — updated TitleStyleProps/TitleConfig"
      contains: "x?: number"
  key_links:
    - from: "services/remotion-renderer/src/compositions/TitleOverlay.tsx"
      to: "services/remotion-renderer/src/pipeline-config.ts"
      via: "TitleStyleProps import (post-sync both files must be consistent)"
      pattern: "TitleStyleProps"
---

<objective>
Sync the updated studio files to the renderer, run the full vitest suite to confirm no regressions, build the editor, and do a human visual verification of all three TITLE requirements in the live studio preview.

Purpose: The renderer is the component that produces actual video output. Without this sync, `renderMedia()` calls still use the old topOffset/centering/subtitle code. Build verification confirms the editor SPA has no dead import errors from the schema migration.
Output: Renderer files synced; full test suite green; editor build clean; human confirms pixel positioning, borderRadius slider, and subtitle removal all work in the studio UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

IMPORTANT (AGENTS.md requirement): Invoke `impeccable` skill + `frontend-design` plugin at start of session if not already active.
Studio port: 3123 (always — AGENTS.md).
Renderer sync command: from project root, NOT from within a service directory.
</execution_context>

<context>
@.planning/phases/20-title-block-precision/20-CONTEXT.md
@.planning/phases/20-title-block-precision/20-PATTERNS.md
@.planning/phases/20-title-block-precision/20-02-SUMMARY.md
@.planning/phases/20-title-block-precision/20-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renderer sync — cp studio files to renderer; run full vitest suite; build editor</name>
  <files>
    services/remotion-renderer/src/compositions/TitleOverlay.tsx,
    services/remotion-renderer/src/pipeline-config.ts
  </files>
  <read_first>
    - AGENTS.md §"Development Conventions — Renderer sync pattern" (authoritative sync command)
    - .planning/phases/20-title-block-precision/20-PATTERNS.md §"renderer sync targets" (exact cp commands)
    - services/remotion-studio/src/compositions/TitleOverlay.tsx (confirm it is the post-Plan-02 version: contains "(x / 1080)" and no "subtitle &&")
    - services/remotion-studio/src/pipeline-config.ts (confirm it is the post-Plan-01 version: contains "x?: number" and no "topOffset?: number")
  </read_first>
  <action>
    From the project root (`/home/luis/proyectos/reel-factory`):

    1. Run the renderer sync — two cp commands per AGENTS.md renderer-sync rules:
       `cp services/remotion-studio/src/compositions/TitleOverlay.tsx services/remotion-renderer/src/compositions/TitleOverlay.tsx`
       `cp services/remotion-studio/src/pipeline-config.ts services/remotion-renderer/src/pipeline-config.ts`
       Do NOT copy Root.tsx or SubtitledVideo.tsx — AGENTS.md explicitly forbids this.

    2. Run the full renderer vitest suite:
       `cd services/remotion-renderer && npx vitest run`
       All tests must pass. The 6 new Phase 20 tests (added in Plan 01 Task 1) must now be green because the synced pipeline-config.ts has the updated schema.

    3. Build the editor:
       `cd services/remotion-studio && npm run build:editor`
       Must exit 0. This confirms no dead imports or TypeScript errors survive in the full Vite build (which is stricter than tsc --noEmit for JSX).
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-renderer && npx vitest run 2>&1 | tail -20 && echo "--- BUILD ---" && cd /home/luis/proyectos/reel-factory/services/remotion-studio && npm run build:editor 2>&1 | tail -10</automated>
  </verify>
  <done>
    Renderer vitest run exits 0 with all tests passing (including the 6 new Phase 20 tests). `npm run build:editor` exits 0. `grep -c "(x / 1080)" services/remotion-renderer/src/compositions/TitleOverlay.tsx` returns at least 1. `grep -c "x?: number" services/remotion-renderer/src/pipeline-config.ts` returns at least 1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01–04 implement the three Phase 20 changes:
    - TITLE-01: topOffset replaced by X (px) / Y (px) number inputs; blocks rendered at pixel coordinates
    - TITLE-02: Border Radius slider (0–50px) replaces hardcoded 12px
    - TITLE-03: Subtitle field removed from schema, renderer, and editor UI
    The renderer is synced and the editor build is clean.
  </what-built>
  <how-to-verify>
    Start the studio if not already running (from project root):
    ```
    cd services/remotion-studio && setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor ACTIVE_PIPELINE_CONFIG_PATH=$(pwd)/../../pipeline/pipeline-config.json npx tsx src/server.ts > /tmp/remotion-server.log 2>&1 &
    ```
    Then open http://localhost:3123 in the browser and navigate to the Titles tab.

    Check 1 — TITLE-01 (pixel positioning):
    a. Click "+ Add Title", enter any text, set X=0, Y=0 → "Add Title". Confirm the title block appears at the TOP-LEFT corner of the video frame (no half-block off-screen, no centering offset). The block's top-left corner should be flush with the frame's top-left.
    b. Edit the title, set X=540, Y=960 → Save Changes. Confirm the block moves to the horizontal center / vertical center of the frame (540 is half of 1080; 960 is half of 1920).

    Check 2 — TITLE-02 (border-radius):
    c. Edit a title. Drag the Border Radius slider to 0 → observe the title block has sharp corners in the preview.
    d. Drag to 50 → observe the block has fully rounded (pill-shaped) corners.

    Check 3 — TITLE-03 (subtitle removal):
    e. In the add/edit form, confirm there is NO "Subtitle" text input, NO "Subtitle Color" control, NO "Subtitle Size" slider, NO "Subtitle Font" select.
    f. In the titles list, confirm existing title entries show only title text + timing line + buttons (no subtitle line).
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass. Describe any issues if checks fail.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| studio source → renderer cp | File copy is a local filesystem operation; no network; destination is a trusted sibling directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | renderer sync cp command | accept | Local filesystem cp; source is the verified studio build; no external input |
| T-20-SC | Tampering | npm installs | accept | No new packages; build:editor uses existing Vite config |
</threat_model>

<verification>
After Task 1 completes:
- `grep -c "(x / 1080) * 100" services/remotion-renderer/src/compositions/TitleOverlay.tsx` returns at least 1
- `grep -c "topOffset" services/remotion-renderer/src/compositions/TitleOverlay.tsx` returns 0
- `grep -c "subtitle?" services/remotion-renderer/src/pipeline-config.ts | grep TitleConfig` — returns 0 (subtitle field gone from TitleConfig)
- `cd services/remotion-renderer && npx vitest run` exits 0
- `cd services/remotion-studio && npm run build:editor` exits 0

After human checkpoint:
- Title block at x=0,y=0 is flush with frame top-left (TITLE-01)
- Border radius slider from 0 to 50 changes corner style live (TITLE-02)
- No subtitle field in add/edit form (TITLE-03)
</verification>

<success_criteria>
- Renderer and studio TitleOverlay.tsx are identical (pixel-coordinate positioning active in renders)
- Renderer and studio pipeline-config.ts are identical (no topOffset/subtitle in schema)
- Full vitest suite green (0 failing tests)
- editor build clean (npm run build:editor exits 0)
- Human confirms pixel positioning, border-radius slider, and subtitle removal all work visually
</success_criteria>

<output>
Create `.planning/phases/20-title-block-precision/20-04-SUMMARY.md` when done
</output>
