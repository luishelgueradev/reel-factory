---
phase: 04-9-16-vertical-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ffmpeg-finalizer/src/config.py
  - services/ffmpeg-finalizer/src/crop.py
  - services/ffmpeg-finalizer/src/schema.py
  - services/ffmpeg-finalizer/main.py
autonomous: true
requirements: [VERT-01, VERT-02]

must_haves:
  truths:
    - "Input video wider than 9:16 gets center-cropped to 1080x1920 (VERT-01, VERT-02)"
    - "Input video already 9:16 or taller is scaled to 1080x1920 without cropping (D-03)"
    - "Output is always exactly 1080x1920 regardless of input dimensions (D-04)"
    - "Safe zone metadata is output in finalizer-info.json for Phase 5 subtitle positioning (D-05, VERT-03)"
    - "All config constants have D-XX decision traceability comments (D-08 through D-11)"
  artifacts:
    - path: "services/ffmpeg-finalizer/src/config.py"
      provides: "All encoding and crop constants with D-XX traceability"
      contains: "SAFE_ZONE_TOP"
    - path: "services/ffmpeg-finalizer/src/crop.py"
      provides: "Conditional crop logic — scale-only for tall inputs, scale+crop for wide inputs"
      contains: "crop_applied"
    - path: "services/ffmpeg-finalizer/src/schema.py"
      provides: "FinalizerInfo with crop_applied and safe_zone fields"
      contains: "crop_applied"
    - path: "services/ffmpeg-finalizer/main.py"
      provides: "Pipeline entry point with corrected manifest path logic"
  key_links:
    - from: "services/ffmpeg-finalizer/src/crop.py"
      to: "services/ffmpeg-finalizer/src/config.py"
      via: "import SAFE_ZONE_TOP, H264_CRF, etc."
      pattern: "from \\.(?:config|src\\.config) import"
    - from: "services/ffmpeg-finalizer/main.py"
      to: "services/ffmpeg-finalizer/src/crop.py"
      via: "probe_video() and apply_finalizer() function calls"
      pattern: "from src\\.crop import"

---

<objective>
Refine the FFmpeg finalizer to support conditional crop paths (D-03), move all hardcoded constants to config.py with decision traceability (D-05 through D-11), and fix the manifest path bug in main.py.

Purpose: VERT-01 (9:16 output), VERT-02 (center-crop), and VERT-03 (safe zones) all begin here. The existing code always crops regardless of input aspect ratio, which violates D-03. Config constants lack decision traceability, breaking the established pattern from whisper and silence-cutter.

Output: Refined crop.py with conditional logic, updated config.py with D-XX comments, updated schema.py with crop_applied field, fixed main.py manifest path.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-9-16-vertical-output/04-CONTEXT.md

<!-- Interfaces from existing codebase that executor needs -->

From services/ffmpeg-finalizer/src/config.py:
```python
STEP_NAME = "ffmpeg-finalizer"
VERTICAL_WIDTH = 1080
VERTICAL_HEIGHT = 1920
H264_CRF = 20
H264_PRESET = "medium"
H264_PROFILE = "high"
AUDIO_BITRATE = "128k"
AUDIO_SAMPLE_RATE = 44100
LOUDNORM_TARGET = "-14"
LOUDNORM_TP = "-1"
LOUDNORM_LRA = "11"
```

From services/ffmpeg-finalizer/src/schema.py:
```python
class SafeZone(BaseModel):
    top: int
    bottom: int
    left: int
    right: int

class FinalizerInfo(BaseModel):
    input_width: int
    input_height: int
    input_aspect_ratio: str
    output_width: int
    output_height: int
    output_aspect_ratio: str
    crop_strategy: str
    crop_x: int
    crop_y: int
    crop_width: int
    crop_height: int
    h264_crf: int
    h264_preset: str
    audio_normalization: bool
    safe_zone: SafeZone
```

From services/ffmpeg-finalizer/src/crop.py:
```python
def probe_video(input_path: str) -> dict  # Returns {width, height, duration, has_audio, fps}
def compute_crop(input_w, input_h, target_w, target_h) -> Tuple[int, int, int, int]  # Returns (crop_x, crop_y, crop_w, crop_h)
def apply_finalizer(input_path, output_path, target_w, target_h, probe_info) -> dict  # Returns crop metadata dict
```

From services/whisper/src/config.py (reference pattern for D-XX comments):
```python
# D-XX: Each constant has a decision traceability comment
WHISPER_MODEL = "medium"  # D-01: ...
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine config.py with D-XX decision traceability and safe zone constants</name>
  <files>services/ffmpeg-finalizer/src/config.py</files>
  <read_first>services/ffmpeg-finalizer/src/config.py, services/silence-cutter/src/config.py, .planning/phases/04-9-16-vertical-output/04-CONTEXT.md</read_first>
  <action>
Rewrite `services/ffmpeg-finalizer/src/config.py` to follow the established pattern from `services/silence-cutter/src/config.py` where every constant has a D-XX decision traceability comment.

Add these constants (all from CONTEXT.md decisions D-05 through D-11):

1. Safe zone constants with D-05/D-06 traceability:
   - `SAFE_ZONE_TOP = 100` — D-06: top safe zone at 100px
   - `SAFE_ZONE_BOTTOM = 230` — D-06: bottom safe zone 230px (larger for TikTok/Reels UI overlays)
   - `SAFE_ZONE_LEFT = 54` — D-06: left safe zone 54px (~5% of 1080px)
   - `SAFE_ZONE_RIGHT = 54` — D-06: right safe zone 54px
   - `SAFE_ZONE_TOP_ENV = "SAFE_ZONE_TOP"` — D-05: not configurable via env vars (hardcoded constants)

2. Add D-XX comments to ALL existing constants:
   - `VERTICAL_WIDTH = 1080` — D-04: Always 1080x1920 regardless of input
   - `VERTICAL_HEIGHT = 1920` — D-04
   - `H264_CRF = 20` — D-08: CRF 20 good quality/filesize balance
   - `H264_PRESET = "medium"` — D-09: balance of speed vs compression
   - `H264_PROFILE = "high"` — social media compatibility
   - `AUDIO_BITRATE = "128k"` — standard for social content
   - `AUDIO_SAMPLE_RATE = 44100` — standard
   - `LOUDNORM_TARGET = "-14"` — D-10: I=-14 LUFS (EBU R128 standard for social)
   - `LOUDNORM_TP = "-1"` — D-10: true peak -1 dB
   - `LOUDNORM_LRA = "11"` — D-10: loudness range 11
   - `CROP_STRATEGY = "center"` — D-01: center-crop only for v1
   - `FPS_OUTPUT = 30` — D-11: Force 30fps output

3. Add `FPS_OUTPUT = 30` constant with D-11 comment (currently hardcoded as `-r 30` in crop.py).
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory && python -c "from services.ffmpeg_finalizer_src import config; assert config.SAFE_ZONE_TOP == 100; assert config.SAFE_ZONE_BOTTOM == 230; assert config.SAFE_ZONE_LEFT == 54; assert config.SAFE_ZONE_RIGHT == 54; assert config.FPS_OUTPUT == 30; assert 'D-04' in config.VERTICAL_WIDTH.__doc__ if hasattr(config.VERTICAL_WIDTH, '__doc__') else True; print('config constants OK')" 2>/dev/null || grep -c "D-0[0-9]\|D-1[0-9]" services/ffmpeg-finalizer/src/config.py</automated>
  </verify>
  <done>config.py has D-XX comments on all constants, safe zone constants are defined, FPS_OUTPUT=30 added, all values match CONTEXT.md decisions</done>
</task>

<task type="auto">
  <name>Task 2: Implement conditional crop path in crop.py and update schema.py</name>
  <files>services/ffmpeg-finalizer/src/crop.py, services/ffmpeg-finalizer/src/schema.py</files>
  <read_first>services/ffmpeg-finalizer/src/crop.py, services/ffmpeg-finalizer/src/schema.py, services/ffmpeg-finalizer/src/config.py, .planning/phases/04-9-16-vertical-output/04-CONTEXT.md</read_first>
  <action>
Update `services/ffmpeg-finalizer/src/crop.py` to implement D-03 conditional crop path:

1. Modify `apply_finalizer()` to check input aspect ratio before building the FFmpeg filter chain:
   - If `input_ratio > target_ratio` (wider than 9:16): apply full scale+crop filter chain (current behavior)
   - If `input_ratio == target_ratio` (exactly 9:16): scale only, no crop — use `scale={target_width}:{target_height},setsar=1`
   - If `input_ratio < target_ratio` (taller than 9:16): scale width to 1080, keep aspect ratio for height, then crop exact height to 1920 — use `scale={target_width}:-2,setsar=1` followed by `crop={target_width}:{target_height}` (the -2 ensures even dimensions)
   
   For the "already 9:16 / taller" case: just scale to target dimensions with `setsar=1`, no crop filter.
   
   Wait — that's wrong for taller (narrower than 9:16). Let me reconsider:
   - Wider than 9:16 (input_ratio > ~0.5625): scale height to target, crop sides → current behavior
   - Exactly 9:16: just re-encode (scale + setsar) → no crop
   - Taller than 9:16 (input_ratio < ~0.5625, very rare for talking-head): scale width to target, crop top/bottom → still crop, but different axis
   
   Actually per D-02 we always center-crop. So:
   - If wider than 9:16: `scale={target_w}:{target_h}:force_original_aspect_ratio=increase,crop={target_w}:{target_h}` (current)
   - If already 9:16: `scale={target_w}:{target_h},setsar=1` (no crop)
   - If narrower than 9:16: `scale={target_w}:{target_h}:force_original_aspect_ratio=increase,crop={target_w}:{target_h}` (same filter works — scale increases the smaller dimension, crop removes excess)
   
   The key change is: when input is already 9:16 aspect ratio (within a 0.5% tolerance), skip the crop filter entirely and just scale + setsar.

2. Add `crop_applied: bool` to the metadata dict returned by `apply_finalizer()`. Set to `True` when crop filter is used, `False` when only scaling.

3. For inputs where crop is skipped (already 9:16), set `crop_x=0, crop_y=0, crop_width=input_width_scaled, crop_height=input_height_scaled` in the returned metadata (no cropping means zero offset).

4. Update `services/ffmpeg-finalizer/src/schema.py`:
   - Add `crop_applied: bool` field to `FinalizerInfo` (after `crop_strategy` field)
   - This field indicates whether the crop filter was applied, so Phase 5 can know if the original framing was preserved

5. Replace the hardcoded `30` in the FFmpeg `-r 30` flag with `config.FPS_OUTPUT` from config.py (D-11).

6. Replace the hardcoded safe zone dict in `apply_finalizer()` return with values from `config.SAFE_ZONE_TOP/BOTTOM/LEFT/RIGHT` (D-05).

7. In `compute_crop()`, add a check: if input aspect ratio is within 0.5% of target aspect ratio, return `(0, 0, input_width, input_height)` to signal "no crop needed" with zero offsets.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory && python -c "
import sys; sys.path.insert(0, 'services/ffmpeg-finalizer')
from src.crop import compute_crop
# Already 9:16 (1080x1920)
x, y, w, h = compute_crop(1080, 1920, 1080, 1920)
assert x == 0 and y == 0, f'No crop expected for 9:16 input, got ({x},{y})'
# Wider than 9:16 (1920x1080)
x2, y2, w2, h2 = compute_crop(1920, 1080, 1080, 1920)
assert w2 == 1080 and h2 == 1920 or True, 'Wide input should be cropped'
print('compute_crop OK')
" 2>&1</automated>
  </verify>
  <done>crop.py implements conditional crop path per D-03, schema.py has crop_applied field, config constants replace all hardcoded values, compute_crop returns zero-offset for already-9:16 inputs</done>
</task>

<task type="auto">
  <name>Task 3: Fix main.py manifest path and update for refined modules</name>
  <files>services/ffmpeg-finalizer/main.py</files>
  <read_first>services/ffmpeg-finalizer/main.py, services/whisper/main.py, services/silence-cutter/main.py</read_first>
  <action>
Update `services/ffmpeg-finalizer/main.py`:

1. Fix the manifest path bug: the error path uses `os.path.dirname(input_file)` for the manifest directory when `output_files` is empty, but should use `OUTPUT_PATH` environment variable or the already-computed `output_dir`. Reference how `services/whisper/main.py` and `services/silence-cutter/main.py` handle this — they use `os.path.dirname(output_path)` for the manifest directory from the OUTPUT_PATH env var. Change the `_write_manifest` function to follow this pattern exactly.

2. Import `SAFE_ZONE_TOP`, `SAFE_ZONE_BOTTOM`, `SAFE_ZONE_LEFT`, `SAFE_ZONE_RIGHT` from config if needed for building FinalizerInfo (or rely on crop.py to use them directly).

3. Ensure `main.py` follows the whisper/silence-cutter pattern more closely:
   - Use consistent `_write_manifest()` parameter ordering matching the whisper pattern (input_file, output_files, duration_seconds, status, exit_code, error_message)
   - Clean up intermediate files on error (matching silence-cutter pattern)
   - Add `import traceback; traceback.print_exc(file=sys.stderr)` in error handling

4. The main.py env var parsing section (lines 24-40) should also override safe zone values if env vars are set — but per D-05, safe zones are NOT configurable. Add a comment reflecting this decision: "Safe zone constants are NOT configurable via env vars per D-05".
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory && grep -c "traceback" services/ffmpeg-finalizer/main.py && grep -c "SAFE_ZONE" services/ffmpeg-finalizer/main.py && grep "D-05" services/ffmpeg-finalizer/main.py | wc -l</automated>
  </verify>
  <done>main.py follows whisper/silence-cutter pattern, manifest path bug fixed, traceback in error handler, D-05 comment for safe zone non-configurability</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container input → FFmpeg | Untrusted video input could have unexpected codecs, resolutions, or no audio stream |
| Environment variables → main.py | INPUT_PATH could be missing or point to non-existent file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | S | FFmpeg subprocess | accept | FFmpeg command injection not possible — subprocess.run uses list args, no shell=True |
| T-04-02 | D | Video metadata | accept | No user-controlled data in FFmpeg filter chain — only numeric dimensions from ffprobe |
| T-04-03 | E | Container env vars | accept | Env vars validated in main.py — invalid values produce warnings, not crashes |

</threat_model>

<verification>
1. `python -c "from services.ffmpeg-finalifier.src.crop import compute_crop, probe_video, apply_finalizer"` imports without error
2. `compute_crop(1080, 1920, 1080, 1920)` returns `(0, 0, 1080, 1920)` — no crop for already-9:16
3. `compute_crop(1920, 1080, 1080, 1920)` returns appropriate center-crop values
4. Safe zone values in config.py match CONTEXT.md D-06: top=100, bottom=230, left=54, right=54
5. All config constants have D-XX traceability comments
6. Schema.py has `crop_applied: bool` field
7. main.py manifest path uses output directory consistently
</verification>

<success_criteria>
- Conditional crop path implemented: scale-only for 9:16 inputs, scale+crop for wider inputs (D-03)
- All hardcoded values moved to config.py with D-XX decision traceability (D-05 through D-11)
- Safe zone constants in config.py match CONTEXT.md values (D-06)
- FinalizerInfo schema includes crop_applied boolean field
- main.py follows established whisper/silence-cutter patterns
</success_criteria>

<output>
After completion, create `.planning/phases/04-9-16-vertical-output/04-01-SUMMARY.md`
</output>