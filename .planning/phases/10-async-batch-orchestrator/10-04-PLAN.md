---
phase: 10-async-batch-orchestrator
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - services/api-server/src/queue.ts
  - services/api-server/src/routes/batch.ts
  - services/api-server/src/routes/health.ts
  - services/api-server/src/worker.ts
  - docker-compose.yml
  - services/api-server/src/__tests__/batch.test.ts
autonomous: false
requirements: [APIA-01, APIA-02, APIA-03]

must_haves:
  truths:
    - "MAX_CONCURRENT_JOBS env var limits concurrent pipeline runs (default 2)"
    - "MAX_BATCH_SIZE env var limits files per batch request (default 10)"
    - "Concurrent jobs run without resource contention crashes"
    - "GET /health includes Redis and BullMQ connection status"
    - "Submitting 3 videos via POST /batch creates 3 independent jobs that all process successfully"
    - "A failed job in a batch does not affect other jobs in the same batch"
    - "GPU serialization is handled naturally by Docker (one GPU per node)"
  artifacts:
    - path: "services/api-server/src/queue.ts"
      provides: "MAX_CONCURRENT_JOBS configuration for BullMQ Worker concurrency"
      contains: "MAX_CONCURRENT_JOBS"
    - path: "services/api-server/src/routes/health.ts"
      provides: "Health endpoint with Redis/BullMQ status"
      contains: "redis"
    - path: "services/api-server/src/routes/batch.ts"
      provides: "MAX_BATCH_SIZE enforcement in batch upload"
      contains: "MAX_BATCH_SIZE"
  key_links:
    - from: "services/api-server/src/worker.ts"
      to: "services/api-server/src/queue.ts"
      via: "MAX_CONCURRENT_JOBS env var for worker concurrency"
      pattern: "MAX_CONCURRENT_JOBS"
    - from: "services/api-server/src/routes/batch.ts"
      to: "services/api-server/src/constants.ts"
      via: "MAX_BATCH_SIZE read from env"
      pattern: "MAX_BATCH_SIZE"
    - from: "services/api-server/src/routes/health.ts"
      to: "services/api-server/src/queue.ts"
      via: "Redis ping for health check"
      pattern: "redis.*ping"
---

<objective>
Wire concurrency configuration, enforce batch size limits, add Redis/BullMQ health checking, and run end-to-end batch test.

Purpose: Per D-08, D-09, D-10: Concurrency limiting ensures no resource contention. Health checks verify Redis connectivity. E2E test validates the full batch flow from upload through processing to status retrieval.
Output: Configurable concurrency, health checks, and a verified end-to-end batch flow.
</objective>

<execution_context>
@/home/luis/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-async-batch-orchestrator/10-CONTEXT.md
@.planning/phases/10-async-batch-orchestrator/10-01-SUMMARY.md
@.planning/phases/10-async-batch-orchestrator/10-02-SUMMARY.md
@.planning/phases/10-async-batch-orchestrator/10-03-SUMMARY.md
@services/api-server/src/queue.ts
@services/api-server/src/worker.ts
@services/api-server/src/routes/batch.ts
@services/api-server/src/routes/health.ts
@services/api-server/src/constants.ts
@services/api-server/src/progress.ts
@docker-compose.yml

<interfaces>
From Plan 02 (batch route):
```typescript
export const batchRouter: Router; // POST /batch, GET /batch/:batchId
```

From Plan 03 (worker):
```typescript
export function startWorker(): void;
export function stopWorker(): Promise<void>;
```

From Plan 01 (queue):
```typescript
export const videoQueue: Queue;
export function createQueueConnection(): Redis;
export function closeQueueConnection(): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire concurrency configuration + batch size enforcement + health checks</name>
  <files>services/api-server/src/queue.ts, services/api-server/src/worker.ts, services/api-server/src/routes/batch.ts, services/api-server/src/routes/health.ts, services/api-server/src/constants.ts, docker-compose.yml</files>
  <action>
Per D-08, D-09, D-10: Configure concurrency limits, enforce batch size, and add health endpoint checks for Redis and BullMQ.

1. **Update services/api-server/src/constants.ts — add batch constants:**
   - Export `MAX_BATCH_SIZE = parseInt(process.env.MAX_BATCH_SIZE || "10", 10)` (per D-10)
   - Export `MAX_CONCURRENT_JOBS = parseInt(process.env.MAX_CONCURRENT_JOBS || "2", 10)` (per D-08)
   - Export `REDIS_URL = process.env.REDIS_URL || "redis://localhost:6379"`

2. **Update services/api-server/src/worker.ts — use MAX_CONCURRENT_JOBS from constants:**
   - Replace the hardcoded concurrency value with `MAX_CONCURRENT_JOBS` from constants
   - Ensure the Worker constructor uses `concurrency: MAX_CONCURRENT_JOBS`
   - Log the concurrency level: `console.log(Batch worker started with concurrency: ${MAX_CONCURRENT_JOBS})`

3. **Update services/api-server/src/routes/batch.ts — enforce MAX_BATCH_SIZE:**
   - Use `MAX_BATCH_SIZE` from constants in the Multer `upload.array("videos", MAX_BATCH_SIZE)` call
   - Replace any hardcoded size limits with the constant
   - Note: the `LIMIT_UNEXPECTED_FILE` error in index.ts already handles the 413 response

4. **Update services/api-server/src/routes/health.ts — add Redis and BullMQ status:**
   - Import `createQueueConnection` from `../queue.js`
   - In the GET /health handler:
     - Check Redis connection: `redis.ping()` → if resolves, redis_status = "connected", else redis_status = "disconnected"
     - Check BullMQ queue: `videoQueue.getJobCounts()` → if resolves, queue_status = "connected", else queue_status = "disconnected"
     - These checks should be optional/best-effort — if Redis is down, the health endpoint should still return 200 with status indicating degraded state
     - Return: `{ status: "ok" (or "degraded" if Redis/BullMQ unavailable), timestamp, uptime_seconds, redis: redis_status, queue: queue_status }`
   - Add timeout (5 seconds) for Redis/BullMQ checks to prevent hanging

5. **Update services/api-server/src/queue.ts — use REDIS_URL from constants:**
   - Replace the hardcoded Redis URL in `createQueueConnection()` with `REDIS_URL` from constants
   - Ensure BullMQ Queue uses the same connection config

6. **Verify docker-compose.yml already has the env vars from Plan 01:**
   - REDIS_URL, MAX_CONCURRENT_JOBS, MAX_BATCH_SIZE should already be in the api-server service
  </action>
  <verify>
    <automated>cd services/api-server && npx tsc --noEmit 2>&1 | head -10 && echo "--- TSC OK ---"</automated>
  </verify>
  <done>
    - MAX_CONCURRENT_JOBS (default 2) controls worker concurrency per D-08
    - MAX_BATCH_SIZE (default 10) limits batch upload per D-10
    - Health endpoint reports Redis and BullMQ connection status
    - Both values are configurable via env vars
    - TypeScript compiles clean
    - All existing tests still pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E batch test — submit 3 videos, verify all process independently</name>
  <files>services/api-server/src/__tests__/batch.test.ts, services/api-server/src/__tests__/worker.test.ts</files>
  <action>E2E verification by human — no automated action needed. All code changes were completed in Task 1. This task verifies the complete batch processing pipeline works end-to-end in Docker.</action>
  <verify>Manual verification per the how-to-verify steps below</verify>
  <done>All 3 videos process successfully, failed job isolation confirmed, concurrency limit respected</done>
  <what-built>
End-to-end batch processing system:
- POST /batch accepts multiple MP4 files and creates BullMQ jobs
- BullMQ worker processes jobs with concurrent execution (limit 2)
- Per-step progress tracked in Redis hashes
- GET /batch/:batchId returns status for all jobs in a batch
- Failed jobs retry once with cleanup
- Health endpoint shows Redis + BullMQ connectivity
  </what-built>
  <how-to-verify>
### Prerequisites
1. Start Redis: `docker compose up -d redis`
2. Build and start API server: `docker compose up -d api-server`
3. Verify health: `curl http://localhost:3000/health` — should show `"redis": "connected"` and `"queue": "connected"`

### E2E Batch Test
4. Submit a batch of test videos:
   ```bash
   curl -X POST http://localhost:3000/batch \
     -F "videos=@test-video-1.mp4" \
     -F "videos=@test-video-2.mp4" \
     -F "videos=@test-video-3.mp4"
   ```
5. Expected response: `{ "batchId": "...", "jobs": [{ "jobId": "...", "filename": "...", "status": "queued" }, ...], "createdAt": "..." }`

6. Check batch status:
   ```bash
   curl http://localhost:3000/batch/{batchId}
   ```
7. Expected: All 3 jobs should progress through "active" → "completed" (or "failed" with error details)

8. Verify job isolation — one video failure should NOT affect others:
   - Submit a batch including 1 invalid file (e.g., a text file renamed to .mp4) and 1 valid file
   - The valid file should process successfully while the invalid one fails
   - Check /batch/{batchId} to see both statuses independently

9. Verify concurrency limit:
   - Submit 3+ videos simultaneously
   - At most 2 should be processing at any time (MAX_CONCURRENT_JOBS=2)
   - Check Redis or logs for concurrent processing confirmation

10. Verify Docker Compose integration:
    ```bash
    docker compose ps  # Should show redis and api-server running
    ```
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found during verification</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External → POST /batch | Untrusted input validated and rate-limited |
| Worker → Docker daemon | Trusted internal (container creation) |
| Health endpoint → Redis/BullMQ | Trusted internal (diagnostic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Denial of Service | Concurrency exhaustion | mitigate | MAX_CONCURRENT_JOBS=2 prevents container explosion; queue absorbs bursts |
| T-10-13 | Denial of Service | Batch flood | mitigate | MAX_BATCH_SIZE=10 caps per-request file count; further mitigated by queue backpressure |
| T-10-14 | Information Disclosure | Health endpoint | accept | Health data (redis/queue status) is non-sensitive operational info; no authentication for v1 |
</threat_model>

<verification>
1. Health endpoint reports Redis and BullMQ status
2. MAX_CONCURRENT_JOBS env var controls worker concurrency
3. MAX_BATCH_SIZE env var limits batch upload
4. E2E test: submit 3 videos via POST /batch, all process independently
5. E2E test: verify failed job doesn't affect other jobs in batch
6. E2E test: verify concurrency limit respected (max 2 jobs at a time)
</verification>

<success_criteria>
- Concurrency controlled by MAX_CONCURRENT_JOBS=2 per D-08
- GPU serialization via Docker naturally handled per D-09
- Batch size limited to MAX_BATCH_SIZE=10 per D-10
- Health endpoint shows Redis and BullMQ connection status
- E2E batch test passes: 3 videos submitted, all process independently
- Failed job isolation confirmed: one failure doesn't affect others
- All locked decisions (D-01 through D-10) implemented
</success_criteria>

<output>
After completion, create `.planning/phases/10-async-batch-orchestrator/10-04-SUMMARY.md`
</output>