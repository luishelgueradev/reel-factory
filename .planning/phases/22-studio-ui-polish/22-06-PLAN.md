---
phase: 22-studio-ui-polish
plan: 06
type: execute
wave: 3
depends_on: ["22-03", "22-04", "22-05"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11]
must_haves:
  truths:
    - "The running Studio at port 3123 shows the 3-column shell with the static metadata placeholder"
    - "Tabs read Títulos | Overlays | Subtítulos and sample text in the Subtítulos tab drives the live preview"
    - "A back-layer overlay renders behind subtitles/titles; a front-layer overlay renders above them, in the live Player"
    - "The 9-point presets reposition titles and overlays (and the migrated subtitle presets work)"
    - "The panel reads as a dense, deliberate control surface with blue selection / single green Render CTA"
  artifacts: []
  key_links: []
---

<objective>
Final human visual verification of the whole phase against the 5 ROADMAP
success criteria, on the running Studio at port 3123. This is the gate that
confirms the impeccable densification, the 3-column shell, the layering model,
and the shared presets all cohere in the live app — things automated checks
cannot judge.

Purpose: Sign off the UI quality bar (the project's non-negotiable visual
guarantee) before the phase closes.
Output: Human approval or a concrete defect list to route into gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-UI-SPEC.md
@.planning/phases/22-studio-ui-polish/22-03-SUMMARY.md
@.planning/phases/22-studio-ui-polish/22-04-SUMMARY.md
@.planning/phases/22-studio-ui-polish/22-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the editor and start the Studio on port 3123</name>
  <files>services/remotion-studio (build + serve; no source files modified)</files>
  <read_first>
    - AGENTS.md § "Development Conventions" (the EXACT port-3123 start command + build:editor command)
  </read_first>
  <action>
    Build the editor: from services/remotion-studio run `npm run build:editor`. Then start the Studio using the EXACT command from AGENTS.md (port 3123, setsid, EDITOR_DIST, ACTIVE_PIPELINE_CONFIG_PATH pointing at the project-root pipeline/pipeline-config.json), redirecting logs to /tmp/remotion-server.log. Confirm the server is up (curl http://localhost:3123 returns 200 / HTML). Do NOT use any other port.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3123 | grep -q 200 && echo "studio up on 3123"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:editor` exits 0.
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3123` returns 200.
    - The process was started with the AGENTS.md port-3123 setsid command (not `npm run studio` on a default port).
  </acceptance_criteria>
  <done>Editor built; Studio serving on port 3123.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual sign-off against the 5 ROADMAP success criteria</name>
  <files>none (human verification)</files>
  <read_first>
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md (the authoritative visual/interaction contract being verified)
  </read_first>
  <what-built>
    The full Phase 22 Studio UI polish: 3-column shell (preview · controls · static "Metadata de redes — Próximamente" placeholder), Títulos | Overlays | Subtítulos tabs with sample text relocated atop Subtítulos, the overlay back/front layering model with a Capa control, and the shared 9-point PositionPresets across all three tabs — all under the default.css OKLCH theme with blue selection and a single green Render CTA.
  </what-built>
  <action>
    Pause for human verification. Present the verification steps below and wait for the resume signal.
  </action>
  <how-to-verify>
    Open http://localhost:3123 in Chrome (NOT Firefox — resistFingerprinting blocks web fonts; basic auth may prompt). Verify, mapping to the 5 ROADMAP criteria:
    1. (Crit 1 — D-01/D-02) Three columns are visible: live 9:16 preview on the left, tabbed controls in the center, and a 320px right column reading "Metadata de redes" + "Próximamente — descripción, hashtags y más generados a partir de tus subtítulos." The right column has NO buttons/inputs (placeholder only).
    2. (Crit 2 — D-05/D-06/D-11) The control panel reads dense and deliberate (not stacked forms): controls are grouped Posición → Estilo → Avanzado, the dark indigo theme is intact, selection/active states are BLUE, and the ONLY green button is Render Video (Guardar config is an outline button).
    3. (Crit 3 — D-10) Tabs read Títulos | Overlays | Subtítulos (no "Text" tab). The sample-text box is at the TOP of the Subtítulos tab; edit it and confirm the left preview's captions update to the new text.
    4. (Crit 4 — D-03/D-04) In Overlays, add an overlay (a PNG ≤5MB) and set its Capa to "Detrás" — confirm in the preview it renders BEHIND the subtitle/title text. Switch the same overlay to "Delante" — confirm it now renders ABOVE the text. With two overlays in the same band, confirm the later one paints on top.
    5. (Crit 5 — D-07/D-08/D-09) In Títulos and Overlays, click the 9-point preset cells (corners/edges/center) and confirm the element jumps to the expected position in the preview (size-aware: a right/bottom preset keeps the element fully on-frame). In Subtítulos, confirm the migrated 9-grid presets still move the captions correctly (no regression): the bottom-center / top-center / center cells move the subtitles to those positions, and the other 6 cells are visibly disabled/inert (D-08 enum mode).
    Note any visual defect, off-pattern density, wrong color (e.g. a second green), broken preset math, or layering mismatch.
  </how-to-verify>
  <verify>
    <human-check>All 5 ROADMAP success criteria confirmed in the live Studio at port 3123.</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms criteria 1–5 hold in the running app, or provides a specific defect list.
  </acceptance_criteria>
  <done>Human types "approved", or a concrete defect list is captured for gap closure.</done>
  <resume-signal>Type "approved" if all 5 criteria hold and the panel meets the quality bar, or describe the specific defects to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan. The Studio runs behind the existing basic auth / Cloudflare tunnel; no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-12 | Elevation of privilege | Studio server on 3123 | accept | Bound to the dev host as in all prior studio phases; auth unchanged; no new endpoints added this phase. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No installs (build + serve existing code only). |
</threat_model>

<verification>
- Studio reachable on 3123 (200).
- Human confirms all 5 ROADMAP success criteria in the live app.
</verification>

<success_criteria>
- Human sign-off ("approved") that the 3-column shell, densified panel, tab restructure, overlay layering, and 9-point presets all work and meet the visual quality bar.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-06-SUMMARY.md` when done.
</output>
