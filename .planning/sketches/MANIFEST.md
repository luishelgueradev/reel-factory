# Sketch Manifest

## Design Direction
Polish the Remotion Studio control surface into a **dense, deliberate, professional control panel** (product register — earned familiarity, the tool disappears into the task) while preserving the established **dark indigo design system** (canvas `#1a1a2e`, chrome `#16213e`, blue accent `#90caf9`, action green `#4CAF50`). Phase 22 reshapes the workspace into a **3-column shell** (live 9:16 preview · controls · social-media metadata placeholder), reorders each tab's controls **Position → Style → Advanced** with rarely-used controls disclosed, and introduces a shared **9-point position-preset** affordance for titles / overlays / subtitles. Color stays Restrained: tinted-indigo neutrals (OKLCH-tuned, no pure black/white), blue reserved for selection/focus, green reserved for the single primary action. This sketch pass (Phase 22 decision D-05) explores the layout and density questions before planning the real React redesign.

## Reference Points
- Existing Reel Factory Studio panel (`services/remotion-studio/src/preview/PreviewApp.tsx`) — the dark theme + 2-column shell being elevated.
- Product-tool craft bar: Linear / Figma / Raycast-grade control panels (dense, consistent, calm).
- Phase 22 context: `.planning/phases/22-studio-ui-polish/22-CONTEXT.md` (decisions D-01 … D-11).

## Sketches

| # | Name | Design Question | Winner | Tags |
|---|------|----------------|--------|------|
| 001 | three-column-shell | Does the preview \| controls \| metadata 3-column layout feel right, and how much room should the metadata placeholder get? | **D** — slim (content-sized) preview + controls grow into 2 internal columns + persistent (non-collapsing) metadata column | layout, shell, metadata-placeholder |
| 002 | control-density-disclosure | How should controls group/disclose within a tab so it reads as a deliberate panel, not stacked forms? | **A** — always-open titled sections, Position→Style→Advanced (height absorbed by 001-D's two columns) | density, disclosure, impeccable |
| 003 | position-presets | What should the shared 9-point position-preset affordance look like next to the X/Y inputs? | **B** — 9 arrow buttons (directional glyphs), compact enough for the 2-col panel | presets, position, shared-component |
| 004 | overlay-list-and-layering | How does the Overlays tab manage a multi-item list (add/select/remove + reorder=paint order + per-overlay back/front layer toggle) inside the dense panel, and how is "behind text" communicated? | **A** — overlay list (cards) + detail form below; list separate from the shared Posición→Estilo→Avanzado form, `Capa: Detrás/Delante` segmented in Estilo, drag-handle reorder = paint order | overlays, layering, list, D-03, D-04 |
| 005 | subtitles-tab-restructure | Does the sample-text textarea moved to the top of Subtitles (D-10) sit gracefully above Posición→Estilo→Avanzado, or break the dense row rhythm? | **C** — condensed single-line field at the top of Subtitles that expands on focus; keeps the dense grid intact ("set-once" input, "no se exporta" cue) | subtitles, tabs, textarea, D-10 |
| 006 | all-three-tabs-coherence | Composed in the 001-D shell, do Titles / Overlays / Subtitles hold the same section vocabulary when switching — or does the pattern drift? (integration check of 004 + 005 winners) | **A** — coheres at 2-col desktop; rule: lists + textarea full-width, form always 2-col. Titles/Overlays share the 004-A list+form (deliberate symmetry); B kept as the responsive single-column collapse | consistency, tabs, integration |
| 007 | preview-as-layer-map | Could the full-size live preview itself be a drag-to-position + layer-stack surface (overlays z-stacked vs text), instead of only the in-panel controls? | **A** — drag-to-position on the full preview, snapping to the 9 anchors and sharing the X/Y path (scope-expanding; click-to-select is the cheap subset if full drag is deferred) | frontier, preview, drag, layering |
| 008 | states-and-empties | What does the dense panel look like *off* the happy path — 0 overlays, the 3-overlay cap (Add disabled), the Guardar config dirty→saving→saved loop, and a no-video / loading preview? Which save-feedback mechanism fits the calm pro tool? | **B** — header status chip ("● Cambios sin guardar" / "Guardando…" / "✓ Guardado") left of a button that stays put; status is info, button is action. Empty/cap/no-video/Whisper-loading states validated and carry across all treatments | consistency, states, empty, save, edge-cases |
| 009 | motion-coherence | Composed in the shell, do the per-sketch micro-motions (tab switch · textarea expand · preset flash · drag snap · save confirm) read as one motion vocabulary? Which timing calibration is right? | **A** — Calm (170ms, ease-out-quart, current tokens). The 5 motions cohere as one vocabulary; two-tier timing (state `--dur`, travel `--dur2`). Snappy felt abrupt, Expressive felt slow on a high-frequency panel | consistency, motion, timing, feel |
| 010 | render-export-surface | The whole tool exists to produce a reel — where does "render / export" live in the shell, and what are its in-progress / done / failed states? (Touches the single-job pipeline: Whisper → silence-cut → Remotion.) | **A** — render on the dimmed preview (progress ring + 3-step pipeline → "Reel listo" card); no modal. **Render takes the green primary, Guardar config demotes** (reconcile with 008-B: green = the surface's true primary action). C's output+metadata pairing reserved for the AI phase | frontier, render, export, pipeline, states |
| 011 | subtitle-style-density | With the **full real caption field set** (~20 controls: font + weight/italic + glow + 4 colors + outline + size + highlight mode + bg + animation), does the always-open Posición→Estilo→Avanzado vocabulary survive, or become a wall — and where is the styling previewed? (The density stress-test 002 anticipated.) | **C** — specimen-led + 4-up **layout-mode preset cards** (mode is the highest-leverage decision, promoted above the form), then the 2-col form. Absorbs B's **2×2 color-role matrix** + **collapsible effect-rows** (Glow/Fondo = one switch line when off). A's flat ~20 rows = the wall; mode-in-dropdown hides the key control. Coherence holds (full-width lead, form stays 2-col) | frontier, subtitles, density, styling, effects, stress-test |
| 012 | subtitle-density-in-shell | 006's coherence check was on a *simplified* Subtitles. With 011-C's real dense Subtitles (specimen + 4-up mode cards + color matrix + effect-rows) composed next to the lean Titles/Overlays, does it still read as one panel or balloon into a different screen? | **B** — lead-slot normalized: a shared full-width `tab-lead` (list / textarea+specimen+modes) + always-2-col `tab-form` skeleton every tab fills, so the density jump reads as "same panel, fuller lead" and the build gets a literal TabLead/TabForm contract. A (011-C verbatim) already coheres; C trims the in-panel specimen | consistency, subtitles, density, coherence, integration |
| 013 | header-action-zone | 008-B's save status-chip and 010-A's Render-takes-green / Guardar-demotes collided on the same header and were never drawn together. Legible across idle→dirty→saving→rendering? Where does the chip go mid-render? | **B** — split zones: status chip on the left (ambient, next to the brand), Guardar+Render on the right separated by a hairline. **Render is the only green; Guardar is always an outline button; the chip carries dirty/saving/saved** (never a second green). The chip keeps a stable home during render | consistency, header, save, render, actions, states |
| 014 | title-style-density | 011 stress-tested *Subtitles*. Against the real `TitleStyleProps` (a **boxed** text card + **entrance animation** + glow, sharing font/color), does the Titles tab hit the same wall, and does the 011-C vocabulary transfer to titles? | **C** — specimen showing the **boxed** title + **entrance-animation preset cards** (Slide ↑/↓ · Fade · Ninguna) promoted above the 2-col form. 011-C's vocabulary transfers cleanly: mode-cards→entrance-cards, color-matrix→box/text pairing, effect-rows→glow. A (flat ~16 rows) tips into the same wall subtitles did | frontier, titles, density, styling, box, entrance, stress-test |
| 015 | north-star-composite | Does the whole vision hold in one screen, and where is the line between the committed editing surface and the scope-expanding frontier (007 drag, 010 render)? | **A** — the north star: everything composed (013-B header, 001-D shell, all three tabs, **007 drag-on-preview**, **010 render-on-stage**, metadata placeholder) coheres as one tool. **B** is kept as the committed-scope-only slice (Render ghosted, click-to-select instead of drag); the A↔B contrast names the plan-split boundary — B ships first, A's frontier layers add on without rework | frontier, composite, north-star, integration, scope |
| 016 | font-picker | The font control was a 4-card *stand-in* across 011/014 — 011 flagged it for its own sketch. How do you browse / search / pick among the **real 26 live-specimen fonts** (shared Titles + Subtitles) without breaking the dense panel? Inline scroll-box vs anchored popover vs slide-over gallery. | **C** — slide-over gallery: the font trigger opens a dedicated sheet over the controls column with search + category chips (Sans/Condensada/Display/Serif/Script/Mono) + a 2-up gallery of cards each rendering the **sample text** in its own face. Selection = blue; commits & dismisses. Browsing all 26 needs room the inline/popover treatments can't give without swamping the dense form. Self-contained → drops into the Títulos tab unchanged | frontier, fonts, picker, shared-component, density |
| 017 | first-run-empty-workspace | 008 covered the no-video *preview tile*; this is the **whole 3-column shell cold** — no video, nothing configured. Where does the single primary action (load a video → green) live, and are the rich controls gated, live-with-defaults, or behind a welcome takeover? | **B** — controls **live with defaults**: stage shows the dropzone (green = the one primary action here), but the dense panel is fully editable so you can pre-set your look; a banner frames it as "configurando los valores por defecto". Tool never feels locked. Guardar disabled, Render ghosted (green discipline holds). Metadata column = persistent "Próximamente". A (gated) hides the panel's value; C (welcome takeover) is too heavy for a single-purpose local studio | frontier, empty, onboarding, first-run, states |
| 018 | dense-tabs-at-breakpoint | Consistency check: do 011-C/014-C's specimen + **4-up mode/entrance cards** + 2×2 color matrix survive **006-B's single-column collapse** at narrow width — literal stack, reflowed multi-up grids, or does width pressure justify bending 002-A's always-open rule into disclosure? | **B** — **reflow the multi-up grids**, keep every section always-open (002-A intact): at the breakpoint the 2-col form goes 1-col, **mode/entrance cards → 2×2**, font grid → 1-up (full name), color matrix stays 2×2 (already compact). Nothing cramps; cards keep their behavior previews. One reflow rule that generalizes to all three tabs. A (literal stack) = the cramped anti-pattern; C (disclosure) bends the always-open rule unnecessarily | consistency, responsive, density, breakpoint, disclosure |

## Combined Direction (Phase 22)
Three columns: **content-sized 9:16 preview** · **controls that grow into two internal columns** (always-open Position → Style → Advanced sections, with **arrow-button 9-point presets** in Position) · **persistent ~320px social-metadata placeholder** ("Próximamente"). Dark indigo design system preserved, Restrained color. The realized synthesis is sketch **001 variant D**. Real-build note: collapse the controls' two internal columns to one below a width breakpoint.

### Per-tab structure (sketches 004 / 005 / 006)
- **Titles & Overlays** = **list + detail form** (004-A). Card list at the top (thumb · name · drag-handle reorder; Overlays also carry a `Detrás/Delante` badge), then the shared Posición→Estilo→Avanzado form below. Reorder = paint order (D-04). Overlay layer toggle (`Detrás del texto / Delante`, default `back` per D-03) lives in the form's Estilo section. Overlays capped at 3.
- **Subtitles** = **condensed/expanding sample-text textarea** (005-C) at the top (D-10, single-line, grows on focus, "no se exporta" cue), then the same Estilo/Avanzado form.
- **Coherence rule (006-A):** lists + textarea span **full width**; the Posición/Estilo/Avanzado form is always the **2-col grid**, collapsing to single-column under the responsive breakpoint (006-B).
- **Preview is also an editing surface (007-A, scope-expanding):** drag elements directly on the full-size preview, snapping to the same 9 anchors and sharing the X/Y path. Cheap subset if deferred: click-to-select an element to focus its controls.

### Follow-on resolutions (sketches 016 / 017 / 018)
- **Font picker (016-C):** the font control is **not** a few cards — it's a **slide-over gallery** opened from a current-font trigger. Search + category chips (Sans / Condensada / Display / Serif / Script / Mono) over a 2-up grid of cards, each rendering the sample text in its **own face**, against the real 26-font `AVAILABLE_FONTS` set (`src/fonts.ts`). Selection = blue; it's a **self-contained shared component** dropped into both Títulos and Subtítulos. Resolves the open sub-problem 011 flagged.
- **First-run / empty workspace (017-B):** cold start = dropzone on the stage with the dense controls **live on their defaults** (pre-configure the look before a video loads), framed by a "valores por defecto" banner. The single green is the upload action; Guardar disabled, Render ghosted; metadata column persists its "Próximamente" placeholder. Not gated, not a welcome takeover.
- **Responsive collapse of dense tabs (018-B):** at the narrow breakpoint, **reflow the multi-up grids** rather than cram or hide — 2-col form → 1-col, mode/entrance cards → 2×2, font grid → 1-up, color matrix stays 2×2. **002-A's always-open rule stays intact** (no disclosure-under-pressure). One reflow rule across all three tabs.
