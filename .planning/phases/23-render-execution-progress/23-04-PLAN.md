---
phase: 23-render-execution-progress
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - services/remotion-studio/src/preview/PreviewApp.tsx
  - services/remotion-studio/src/preview/PreviewPlayer.tsx
  - services/remotion-studio/src/preview/render-overlay.test.tsx
autonomous: true
requirements: [RENDER-01, RENDER-02, RENDER-03, RENDER-04]
must_haves:
  truths:
    - "The user uploads an MP4 (drop or picker) and it becomes the live @remotion/player background (D-01/D-02)"
    - "Clicking Render submits the upload via POST /api/render and the Studio shows live step name + honest % polling /api/status (RENDER-01/02)"
    - "While a render runs the editor dims, controls are visible-but-disabled, and a second submit is blocked (D-04/D-06)"
    - "On completion the finished 9:16 plays inline with Descargar (accent) + Renderizar de nuevo (green) (RENDER-04/D-07/D-08)"
    - "On failure an inline low-chroma danger surface shows the plain reason + muted-mono cause line + Reintentar (RENDER-03/D-09)"
    - "Green discipline holds: Render is the only green; Descargar uses accent; failure uses danger — never two greens"
  artifacts:
    - path: "services/remotion-studio/src/preview/PreviewApp.tsx"
      provides: "Render submit handler, ~1.5s poll loop with ref cleanup, 4 inline states (upload/progress/success/failure), header CTA state machine"
      contains: "/api/render"
    - path: "services/remotion-studio/src/preview/PreviewPlayer.tsx"
      provides: "rawVideoSrc driven by the uploaded-file object URL with revoke-on-change"
      contains: "rawVideoSrc"
    - path: "services/remotion-studio/src/preview/render-overlay.test.tsx"
      provides: "Component coverage: completed→success surface, failed(exit137)→danger cause line, progress→step label, single-green assertion"
      contains: "exit 137"
  key_links:
    - from: "services/remotion-studio/src/preview/PreviewApp.tsx"
      to: "/api/render and /api/status/:jobId and /api/result/:jobId (Plan 02 proxy routes)"
      via: "fetch FormData submit → poll interval → inline <video src=/api/result/...>"
      pattern: "/api/status/"
    - from: "services/remotion-studio/src/preview/PreviewApp.tsx"
      to: "render-status.ts (Plan 01: stepLabel, causeLine, isLongStep, parseStatusError)"
      via: "import and call in the progress + failure surfaces"
      pattern: "stepLabel"
---

<objective>
Wire the four NEW inline render surfaces into the existing Phase-22 preview stage, consuming the
Plan 02 proxy routes and the Plan 01 mapping functions. This is the user-facing payoff: upload an
MP4 → click Render → watch live step progress on the dimmed stage → land an inline "Reel listo"
(play + download) or an inline failure with the honest cause line. The 3-column shell, header, tabs,
and @remotion/player already exist (Phase 22) and are NOT redesigned — only the four inline states
overlaying the stage are new (23-UI-SPEC.md scope).

**BINDING UI GATE (non-negotiable, AGENTS.md + 23-UI-SPEC.md):** At the START of execution (not as
an afterthought), invoke the `impeccable` skill AND the `frontend-design` plugin, and build every
surface against the canonical OKLCH tokens in
`.claude/skills/sketch-findings-reel-factory/sources/themes/default.css` (already wired into the
live Studio). Do NOT invent colors/sizes/spacing — use `--s-*`, `--t-*`, `--accent`, `--action`,
`--danger`, `--success` verbatim per the 23-UI-SPEC.md contract.

Purpose: RENDER-01 (submit from Studio), RENDER-02 (live progress), RENDER-03 (success/failure
notification), RENDER-04 (finished-video access) — the user watches their reel get made.
Output: PreviewApp.tsx + PreviewPlayer.tsx wired; render-overlay.test.tsx covering the states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-render-execution-progress/23-UI-SPEC.md
@.planning/phases/23-render-execution-progress/23-RESEARCH.md
@.planning/phases/23-render-execution-progress/23-PATTERNS.md
@.claude/skills/sketch-findings-reel-factory/SKILL.md
@.claude/skills/sketch-findings-reel-factory/sources/themes/default.css

@services/remotion-studio/src/preview/PreviewApp.tsx
@services/remotion-studio/src/preview/PreviewPlayer.tsx
@services/remotion-studio/src/preview/render-status.ts

<interfaces>
Plan 01 — render-status.ts exports (import these; do NOT re-derive the maps):
  STEP_LABELS, stepLabel(currentStep), causeLine(step, exitCode?), isLongStep(step), parseStatusError(errStr)

Plan 02 — proxy routes the UI calls:
  POST /api/render  — body = FormData with field `videos` = the uploaded File (api-server /batch field name).
    Do NOT set Content-Type manually (let the browser set the multipart boundary). Returns
    `{ jobs:[{ jobId }] }` → read `jobs[0].jobId`. On 502 → `{ error:{ step:"proxy", message } }`.
  GET /api/status/:jobId — returns `{ status, currentStep, progress, stepInfo, steps, error }`.
    Terminal: status==="completed" → success; status==="failed" → failure (read `error` string,
    feed parseStatusError); HTTP 404 → "Job no encontrado".
  GET /api/result/:jobId — Range-aware MP4 for inline <video src>; append ?download=1 for download.

PreviewApp.tsx existing anchors:
  - handleSave async tri-state (L300-343): try/catch/finally + setSaving/setSaveError/setSaveSuccess —
    the analog to copy for the render-submit handler.
  - saveTimeoutRef lifecycle (declared L221, set in handler, cleared on unmount L283-287) — the analog
    to MIRROR for the poll interval (a pollRef cleared on unmount AND on terminal state).
  - config-load useEffect (L243-280): .then(res=>res.json()).catch(defaults) fetch shape.
  - header CTA: the disabled green "▶ Render Video" button (L424-442) — wire its 4 states.
  - save-status chips (L369-399): token-styled status-chip markup analog for inline surfaces.

PreviewPlayer.tsx: inputProps useMemo (L49-63) sets rawVideoSrc:"/sample-video.mp4" (L52) — D-02
  swaps this to the uploaded File's object URL passed down as a prop. useMemo deps L62.

Copy + tokens: 23-UI-SPEC.md Copywriting Contract (verbatim Spanish strings), §1-§4 surface specs,
  Color (accent vs action-green discipline), Typography (--t-* scale), Spacing (--s-* scale), Motion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upload affordance + D-02 background swap (PreviewApp + PreviewPlayer)</name>
  <files>services/remotion-studio/src/preview/PreviewApp.tsx, services/remotion-studio/src/preview/PreviewPlayer.tsx</files>
  <read_first>
    - services/remotion-studio/src/preview/PreviewApp.tsx (state region near L221; the col-1 preview stage block near L447-449)
    - services/remotion-studio/src/preview/PreviewPlayer.tsx (inputProps useMemo L49-63, rawVideoSrc L52, deps L62)
    - .planning/phases/23-render-execution-progress/23-UI-SPEC.md (§1 Upload affordance; Copywriting "Subí tu video"/"Elegir archivo"/"MP4 · hasta 10 min"/preview-sync note)
    - .planning/phases/23-render-execution-progress/23-RESEARCH.md (Pitfall 4 object-URL revoke; D-02 swap Code Example)
    - .planning/phases/23-render-execution-progress/23-PATTERNS.md (PreviewPlayer section L168-190: object URL + revoke)
  </read_first>
  <behavior>
    - Selecting/dropping an MP4 sets a `file` state; the @remotion/player background (rawVideoSrc) becomes `URL.createObjectURL(file)`.
    - Re-uploading a new file revokes the previous object URL (no leak).
    - With no file uploaded, the stage shows the dropzone hero (not the sample video as the "real" bg).
    - The preview-sync note copy is present so the user knows subtitles are sample-timed until the real render.
  </behavior>
  <action>
    INVOKE `impeccable` + `frontend-design` FIRST. Add a `file: File | null` state and an `objectUrl`
    derived via `useMemo(() => file ? URL.createObjectURL(file) : null, [file])` with a cleanup
    `useEffect(() => () => { if (objectUrl) URL.revokeObjectURL(objectUrl); }, [objectUrl])` (RESEARCH
    Pitfall 4). Add the dropzone-hero upload affordance ON the stage per 23-UI-SPEC §1: aspect-ratio
    9/16, 18px radius, `2px dashed var(--border-strong)`, fill `oklch(0.20 0.025 280 / 0.5)`, hover →
    accent border; supports drag-drop AND an "Elegir archivo" picker (hidden `<input type=file
    accept="video/mp4">`). The picker button is THE single green at cold start (load-a-video is the only
    primary action); once a file loads, green returns to the header Render CTA. Use the verbatim Spanish
    copy from the UI-SPEC (heading "Subí tu video", sublabel "MP4 · hasta 10 min", preview-sync note
    "Vas a ver tus subtítulos sincronizados recién en el video final."). Pass `objectUrl` down to
    PreviewPlayer as a prop (e.g. `rawVideoSrc`); in PreviewPlayer.tsx use it for the inputProps
    `rawVideoSrc` (replacing the hardcoded "/sample-video.mp4" at L52, falling back to "/sample-video.mp4"
    when null) and add the prop to the useMemo deps (L62). Subtitles stay sample-timed (D-02 nuance) —
    do NOT attempt real sync. Reuse the existing OKLCH tokens; no new color values.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v node_modules | (! grep -iE "PreviewApp|PreviewPlayer")</automated>
  </verify>
  <acceptance_criteria>
    - PreviewApp.tsx has a `file` state, an object-URL useMemo, and a revoke cleanup useEffect.
    - `grep -c "createObjectURL" services/remotion-studio/src/preview/PreviewApp.tsx` ≥ 1 and `grep -c "revokeObjectURL" ...` ≥ 1.
    - PreviewPlayer.tsx `rawVideoSrc` is driven by the passed object-URL prop (not the bare "/sample-video.mp4" literal) and the prop is in the useMemo deps.
    - The dropzone copy strings "Subí tu video" and "MP4 · hasta 10 min" appear in PreviewApp.tsx.
    - tsc reports no new errors in PreviewApp/PreviewPlayer.
  </acceptance_criteria>
  <done>An uploaded MP4 drives the live preview background with object-URL revoke; dropzone hero + copy in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render submit + poll loop + 4 inline states + header CTA state machine</name>
  <files>services/remotion-studio/src/preview/PreviewApp.tsx, services/remotion-studio/src/preview/render-overlay.test.tsx</files>
  <read_first>
    - services/remotion-studio/src/preview/PreviewApp.tsx (handleSave tri-state L300-343; saveTimeoutRef lifecycle L221/L283-287; header CTA L424-442; status chips L369-399)
    - services/remotion-studio/src/preview/render-status.ts (Plan 01: stepLabel, causeLine, isLongStep, parseStatusError)
    - .planning/phases/23-render-execution-progress/23-UI-SPEC.md (§2 progress, §3 success, §4 failure; Copywriting Contract; Color green-discipline; Motion)
    - .planning/phases/23-render-execution-progress/23-RESEARCH.md (Pattern 2 terminal states; Pitfall 1 long-step shimmer; Pitfall 5 OOM cause)
    - .planning/phases/23-render-execution-progress/23-PATTERNS.md (PreviewApp section L92-165: submit handler, poll loop, CTA state machine, single-green law)
  </read_first>
  <behavior>
    - Clicking Render with a `file` present POSTs FormData (field `videos`=file) to /api/render and reads `jobs[0].jobId`; no Content-Type set manually.
    - A render-active flag dims the stage, disables the controls + Guardar config + a second Render submit (single-job, D-06).
    - A poll loop hits /api/status/:jobId every ~1.5s; the progress surface shows stepLabel(currentStep) prominently + the honest `progress` % from /status + "paso n/6".
    - While currentStep is the long step (isLongStep true) the surface shows "este paso toma más tiempo" + an indeterminate shimmer (not faked forward movement).
    - status==="completed" stops polling → success surface: inline <video src="/api/result/:jobId"> plays, "✓ Reel listo", Descargar (accent, href /api/result/:jobId?download=1) + Renderizar de nuevo (green).
    - status==="failed" stops polling → danger surface: "No se pudo generar el reel" + causeLine(step,exitCode) in muted mono (exit 137 → "— sin memoria") + Reintentar (green).
    - HTTP 404 from /status → "Job no encontrado".
    - The poll interval is cleared on every terminal state AND on component unmount (no leak).
    - Header CTA cycles: "▶ Render Video" (idle) → "⟳ Renderizando…" (disabled) → "▶ Renderizar de nuevo" (success) → "↻ Reintentar" (failure).
    - At most ONE green control is visible at any time (Render/Reintentar/Renderizar-de-nuevo); Descargar is accent, never green.
  </behavior>
  <action>
    INVOKE `impeccable` + `frontend-design` FIRST (continued from Task 1). Import stepLabel, causeLine,
    isLongStep, parseStatusError from ./render-status. Add a render state machine (e.g.
    `renderState: "idle"|"submitting"|"running"|"success"|"failure"`, plus `jobId`, `status`,
    `resultUrl`). Build the submit handler by copying the handleSave try/catch/finally tri-state shape
    (L300-343): set submitting, POST FormData (`videos`=file) to /api/render, parse `jobs[0].jobId`,
    transition to running, start the poll. Build the poll loop by MIRRORING the saveTimeoutRef cleanup
    discipline (L221/L283-287): a `pollRef` (setInterval ~1500ms) cleared in a useEffect unmount cleanup
    AND whenever a terminal state is reached (RESEARCH Pattern 2). Each tick fetches /api/status/:jobId;
    on completed → set success + resultUrl=`/api/result/${jobId}`; on failed → feed `error` through
    parseStatusError then causeLine; on 404 → "Job no encontrado". Render the four inline surfaces over
    the dimmed stage per 23-UI-SPEC §1-§4 (dim via `filter: brightness(0.32) saturate(0.5)`; overlay
    fadein ~200-250ms; failure single nudge shake; `prefers-reduced-motion` collapses shimmer+nudge).
    Use the status-chip markup (L369-399) as the token analog for the success/failure cards. Wire the
    header CTA (L424-442) through its 4 states + enable/disable. Enforce GREEN DISCIPLINE: exactly one
    green at a time (the CTA or Reintentar or Renderizar-de-nuevo); Descargar uses `var(--accent)`;
    the failure surface uses `var(--danger)` (low-chroma, tint behind icon + thin border — never a
    saturated banner, never action-green). Use the verbatim Spanish copy from the UI-SPEC Copywriting
    Contract. Disable a second concurrent submit while running (D-06 / MAX_CONCURRENT_JOBS=1).
    Write render-overlay.test.tsx (vitest + React Testing Library) that renders the component (or the
    extracted surface components) and asserts: a `completed` status shows "✓ Reel listo" + a <video>
    with src containing /api/result/; a `failed` status with an error string carrying exit 137 renders
    a cause line containing "remotion-renderer · exit 137 — sin memoria"; a running status with
    currentStep "remotion-renderer" shows "Renderizando" + the long-step hint; and there is at most one
    element styled with the action-green token at a time (single-green law).
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx vitest run src/preview/render-overlay.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/render" services/remotion-studio/src/preview/PreviewApp.tsx` ≥ 1 and `grep -c "/api/status/" ...` ≥ 1 and `grep -c "/api/result/" ...` ≥ 1.
    - PreviewApp.tsx imports stepLabel + causeLine from ./render-status.
    - A `pollRef`/interval is cleared in a useEffect cleanup AND on terminal state (grep for clearInterval).
    - The render submit uses FormData with the field name `videos` and does NOT set a manual Content-Type for it.
    - render-overlay.test.tsx asserts the success surface (<video> with /api/result/), the exit-137 cause line, the long-step hint, and the single-green law.
    - `npx vitest run src/preview/render-overlay.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Submit→poll→inline success/failure works end-to-end against the proxy; header CTA cycles; green discipline + copy honored; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → Studio proxy | The uploaded File + jobId originate in the browser; all validation/limits are enforced server-side (Plan 02 + api-server multer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Denial of service | second concurrent submit from the UI | mitigate | The render-active state disables the Render CTA + Guardar config + a second submit while running (D-06); `MAX_CONCURRENT_JOBS=1` is respected by not allowing a second POST /api/render. |
| T-23-04-02 | Information disclosure | failure cause line shown to user | accept | Cause line is step + exit code only (via causeLine/parseStatusError) — no secrets or filesystem paths; single-user local Studio. |
| T-23-04-03 | Resource exhaustion | object URL / poll-interval leak | mitigate | Object URL revoked on file change (Task 1); poll interval cleared on terminal state + unmount (Task 2). |
| T-23-SC | Tampering | npm installs | mitigate | No new runtime packages in this plan (RTL/testing libs, if any, are dev-only and standard); nothing to gate. |
</threat_model>

<verification>
- `cd services/remotion-studio && npx vitest run` — full studio suite green (render-status, server, render-overlay).
- `npm run build:editor` from services/remotion-studio — the editor SPA builds without error.
- `npx tsc --noEmit` — no new type errors in PreviewApp/PreviewPlayer.
- Real end-to-end render + visual sign-off: Plan 05 (human checkpoint).
</verification>

<success_criteria>
- Upload → live preview background; Render submit → live step+% progress; inline success (play+download) / inline failure (cause+retry).
- Green discipline held: one green at a time; Descargar accent; failure danger.
- Poll + object-URL lifecycles leak-free.
- Component tests cover the success/failure/progress/single-green behaviors.
- impeccable + frontend-design pass executed at start (UI gate).
</success_criteria>

<output>
Create `.planning/phases/23-render-execution-progress/23-04-SUMMARY.md` when done.
</output>
