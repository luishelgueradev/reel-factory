---
phase: 21-png-overlays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/pipeline-config.ts
  - services/remotion-renderer/src/pipeline-config.ts
  - services/remotion-studio/src/server.ts
  - services/remotion-renderer/src/pipeline-config.test.ts
autonomous: true
requirements:
  - OVERLAY-01
  - OVERLAY-02
  - OVERLAY-03

must_haves:
  truths:
    - "PipelineConfig accepts overlays?: PngOverlayConfig[] without validation errors"
    - "PngOverlayConfig requires imageData (string), x (number), y (number), displayWidth (number); opacity and _resolvedFile are optional"
    - "validatePipelineConfig rejects overlays with non-string imageData, negative x/y/displayWidth, opacity outside 0-1"
    - "Express JSON body limit is 10mb (rejects payloads between 1mb and 10mb are now accepted)"
    - "Pipeline-config test suite remains green after schema additions"
  artifacts:
    - path: "services/remotion-studio/src/pipeline-config.ts"
      provides: "PngOverlayConfig interface + overlays field on PipelineConfig + validation block"
      contains: "PngOverlayConfig"
    - path: "services/remotion-renderer/src/pipeline-config.ts"
      provides: "Synced copy of studio pipeline-config.ts"
      contains: "PngOverlayConfig"
    - path: "services/remotion-renderer/src/pipeline-config.test.ts"
      provides: "OVERLAY-01/02/03 test cases appended to existing suite"
      contains: "OVERLAY-01"
    - path: "services/remotion-studio/src/server.ts"
      provides: "JSON body limit raised to 10mb"
      contains: "10mb"
  key_links:
    - from: "services/remotion-renderer/src/pipeline-config.ts"
      to: "services/remotion-studio/src/pipeline-config.ts"
      via: "cp command (renderer sync)"
      pattern: "PngOverlayConfig"
    - from: "services/remotion-renderer/src/pipeline-config.test.ts"
      to: "validatePipelineConfig"
      via: "import"
      pattern: "validatePipelineConfig.*overlays"
---

<objective>
Establish the schema contract for PNG overlays: add the `PngOverlayConfig` interface and `overlays?: PngOverlayConfig[]` field to `PipelineConfig`, implement validation in `validatePipelineConfig`, sync the updated config to the renderer, and raise the Express JSON body limit to 10 MB so large base64 PNG payloads are not rejected.

Purpose: Every subsequent plan (render path, studio UI) depends on this type definition and validation existing. This is the contract everything else builds against.
Output: Updated pipeline-config.ts (both studio + renderer), updated server.ts, extended pipeline-config.test.ts with OVERLAY-01/02/03 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-png-overlays/21-CONTEXT.md
@.planning/phases/21-png-overlays/21-RESEARCH.md
@.planning/phases/21-png-overlays/21-PATTERNS.md
@.planning/phases/21-png-overlays/21-VALIDATION.md

<interfaces>
<!-- Key contracts extracted from codebase. Executor reads, does not re-discover. -->

From services/remotion-studio/src/pipeline-config.ts (current state):

```typescript
// Current PipelineConfig (lines 129-133) — Phase 21 adds overlays field:
export interface PipelineConfig {
  subtitle: SubtitleConfig;
  titles?: TitleConfig[];
  visualEffects?: VisualEffectsConfig;
  // Phase 21 adds: overlays?: PngOverlayConfig[];
}

// TitleConfig (lines 90-95) — mirror pattern for PngOverlayConfig:
export interface TitleConfig {
  text: string;
  startTimeMs: number;
  durationMs: number;
  style?: TitleStyleProps;
}

// validatePipelineConfig signature (line 228):
export function validatePipelineConfig(config: unknown): { valid: boolean; errors: string[] }
```

From services/remotion-studio/src/server.ts (line 85 — current):
```typescript
app.use(express.json({ limit: "1mb" }));  // Phase 21 changes to "10mb"
```

From services/remotion-renderer/vitest.config.ts:
```typescript
// include: ["src/**/*.test.ts"] — renderer vitest picks up all .test.ts in src/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PngOverlayConfig interface and validation to pipeline-config.ts</name>
  <files>
    services/remotion-studio/src/pipeline-config.ts
    services/remotion-renderer/src/pipeline-config.test.ts
  </files>
  <read_first>
    - services/remotion-studio/src/pipeline-config.ts — full file; read the TitleConfig block (lines 67–95), PipelineConfig block (lines 129–133), and validatePipelineConfig titles validation block (lines 361–448) before touching anything
    - services/remotion-renderer/src/pipeline-config.test.ts — read lines 490–524 (end of file) to know where to append new tests
    - .planning/phases/21-png-overlays/21-PATTERNS.md — PngOverlayConfig interface definition (section "New interface to add") and validation pattern
    - .planning/phases/21-png-overlays/21-CONTEXT.md — D-03 (exact PngOverlayConfig fields), D-04 (imageRendering), D-05 (upscale warning heuristic), D-06 (static opacity)
  </read_first>
  <behavior>
    - Test 1: validatePipelineConfig({subtitle:{layout:"tiktok"}, overlays:[]}) returns valid:true
    - Test 2: validatePipelineConfig({subtitle:{layout:"tiktok"}, overlays:[{imageData:"data:image/png;base64,abc", x:40, y:40, displayWidth:200}]}) returns valid:true
    - Test 3: overlays[0].imageData missing → errors includes "imageData"
    - Test 4: overlays[0].x is negative → errors includes "x"
    - Test 5: overlays[0].displayWidth is 0 → errors includes "displayWidth" (must be > 0)
    - Test 6: overlays[0].opacity is 1.5 → errors includes "opacity"
    - Test 7: overlays is not an array (e.g., {}) → errors includes "overlays"
    - Test 8: overlays[0]._resolvedFile present → valid:true (runtime-only field, not rejected by validation)
  </behavior>
  <action>
    In services/remotion-studio/src/pipeline-config.ts, make the following additions:

    1. Insert the PngOverlayConfig interface after the TitleConfig block (after line 95) and before the TransitionType line. The interface has exactly these fields per D-03:
       - imageData: string (base64 data URL, required)
       - x: number (pixel x from left edge of 1080px frame, required)
       - y: number (pixel y from top edge of 1920px frame, required)
       - displayWidth: number (CSS display width in pixels, required, must be > 0)
       - opacity?: number (0–1 inclusive, default 1 at render time)
       - _resolvedFile?: string (runtime-only, set by render.ts, NOT persisted; validation ignores it)

    2. Add overlays?: PngOverlayConfig[] as an optional field to the PipelineConfig interface, after the titles field.

    3. In validatePipelineConfig, append a validation block for overlays after the titles validation block (after line 448). Follow the exact pattern used for titles:
       - cfg.overlays !== undefined: check it is an array; if not, push error "PipelineConfig.overlays must be an array"
       - For each overlay entry: validate it is a non-null object; validate imageData is a string and non-empty; validate x is a finite number >= 0; validate y is a finite number >= 0; validate displayWidth is a finite number > 0; validate opacity (if present) is a number between 0 and 1 inclusive. Do NOT validate or reject _resolvedFile (mirrors the _meta pattern).

    In services/remotion-renderer/src/pipeline-config.test.ts, append a new describe block "PNG overlays (OVERLAY-01/02/03)" at the end of the file. Write tests covering the 8 behaviors listed in the behavior section above. Import validatePipelineConfig from "./pipeline-config" (already imported at top of file — reuse).

    Run tests BEFORE writing implementation (RED phase), then implement (GREEN phase).
  </action>
  <verify>
    <automated>cd services/remotion-renderer && npx vitest run src/pipeline-config.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PngOverlayConfig" services/remotion-studio/src/pipeline-config.ts` returns >= 2 (interface definition + PipelineConfig field)
    - `grep -c "overlays" services/remotion-studio/src/pipeline-config.ts` returns >= 3 (interface, PipelineConfig field, validation block)
    - `grep "10mb\|\"10mb\"" services/remotion-studio/src/pipeline-config.ts` returns nothing (limit is in server.ts, not here)
    - All OVERLAY-* test cases in pipeline-config.test.ts pass green: `cd services/remotion-renderer && npx vitest run src/pipeline-config.test.ts` exits 0
    - Full renderer suite still green: `cd services/remotion-renderer && npx vitest run` exits 0
  </acceptance_criteria>
  <done>PngOverlayConfig interface exported from pipeline-config.ts; validatePipelineConfig enforces imageData (string), x/y (non-negative finite numbers), displayWidth (positive finite number), opacity (0–1 if present); overlays[] field on PipelineConfig; all 8 test behaviors green in pipeline-config.test.ts</done>
</task>

<task type="auto">
  <name>Task 2: Raise Express JSON body limit and sync pipeline-config to renderer</name>
  <files>
    services/remotion-studio/src/server.ts
    services/remotion-renderer/src/pipeline-config.ts
  </files>
  <read_first>
    - services/remotion-studio/src/server.ts — read line 85 (express.json limit) and surrounding context (~lines 80–100) before editing
    - services/remotion-renderer/src/pipeline-config.ts — read the first 10 lines to confirm it is indeed a copy of studio's pipeline-config.ts (same structure)
  </read_first>
  <action>
    1. In services/remotion-studio/src/server.ts, change the express.json limit on line 85 from "1mb" to "10mb" (per D-10). The line currently reads: app.use(express.json({ limit: "1mb" })). Change only the limit string. Add a comment: // Raised to 10mb for base64 PNG overlay payloads (Phase 21 D-10).

    2. Sync the updated pipeline-config.ts from studio to renderer. Run this exact command (per AGENTS.md renderer-sync convention):
       cp services/remotion-studio/src/pipeline-config.ts services/remotion-renderer/src/

    Verify the sync produced an exact copy by checking that PngOverlayConfig appears in both files.
  </action>
  <verify>
    <automated>grep -n "10mb" services/remotion-studio/src/server.ts && grep -c "PngOverlayConfig" services/remotion-renderer/src/pipeline-config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "10mb" services/remotion-studio/src/server.ts` returns exactly one match on the express.json line
    - `grep -c "PngOverlayConfig" services/remotion-renderer/src/pipeline-config.ts` returns >= 2 (synced copy matches studio)
    - `diff services/remotion-studio/src/pipeline-config.ts services/remotion-renderer/src/pipeline-config.ts` exits 0 (files are identical)
    - Renderer test suite still green: `cd services/remotion-renderer && npx vitest run` exits 0
  </acceptance_criteria>
  <done>server.ts JSON limit is "10mb"; renderer pipeline-config.ts is an exact copy of studio's updated version; both files contain PngOverlayConfig interface and overlays field on PipelineConfig</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| studio browser → PUT /api/config | Untrusted JSON payload crosses here; contains base64 imageData |
| render.ts → public/ filesystem | Decoded base64 bytes written to public/overlay-N.png before bundle() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | validatePipelineConfig: imageData field | mitigate | Validate imageData is a non-empty string (format prefix validated in Plan 21-02 at render.ts decode point); client-side MIME check in OverlayEditor (Plan 21-03) |
| T-21-02 | DoS | express.json limit raised to 10mb | accept | Client-side 5 MB file gate in OverlayEditor (Plan 21-03) bounds realistic input; 10mb server limit prevents HTTP body bomb; single-job pipeline means no concurrent amplification |
| T-21-03 | Tampering | PipelineConfig.overlays[].x/y/displayWidth | mitigate | validatePipelineConfig enforces non-negative x/y and positive displayWidth; malformed values produce validation error before reaching render |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 21 — zero install surface |
</threat_model>

<verification>
After both tasks complete:
1. `cd services/remotion-renderer && npx vitest run` — full suite green (includes new OVERLAY-* tests)
2. `grep "10mb" services/remotion-studio/src/server.ts` — returns one match
3. `diff services/remotion-studio/src/pipeline-config.ts services/remotion-renderer/src/pipeline-config.ts` — no diff (files identical)
4. TypeScript check: `cd services/remotion-renderer && npx tsc --noEmit` exits 0 (new interface compiles cleanly)
</verification>

<success_criteria>
- PngOverlayConfig interface is exported from pipeline-config.ts in both studio and renderer
- PipelineConfig.overlays?: PngOverlayConfig[] field exists in both
- validatePipelineConfig rejects malformed overlays (missing imageData, negative x/y, zero/negative displayWidth, opacity outside 0–1)
- validatePipelineConfig accepts well-formed overlays and ignores _resolvedFile
- Express JSON body limit is "10mb" in server.ts
- All renderer tests pass (zero regressions)
</success_criteria>

<output>
Create `.planning/phases/21-png-overlays/21-01-SUMMARY.md` when done
</output>
