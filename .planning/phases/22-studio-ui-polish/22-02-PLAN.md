---
phase: 22-studio-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/editor/components/PositionPresets.tsx
autonomous: true
requirements: [D-07, D-08, D-09]
must_haves:
  truths:
    - "A shared PositionPresets component renders a 3×3 grid of 9 directional arrow buttons"
    - "In px mode (titles/overlays) clicking a preset computes a size-aware X/Y against the 1080×1920 frame and calls onApply(x, y)"
    - "In enum mode (subtitles) only the 3 reachable cells (bottom-center, top-center, center) are enabled; the other 6 are disabled/hidden, and clicking calls onApplyAnchor with the mapped enum value"
    - "Center/right presets subtract element width; center/bottom subtract element height"
    - "The component owns no config state — it only reports x/y (px mode) or an enum value (enum mode)"
  artifacts:
    - path: "services/remotion-studio/src/editor/components/PositionPresets.tsx"
      provides: "Shared 9-point preset grid (px mode) + reduced enum mode, consumed by StyleControls, TitleEditor, OverlayEditor"
      min_lines: 70
      exports: ["PositionPresets", "computePresetXY"]
  key_links:
    - from: "PositionPresets (px mode)"
      to: "host handleDraftChange (titles/overlays)"
      via: "onApply(x, y) callback"
      pattern: "onApply\\("
    - from: "PositionPresets (enum mode)"
      to: "StyleControls onChange({ position })"
      via: "onApplyAnchor(SubtitlePosition) callback"
      pattern: "onApplyAnchor\\("
---

<objective>
Build the new shared `PositionPresets.tsx` component: a 3×3 arrow-button grid
(`↖ ↑ ↗ / ← • → / ↙ ↓ ↘`) with size-aware X/Y math against the 1080×1920 frame
and a top-left anchor. It is a pure, stateless presentational component with TWO
modes so a SINGLE shared affordance serves all three tabs (D-07/D-08/D-09):

- **px mode (default)** — full 9-cell grid; clicking calls `onApply(x, y)` with
  size-aware pixel coords. Used by Titles + Overlays.
- **enum mode** — for the subtitle `SubtitlePosition` 3-value model
  (`bottom-center` | `top-center` | `center-screen`). Only the 3 reachable cells
  are enabled (bottom-center → `bottom-center`, top-center → `top-center`, center →
  `center-screen`); the other 6 cells are disabled/hidden. Clicking an enabled cell
  calls `onApplyAnchor(value)`. This is the DECIDED subtitle migration path — the
  9-point grid and the 3-enum model are reconciled here, in the locked signature,
  so 22-05 has exactly ONE path to build (no either/or).

Consumers (StyleControls, TitleEditor, OverlayEditor in 22-05) wire the
appropriate mode into their existing draft-update paths.

Purpose: One DRY affordance for all three tabs, defined as a contract BEFORE
its consumers so 22-05 builds against a known prop signature (interface-first),
with the subtitle enum case resolved at the contract level (not deferred to a
build-time either/or).
Output: A self-contained component file, no consumer wiring yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-studio-ui-polish/22-CONTEXT.md
@.planning/phases/22-studio-ui-polish/22-PATTERNS.md
@.planning/phases/22-studio-ui-polish/22-UI-SPEC.md

<interfaces>
<!-- LOCKED signature. Two modes: px (titles/overlays) and enum (subtitles). -->
<!-- D-07 keeps the 9-point grid canonical for titles/overlays (full px path). -->
<!-- D-08 migrates subtitles onto the SAME component via the reduced enum mode. -->
type PresetAnchorX = "left" | "center" | "right";
type PresetAnchorY = "top" | "center" | "bottom";
type SubtitleAnchor = "bottom-center" | "top-center" | "center-screen"; // == SubtitlePosition

interface PositionPresetsProps {
  // ── px mode (default — Titles & Overlays) ──
  elementWidth?: number;          // required in px mode
  elementHeight?: number;         // required in px mode
  frameWidth?: number;            // default 1080
  frameHeight?: number;           // default 1920
  onApply?: (x: number, y: number) => void;   // px-mode click handler

  // ── enum mode (Subtitles) ──
  mode?: "px" | "enum";           // default "px"
  // Maps the (anchorX, anchorY) cell → SubtitlePosition enum value.
  // ONLY the 3 reachable cells are present; absent cells are disabled/hidden.
  // Canonical mapping: bottom-center→"bottom-center", top-center→"top-center",
  // center(center,center)→"center-screen".
  anchorToValue?: Partial<Record<`${PresetAnchorX}-${PresetAnchorY}`, SubtitleAnchor>>;
  onApplyAnchor?: (value: SubtitleAnchor) => void; // enum-mode click handler

  // ── shared ──
  activeAnchor?: string;          // drives blue highlight on the matching cell
}

<!-- Behavior rules:
     - mode defaults to "px". In px mode: full 9-cell grid; click → onApply(computePresetXY(...)).
     - In enum mode: a cell is ENABLED iff its `${anchorX}-${anchorY}` key exists in anchorToValue.
       Enabled cell click → onApplyAnchor(anchorToValue[key]). Disabled cells render dimmed +
       `disabled` (or are hidden) and never call back. With the canonical 3-cell map, exactly the
       bottom-center / top-center / center cells are reachable; the 6 corner/edge cells are inert.
     - activeAnchor highlights the matching cell in BOTH modes (px: anchor key; enum: enum value's cell). -->

<!-- Size-aware math — UI-SPEC §"Position Presets", 1080×1920 top-left anchor -->
left=0 ; centerX=round((1080-elementWidth)/2) ; right=round(1080-elementWidth)
top=0  ; centerY=round((1920-elementHeight)/2); bottom=round(1920-elementHeight)

<!-- Theme tokens (default.css). Active cell = BLUE per color law (NOT green). -->
active cell:   background var(--accent-tint); border-color var(--accent-strong); color var(--accent)
idle cell:     background var(--surface-2);   border 1px var(--border);          color var(--text-2)
disabled cell: background var(--surface-2);   color var(--text-muted); opacity 0.4; cursor not-allowed
grid:          display grid; gridTemplateColumns repeat(3, 30px); gap 3px
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invoke impeccable + frontend-design, then build PositionPresets.tsx (px + enum modes)</name>
  <read_first>
    - AGENTS.md § "UI/frontend work — REQUIRED tooling" (impeccable skill + frontend-design plugin are NON-NEGOTIABLE; invoke at the START of this task, not as an afterthought)
    - .claude/skills/sketch-findings-reel-factory/sources/themes/default.css (the OKLCH token set — use var(--…) names verbatim)
    - services/remotion-studio/src/editor/components/StyleControls.tsx (L17-21 POSITION_OPTIONS array + L185-213 segmented-button render — the affordance being generalized; note the active state is GREEN there and must be BLUE here per UI-SPEC color law). This is the 3-value SubtitlePosition model the enum mode must serve.
    - services/remotion-studio/src/pipeline-config.ts (L12 `export type SubtitlePosition = "bottom-center" | "top-center" | "center-screen"` — import this type for the enum mode so the contract is type-checked, not stringly-typed)
    - .planning/phases/22-studio-ui-polish/22-UI-SPEC.md § "Position Presets" and § "Color" (color law: green = Render CTA only; selection = blue)
  </read_first>
  <behavior>
    - Test (pure math helper `computePresetXY(anchorX, anchorY, elementWidth, elementHeight, frameWidth=1080, frameHeight=1920)`):
      - ("left","top", 200, 100) → { x: 0, y: 0 }
      - ("center","center", 200, 100) → { x: 440, y: 910 }   // round((1080-200)/2)=440, round((1920-100)/2)=910
      - ("right","bottom", 200, 100) → { x: 880, y: 1820 }   // 1080-200=880, 1920-100=1820
      - ("center","bottom", 0, 0) → { x: 540, y: 1920 }
    - Test (px mode render): rendering the grid in px mode produces exactly 9 ENABLED buttons, each with an accessible name (aria-label) describing its anchor (e.g. "Posición arriba-izquierda").
    - Test (enum mode render): rendering with `mode="enum"` and the canonical 3-cell `anchorToValue` map produces exactly 3 ENABLED cells (bottom-center, top-center, center) and 6 disabled/inert cells; clicking the bottom-center cell calls `onApplyAnchor("bottom-center")`, top-center → `onApplyAnchor("top-center")`, center → `onApplyAnchor("center-screen")`; clicking any disabled cell does NOT fire any callback.
  </behavior>
  <action>
    FIRST invoke the `impeccable` skill and the `frontend-design` plugin (AGENTS.md non-negotiable). Create services/remotion-studio/src/editor/components/PositionPresets.tsx. Export a pure function `computePresetXY(anchorX: "left"|"center"|"right", anchorY: "top"|"center"|"bottom", elementWidth, elementHeight, frameWidth=1080, frameHeight=1920)` returning `{x, y}` using: left/top=0, center=round((frame-element)/2), right/bottom=round(frame-element). Export the `PositionPresets` React component with the LOCKED prop signature from <interfaces> — import `SubtitlePosition` from `../../pipeline-config.js` for the enum-mode types. Render a 3×3 CSS grid (`gridTemplateColumns: repeat(3, 30px)`, `gap: 3px`) of 9 buttons with glyphs in row order `↖ ↑ ↗ / ← • → / ↙ ↓ ↘`, each cell mapped to an `{anchorX, anchorY}` pair. Implement the two modes: (px mode, `mode` undefined or "px") all 9 cells enabled, click calls `onApply(computePresetXY(...))`; (enum mode, `mode="enum"`) a cell is ENABLED iff its `${anchorX}-${anchorY}` key exists in `anchorToValue`, enabled-cell click calls `onApplyAnchor(anchorToValue[key])`, and disabled cells get the disabled treatment (`var(--text-muted)`, `opacity 0.4`, `cursor: not-allowed`, `disabled` attribute) and fire no callback. Active cell (matching `activeAnchor` — by anchor key in px mode, by enum value's cell in enum mode) gets BLUE treatment via the default.css tokens (`var(--accent-tint)` bg, `var(--accent-strong)` border, `var(--accent)` color) — NEVER green (color law). Each button MUST carry an `aria-label` (Spanish, e.g. `Posición centro`, `Posición arriba-derecha`) and a 44×44px minimum hit area (use padding/wrapper even though the visible cell is 30px) per UI-SPEC Accessible Name Contract; disabled cells keep their aria-label but add `aria-disabled`. Component owns NO config state. Do NOT wire it into any consumer in this plan (that is 22-05). Co-locate the test as PositionPresets.test.ts if a studio test runner exists; otherwise put the `computePresetXY` cases in an inline `if (import.meta.vitest)` block or a sibling test file the renderer vitest can pick up — prefer a `services/remotion-studio/src/editor/components/PositionPresets.test.ts` that imports the pure helper AND exercises the enum-mode click mapping.
  </action>
  <verify>
    <automated>cd services/remotion-studio && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "PositionPresets" || echo "no PositionPresets type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export function computePresetXY' services/remotion-studio/src/editor/components/PositionPresets.tsx` returns 1.
    - Source assertion: `grep -c 'export function PositionPresets\|export const PositionPresets' services/remotion-studio/src/editor/components/PositionPresets.tsx` returns 1.
    - Source assertion: the component accepts BOTH the px path (`onApply`) AND the enum path (`mode`, `anchorToValue`, `onApplyAnchor`) — `grep -c 'onApply\b' ...PositionPresets.tsx` ≥ 1 AND `grep -c 'onApplyAnchor' ...PositionPresets.tsx` ≥ 1 AND `grep -c 'anchorToValue' ...PositionPresets.tsx` ≥ 1.
    - Source assertion: `SubtitlePosition` is imported from pipeline-config (`grep -c 'SubtitlePosition' ...PositionPresets.tsx` ≥ 1) so the enum mode is type-checked.
    - Source assertion: file contains all 9 glyphs `↖ ↑ ↗ ← • → ↙ ↓ ↘` and references `var(--accent` (blue active) with ZERO `#4CAF50`/`rgba(76, 175, 80` green literals (color law).
    - Source assertion: `grep -c 'aria-label' services/remotion-studio/src/editor/components/PositionPresets.tsx` returns 9 (one per button), and disabled cells carry `aria-disabled`/`disabled`.
    - Behavior (px): the 4 computePresetXY cases in <behavior> return the stated {x,y} exactly; px-mode render shows 9 enabled cells.
    - Behavior (enum): with the canonical 3-cell map, exactly 3 cells are enabled and 6 are disabled; the 3 enabled clicks call onApplyAnchor with "bottom-center" / "top-center" / "center-screen" respectively; disabled-cell clicks fire nothing.
    - Type check: tsc reports no errors referencing PositionPresets.
    - Negative: no consumer (StyleControls/TitleEditor/OverlayEditor) is modified in this plan (`git diff --name-only` lists only PositionPresets.tsx[.test.ts]).
  </acceptance_criteria>
  <done>PositionPresets.tsx exists with the locked dual-mode prop signature (px `onApply` + enum `onApplyAnchor`/`anchorToValue`/`mode`) + exported pure math helper, 9 aria-labeled blue-active cells, px math AND enum-mapping cases verified, no consumer wiring, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → onApply(x,y) / onApplyAnchor(enum) → host draft state | Preset writes a numeric x/y (px mode) or a fixed enum value (enum mode) into the existing config draft path. Numbers + fixed enum only; no string interpolation into markup or paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | computePresetXY output | accept | Output is bounded integer px derived from frame/element constants; consumers already clamp x∈[0,1080]/y∈[0,1920] via existing number inputs. No injection surface. |
| T-22-04 | Denial of service | render math | accept | Pure synchronous arithmetic, O(1); no loops over user data. |
| T-22-13 | Tampering | enum-mode value | accept | Enum-mode emits only values present in the caller-supplied `anchorToValue` map (typed as SubtitlePosition); disabled cells fire nothing — no arbitrary string can reach the subtitle config. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (new React component using existing deps only). |
</threat_model>

<verification>
- PositionPresets.tsx compiles, exports the component + pure helper.
- 9 aria-labeled cells; blue (not green) active treatment.
- px mode: 9 enabled cells, onApply(x,y). enum mode: 3 enabled cells, onApplyAnchor(enum), 6 disabled.
- No consumers touched.
</verification>

<success_criteria>
- Shared, stateless, dual-mode 9-point grid component with size-aware math (px) and reduced enum mapping (subtitles), ready for 22-05 to consume on a single decided path.
</success_criteria>

<output>
Create `.planning/phases/22-studio-ui-polish/22-02-SUMMARY.md` when done.
</output>
