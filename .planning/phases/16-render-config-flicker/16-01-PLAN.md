---
phase: "16-render-config-flicker"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/remotion-studio/src/server.ts
  - docker-compose.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "PUT /api/config writes /data/pipeline/pipeline-config.json (ACTIVE_PIPELINE_CONFIG_PATH) in addition to resolveConfigPath()"
    - "The written file contains the clean config (no _meta key)"
    - "The written file survives on an empty pipeline/ directory (mkdirSync guard fires)"
    - "ACTIVE_PIPELINE_CONFIG_PATH is explicit in docker-compose remotion-studio environment block"
  artifacts:
    - path: "services/remotion-studio/src/server.ts"
      provides: "Active-config mirror write after existing configPath write"
      contains: "ACTIVE_PIPELINE_CONFIG_PATH"
    - path: "docker-compose.yml"
      provides: "ACTIVE_PIPELINE_CONFIG_PATH env var in remotion-studio service"
      contains: "ACTIVE_PIPELINE_CONFIG_PATH=/data/pipeline/pipeline-config.json"
  key_links:
    - from: "services/remotion-studio/src/server.ts PUT /api/config"
      to: "/data/pipeline/pipeline-config.json"
      via: "fs.writeFileSync(activePath, ...)"
      pattern: "writeFileSync.*activePath"
    - from: "services/api-server/src/routes/process.ts existsSync(ACTIVE_PIPELINE_CONFIG_PATH)"
      to: "per-job pipeline-config.json seed"
      via: "copyFileSync in the seeding block (process.ts:123-128)"
      pattern: "existsSync.*ACTIVE_PIPELINE_CONFIG_PATH"
---

<objective>
Fix Issue A: studio PUT /api/config must ALSO write to ACTIVE_PIPELINE_CONFIG_PATH
(/data/pipeline/pipeline-config.json) so that subsequent /process renders pick up the
studio config instead of falling back to env defaults.

Purpose: v1.1 wired the consumer (process.ts seeds from ACTIVE_PIPELINE_CONFIG_PATH) but
never the producer (studio PUT /api/config only writes to resolveConfigPath()). This one
missing write means every /process render falls back to tiktok layout + zero titles.

Output: server.ts with the second writeFileSync call; docker-compose.yml with the env var
made explicit in the remotion-studio service block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-render-config-flicker/16-CONTEXT.md
@.planning/phases/16-render-config-flicker/16-RESEARCH.md
@.planning/phases/16-render-config-flicker/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add active-config mirror write to PUT /api/config (server.ts)</name>
  <files>services/remotion-studio/src/server.ts</files>
  <read_first>
    services/remotion-studio/src/server.ts — read the full PUT /api/config handler (lines 101–143)
    to understand: existing configPath write at line 128, console.log at line 130, the try/catch
    scope (lines 115–142), and the imports at lines 1–15 (path and fs are already imported).
    services/api-server/src/constants.ts — read lines 20–21 for ACTIVE_PIPELINE_CONFIG_PATH
    default value ("/data/pipeline/pipeline-config.json") to match exactly.
  </read_first>
  <action>
    Inside the PUT /api/config handler in server.ts, after the existing console.log at line 130
    ("[studio] Config written to:", configPath) and BEFORE the return res.json(...) at line 131,
    insert the active-config mirror block:

    1. Declare activePath using: process.env.ACTIVE_PIPELINE_CONFIG_PATH || "/data/pipeline/pipeline-config.json"
       — the default must match api-server/src/constants.ts:21 exactly.
    2. Declare activeDir using: path.dirname(activePath)
    3. Guard: if (!fs.existsSync(activeDir)) { fs.mkdirSync(activeDir, { recursive: true }); }
       — same mkdirSync pattern already used for configPath at server.ts:117–120.
    4. Write: fs.writeFileSync(activePath, JSON.stringify(configToWrite, null, 2));
       — reuse configToWrite (already has _meta stripped at line 123 and titles sanitized at lines 125–127).
       Do NOT re-strip or re-sanitize.
    5. Log: console.log("[studio] Active config mirrored to:", activePath);

    Do NOT change resolveConfigPath(), the first writeFileSync to configPath, or the response JSON.
    The second write is independent of resolveConfigPath() and must NOT be routed through it.
    Both writes must remain inside the existing try/catch block (lines 115–142) so the existing
    error handler covers both.

    No new imports needed — path and fs are already imported.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/reel-factory/services/remotion-studio && grep -c "ACTIVE_PIPELINE_CONFIG_PATH" src/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - server.ts contains exactly one reference to ACTIVE_PIPELINE_CONFIG_PATH (the activePath declaration line)
    - server.ts contains a second fs.writeFileSync call (for activePath) after the existing writeFileSync to configPath
    - The second writeFileSync uses configToWrite (same object, not a re-destructured body)
    - The mkdirSync guard for activeDir appears before the activePath writeFileSync
    - Both writeFileSync calls remain inside the try/catch block spanning lines 115–142
    - resolveConfigPath() is not changed (grep: resolveConfigPath appears unchanged in the file)
    - The return res.json(...) statement is not moved or altered
  </acceptance_criteria>
  <done>server.ts PUT /api/config handler writes to both resolveConfigPath() and ACTIVE_PIPELINE_CONFIG_PATH with the sanitized configToWrite</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add ACTIVE_PIPELINE_CONFIG_PATH to remotion-studio environment in docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    docker-compose.yml — read lines 163–185 (the remotion-studio service definition) to see the
    current environment block (lines 174–178) and confirm the existing vars:
    INPUT_PATH, PIPELINE_JOB_ID, PIPELINE_CONFIG_PATH, PORT=3123.
    services/api-server/src/constants.ts — lines 20–21 to confirm the canonical default value.
  </read_first>
  <action>
    In docker-compose.yml, inside the remotion-studio service's environment block (lines 174–178),
    add the following line after the existing "- PORT=3123" entry:

      - ACTIVE_PIPELINE_CONFIG_PATH=/data/pipeline/pipeline-config.json

    The value "/data/pipeline/pipeline-config.json" must match api-server/src/constants.ts:21
    exactly (same default used by server.ts). Preserve YAML indentation (2 spaces relative to
    the environment key, 6 spaces from the root — matching the other entries in that block).

    Do not change any other part of the service definition (healthcheck, ports, volumes).
  </action>
  <verify>
    <automated>grep -c "ACTIVE_PIPELINE_CONFIG_PATH=/data/pipeline/pipeline-config.json" /home/luis/proyectos/reel-factory/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains exactly the line "- ACTIVE_PIPELINE_CONFIG_PATH=/data/pipeline/pipeline-config.json" inside the remotion-studio environment block
    - The value matches api-server/src/constants.ts:21 default verbatim
    - The healthcheck, ports, build, and volumes sections of remotion-studio are unchanged
    - No other service's environment block was modified
    - docker-compose config validates (run: cd /home/luis/proyectos/reel-factory && docker compose config --quiet 2>&1; echo exit=$?)
  </acceptance_criteria>
  <done>docker-compose.yml remotion-studio service explicitly declares ACTIVE_PIPELINE_CONFIG_PATH=/data/pipeline/pipeline-config.json</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio UI → PUT /api/config | Request body from the studio editor browser; already validated by validatePipelineConfig + sanitizeTitles before the new write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | server.ts active-path write | accept | The new write reuses the same sanitized configToWrite already written to configPath — no new unsanitized path. validatePipelineConfig + sanitizeTitles already run before both writes. |
| T-16-02 | Denial of Service | mkdirSync on empty pipeline/ | accept | mkdirSync with { recursive: true } is idempotent and bounded to one path; no recursive unbounded writes. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
After both tasks complete:

1. Manually start the studio container (or run it locally):
   cd services/remotion-studio && setsid env PORT=3123 EDITOR_DIST=$(pwd)/dist/editor npx tsx src/server.ts > /tmp/remotion-server.log 2>&1 &

2. Send a PUT /api/config request:
   curl -s -X PUT http://localhost:3123/api/config \
     -H "Content-Type: application/json" \
     -d '{"subtitle":{"layout":"bar","fontFamily":"Inter","fontSize":60,"activeColor":"#FFFFFF","inactiveColor":"#AAAAAA","letterSpacing":0,"lineHeight":1.4,"position":"bottom-center","bottomOffset":120,"pastWordOpacity":0.5},"titles":[]}'

3. Verify the active file was created:
   cat pipeline/pipeline-config.json | python3 -m json.tool | grep -E '"layout"|"fontFamily"'
   Expected: "layout": "bar", "fontFamily": "Inter"

4. Verify _meta is absent:
   cat pipeline/pipeline-config.json | python3 -m json.tool | grep "_meta"
   Expected: no output (empty)

5. Verify docker-compose is still valid:
   cd /home/luis/proyectos/reel-factory && docker compose config --quiet
   Expected: exit 0, no errors
</verification>

<success_criteria>
- PUT /api/config response returns 200 with the config (unchanged behavior)
- pipeline/pipeline-config.json exists after the PUT with the submitted config (no _meta key)
- docker compose config --quiet exits 0
- The existing per-job write path (resolveConfigPath) continues to work unchanged
</success_criteria>

<output>
Create .planning/phases/16-render-config-flicker/16-01-SUMMARY.md when done
</output>
